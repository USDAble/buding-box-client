# 开发计划 · 需求批次 20260911

| 项 | 内容 |
| --- | --- |
| 文档状态 | v0.1 草案，待评审 |
| 日期 | 2026-09-11 |
| 定位 | 本批次的**落地计划**：PR 拆分与顺序、每个 PR 的范围与验收、能力检索结论（开发规范 §3.5）、人工确认登记（§3.7）、跨文档一致性矩阵（§4.2） |
| 上游 | [需求基线](需求基线.md)（要什么 / 怎么验收）→ [E2E闭环清单](E2E闭环清单.md)（本轮 35 条闭环与判定）→ 本文档（**怎么拆、按什么顺序落地**） |

> **本计划服从 `§0 当前阶段原则`：先走通流程，不增加复杂度。** 因此 PR-1 刻意取最小范围（4 个端点），**不做**网关、目录、词库。

---

## 0. 本文档怎么读

- **§1** 落地顺序 —— 只看这里就知道先做什么。
- **§2** 每个 PR 的范围 / 明确不做什么 / 验收。
- **§3** 能力检索结论 —— §3.5 强制；**每个"新建 X"都必须有这一节的三行结论**。
- **§4.1** 人工确认与决策表 —— §3.7 的登记处。
- **§4.2** 一致性矩阵 —— 一个取值散落在哪几处（改动时照它同步）。
- **§5** 本轮明确不做。

**编号约定：** PR 序号用 **`PR-n`**（`P1`–`P6` 已被占用为"闭环公共件"，`PQn` 是待确认项，`D-nnn` 是暂不解决项，`L-*` 是闭环，`V-n` 是缺口 —— 互不重编）。

---

## 1. 落地顺序

| PR | 一句话 | 能判的闭环 | 前置 |
| --- | --- | --- | --- |
| **`PR-1`** | ✅ **已完成** —— 契约层 + **中台替身（4 端点）** + 中台客户端骨架 | `L-A1a` `L-A2`–`L-A6` 的**客户端侧**（`L-A1b` 有意不实现） | 无 |
| **`PR-2a`** | ✅ **已完成** —— 持久层：`internal/productstate` + `internal/credentialstore` + `internal/atomicfile` | `L-E1`、`L-E2`、`L-E4a`、`L-E4b` 的**存储侧**；`L-A5` 的落盘（`boxCode`） | `PR-1` |
| **`PR-2b1`** | ✅ **已完成** —— `internal/productruntime` 的 5 个端点（状态 / 发码 / 登录 / 登出 / 语言）+ 三类信封 + 中台错误映射 = **服务端侧** | `L-A1a` `L-A2`–`L-A6` 的**服务端侧**；`L-A4` 四码分流；`L-B1a` 的端点可达性 | `PR-2a` |
| **`PR-2b2a`** | ✅ **已完成** —— 接线：把端点挂进服务（动 2 个上游文件：`main.go` **+1 行**、`server.go` **+18 行**；装配收进新文件 `product.go`） | `L-A1a` `L-A2`–`L-A6` 的**端到端**（4 条新测试经真服务跑通）、`P3` 前提 | `PR-2b1` |
| **`PR-2b2b`** ✅ **已完成（2026-09-11）** | 窗口令牌与请求门（**门只覆盖 `MountAPI` 缝注册的产品路由** —— 曾定"宽门 `/api/*`"，落地前实测推翻，见 §4.1 第 12 行 / `D-006`）+ 上游 3 文件最小改动 | `L-B1b`（请求门）；`L-E5` 的**壳带入**实为 `PR-2b2a` 已落地（见该节收尾） | `PR-2b2a` |
| **`PR-2c`** ✅ **已完成（2026-09-12）** | **拦截页与控制面失败面**（见 `V-15` / `V-16`）：`L-B2` 未配置/无公钥**两页不同文案** + `L-B3` **四档失败**各自的重试性与凭证语义 + `L-A6` 会话失效 → **清凭证** → 回拦截页。**`P4-拦截页.md` 已补出**（`V-15` 收敛） | ✅ `L-A6` ／ ⚠️ `L-B2` `L-B3` | `PR-2b2a`（拦截页与门已在）、`PR-1`（两个判据函数已在） |
| **`PR-2d`** | **授权被平台否认时切回激活表单（`V-44`）**：平台对一次**没带激活凭证**的二次登录回激活类错误（`activation_invalid` 等四码）⇒ 本地 `activated` **跟随平台翻回 false**，拦截页因此从两字段短表单切回五字段激活表单（`account`/`activation` 记录保留）。**根与 `V-22` 同源**：`activated` 的 owner 是平台，本地那份只是投影。方案见 §2 `PR-2d`；已拍板取方案 A | 使 `L-A4` 的四个码**落在能补救它们的表单上**（今天文案指着一个界面上不存在的字段）；`L-A2` 的"二次登录不出现激活码"依然成立 | `PR-2c`（拦截页形状的 owner 是服务端应答，那条原则由它确立）；**独立于 `PR-4c` / `PR-4e`** |
| `PR-3` | 拆除 `devBackend.ts`（触发条件达成后） | **解锁全部 35 条的可信判定** | `PR-2b2a` + `P2`。**触发条件已按 `V-24` 收紧**：不只是"本地 Go 服务能应答 `/api/product/*`"，而是**那 6 条缺失路由也挂上之后**（`chat-modes` 已由 `PR-4d` 挂上 ⇒ 剩 5 条）—— 否则拆掉假后端会让设置页、词库页、模式选择器当场失效。**逐条归属见 §1.1 表**（该表是唯一 owner，此处不复制清单） |
| `PR-4a` | ✅ **已完成（2026-09-11）** —— **验签核心**：策略信封解析 + ed25519 验签（签**原始字节**，见 `D-007`）+ 信任锚复用 + **替身签名能力** | 打底（单测可判，无 UI）—— `L-C1a` 的**验签半边** | `PR-1` |
| `PR-4b` | ✅ **已完成（2026-09-12）** —— **目录获取与缓存**：登录后（`bootstrap`）拉取签名目录 → 验签 → **原子落 `data/catalog.json`** + 版本单调拒绝 + 缓存读取路径。**TTL 到期后的条件请求（`ETag`/`304`）与周期刷新改归 `PR-4c`** —— 理由见 §2.1 第 0 步 | `L-C1a`；**顺带使 `L-A6` 端到端可走通**，并补 `L-A2` / `L-A3` 的端到端测试（`V-29`） | `PR-4a` |
| **`PR-4c1`** | **会话续期与轮换落盘**（`V-39` + `V-42`）：让重启后的第一次发言能拿到令牌（`Ensure` 接缝在 `GatewayEndpoint`，上游零改动），并把**平台轮换后的新 refresh token 落盘** —— 否则第一次刷新就把磁盘凭证作废；并顺带修掉 `V-43`（传输抖动被当成"会话结束"）。**从 `PR-4c` 劈出并前置**（方案与落地记录见 §2 `PR-4c1`；三项已拍板，§4.1 第 31–33 行）| `L-C3a` 的**入口条件**；`E6` 的「重启自动登录」与 `E2E-11` 的换机复用 | `PR-5a`（`GatewayEndpoint` 是接缝）—— **执行顺序在 `PR-4c` 之前**，也是 `PR-5b2` / `PR-5d` 手工验收的共同前置 |
| `PR-4c` | 三条降级语义 + 空名单四种文案 + **TTL 到期后的条件请求与按需刷新**（方案见 §2，第 0 步结论：**与 `PR-4d` 换序**） | `L-C2` | **`PR-4d`**（原写 `PR-4b`；四条文案有两条只在选择器里，选择器读目录是 `4d` 的交付物 —— 见 §2 `PR-4c` 第 0 步） |
| `PR-4d` | ✅ **已完成（2026-09-12）** —— 投影 + `internal/chatmode` 搬运 + `chat-modes` 去 `fallback` + 前端渲染 `displayName` | ✅ `L-C1b` | `PR-4b`（**先于 `PR-4c` 开工**） |
| **`PR-4c0`** | ✅ **已完成（2026-09-13）** —— **`V-35` 前置拦截**：网关绑定的模型名不得落到默认 sender（注入 `Config.GatewayModelPrefix` + `senderForSession` 一处拒绝型 sender） | 使 `L-C5` 的**「网关绑定 id 不落到其它模型源」这一分支**落地（production 半边仍归 `PR-5`）；`L-C3a` 不再以"第三方 400 + 内容外泄"的形式失败 | `PR-4d`（它的手工验收逼出 `V-35`）；**执行顺序在 `PR-4c` 之前** |
| `PR-4e` | 能力矩阵消费与入口可见性（`internal/productpolicy`） | `L-C6` | `PR-4a` |
| **`PR-5`** | 网关流式回合（**复用** `internal/provider/openai`）+ 动态令牌注入（`C2`）+ 模型下架前置校验 + `V-36` ⇒ **粒度自检结论：拆 5 个子 PR，执行顺序 `5a → 5c → 5b → 5d → 5e`**（原一条 PR 覆盖 6 条闭环 + 2 处缺口，判据 §6.4 第 0 步） | — | 见下行 |
| **`PR-5a`** | **网关回合跑通**：内置端点（`C1`）+ 动态令牌注入（`C2`）+ `senderForSession` 路由到网关 + **`ensureSender` 条件化（`V-36`）** ⇒ **取代 `V-35` 的拒绝型 sender**（用户可见结果：选中目录模型 → 发消息 → 拿到流式回复）。**✅ 已完成（2026-09-13）**；`V-37` 已于实现时证伪撤回 | ✅ `L-C3a` | `PR-4b`（目录）、`PR-4d`（复合 id 与端点 id）、`PR-4c0`（它留下的 `gatewayBound` 就是本条的接缝） |
| **`PR-5c`** | **不回落六条失败面**（`L-C5`）+ `V-35` 的 production 半边（`RequiresControlPlane()` 接上）。**✅ 已完成（2026-09-13）** —— 注入 `Config.RequireGateway` / `Config.ControlPlaneReady`，在 `senderForSession` 一处拒绝默认 sender；9 条钉子 + 4 个变异体，断言口径是**第三方替身命中数 = 0** | ✅ `L-C5` | **`PR-5a`**（先有能跑的网关，才有"网关失败时不许回落"可言）。**排在最前是有意的**：它只收窄失败路径，越早接上，后续能力越不会长在一条会回落的路径上 |
| **`PR-5b1`** | **思考块**（`L-C3b`）：把推理偏好（`show_reasoning` / `reasoning_effort`）接到网关 sender 工厂（**今天完全没接** ⇒ 网关回合永不显示思考，与系统默认 true 相反）+ 替身补 `reasoning_content`（不补则无法验收）。**原 `PR-5b` 两能力中代码改动的一半；工具那半无需改动，见下行** | ✅ `L-C3b` | `PR-5a`（工厂）、`PR-5c`（工厂被注入的判据） |
| **`PR-5b2`** | **工具调用**（`L-C3c`）：`app.sender` **已实现** `StreamMessagesWithTools`（`sender.go:384`）、本地权限门**在 `internal/server` 每回合构建**（与 sender 无关）⇒ 本条是**钉子**（工具调用过同一道门）+ 替身补 `tool_calls` **+ 一行产品代码都不用改** | ✅ `L-C3c` | `PR-5b1`（共用替身扩展；顺序在后） |
| **`PR-5d`** | **余额与用量上屏**（`L-C4`）：`GET /v1/credits/ledger` 拉模式（`C7` 联调兜底）+ `credits_update` 发射 + 充值入口常驻 | ✅ `L-C4` | `PR-5a` |
| **`PR-5e`** | **模型下架前置校验**（`L-C7`）：老会话可继续读，下一条消息前提示重选，不静默换模型 | ✅ `L-C7` | `PR-5a` |
| `PR-6` | 内容安全三层（内置 / 用户 / 服务器） | `L-D1`–`L-D5`（含拆分后的 `L-D4a`/`b`/`c`） | `PR-1` |
| `PR-7` | 便携 / 拔盘冻结 / 数据升级投影 | `L-E3` `L-E5`；**`L-E2` 的换机场景测试归本条**（`PR-2a` 只做了存储半边，见 `V-17`）；`L-E1` `L-E4a` `L-E4b` 已在 `PR-2a` 落地 | `PR-2a` |
| `PR-8` | 产品 WS 四事件 | `L-F1` | `N-5` 拍板 |
| `PR-9` | 六个守卫接线（`P5` / `V-1`） | 全部闭环的**自动回归** | 可随时并行 |

**待决项的位置（更新，闭环号已按 §2.0 的拆分同步）：** `PQ25`（激活幂等）**只卡 `L-A1b`**，不阻塞 `PR-2b2a` 主体；`PQ26`（客服载体）卡 `L-A4` 的 UI 判定；`V-7`（首启时序）**✅ 已在 `PR-2a` 定并实现**；`N-5` 卡 `PR-8`。

### 1.1 `/api/product/*` 路由归属表（`V-24`，2026-09-12 新增）

**背景**：前端调用 11 条 `/api/product/*`，`Runtime.Mount` **只注册 5 条**（2026-09-12 起 6 条 —— `chat-modes` 由 `PR-4d` 挂上）。缺的 6 条（现为 5 条）在真机桌面构建下**全部 404**，今天只靠 `devBackend.ts` 在 `fetch` 层接管才"能跑"（`V-24`）。**这张表是它们的归属的唯一 owner** —— 缺一条，那条闭环就判不了，`PR-3` 也就拆不掉。

| 路由 | Go handler | 归属 PR | 缺它会怎样 |
| --- | --- | --- | --- |
| `GET /api/product/state` | ✅ 已注册 | — | — |
| `POST /api/product/send-code` | ✅ 已注册 | — | — |
| `POST /api/product/login` | ✅ 已注册 | — | — |
| `POST /api/product/logout` | ✅ 已注册 | — | — |
| `PUT /api/product/locale` | ✅ 已注册 | — | — |
| `GET /api/product/chat-modes` | ✅ 已注册（`PR-4d`，2026-09-12） | — | — |
| `PUT /api/product/nickname` | ⬜ **未注册** | **`PR-6`** | 改昵称 404；`nickname_sensitive` 永不产生 ⇒ `L-D3` 判不了 |
| `PUT /api/product/prefs` | ⬜ **未注册** | **`PR-6`** | `defaultChatMode` / `inputSensitiveCheck` 改不动 ⇒ `L-E5` 的偏好半边、`L-D1` 的关开关判不了 |
| `POST /api/product/sensitive/check` | ⬜ **未注册** | **`PR-6`** | 输入检测没有服务端半边 ⇒ `L-D1` 判不了 |
| `GET` + `PUT /api/product/sensitive/dict` | ⬜ **未注册** | **`PR-6`** | 词库读不到、增删不落盘 ⇒ `L-D4a` / `L-D4b` 判不了 |
| `POST /api/product/sensitive/dict/import` | ⬜ **未注册** | **`PR-6`** | 导入的"合并非覆盖"无法判定 ⇒ `L-D4c` 判不了 |

> **注**：`PR-6` 一条 PR 认领 5 条路由 + `L-D` 全组 7 条闭环，**按 §2.0 的粒度判据很可能过大** —— 开工前应按同一判据做粒度自检，预计拆为「服务端三层（读/检测/落盘打码）」与「界面接线（昵称/偏好/词库管理）」两个 PR。此处先记归属，拆分在 `PR-6` 的方案里定。

---

## 2. 逐个 PR

### `PR-1` 契约层 + 中台替身 + 客户端骨架（最小范围）

**为什么三者必须同一个 PR：** 替身是客户端的**镜像**。先写替身会发明一份契约，再写客户端又发明一份，两份必然漂移 —— 正是 `§3.8` 禁止的"同一件事两处定义"。**一份 Go 类型，两个实现**，替身才具备证明力。

> **⚠️ 实施期范围修正（2026-09-11，落代码时发现，已按此实现）。** 本节原先把 JCS + ed25519 验签列在 `PR-1`。**这是错的**：`PR-1` 的四个端点（发码 / 登录 / 刷新 / bootstrap）**没有任何签名要验** —— 需要验签的是**签名目录与策略信封**。而 bootstrap 在合同里**同时返回目录**（`中台交付包` §4.1 第 5 行：账户摘要 + 权益 + 余额 + **模型目录** + 能力矩阵 + 策略版本），所以正确做法是**把 bootstrap 收敛为「账户 + 激活 + 余额」，目录与能力矩阵留到 `PR-4`**，签名代码整体下沉到 `PR-4`。理由有二：① 让 `PR-1` 不包含本题**唯一没有现成实现**的一处，符合"先走通流程、别太复杂"；② JCS 是 RFC 8785，手写一份不合规的实现比不写更危险，它应当与目录一起一次做对。**bootstrap 不带目录是合法状态，不是错误**（`B4`：目录未就绪时选择器为空 + 不可用提示）。

**做：**

1. `internal/productclient/` —— **契约层**：`dto.go`（请求/响应类型 + 错误码常量，是字段名与错误码的**唯一 owner**）+ `client.go`（4 个端点 + 内存凭证持有者 + 401 单飞刷新与一次重放，即 `PR-1` 即已落地 `L-A6`）。
2. `internal/productclient/clienttest/` —— **中台替身**：真 HTTP handler、可编排场景、含测试码重置，**与客户端共用上面那份类型**。
3. 测试：用替身驱动客户端，覆盖 `L-A1a` `L-A2`–`L-A6` 的**客户端侧**分支（`L-A1b` 有意不实现）。

**不做（留到后续 PR）：** **JCS + ed25519 签名/验签（下沉 `PR-4`）**、模型目录与能力矩阵（`PR-4`）、网关 SSE（`PR-5`）、词库（`PR-6`）、`/api/product/*` 本地服务（`PR-2`）、前端接线、`devBackend` 拆除、可执行替身文件。

**验收：**

- 用替身跑通 `L-A2`（二次登录**不需要**激活码与盒子编号）、`L-A5`（bootstrap 回填 `boxCode`；缺失时是空值而非报错）、`L-A6`（401 单飞刷新一次 + 重放一次；刷新被拒 → 清凭证 + `ErrSessionExpired`，不重试）。
- 四个激活错误码（`activation_invalid` / `activation_code_used` / `box_code_unknown` / `box_code_mismatch`）**各自**可被触发并断言（`L-A4`）；`phone_mismatch` 亦单列。
- 同一盒子编号 + 两个不同激活码 → **两次都成功**（`L-A4` 的一对多回归）。
- 平台完全不可达 → `network_unavailable`（与上游 5xx 区分，且**不**回落本地模型源）。
- `L-A1b` 的"响应已送达但响应丢失"分支 **不实现**，登记为 `PQ25` 的未决面。

**替身的形态：**

| 项 | 定 |
| --- | --- |
| 落点 | 库 `internal/productclient/clienttest`（仓库内，测试可用），暴露 `Handler()` 由调用方用 `httptest.NewServer` 包起来。**不做**独立的可执行替身文件 —— 最小范围内它只服务 Go 测试；前端 E2E 需要它时再补（那是 `PR-2` 之后的事） |
| 隔离 | 仅被 `_test.go` 引用 ⇒ 不可能进入便携包；`PR-9` 的 preflight 加一条断言兜底 |
| 契约来源 | **import `internal/productclient` 的类型**，不重写 JSON schema |
| 签名 | **`PR-1` 不需要签名**（四个端点都不验签，见上方范围修正）。测试密钥与签名能力随 `PR-4` 的目录/策略一起引入 |
| 场景编排 | 用特定手机号 / 激活码触发特定错误码，使每个分支**按需到达**（Sandbox 做不到这点）。已内置：同一盒子编号的两个激活码（一对多）、只发给特定手机的激活码（`phone_mismatch`）、未知盒子编号、真实但对不上的盒子编号、失效访问令牌、被撤销的刷新令牌、平台不可达 |
| 重置 | `New()` 每次都是干净状态，等价于重置测试激活码，对应 `S-8` 要中台提供的那一项 |

### `PR-2a` 持久层（已完成）

**做：**

1. `internal/productstate` —— `data/product-state.json` 的唯一 owner（`E6.1` 字段表逐行对应）。含 `installId` 生成、`E6.2` 的升级/缺字段/损坏三条护栏、`Logout()` 保留激活记录与绑定手机号（`E7`）。
2. `internal/credentialstore` —— `data/credential.json` 的唯一 owner（`E6.3` 字段表）。**access token 不落盘**；登出＝删文件；损坏即丢弃。
3. `internal/atomicfile` —— 临时文件 + `rename` 的原子写，两个存储共用**一份**实现。
4. 测试：`L-E1`（首启播种 + `installId` 不被登录/登出改变）、`L-E2`（凭证随 `data/` 走）、`L-E4a`/`L-E4b`（缺字段/damaged/版本超前三条护栏）、以及"状态文件里搜不到任何 token"。

**同时定死 `V-7`（首启初始化时序）** —— 此前四处未写：责任人、时刻、初值、只读盘行为。结论落在 `需求基线` `E6.4`：

- **责任人 = Go 本地服务**。`installId` 是「这台安装」的属性，而安装边界＝数据根，数据根的唯一 owner 是 `internal/datapath`；让桌面壳生成会造出**第二个路径与生命周期的 owner**（§3.8）。壳只把**系统语言**带进来一次（`PQ18`）。
- **时刻 = 首次读取时惰性创建**，不新增任何"进程启动写文件"的路径。比 §3.9.1 允许的"启动时幂等播种"更强：**没有任何一次进程启动会写文件**。
- **只读盘 / 写失败 ⇒ 内存默认值让界面可渲染 + 错误向上返回**，不阻塞启动、**不假装已持久化**。
- **与 `E6.2` 规则 4/5 的顺序**：文件**不存在**才播种；版本**超前**与文件**损坏**都**不得**进入播种路径（否则新版本或损坏数据会被空白状态悄悄替换）。

**明确的边界（本 PR 不做）**：HTTP handler、产品门、`internal/server` 的接线、凭证与 `productclient` 的对接（令牌从内存到凭证文件的搬运）。这些是 `PR-2b1` / `PR-2b2a`。

### `PR-2b1` 本地 `/api/product/*` 端点（已完成）

**做：** `internal/productruntime` 承载 5 个端点（`GET /state`、`POST /send-code`、`POST /login`、`POST /logout`、`PUT /locale`），按 [`本地API契约.md`](本地API契约.md) 的三类信封与错误码实现。

**实施期定下三件契约上的事（都已回写文档）：**

1. **信封层级由本地契约决定，不由中台决定。** 中台会在错误体里带 `field`（它把 `activation_invalid` 标成 `activationCode` 的错），但 `本地API契约.md` §3 把这四个激活失败定为**业务级**，所以实现忽略中台的 `field`。理由：用户改不动这些值（`activation_code_used` 只能找客服），落在输入框下面会误导成"改一下就能过"。层级表只有 3 个字段级 code，落在 `envelope.go` 的 `fieldLevelCodes`。
2. **`invalid_phone` / `invalid_code` 同名两层的消解规则**：格式问题在**本地校验**阶段拦下（`validPhone` / `validCode`），所以**凡从中台回来的同码按业务级处理**。这条不写下来，实现者一定会"顺手统一"。
3. **`phoneMasked` 必须由中台给**（新增字段，`中台交付包` §3.2 已补）。触发 `phone_mismatch` 的典型场景是「全新 `data/` + 已被绑定的激活码」，本地从没见过那个号码。
4. **新增 `control_plane_unconfigured`**（已登记入 `本地API契约` §3）：本构建没配中台地址时返回它，使「未配置」与「中台挂了」可区分。S-6/S-7 的**页面口径仍未定**，本码不解决那件事。

**不做（留给 `PR-2b2`）：** 产品门三态与 window token；把 `Handler()` 挂进 `internal/server`。

**验收（已达成）：** 15 个测试（下表），`internal/productruntime` 覆盖率 71.9%。**先写测试、确认全红（`501 not_implemented`）再实现**，符合 §6.4。

| 测试 | 判的闭环/规则 |
| --- | --- |
| `TestStateIsReturnedUnwrapped` | §2.1 状态对象**不包 `state`**（与 login 相反的不对称） |
| `TestStateIsReachableWhileLoggedOut` | 登录页必须能渲染 |
| `TestSendCodeInvalidPhoneUsesBusinessEnvelope` | **§2.2 反直觉信封**：手机号错走业务级 `code` |
| `TestSendCodeReturnsCooldown` | `cooldownSec` |
| `TestFirstActivationReturnsWrappedStateWithServerBoxCode` | `L-A1a`：`boxCode` **来自服务端而非表单** |
| `TestFirstActivationPersistsCredentialButNoTokenInState` | `E6`/`E6.1`：凭证落盘、**状态文件无 token 无明文手机号** |
| `TestEmptyActivationCredentialsAreFieldLevel` | 空值 = 字段级（`invalid_activation` / `invalid_box_code`） |
| `TestActivationFailuresStayDistinguishable` | `L-A4`：四种失败**各自可辨**（含 `phone_masked`） |
| `TestUsedActivationCodeIsReportedAsUsed` | `L-A4` **一码一用**（重复使用报 `activation_code_used`） |
| `TestSecondLoginOmitsCredentialsAndStillReportsBoxCode` | `L-A2`：二次登录不带凭证、仍报 `boxCode` |
| `TestLoginWithoutBoxCodeOnRecordIsNotAnError` | `L-A5`：老记录缺 `boxCode` **不是错误** |
| `TestLogoutDeletesCredentialAndKeepsActivation` | `E7`：登出删凭证、**激活绑定与手机号保留** |
| `TestLocaleIsAcceptedWhileLoggedOut` | `E8` 规则 5 |
| `TestLocaleInvalidValueIsFieldLevel` | §2.5 |
| `TestUnreachablePlatformIsNotABusinessError` | §1.2：不可达是 **5xx**，不是被拒的输入 |

### `PR-2b2a` 接线：把端点挂进服务（✅ **已完成 2026-09-11**，§3.3.1 顺位 3 已批准）

**为什么必须动核心文件** —— 已核实的事实（2026-09-11）：

| 事实 | 证据 |
| --- | --- |
| 便携包打的二进制是 `cmd/octo-desktop`，**该文件在上游 `main` 上** | `git cat-file -e main:cmd/octo-desktop/main.go` 命中；`scripts/package-portable.mjs` 打包该 exe |
| 该入口是**唯一**能挂产品路由的地方 | 全仓 `server.New(` 只有两处：`cmd/octo/serve.go`、`cmd/octo-desktop/main.go`（都在上游） |
| `internal/server` **没有**可用的扩展缝 | 只导出 `ListenAndServe` / `ServeOn`；`s.mux` 私有，无 `Handler()` |
| **不能直接挂在 `s.mux` 上** | 上游测试 `TestRegisterRoutes_OnlyKnownUnauthenticated`（`auth_test.go:266`）断言**直接注册只允许 4 条**（health / version / mcp oauth / `/`），新增直接注册即**测试失败**；且直挂会绕过 `requireAuth`，比其它所有路由都弱 |
| 浏览器侧够不到端点不是因为鉴权，而是**没有路由** | `requireAuth`（`auth.go:290`）对 **loopback** 请求放行，桌面 webview 走的就是这条豁免 —— 与它今天调用 `/api/sessions` 等完全相同 |

**方案（顺位 2：只加一个通用缝，改动以行计）**

1. **`internal/server/server.go`** —— 加**一个可选、无产品名词**的字段，形状**照抄既有 `s.api` 的注册器**（不是给裸 `mux`）：
   ```go
   // OCTO-FORK: 产品路由的挂载缝；只暴露既有 s.api 注册器，不带任何产品词汇 —
   // see dev-docs-usdable/需求/20260911/开发计划.md §PR-2b2a
   MountAPI func(api func(pattern string, h http.HandlerFunc))
   ```
   并在 `s.api(...)` 注册区（native 块之后）加 3 行调用它。
   **为什么不给裸 `mux`**：`requireAuth` 是产品路由应有的鉴权（与其它 `/api` 一致），而直挂 `mux` 会被上面那条上游测试判失败。
2. **`cmd/octo-desktop/main.go`** —— 构造 `productruntime`（`productstate` / `credentialstore` / `productclient`，host 取 `productprofile`），设 `MountAPI`。产品词汇全部集中在这一处，**`internal/server` 里一个产品词都没有**。

**自证（§3.3.1 的三个数）**：核心文件 **2 个**；合计 **约 20 行**；`OCTO-FORK` 标记 **+2**。

**已否决的替代方案**

| 方案 | 否决理由 |
| --- | --- |
| 直挂 `s.mux`（省掉缝） | 上游 `auth_test.go:266` 会失败（4 条直挂是硬断言）；且绕过 `requireAuth`，比其它路由弱 |
| 在 hub 前面套一层反代（只动 `cmd/octo-desktop` 1 个文件） | 多一跳；`/ws` 与 SSE 必须穿过反代（本批次两者都依赖）；产品门要在外层重写，而其余中间件仍在内层 ⇒ **两处决定鉴权**。语义风险大于 3 行缝 —— §3.3.1 的「影响」是语义风险，不是文件数 |
| 让 `internal/server` import `internal/productruntime` | 上游将依赖 fork 包（合并即断），且产品逻辑落进上游文件，违反 `G1` |
| 另建 `cmd/buding-desktop` 入口 | `cmd/octo-desktop` 是 `package main`，启动逻辑不可导入 ⇒ 要复制约 600 行外壳代码，正是 §3.5 禁止的"第二份实现" |

**验收：** `L-A1a` `L-A2`–`L-A6` 的**端到端**路径可跑（§6.4 第 5 步：单元测试 + 端到端两条都要），随后 `P3` 的拆除条件才可能成立。

#### `PR-2b2a` · `L-A1a` 需求分析（§6.4 第 1 步，2026-09-11）

按 `开发规范` §6.4.1 的七项产出，**实施前逐项核实**：

| # | 项 | 结论 |
| --- | --- | --- |
| 1 | **依据** | `需求基线` `E1`（五字段激活）、`E6.1`/`E6.3`（两张字段表）、`E6.4`（首启时序）、`本地API契约.md` §2.2（`POST /api/product/login`）与 §1.2（信封分层） |
| 2 | **可见结果（一句话）** | 用户在拦截页填五个字段并提交 → **进入主界面**，授权页显示服务端返回的盒子编号与激活信息。 |
| 3 | **关键异常分支** | ① **本地校验拦截**：手机号/验证码/昵称格式不合法、昵称命中敏感词 ⇒ **不发请求**，字段级信封（`invalid_phone`/`invalid_code`/`nickname_format`/`nickname_sensitive`）。② **状态已存在时不得重复播种**（`E6.4`：只有"文件不存在"才播种）。③ **凭证明文可带机复制**（`L-E2`），但 **access token 绝不落盘**。*（平台侧四个激活码失败属 `L-A4`，响应丢失属 `L-A1b`，均不在本条。）* |
| 4 | **落盘 / 状态变化** | `product-state.json`：`installId`、`loggedIn=true`、`activated=true`、`activation.boxCode`（**服务端值**）、`account`、`credits`、`plan`。`credential.json`：`refreshToken`、`accountPhoneMasked`、`installId`。**`product-state.json` 里搜不到任何 token**（`PR-2a` 已有测试钉住）。 |
| 5 | **未决与阻塞** | **本条无阻塞**（`PQ25` 只卡 `L-A1b`、`PQ26` 只卡 `L-A4`）。前置：`PR-2b2a` 接线本身。**能判到什么程度**：接线后**端到端可在 CI 内判**（真 `internal/server` + 真 `productruntime` + 替身，不依赖 Sandbox）；**UI 人工走查**还需 `P3`（`devBackend.ts` 让路）+ `P2`（Sandbox 或替身接进运行态）。 |
| 6 | **能力检索（§3.5）/ 单一 owner（§3.8）** | 全部复用，**零新增能力**：契约与客户端 = `internal/productclient`（`PR-1`）；状态与凭证 = `internal/productstate` + `internal/credentialstore`（`PR-2a`）；五个端点 = `internal/productruntime`（`PR-2b1`）；**路由与鉴权 = `internal/server` 的 `s.api`**（自带 `requireAuth` + `no-store`，是唯一 owner）；中台替身 = `internal/productclient/clienttest`。**本 PR 新增代码只有"把 handler 挂进服务"这一步**。 |
| 7 | **边界与反直觉行为** | ① **界面显示的 `boxCode` 来自服务端，不是表单里用户填的值** —— 用户填的只用于"配对校验"（`E1` 规则 2 的一对多）。② **登出保留 `activated`**，只清凭证（`L-A3`）。③ **`/api/product/state` 返回未包裹的 JSON**，与其它端点的信封不同（`本地API契约.md` §2.1）—— 看起来不一致，但契约如此规定。 |

**实施顺序（TDD）**：先写 `internal/productruntime` 的**端到端测试**（用 `server.New` + `ServeOn` 起真服务，`MountAPI` 挂上 handler），**确认它以 `404` 失败**（语义红：缝还没接），再实现缝与装配。

### `PR-2b2b` 产品门 + window token（✅ **已完成，2026-09-11**）

`L-B1b` 本身。**为什么不与 `2b2a` 合并**：它是**另一件事、另一套机制** —— 端点保护靠 `requireAuth`（loopback 豁免，已存在），而 window token（`X-Octo-Window-Token`）是**产品门的窗口身份**，与"能不能调这个 API"无关。

#### 人工确认的三处决定（2026-09-11）

| # | 问题 | 决定 |
|---|---|---|
| 1 | 请求门拦到哪 | 曾定 **宽门：全部 `/api/*`**，**落地前实测推翻** ⇒ 改为**只覆盖本 fork 经 `MountAPI` 缝注册的产品路由**（`/api/product/*`，5 条）。推翻的实证：① 门是**请求头**校验，裸 `<img src="/api/…">` 子资源加不了头 ⇒ 聊天图片缩略图（`GET /api/uploads/{name}`，`ChatView.svelte:2761`）与 artifact 图片（`GET /api/sessions/{id}/artifacts`）被打成 `403 product_gate`；② 桌面壳 `hubAddr`（`cmd/octo-desktop/main.go:62`）与 CLI `localServe()` 探测的默认地址同为 `127.0.0.1:8088` ⇒ CLI `send_message` 被打死。取舍全文见 §4.1 第 12 行与 `待解决问题.md` `D-006` |
| 2 | 为送令牌而改 `bridge.go` | **接受**，见下方"动上游文件" |
| 3 | WebSocket 握手门 | **本轮不做**，只登记（`/ws` 的前端已带 `?window_token=`，后端不校验） |

#### 机制全貌（端到端）

```
桌面壳启动 ── crypto/rand 32 字节 → hex（仅内存，一次）──┬─▶ server.Config.WindowToken
                                                      └─▶ shellURL() 拼进 webview URL ?…&window_token=…
                                                                    │
前端 adoptWindowToken()（早于任何 fetch）──▶ sessionStorage ──▶ 后续请求带 X-Octo-Window-Token
                                                                    │
后端 productAPI 的 wrapper（缝交的注册器）：配置了令牌 ⇒ 比对，失败 403 {"error":"product_gate"}
                                                                    │
前端 api.ts:56 收到该 403 ⇒ productPhase = "blocked"
```

#### 动上游文件：怎么动、为何动（人工要求逐条说明）

| 文件 | 改动量 | 怎么动 | 为何必须动 | 为何不能更小 |
|---|---|---|---|---|
| `cmd/octo-desktop/bridge.go` | **+1 行** | `shellURL()`（`bridge.go:172`）在 `base + "/?" + desktopShellQuery` 之后追加一个 **fork 自有**片段函数（未生成令牌时返回 `""`） | `shellURL` 是窗口 URL 的**唯一构造点**（新建窗口与 `SetURL` 都走它），令牌只能从那里进 query —— 这正是 `P3` §3.2 定的投递方式 | 不能改 `shellURL` 签名：上游 `bridge_test.go` 的 `TestShellURL` 直接以 `shellURL(base, hash)` 调用并断言**精确字符串**，改签名会编译不过、改无令牌时的输出会断言不过 ⇒ 所以片段必须"无令牌时返回空串" |
| `cmd/octo-desktop/main.go` | **+1 行** | Config 里加 `WindowToken: windowToken()` | 门在 `internal/server`，而该包**不能**反向依赖 fork（否则每次上游合并都冲突） | 令牌只能从壳侧进 Config |
| `internal/server/server.go` | **+~50 行**（含注释） | `Config` 加 `WindowToken string`；把注册逻辑收進 `register(pattern, h, windowGated)`，由 `api`（不带门）与 `productAPI`（带门）调用；`MountAPI` 缝改交 `productAPI` | `s.api` 是**唯一**给 API 套 `requireAuth` 的地方（"a new route cannot forget it"）—— 门挂同一处，新路由同样遗漏不了；而缝是 fork 路由的**唯一**入口，门挂那里就成了"本 fork 路由"的精确范围 | 见下 |

**为什么门是"注册器的属性"而不是"路径前缀判定"：** 前缀式（`strings.HasPrefix(r.URL.Path, "/api/")`）本轮**实测有过失败模式** —— 它把浏览器带不上头的子资源（`/api/uploads/*`、`/api/sessions/{id}/artifacts`）和 CLI 的 loopback 路径一起拦了。改成注册器后：`/ws` 与全部上游 `/api/*` 被**结构性**排除（它们走 `api`），`/api/product/*` 被**结构性**包含（走 `productAPI`），既不需要白名单，也不会因上游新增路由而静默漏门。前缀字面量因此只留在 `internal/productruntime` 一处。

**范围代价（登记 `D-006`）：** `P3` §5.4.3「未登录时窗口发起的请求同样不可用」在上游 `/api/*` 上不由本门覆盖 —— 真正实现它需要服务端按 `internal/productstate` 的激活态校验，而不是把窗口令牌铺到全部 `/api/*`（失败模式已实测）。

**`internal/productruntime` 本轮不动**（比原设想更小）：门在服务端的单一注册点，runtime 无需知道令牌存在。

#### 能力检索（§3.5，只列本 PR 新增判断）

| 需要的能力 | 结论 |
|---|---|
| 常量时间比较 | **复用** `internal/server/auth.go` 的 `subtle.ConstantTimeCompare` 用法（`validateAccessKey`），不自写比较 |
| 写 403 JSON | **复用** `internal/server/server.go:1482` 的 `writeJSON` |
| 测试脚手架 | **复用** `mustServer` + `authRequest`（`auth_test.go`，走真 mux + 显式网络身份）与 `newMountedHarness`（`mount_test.go`） |
| 随机令牌 | **复用** 标准库 `crypto/rand` + `encoding/hex`，不引第三方（`go.mod` 不变） |
| 前端带令牌 | **复用** 已存在的 `WINDOW_TOKEN_HEADER` 与 `withWindowToken()`（`web/src/lib/api.ts:69-81`） |

#### 分析阶段发现的前端缺陷（一并修）

`refreshProductState()` 读状态用的是**裸 `fetch`**（`web/src/lib/product.ts:93`），**没带令牌头** —— 而 `login`/`logout` 都带了。门一生效，这一条会自己把自己 403，界面永远 `blocked`（且无出路）。改为经 `withWindowToken()` 发出。

#### 有界降级（§3.9）

| 情形 | 降级到 | 用户被告知 | 何时恢复 |
|---|---|---|---|
| 令牌生成失败（`crypto/rand` 出错，实际不会发生） | 无令牌 ⇒ 门**关**，且 `mountProductAPI` 按既有约定返回 `nil` ⇒ 产品路由**不注册** ⇒ 状态读 404 ⇒ 前端 `blocked` | 是（看到登录页而非主界面） | 重启；**绝不**"没有令牌就放行主界面" |
| 未配置令牌（`octo serve` / CLI / 浏览器直连） | 门**关**，`requireAuth` 的 loopback 豁免照旧 | 不适用（这是 CLI 正常路径，非降级） | 不适用 |
| 令牌不匹配（另一个 loopback 进程 / 陈旧标签页，访问**产品路由**） | `403 {"error":"product_gate"}` | 是（前端转 `blocked`） | 回到壳内窗口，或重启壳换新令牌 |

#### TDD 测试面（先红，逐条对应判据）

| 落点 | 断言 |
|---|---|
| `internal/server/product_gate_test.go`（新） | 无令牌 / 错令牌 → 403 且体为 `{"error":"product_gate"}`；正确令牌 → 放行；**未配置令牌 → 门关**（`octo serve` 行为不变）；**范围钉子（回归测试）**：配置了令牌时 `GET /api/uploads/{name}`、`GET /api/sessions/{id}/artifacts`、`GET /api/sessions` **都不得** 403；**`/ws` 不被拦** |
| `internal/productruntime/mount_test.go`（扩） | 配置了令牌时，真服务 + 真 socket 上：不带头的 `/api/product/state` → 403；带头 → 200 |
| `cmd/octo-desktop/product_test.go`（新） | 未生成令牌时片段为 `""`（**并据此钉住上游 `TestShellURL` 的精确形状**）；生成后形状正确且为 64 位 hex；`shellURL` 仅在令牌非空时变化 |
| `web/src/lib/product.test.ts`（扩） | `refreshProductState()` 发出的请求带 `X-Octo-Window-Token` |

#### 本轮明确不做

- **WS 握手门**（决定 3）：`/ws` 的 `?window_token=` 仍是装饰性携带，登记为后续项。
- **多窗口隔离**：一次壳启动 = 一个令牌；同进程内多窗口共享，不区分。


#### 落地实测（2026-09-11）

**TDD 红→绿逐条有据。** 四处测试面先写、先红（语义失败，非编译失败），再实现转绿：

| 测试面 | 首红证据 | 转绿后 |
|---|---|---|
| `internal/server/product_gate_test.go`（新，5 条） | `status = 200, want 403`（三条） | 全绿；上游 `internal/server` 全量 34s 通过 |
| `internal/productruntime/mount_test.go`（+1 条，扩 harness） | `GET /api/product/state without a token = 200, want 403` | 全绿（真服务 + 真 socket） |
| `cmd/octo-desktop/product_test.go`（新，3 条） | `token length = 0, want 64 hex chars`；`shellURL` 缺片段 | 全绿，且**上游 `TestShellURL` 保持绿**（形状未变） |
| `web/src/lib/product.test.ts`（+1 条） | `expected null to be 'tok'` | 前端全量 **65 文件 / 761 测试**通过 |

**实测改动量**（相对 `v1`）：

| 文件 | 增量 | 性质 |
|---|---|---|
| `internal/server/server.go` | **+49 −0** | 上游：`Config.WindowToken` 字段+注释、`windowTokenHeader` 常量、`windowAllowed()` 门与 `s.api` 接线 |
| `cmd/octo-desktop/bridge.go` | **+8 −1** | 上游：**功能仅 1 行**（其余是 `OCTO-FORK` 注释），把片段拼进唯一 URL 构造点 |
| `cmd/octo-desktop/main.go` | **+5 −0** | 上游：**功能仅 1 行** `WindowToken: windowToken()` |
| `cmd/octo-desktop/product.go` | +84 | fork 自有：令牌生成 / 片段 / 失败即不挂载 |
| `web/src/lib/product.ts` | +29 −? | fork 自有：`windowTokenHeaders()` 抽出，状态读补令牌头 |
| 测试 | +156 | 新增 3 个测试文件/用例组 |

**验收**：`fmt-check` / `vet` / `make test` / `norms-check` / `agents-check` / `brand-check` / `datapath-check` / `portable-check` 全过；**四个未接线守卫**（`release-config` / `release-profile` / `reuse` / `server-diff`）直跑全过 —— 其中 `server-diff-guard` 明确报告 `internal/server debt is within the recorded ceilings`；`cmd/octo-desktop` 的 `go vet` 干净；`svelte-check` **0 errors / 0 warnings**。

#### 收尾：`L-E5` 的「壳带入」其实在 `PR-2b2a` 就落地了

计划原把「locale 壳带入」挂在 `PR-2b2b`，实测**不需要再改任何东西**：`mountProductAPI` 已经把 `resolveLang()`（配置优先 → 否则问平台：macOS `AppleLanguages` / Windows `GetUserDefaultLocaleName`）交给 `productstate.Open` 的 `Locale`，而那正是 `E6.4` 规则 1 / `PQ18` 要求的「壳带入一次」。前端也不猜产品语言（实测：唯一的 `navigator.language` 命中在 `ChatView.svelte:2013` 的 `getExportLocale()`，那是**导出文档**的 `<html lang>`，先读应用已设的 `document.documentElement.lang`，与产品 UI 语言无关）。

**顺带修掉一处指向不存在文件的注释**：`web/src/lib/product.ts:20` 原文写「Header the server's product gate reads (`internal/productgate/gate.go`)」—— 全仓**没有** `internal/productgate` 包。前端是照一个从未落地的包名写的；已改为真实落点 `internal/server/server.go, windowAllowed`。



### `PR-2c` 拦截页与控制面失败面（✅ **已完成 2026-09-12 —— `L-A6` / `L-B2` / `L-B3` 三条全部落地**）

**为什么会有这一条。** 本轮全库核对发现 `L-B2`（未配置 / 无公钥两页）与 `L-B3`（控制面四档失败）在 §1 的 13 个 PR 里 **没有任何归属**（全文只在 §6 修订里顺带出现一次）；
而它们的设计文档 `P4-拦截页.md` 被 `web/src/lib/i18n.ts` 引用两处，**全仓库却不存在**（`V-15`）。同时 `L-A6` 要求的「清凭证」没有任何实现 ——
`productruntime.IsSessionExpired` 是**死导出**（零调用点），`Creds.Delete()` 只在 logout 端点调用（`V-16`）。三条闭环因此「无人能判是否完成」，与 `B7` / `B8` 当初的情形同类（§4.1 第 15 行）。

**范围（三件同一条 PR —— 它们共用同一个状态机）：**

| 闭环 | 要做什么 | 已可复用 / 缺什么 |
| --- | --- | --- |
| `L-B2` | 「未配置」与「无公钥」**两页不同文案**；不出现 URL、不出现技术名词 | ✅ **已落地 2026-09-12**：新增端点 `GET /api/product/control-plane`（`本地API契约` §2.13）把两个判据送到界面，前端 `refreshControlPlane()` 分页。判据本来就在：`productprofile.ControlPlaneConfigured()`（`profile.go:90`）、`HasTrustedKeys()`（`profile.go:97`）；服务端码 `control_plane_unconfigured`（`envelope.go:156`）。**缺前端视图与文案** |
| `L-B3` | `network_unavailable` / `upstream_unavailable` / `unauthorized` / `account_restricted` **四档各有 UI、重试性与凭证语义**；档位判定只在 `internal/productclient` 一处 | `statusForCode`（`envelope.go:118`）已有分档雏形。**缺四档 UI**；`account_restricted` 的客服入口依赖 `V-4`（客服渠道载体） |
| `L-A6` | 会话失效 → **清本地凭证** → 回拦截页 | `credentialstore.Delete()` 已在（`PR-2a`）；`IsSessionExpired` 已在但**无调用点**。**缺「失效即清凭证」的接线与时序** | ✅ **已落地 2026-09-12**：`internal/productruntime` 的 `failPlatform`（唯一汇合处，`runtime.go:346`）+ `forgetSession`（`runtime.go:369`，**先清凭证再应答**；清盘失败只记日志、不阻断「回拦截页」）；`envelope.go:76` 补 `ErrSessionExpired` → `401 unauthorized`；前端 `noteSessionLost`（`product.ts:103`）为规则唯一 owner。**落地时发现并修掉两条缺陷：`V-21`（会话失效被误报成 `503 network_unavailable`，前端因此给一个永远不可能成功的重试按钮）、`V-22`（登出清空 `productState` ⇒ 拦截页改成要重输**一次性**激活码 ⇒ 点一次登出永久锁死）。**残余**：触发点在今天只有一处 —— `PR-4b` 的目录拉取（`client.Bootstrap` 是首个走 `doAuthorized` 的调用），在此之前端到端到不了，故 `L-A6` 的**人工 E2E 验收要等 `PR-4b`** |

**第一件交付物是补出 `P4-拦截页.md`。** 本批次每个 PR 各自出方案（§2 末尾约定），所以本条**不在此处代写设计内容**；
该文档至少要覆盖：两个「未配置」页的文案与判据、四档的判定位置与各自的重试性与凭证语义、会话失效的识别与清凭证时序。

> **落地结果（2026-09-12，部分完成）：** ① `P4-拦截页.md` 已补出（`V-15` 收敛），含逐节「已实现 / 半实现 / 待实现」标注，避免它自己变成 `V` 类缺陷。② **`L-A6` 已闭环**（见上表）——
> 采用**先写测试**：新写的四条测试里有两条一开始就是红的，而它们转红的原因正是 `V-21`（会话失效被报成 `503 network_unavailable`），**缺陷是通过测试暴露的，不是先知道再补测**。另两条缺陷（`V-22` 登出永久锁死用户、`V-21`）已登记进 `需求基线` §5.6。
> ③ **`L-B2` / `L-B3` 于同日收尾完成**（见下方「完成记录」）：两个「未配置」页与四档 UI 均已落地，`E2E闭环清单.md` 的 `L-B2` / `L-B3` 随之由 ⬜ / ⚠️ 改为 ✅。**唯一未闭环的是客服渠道载体**（`V-4`，已登记 `TODO-06`）—— 该档按「文案 + 一个确定动作」处理，即 `P4-拦截页.md` §3.3 写明的约束。
> ④ **一处刻意的取舍**记录在 `P4-拦截页.md` §4.5.1：会话失效后**不重读** `/api/product/state` —— 重读会经 `productFetch` 递归，且服务端保留的 `activated` / 绑定号码恰好都没变，重读只多一次失败机会。

**前置：** `PR-2b2a`（拦截页 `BlockedView` 与产品门已在）、`PR-1`（两个判据函数已在）。

**待人工拍板（§4.1 第 17 行）：** ① 新增 `PR-2c` 这一条 PR 本身；② 「失效即清凭证」触及**认证面**（§3.7）。**两项均已由 2026-09-12 的人工选择"执行选型 A"确认**（③ 一并确认了 `P4-拦截页.md` §2/§3 的方案，含新增第 13 条本地端点）。

**完成记录（2026-09-12，`PR-2c` 收尾）：** 三条闭环全部落地 —— `L-A6`（见上表）、**`L-B2`**（两页 + 第 13 条端点 `GET /api/product/control-plane`，5 条经真服务的端到端测试 + 前端分页 9 条 + 两页 4 条）、**`L-B3`**（四档 UI：仅两档给重试、`account_restricted` 给「知道了」且**不清凭证**、`unauthorized` **不显示错误**；6 条组件测试）。`E2E闭环清单.md` 的计数 **`✅ 6 → 8`、`⚠️ 9 → 8`、`⬜ 19 → 18`**。**落地时被测试逼出两处方案缺陷**：① 分页优先级初稿写反（`hasTrustedKeys` 先判会让 `{false,false}` —— 正是今天的 `production.json` —— 弹出"有地址但没公钥"的假话）；② `account_restricted` 第一版**只给文案、没给动作**，`P4` §3.3 的"必须提供"一栏因此是空的。**两处都不是"实现写错"，是"方案写错"，由先红的测试暴露**（`开发规范` §6.4.5-7 的判据：先分清谁错，再改谁 —— 本次改的是方案与文档，不是测试）。

#### `PR-2c` 剩余部分（`L-B2` / `L-B3`）的开发计划（2026-09-12，§6.4 第 0–2 步）

**第 0 步 粒度自检。** 两条闭环各能用一句话说清"用户看到什么变了"，**且都不需要再拆**：

| 闭环 | 一句话 | 判定 |
| --- | --- | --- |
| `L-B2` | **构建缺中台地址或缺公钥时，用户在输入前就看到「这包不对」而不是先去撞一次失败** | ✅ 够小 |
| `L-B3` | **控制面失败时，用户看到能读懂的原因 + 该不该重试；今天他看到的是一片空白** | ✅ 够小 |

两者共用同一个界面（`BlockedView` 所在的那面墙）与同一个入口（`refreshProductState`），**合成一条 PR 是内聚而不是凑数**：拆开会出现两次"改同一面墙"的冲突。

**第 1 步 需求分析（七项，细节见 [`P4-拦截页.md`](../../2260906/技术方案/P4-拦截页.md) §2/§3，此处只记断言面）。**

1. **依据**：`需求基线` `A1` 规则 6/7、`C12`、`E12`、`E7`；`本地API契约.md` §1.2/§3；`P4-拦截页.md` §2（两页）、§3（四档）。
2. **可见结果**：见上表两句话。
3. **关键异常分支**（每条闭环 ≥ 1）：`L-B2` 的"新端点自己读不到"（→ 保守落在登录表单，**不许谎报"未配置"**）；两个布尔**同时**为假时 `hasTrustedKeys` 优先；`L-B3` 的 `account_restricted` **不清凭证**（与 `unauthorized` 反着来）。
4. **落盘 / 状态变化**：**无**（两个布尔是编译期 Profile 事实，不进 `product-state.json`；四档都不写盘，`unauthorized` 的清凭证由 `L-A6` 已落地的那条路负责）。
5. **未决与阻塞**：`V-4`（客服渠道载体）⇒ `account_restricted` 档本轮**只给"文案 + 一个确定动作"**，不留"请联系客服"却无联系方式的死路（登记 `TODO-06`）。**无硬阻塞**。
6. **能力检索（§3.5）与单一 owner（§3.8）**：
   - 两个判据**已有实现**：`productprofile.ControlPlaneConfigured()`（`profile.go:90`）、`HasTrustedKeys()`（`profile.go:97`）—— **不重写判据**，本 PR 只把它们送进界面。
   - 四档的**档位判定已唯一存在于 `internal/productclient`**（`statusForCode`，`envelope.go:118`）⇒ 前端**只消费档位，不重新分类**（`C12`）。
   - 拦截页外壳、`windowTokenHeaders`、`productFetch`、`ProductError` **全部复用**，不新增取数层。
   - 新增的只有**一个只读端点**（下 D1），理由与"为什么不塞进 `ProductStateDTO`"见 `P4-拦截页.md` §2.2。
7. **边界与反直觉行为**（要进测试注释）：
   - `windowToken()` 为空即 `phase="ready"` **不是降级**（`octo serve` 没有窗口身份，产品门对它不存在）。
   - 新端点读失败 ⇒ 落**登录表单**，这是**有界降级**：目标是"让用户能重试登录"，不是"假装没事"。
   - `network_unavailable` 与 `upstream_unavailable` **可重试性相同但归因不同**，必须分开（前者用户该查网络，后者只能等）—— `P4` §3.2。
   - **分页优先级**：`configured` 先于 `hasTrustedKeys`（`{false,false}` 时报「未配置」）。初稿写反，**落地时两条互相矛盾的测试把它逼出来了** —— 记录在此，因为这是"TDD 抓到方案缺陷"的一个实例。

**§3.7 人工确认登记**：2026-09-12 人工选择"执行选型 A"（＝按 `P4-拦截页.md` §2/§3 把 `L-B2`/`L-B3` 做完），**即确认 P4 的方案**，含"新增 `GET /api/product/control-plane`"这一条。新增端点属**扩展已登记的本地契约**（不是绕开架构），登记进 `本地API契约.md`。

**第 2 步 交付物与验收判据（可断言形式）。**

| # | 交付物 | 落点 | 断言 |
| --- | --- | --- | --- |
| D1 | `Deps.ControlPlane`（两个布尔，由 `cmd/octo-desktop/product.go` 从 `productprofile.Current()` 填入）+ `GET /api/product/control-plane` + 在 `Mount` 注册 | `internal/productruntime/runtime.go`、`cmd/octo-desktop/product.go` | 未配置构建返回 **200** `{"configured":false,"hasTrustedKeys":false}`；**登录前可达**（不要求 `loggedIn`）；经真服务 + 窗口门跑通（端到端） |
| D2 | `controlPlane` store + `refreshControlPlane()`，并入 `refreshProductState()` 的入口 | `web/src/lib/product.ts` | 四种结果的**优先级**：`configured=false` ⇒ `unconfigured`（**在前**，见 `P4` §2.2 的纠正）；`hasTrustedKeys=false` ⇒ `no_keys`；都正常或读失败 ⇒ `login` |
| D3 | `BlockedView` 的两个「未配置」页 + i18n 键（中英各一份） | `web/src/views/BlockedView.svelte`、`web/src/lib/i18n.ts` | 两页**文案不同**、**不出现 URL / 技术名词**；`no_keys` 优先于 `unconfigured` |
| D4 | 四档 UI：`network_unavailable` / `upstream_unavailable` 的可读文案 + **「重试」按钮**；`account_restricted` 的文案 + 确定动作；`unauthorized` **不显示错误**（回拦截页） | `web/src/views/BlockedView.svelte`、`web/src/lib/i18n.ts` | 今天 `503 network_unavailable` 落到 `fieldErrorKey`/`businessErrorKey` 的 default ⇒ **渲染空串**（用户什么都看不到）。断言改为：出现非空文案 + 可点的重试；`account_restricted` **仍保留凭证** |

**本轮明确不做（防扩张）**：`V-4` 的客服渠道载体（只留"确定动作"）；`D-006`（窗口门不覆盖上游 `/api` 路由）；`V-27`/`V-28`（`PR-5`）；`L-A6` 的端到端触发（要 `PR-4b` 的 `bootstrap`）。

**依赖**：`PR-2b2a`（`BlockedView` 与产品门已在）、`PR-1`（两个判据已在）。**无阻塞**。

### `PR-2d` 授权被平台否认时切回激活表单（`V-44`）（🚧 **方案中**，代码未写）

> **这一条是用户手工验收 `PR-4c1` 时问出来的，不是计划内的。** 他照我给的步骤重启了 `productstub`（V-40 要求重启），替身的内存账号因此被清空，而 `data/product-state.json` 仍写着 `activated: true` ⇒ 二次登录被平台按"激活失败"答复，界面在**没有激活码输入框的表单**上显示「激活码不正确」。`V-44` 记的是这件事的**全部**后果，不只是一次手滑。

#### 第 0 步 · 粒度自检

| 事项 | 是不是独立行为 | 判定 |
| --- | --- | --- |
| 「平台不认这份授权时，用户能不能在自己的界面上走出来」 | ✅ 是 —— 用户可见结果＝**拦截页是五字段还是两字段**，以及那句文案是否落在一个能补救它的表单上 | **`PR-2d`** |
| 客服渠道载体（文案里的"请联系客服"指向哪） | ✅ 是，但是**另一条**（`V-4` / `PQ26`），且需要产品/运营定值 | 留在 `V-4`（**本 PR 刻意不碰**） |
| 目录不可用时能不能发消息（`B4`/`B9`） | ✅ 是，判据 `L-C2` | 留在 `PR-4c` |

**结论：`PR-2d` 独立，且不并进 `PR-4c`。** 它与 `PR-2c` 同族（拦截页形状），但 `PR-2c` 已完成；它与 `PR-4c` 的交付物（目录降级与文案）完全不相交。**本 PR 只做一件事**：让本地那份 `activated` 在平台否认时**跟随平台**，从而让表单可补救。**它不造客服入口** —— 若用户的码确实已用尽（`activation_code_used`），出路仍然只有 `V-4`；但那时他看到的是**能做点什么的表单 + 正确文案**，而不是一个死结。

#### 第 1 步 · 需求分析（按代码核对）

**八条事实。**

1. **`/state` 的 `activated` 是本地文件字段的投影，不是平台实时事实。** `publicOf`（[`state.go:371`](../../../internal/productstate/state.go)）：`Activated: st.Activated || st.LoggedIn`。
2. **拦截页的形状完全由它决定。** `BlockedView.svelte:34` `const activated = $derived($productState?.activated ?? false)`；`:144-145` 在 `activated` 时把两个凭证字段送成 `undefined`；`:276` 的 `{#if !activated}` 决定那两口输入框渲染与否。
3. **本地这条路径完全没人动过 `activated`。** `handleLogin`（[`runtime.go:312`](../../../internal/productruntime/runtime.go)）→ `validateLogin` → 平台 `Login` → 失败即 `failPlatform`（`:344-347`）；`failPlatform`（`:543`）只在会话失效时清凭证，**从不碰激活状态**。
4. **"要不要传激活凭证"的决定在客户端，且与平台是否认账无关。** `validateLogin` 的 `activationAttempt := activationCode != "" || boxCode != "" || !rt.deps.State.State().Activated`（[`:456`](../../../internal/productruntime/runtime.go)）—— 已激活的安装因此**不发**这两个字段，而平台那边的判定只能靠它自己记录，两边**没有任何对齐机制**。
5. **契约已经承认这个回答。** 替身对"二次登录、本目录无对应账号、未带激活凭证"的回答是 `activation_invalid`（[`clienttest/server.go:342-347`](../../../internal/productclient/clienttest/server.go)，注释自述 "The registry does not name this case, so it is reported as the activation failure it is"）⇒ 这不是替身的怪癖，是**必须有客户端行为与之对应**的合法应答。
6. **`productstate` 没有任何"降低 `Activated`"的入口。** 只有 `ApplyLogin` 抬高（`:281-295`）与 `Logout` 动 `LoggedIn`（`:320-322`）；`forgetSession` 也只清 `LoggedIn`（`runtime.go:586-590`，E7：会话结束不是取消激活）。
7. **家族定义已存在，不要另造：** `L-A4` 的四个业务码＝`activation_invalid` / `activation_code_used` / `box_code_unknown` / `box_code_mismatch`；**`phone_mismatch` 是分开的一行**（它只回答"带凭证的尝试"）。
8. **前端已有"表单形状听服务端"的成熟做法。** `V-22` 的修法就是让 `logout()` 改成 `await refreshProductState()`（[`product.ts:212`](../../web/src/lib/product.ts)）—— 状态只有一个 owner（服务端），客户端不自己编。本 PR 沿用同一条原则，**不新增第二套判断**。

#### 第 2 步 · 方案与接缝

**服务端（权威半边）**：

- `internal/productstate` 新增 `WithdrawActivationClaim()`：`st.Activated = false`。**保留 `account` 与 `activation`** —— 它们是平台上次说的话（E7 一类：记录不因一次失败被抹掉），且重新激活会覆盖它们；`installId` 不重生成。
- `internal/productruntime.handleLogin` 在 `failPlatform` **之前**判定并调用：

  条件两条**同时**成立才收回 —— ① 本次请求**没带**任何激活凭证（`activationCode` 与 `boxCode` 去空白后都为空）；② 平台错误码属于那四个。**然后照旧应答**（错误码、信封、状态码一个字节都不改）。
- **不做的事**：不删凭证（登录被拒不是会话结束，`C12`）、不改 `loggedIn`、不动 `installId`、不改文案与码。

**前端（跟随半边）**：`BlockedView.doSubmit` 的**业务级**失败分支里 `await refreshProductState()`。**刻意不加"哪些码需要重读"的清单** —— 那会在前端造出第二份家族分类（§3.8）。代价是"验证码敲错"时多一次 `/state` 读；收益是规则只有一条：**失败之后，表单形状听服务端**（与 `V-22` 同一句）。字段级失败分支不重读（服务端状态没变）。

**作用域（§3.10）**：

| 维度 | 本 PR 管的 | 明确不管 |
| --- | --- | --- |
| 路径 | 只 `POST /api/product/login` | `bootstrap`、网关回合（一次被拒的回合不许取消激活）|
| 触发 | 平台**明确否认**（四个业务码）+ **本次没带激活凭证** | 传输失败 / 5xx / `invalid_code` / `code_not_sent` / `phone_mismatch` |
| 数据 | 只 `activated` 一个布尔 | `account`、`activation`、`credential.json`、`installId` |
| 环境 | 全 profile（不是 production 专属） | 客服渠道（`V-4` / `PQ26`）|

**上游足迹 0 字节**：改动全在 `internal/productstate`、`internal/productruntime`、`web/src/views`。

#### 第 3 步 · 钉子与变异

**七条钉子**（前两条是"能过"，后五条是"不许变坏"；最后一条在前端）：

1. `TestARefusedSecondLoginSwitchesTheWallBackToTheActivationForm`（**主钉**，`internal/productruntime` 外部包，走真 HTTP + 真替身）：本地 `activated: true` 且已登出，用另一个手机号做**两字段**登录 ⇒ 应答仍是 `activation_invalid`（**码不变**），而 `GET /api/product/state` 变成 `activated: false`，且 `account.phoneMasked` 与 `activation` 记录**仍在**。
2. `TestTheWithdrawnClaimCanBeReclaimedWithAFreshCode`：紧接第 1 条，用第二个夹具码走完整五字段激活 ⇒ `activated: true` 恢复。**证明这不是单向门。**
3. `TestATransportFailureNeverWithdrawsTheClaim`：关掉替身再登录 ⇒ `network_unavailable` ⇒ `activated` **不变**（本地故障不得取消用户的授权）。
4. `TestAWrongSMSCodeNeverWithdrawsTheClaim`：短信码错误 ⇒ 不变。
5. `TestAnAttemptThatCarriedAnActivationCredentialLeavesTheClaimAlone`：已激活的安装带一个错码提交 ⇒ `activation_invalid` ⇒ `activated` **仍为 true**。压的是条件 ①（"平台否认"必须与"这次没带凭证"同时成立）。
6. `TestTheRefusalInventedNoSession`：第 1 条之后 `loggedIn` 仍 false、`installId` 逐字不变、`credential.json` 未被写入（拒绝不是会话，也不是授权）。
7. **前端**（`BlockedView.test.ts`）：把 `/login` 桩成 `activation_invalid`、`/state` 桩成 `activated:false` ⇒ 提交后 **`#activationCode` 与 `#boxCode` 出现**，且「激活码不正确」仍显示（文案落在能做点什么的表单上）。

**四个变异**（每个都要落在预测位置）：

| # | 变异 | 应变红 |
| --- | --- | --- |
| M1 | 收回时不看错误码（任何平台错误都收回） | 3、4 |
| M2 | 不看"本次有没有带激活凭证" | 5 |
| M3 | 完全不收回（现状） | 1、2、7 |
| M4 | 前端删掉失败后的 `refreshProductState()` | 7 |

**手工验收**（就是用户刚走过的场景，现在应当走得通）：① 重启 `productstub`（内存账号被清）；② 保留 `data/product-state.json`（`activated: true`）启动客户端；③ 直接登录 ⇒ 界面**变成五字段激活表单**并显示「激活码不正确」；④ 用 `BUDING-DEMO-0001` + `BOX-DEMO-0001` 激活 ⇒ 进主界面。**注意第 ③ 步是本 PR 的用户可见结果** —— 它把"删 `data/product-state.json`"这个界面外的动作变成了界面内的。

#### 第 4 步 · 文档与契约

- **`本地API契约.md` §2.3**：补一句 —— 一次**未携带激活凭证**的登录若被平台以四个激活类码拒绝，本地 `activated` 随之降为 false（`/state` 可见），**应答码不变**；并写明它为什么是服务端的判定而不是前端的。
- **`E2E闭环清单.md` `L-A4`**：补一句 —— 四个码必须落在**能补救它们的表单**上；客服入口的残余仍归 `V-4`。
- **`需求基线`**：`E1` 规则 7 与 `E7` 各补一句 `activated` 的 owner 是平台、本地是投影（`V-44` 的正面结论）。
- **`中台交付包`**：**无新增**。这个答复已在 §3.2 的错误码表里。

---

### `PR-3` 拆除 `devBackend.ts`

**触发条件（写死，不留解释空间）：** `developer` 构建下，本地 Go 服务能真实应答 `/api/product/*`，且 `/api/product/*` 的链路可跑通 —— **即 Sandbox（或替身）通了之后**。这正是 `G5` 的判据：`rg devBackend web/src --glob '!dev/**'` 无命中。**注意：`PR-2b1` 落地后这个条件仍未达成**，因为端点还没挂进服务（`PR-2b2`），`devBackend` 依旧在 `fetch` 层劫持 `/api*`。

**保留期的硬约束（`G5`）：** 若为纯 UI 开发保留，必须是**显式且默认关闭**的开关，开启时界面/日志可见；**4 个 WS 事件在它下面不可测**。

### `PR-4a` 验签核心（✅ **已落地 2026-09-11**）

> **落地结果：** `internal/productclient/{policy,signing}.go` + 替身签名能力（`internal/productclient/clienttest/sign.go`）+ `developer.json` 测试锚；14 条测试，两条钉子经变异验证会红。**实现与本文档的差异只有一处**：原计划把 `SignPolicy` 写成"不会失败"的辅助函数，实现改为**返回 error**（密钥长度不对时报错而非 panic）—— 因为替身是可执行进程，panic 会杀掉服务而不是给出可诊断的错误。详见 §6 修订。

**为什么它是打底而不是闭环。** 它不产生任何用户可见行为 —— 与 `PR-2a` 同性质。它的判据是**单元测试**：给定一段字节、一个 `keyId`、一个签名，判定"接受/拒绝"是否正确。后续 `4b`/`4c`/`4d`/`4e` 都建立在它之上。理由同 §2 `PR-1` 的范围修正：让本轮**唯一没有现成实现**的一处独立出来，一次做对。

**做：**

1. `internal/productclient/signing.go` —— **签名与验签的唯一一份实现**（§3.3 写死的约束：不得让签名方与验签方各写一份）。
   - 验签：输入 = **被签名的那段原始字节** + `keyId` + base64 签名；用 `crypto/ed25519`（**stdlib，复用，零新依赖**）对 `productprofile.TrustedKeyIDs[keyId]` 的公钥验签。
   - 签名：同一份实现对测试/替身可逆调用（替身要产出真签名）。
   - 四道校验，**任一条失败即 fail-closed**（`B2` 规则 2）：① 签名；② `keyId` 在 `trustedKeyIDs` 内；③ `audience` 等于本产品；④ 时钟窗口（`issuedAt`/`expiresAt`，含允许的偏斜），**时钟不可信时拒绝而非放行**。
2. 信封解析：`data.policy` 以 `json.RawMessage` 取出（**保留原始字节**，`encoding/json` 不重排 RawMessage 内容）+ 同级 `data.policySignature{keyId,sig}`。`policy` 内部字段（`policyVersion`/`catalog`/`capabilities`）此时只解析 `4a` 要用的那几个。
3. 复用信任锚 `internal/productprofile`：`developer.json` 的 `trustedKeyIDs` 填入**一对测试密钥**（§4.1 第 1 行已批准；`production.json` **不变**，仍 fail-closed）。
4. 替身（`clienttest`）获得**签名能力**（`PR-1` 的四个端点不验签，故此前没有）。
5. 测试（`L-C1a` 的验签半边）：**篡改一处字节 ⇒ 拒绝**；`keyId` 不在信任锚 ⇒ 拒绝且**不请求网络重试**；`audience` 不符 ⇒ 拒绝；过期/未生效 ⇒ 拒绝；时钟超前/回拨 ⇒ 拒绝；**用替身签的真签名 ⇒ 接受**；`policy` 内出现第二份 `policyVersion` ⇒ 被忽略（版本只取签名覆盖范围内的那份）。

**不做（留给 `4b`+）：** 拉取、`data/catalog.json` 缓存、版本单调与 TTL/ETag 刷新（`4b`）；三条降级与文案（`4c`）；投影与上屏（`4d`）；`capabilities` 的**消费**（`4e`）。`4a` 不做任何网络请求，也不写任何文件。

**验收：**

- 上列 7 类断言**各自**可被触发并断言；**先红后绿**。
- 反向钉子：把验签函数换成 `return nil` ⇒ 上列拒绝类断言**必须全部转红**（证明它们断言的是验签本身，而不是"某处恰好报了错"）。
- `go test ./internal/productclient/` 与 `./internal/productprofile/` 全绿；`make vet`、`gofmt -l .` 干净。

### `PR-4b` 目录获取与缓存（✅ **已完成（2026-09-12）**；方案见下方 §第 0–2 步，完成记录见本节末）

#### 第 0 步 · 粒度自检

§1 表把这条 PR 写成四件事："目录拉取 + `catalog.json` 缓存 + 版本单调 + TTL/ETag 刷新"。按 `E2E闭环清单` §2.0 的判据（**一条闭环 = 一次可独立判定的用户可见行为**）逐项过：

| 机制 | 是不是独立行为 | 判定 |
| --- | --- | --- |
| 拉取 + 验签 + 落缓存 | ✅ 是 —— 用户可见结果＝"模型列表来自签名目录"（`L-C1a` 本体的判据） | **留在 `PR-4b`** |
| 版本单调（拒绝回滚版本） | ❌ 不是 —— 它是**同一行为的另一个结局**（拿到了、但版本比缓存旧）。`§2.0` 明写"同一行为的不同结局算一条" | **留在 `PR-4b`**，写进 `L-C1a` 的异常分支栏（见第 1 步的缺口 ③） |
| TTL 到期后的条件请求（`ETag`/`304`）与周期刷新 | ❌ **不是** —— 它的可见结果不是"目录来自签名目录"，而是"**目录过期时还能不能继续用**"，那正是 `L-C2`（降级）的判据；且 `L-C2` 的三条降级里有一条就是"陈旧 + 刷新失败" | **移出，归 `PR-4c`** |

**结论：`PR-4b` 收窄为「拿到了、验过了、存对了、旧的不要」；「过期了怎么办」归 `PR-4c`。** 这样`4b` 有可独立判定的结果（落盘的 `catalog.json` 内容正确且可验签），`4c` 的降级判据也拿到了它需要的前提（缓存里有 `expiresAt`）。**已同步改 §1 表两行。**

#### 第 1 步 · 需求分析（六处**必须在开工前定**的缺口）

`L-C1a` 的行文是"拉签名目录 → 验签 → 落缓存"，异常分支只写了"签名不过 → 不上屏"。按契约（`中台交付包` §4.1 第 5/6 行、§4.3、§4.3 末表）核对后，有六处**行文没有覆盖、而实现必须选一个**的地方：

① **入口未定（二选一，必须定）**。契约里有两条路能拿到目录：`bootstrap` 的签名信封内含 `policy.catalog`（§4.1 第 5 行），以及 `GET /v1/catalog/models` 返回"同一份已签名 `policy.catalog`"（§4.1 第 6 行，用于主动刷新）。**现状两条都没实现**：`dto.go:110-113` 的路径常量只有 sms / login / refresh / bootstrap（**没有 `/catalog/models`**），而 `BootstrapData`（`dto.go:190-199`）**按合同明确排除 catalog**。

> **建议：`4b` 走 `bootstrap`**（扩 `BootstrapData` 承载目录信封），**`/catalog/models` 作为客户端方法留到 `4c` 的条件请求**。理由有两条，都不是偏好：㈠ `bootstrap` 是**登录后本来就该走的**，而它今天**没有任何生产调用点** —— 这正是 `L-A6` 端到端走不通的根因（`V-29`），走它一次性解决两个问题；㈡ `/catalog/models` 存在的意义是"不重新登录也能刷新"，那是 `4c` 的场景。

② **缓存的 owner 未定（`§3.8` 单一 owner，必须定）**。`data/catalog.json` 的写者是谁？**不能塞进 `internal/productstate`** —— 该包的 owner 声明是 `product-state.json`（`E6.1`），一个包管两个文件会让"state = 磁盘状态"这条映射失效（`P4-拦截页.md` §2.2 已经因同一理由否掉过一次把编译期事实塞进 `ProductStateDTO`）。

> **建议：新包 `internal/catalogstore`**，与 `productstate` / `credentialstore` 同构（`Open` / 读 / 原子写 / schema 版本）。它需要一份 §3.5 能力检索增量（见第 2 步）。

③ **`L-C1a` 的异常分支栏不完整（要改文档）**。契约 §4.3 末表要求客户端**拒绝回滚版本**（防降级攻击）、校验 `audience`、校验时钟窗口；而 `signing.go` 已定义 `ErrAudience` / `ErrClockWindow` / `ErrBadSignature` / `ErrUntrustedKey` / `ErrPolicyMalformed`，**唯独没有"版本回滚"这一类** —— 说明版本比较**不在验签层**，它属于缓存层（验签通过后才可能与缓存比较）。

> **动作**：① `L-C1a` 的异常分支栏补上"版本回滚 → 拒绝且不改缓存""`audience` 不符 → 拒绝""时钟窗口外 → 拒绝"（同一行为的不同结局）；② `internal/catalogstore` 定义自己的拒绝原因（**不往 `signing.go` 里加** —— 验签层不知道缓存，加进去会引入一条反向依赖）。

④ **触发时机未定（要定，否则会两处拉）**。契约 §4.3 末说"**启动**异步刷新"，但未登录时按 `L-B2` 拦截页会挡住主界面，拉了也没人用；而目录信封按"无凭证依赖"义务**不需要 token**（所以"未登录也能拉"在技术上成立，但那不是需求）。

> **建议：登录成功后拉一次**（即 `bootstrap` 顺带，与 ① 同一条路径），**已登录的启动路径只读缓存 + 按 TTL 判断是否该刷新（`4c`）**。这样**全仓只有一处发起拉取**，`grep` 可断言。

⑤ **`L-A6` 与 `L-A2`/`L-A3` 的端到端测试顺带补（`V-29`）**。走 `bootstrap` 意味着**首个 `doAuthorized` 调用诞生**，`L-A6` 的链路随之可达；同时 `mount_test.go` 缺的"二次登录"与"登出→再登录"两条端到端测试也有了真实调用链可依。

> **动作**：`PR-4b` 的验收**必须包含**这三条端到端测试，否则 `L-A6` 仍不满足 §6.4 第 5 步。

⑥ **失败面要落到哪（`§3.9` 有界降级）**。拉取失败/验签失败时用户看到什么？**`4b` 不做 UI**（那是 `4c` 的三条降级），但 `4b` **必须把"失败原因"作为可判定的返回值暴露出来**（`productclient` 的错误值 + `catalogstore` 的拒绝原因），否则 `4c` 无从分辨"网络不通"与"签名不过"。

> **动作**：`4b` 的测试里断言**四种拒绝原因可区分**（传输失败 / 签名不过 / 版本回滚 / 无缓存），**不在 `4b` 里决定文案**。

#### 第 2 步 · 开发计划（交付物 + 能力检索增量 + 测试红清单）

**能力检索增量（`§3.5`，只列 `4b` 新增的判断）**

```text
既有能力检索：internal/atomicfile（一份原子写、两个调用方 —— 直接复用，见 §3.5）
              internal/productprofile.TrustedKeyIDs（信任锚唯一 owner —— 直接复用）
              internal/productclient/signing.go 的 PolicyEnvelope.Verify（验签唯一实现 —— 直接复用）
              internal/productclient/client.go 的 doAuthorized（已含 401 单飞刷新 + 一次重放 —— 直接复用）
              internal/productstate（同构的 store 形状：Open/读/原子写/schema 版本 —— 抄形状，不抄代码）
为什么不能复用：**缓存包无现成实现**。全仓无 catalog.json 读写者；`productstate` 不能兼任（它
                的 owner 是 product-state.json）；`internal/agentprofile/store.go` 的 store 与本包类型耦合。
复用后的形态：新建 internal/catalogstore（约 1 个文件 + 1 个测试文件）：承载 catalog.json 的
              原子读写、schema 版本、以及**版本单调拒绝**。**不新建**：HTTP 客户端、SSE、验签、
              信任锚、原子写、401 刷新 —— 全部复用上面五处。
```

**交付物（4 件）**

| # | 落在哪 | 做什么 |
| --- | --- | --- |
| 1 | `internal/productclient/dto.go` | `BootstrapData` 扩字段承载目录信封（按 ① 的建议）。**字段名照 `中台交付包` §4.3**（`data.policy` + `data.policySignature`），不新造 |
| 2 | `internal/catalogstore/`（新建） | `catalog.json` 唯一 owner：原子写（复用 `atomicfile`）、schema 版本、`catalogVersion` / `fetchedAt` / `expiresAt` / `keyId` / `audience` 元数据、**版本单调拒绝**、四种拒绝原因可区分（缺口 ⑥） |
| 3 | `internal/productruntime/` | 登录成功后（`handleLogin` 的 `ApplyLogin` 之后）调用 `bootstrap` → 验签 → 落缓存；**拉取只有这一处发起**（缺口 ④，`grep` 可断言） |
| 4 | 测试 | 见下 |

**测试红清单（先红后绿；每一条对应一个判据）**

| 测试 | 钉住什么 |
| --- | --- |
| `TestBootstrapCarriesTheCatalogEnvelope` | `BootstrapData` 真的承载了签名信封（缺口 ①） |
| `TestCatalogIsWrittenAtomicallyAndVerifies` | 落盘的 `catalog.json` **可被独立验签**（一条闭环的判据本体） |
| `TestRolledBackVersionIsRefusedAndCacheUntouched` | **版本单调**：回滚版本被拒**且缓存未改**（缺口 ③，防降级攻击） |
| `TestUntrustedKeyIsRefused` / `TestBadSignatureIsRefused` | 验签失败 → 不落盘（`L-C1a` 的异常分支） |
| `TestFetchFailureReasonsAreDistinguishable` | 四种拒绝原因可区分（缺口 ⑥，`4c` 的前提） |
| `TestOnlyOnePlaceFetchesTheCatalog` | 全仓**只有一处**发起拉取（缺口 ④，可用 `grep` 断言代替） |
| **`TestSessionExpiryIsReachableThroughBootstrap`** | **`L-A6` 端到端**（缺口 ⑤，`V-29`） |
| **`TestSecondLoginEndToEnd`** / **`TestLogoutThenReloginEndToEnd`** | **`L-A2` / `L-A3` 的端到端**（缺口 ⑤，`V-29`） |

#### 第 2 步补充（开工时定下的实现级决定）

上面六处是**需求级**缺口。真去读代码又碰到八处**实现级**决定，全部记在这里 —— 计划里没写的实现选择，事后没人能判断它是有意的还是随手写的。

**① 落缓存有个会「静默写坏签名」的陷阱（已实测，不是推测）。** 需求说缓存**原始签名信封**、客户端**不做任何规范化**（`中台交付包` §4.3）。而 Go 的 `json.Marshal` 对 `json.RawMessage` 做两件事：**压缩空白**、**HTML 转义**。实测五种输入：

| 输入（`policy` 的字节） | `json.Marshal` 写出 | `Encoder` + `SetEscapeHTML(false)` | 读回是否逐字节相同 |
| --- | --- | --- | --- |
| `{"a":"x&y<z","b":1}` | `{"a":"x\u0026y\u003cz","b":1}` | 原样 | ❌ / ✅ |
| 带缩进的 `{\n "a": 1\n}` | `{"a":1}`（压缩） | `{"a":1}`（同样压缩） | ❌ / ❌ |
| 线上就是 `x\u003cy` | 原样 | 原样 | ✅ / ✅ |
| 中文 | 原样 | 原样 | ✅ / ✅ |
| `\"` 与 `\\` | 原样 | 原样 | ✅ / ✅ |

**第一行不是理论风险**：模型展示名里出现 `&` 或 `<` 完全正常（`Tools & Agents`），而 `json.Marshal` 会把签名覆盖的字节改掉 ⇒ **缓存写下来就已经验不过**，表现为"这台机器永远缓存不住目录"，且看不出原因。

**决定**：写 `catalog.json` 用 `json.Encoder` + `SetEscapeHTML(false)`；**并在写之前自检** —— 把要写的字节按读路径解析回来，`policy` 不逐字节相同就**拒绝写**并报错。即"**不写一份自己读不回来的缓存**"。第三行（平台自己缩进）因此变成一条**响亮的失败**而不是一条静默的坏缓存；修法是局部换一个 verbatim writer，已在错误值上写明。

**② `expiresAt` 取两者的较早者。** 契约给了两个时间轴：policy 的签名窗口（`issuedAt`/`expiresAt`）与目录自己的新鲜度（`catalog.ttlSec` + `issuedAt`）。`4c` 需要**一个**字段判断"离线还能不能用"，取 `min(policy.expiresAt, issuedAt + ttlSec)`（`ttlSec` 缺失或非法时不取第二项）。保守方向，且两条都成立才可用 —— 政策窗口过了要刷新，目录本身陈旧了也要刷新。

**③ 单调性的轴是 `catalog.version`，不是 `policyVersion`。** `需求基线` B2 规则 3 写的是 `catalogVersion`；两个版本是两个事实，把它们合成一个判据会让"改了策略但目录没变"被判成回滚。`policyVersion` 的消费（撤销/最小客户端版本）不归 `4b`。

**④ 缓存损坏时**单调性的比较基准随之消失**（残留缺口，写明而不是假装没有）。** 文件读不出来就没有"当前版本"可比，此时只能接受新目录 —— 否则用户被永久锁在损坏文件外面、且没有任何恢复路径。**代价**：能改本地文件的攻击者可以"先弄坏缓存再重放旧目录"。判定为可接受（能改本地文件的攻击者已在机器内），但登记在此，因为它是一个**有意的**取舍。

**⑤ 四个拒绝原因是本 PR 的交付物之一，但**不是四类错误****。** 定义 `catalogOutcome` 一个枚举：`ready` / `transport`（没到达或 5xx）/ `verification`（签名、`audience`、时钟窗口）/ `rolled_back`（签名有效但更旧）/ `unavailable`（没有信封可用，也没有可用缓存）。**`4b` 只做分类与日志，一个字的文案都不定**（`4c` 的三条降级与四种文案消费这个枚举）。平台没给信封时归 `unavailable`：用户面对的是"没有目录"，而分类是按**用户面对什么**分的，不是按**哪一层先发现**。

**⑥ 信任锚与受众走 `Deps` 字段，不 import。** 与 `ControlPlane` 同形：`productruntime` 不 import `internal/productprofile`（判据的 owner）也不 import `internal/brand`（受众 = `brandId` 的 owner），装配层把值填进 `Deps`。好处有三：owner 不变、`productruntime` 保持"只转发"、四种失败面在测试里可造。**受众的取值来源是 `brand.Load().BrandID`**（不写品牌字面量，`开发规范` §3.1 规则 2）。

**⑦ `Deps.Catalog` 为 nil 时**根本不发起拉取****（没有地方可放）。这是"装配失败也不要让登录挂掉"的边界，不是降级路径：`4b` 里拉取只有一处发起，没有缓存时拉回来也没人能用。

**⑧ 替身补三个故障开关，其中一个让 `L-A6` 第一次端到端可达。** `RefuseSessionsAfterLogin()`（登录成功但发出的令牌不被承认 ⇒ `bootstrap` 401 → 刷新被拒 ⇒ `ErrSessionExpired`）、`FailBootstrap(status, code)`（会话正常、目录拉不到）、`SetCatalogVersion(v)`（签名有效但更旧的目录）。**第一条是本 PR 顺带解决 `V-29` 的关键**：`bootstrap` 是首个 `doAuthorized` 调用，它一进 `handleLogin`，`L-A6` 的清凭证链路就从"机制在、触发点不存在"变成真的能走一遍。

**⑨ 受众一致性加一条钉子。** 替身签的 `FixturePolicyAudience` 必须等于 `brand.Load().BrandID`，否则 developer 构建**对每一份目录都验不过**，而现象读起来像"中台坏了"。与已有的"替身密钥 = developer 信任锚"钉子（`profile_key_test.go`）同一性质，同一个文件里加一条。

**⑩ 测试用的锚与受众必须是**显式值**，不能读 `productprofile.Current()`**。`Current()` 由构建标签决定，`test-production`（`PR-9` 接线）会以 `product_production` 跑同一批测试 —— 那时 `Current()` 是 production 锚（空 `trustedKeyIDs` + `.invalid` 主机），任何依赖它的测试都会以"环境不同"失败。故测试填 `clienttest.FixtureSigningPublicKey()` + `brand.Load().BrandID`，两个都是与标签无关的显式事实。

**本 PR 明确不做**：TTL 到期后的条件请求（`ETag`/`304`）与周期刷新、三条降级语义与文案、投影与前端渲染、`/catalog/models` 客户端方法 —— 前三件归 `PR-4c`，后两件归 `PR-4d`。

**开工前置**：本方案的六处缺口里 ①②③ 是**文档级改动**（`L-C1a` 的异常分支栏、`catalogstore` 的 owner 声明、§1 表已改），其余按上表执行。**代码未写，测试未红。**（*本节为开工前的方案原文，保留不改，以便对照实际落地时的偏差。*）

#### 完成记录（2026-09-12）

**落地物（4 件，与第 2 步的交付物表逐条对应）**

| # | 落在哪 | 实际做了什么 |
| --- | --- | --- |
| 1 | `internal/productclient/dto.go` | `BootstrapData` **内嵌** `PolicyEnvelope`（不是加一个命名字段）—— 合同里 `policy` / `policySignature` 与 `account` 同级，内嵌才对得上这个形状，也不用手写解码器 |
| 2 | `internal/catalogstore/`（新建，2 文件） | `catalog.json` 唯一 owner：`atomicfile` 原子写、`schemaVersion`、五项元数据、**版本单调拒绝**、五种拒绝原因（`ErrNoCache` / `ErrCorrupt` / `ErrIncompatibleVersion` / `ErrRolledBack` / `ErrNotByteExact`） |
| 3 | `internal/productruntime/catalog.go` + `runtime.go` | `fetchCatalog` 是**全仓唯一拉取点**；`handleLogin` 在 `ApplyLogin` 与 `Creds.Save` 之后调用它；`catalogOutcome` 五档 |
| 4 | `cmd/octo-desktop/product.go` | 装配 `Catalog` 与 `CatalogTrust`（信任锚取 `productprofile.Current().TrustedKeyIDs`、受众取 `brand.Load().BrandID`、`Skew = 2min`）。**缓存打不开只告警、不卸载路由** —— 与 `product-state.json` 不同，缓存是可再取的副本，不值得为它停掉整个产品面 |

**按交付物表的偏差（写明，不留悬空）**

1. **测试名与计划表不完全一致**：计划写的是 `TestCatalogIsWrittenAtomicallyAndVerifies` / `TestRolledBackVersionIsRefusedAndCacheUntouched` / `TestUntrustedKeyIsRefused` / `TestBadSignatureIsRefused` / `TestFetchFailureReasonsAreDistinguishable`；实际落成 `TestCatalogIsCachedAndVerifiesOfflineAfterLogin`、`TestARolledBackCatalogDoesNotReplaceTheCache`（另有一条 `TestARefusedCatalog...` 族）、`TestCatalogOutcomesAreDistinguishable`、`TestARefusalCarriesTheUnderlyingError`。**计划的测试名是意图占位**，实际名按"钉住哪条行为"重取；覆盖面对得上，逐条映射见 `E2E闭环清单` 的 `L-C1a` 行。
2. **`TestCatalogIsCachedAndVerifiesOfflineAfterLogin` 比计划多断言了一件事**：验签前后各取一次替身的 `BootstrapCount`，断言**没有增加**。理由：`L-C1a` 的判据是"离线可用"，而"读缓存时顺手又拉了一次"会让这个判据看起来成立、实际不成立。计划的表里没有这条，是本轮补的。
3. **新增 `compareCatalogVersions`**（计划里没有这一件）：版本单调**必须先有一个次序**，而计划只定了"比哪个字段"。详见下方缺陷 ④。
4. **`ErrNotByteExact` 是计划外新增的拒绝原因**（计划只有四种）。它不是第五种**用户面对的**结局（仍归 `verification`），而是**写盘前的自检**：字节保不住就拒绝写，而不是写一份永远验不过的缓存。理由见第 2 步补充 ①。

**本 PR 暴露并修掉的四处缺陷（都由先红的测试逼出，不是先知道再补测）** —— 第 5 条是文档级改正；**第五处代码缺陷在自审补记里**（回滚守卫只认 -1，见下文）

1. **替身自己在毁签名。** `clienttest.writeData` 用默认 `json.Encoder`，`&` / `<` 被转义成 `\u0026` / `\u003c`，而签名覆盖的是**转义前**那串字节 ⇒ 任何含 `&` 的模型名（`Tools & Agents`）到客户端都验不过，现象读起来像"中台签错了"。**它与第 2 步补充 ① 是同一个陷阱的两端**：计划里想到了客户端写入器，没想到替身也有一个。修法 `SetEscapeHTML(false)`，并在 `wire_test.go` 加钉子（该文件此前不存在，本次新建）。
2. **`"policy": null` ≠ 字段省略。** `json.RawMessage` 会把 JSON 的 `null` 解成 4 字节字面量，于是"平台没给信封"有两种写法、而 `len(policy) == 0` 只认其中一种；只认一种的实现会把一个 `null` 当成"有 4 字节的目录"。新增 `PolicyEnvelope.IsEmpty()`，把缺席收成**一个**判据（而不是让每个调用方各写各的）。
3. **`catalog.json` 不能缩进。** `json.MarshalIndent` / `json.Indent` 会重排**策略内部**的空白 ⇒ "给人看的缩进"与"签名覆盖的字节"不可兼得。取字节保真：缓存写成紧凑单行，可读性交给诊断包里的信封。
4. **版本比较不能是字符串比较（本 PR 自己引入、又被本 PR 的测试当场抓住）。** 计划的第 2 步补充 ③ 只说"单调性的轴是 `catalog.version`"，**没有说怎么比**；初版实现写了 `current > offered`（Go 的字符串比较），因为 `2026-09-12.1` 这种形状看起来天然可排。补钉子时才发现它在**同一天的第十个目录**上就断：`"…-09.10"` 的字符串序**小于** `"…-09.9"` ⇒ 当天的第十次刷新会被判成回滚，**从此拒绝刷新到第二天零点，且日志里没有任何线索**。已改为解析成 `(年,月,日,计数器)` 后按数值比；形状解析不出来的版本退回字符串比较（**不假装有次序**，也不因为解析不出就跳过防降级）。**这条值得单独记一笔**：它不是"计划没想到的风险"，而是"计划想到了风险却给了个看似够用的实现"，靠的正是 `§6.4.5` 要求的那句"先证明钉子会红"。
5. **（文档级）`nickname` 在二次登录里是必填**。写端到端测试时按"二次登录只需手机号 + 验证码"的直觉省略了它，被本地校验以 `nickname_format` 拒掉 —— 而 `本地API契约` §2.3 的请求字段表**明确列了** `nickname`。**这是测试写错，不是实现缺陷**；已按契约改正测试，并把这一条写进 `L-A2` 行，避免下一个人再猜一次。

**验收与验证**

- `go test ./...`（含 `-race` 目标）、`go vet ./...`、`gofmt -l .` 全绿；`cmd/octo-desktop` 单独构建与 `go vet` 通过。
- 六个守卫脚本（`datapath-guard` / `norms-guard` / `reuse-guard` / `server-diff-guard` / `release-profile-guard` / `release-config-guard`）全部通过。
- **变异验证**（`开发规范` §6.4.5：先证明钉子会红）：去掉 `SetEscapeHTML(false)` ⇒ 5 条红；去掉版本比较 ⇒ 5 条红（跨两包，含挂载态端到端）；去掉字节自检 ⇒ 红；去掉 fail-closed（验签失败当成功）⇒ 3 条红；**把版本比较退回字符串序 ⇒ `TestVersionOrderingIsNotStringOrdering` 红**（并给出"cached `…-09.9`, offered `…-09.10`"的原文错误，证明它抓的正是那一天）；**自审轮追加两条**：回滚守卫回 `case -1:` ⇒ `TestARollbackOfMoreThanOneDayIsStillARollback` 红而旧测试仍绿；删掉 `catalogExpiry` 的 TTL 夹取 ⇒ `TestAnAbsurdTTLDoesNotMakeAGoodCatalogLookExpired` 红在预测位置。
- `E2E闭环清单` 复算并**机器核对**：`✅ 12 ｜ ⚠️ 5 ｜ ⛔ 1 ｜ ⬜ 17`（合计 35）。

#### 自审补记（2026-09-12，同一 PR）

合入 `v1` 后的自查发现**这一轮的修改整个没提交**（`git status` 在 `v1` 上显示 8 个文件是脏的），而 `HEAD` 里仍是自审前的实现。两件事：

**流程问题。** `v1` 上留脏工作区会让下一个 PR 误带进去。已按 `上游合并策略` 走：起 `fix/pr-4b-self-review` 分支 → 两个提交 → `--no-ff` 并回 `v1`。

**第五处缺陷（`§6.4.5` 那条"先证明钉子会红"漏网的一条）。** 回滚守卫写成 `case -1:`，而 `compareCatalogVersions` 返回的是**首个不同部分的差值**（跨三天是 `-3`，不是 `-1`），不是 `-1/0/1` 的判决 —— 于是它只拦住"正好旧一天"的回滚，旧三天的目录会**静默覆盖当前目录**。

这条最值得记的地方是**为什么原先没被发现**：已有的 `TestVersionOrderingIsNotStringOrdering` 用的正是 `.9` vs `.10`，**差值恰好为 1**，所以守卫是错的而测试全绿。已补 `TestARollbackOfMoreThanOneDayIsStillARollback`（跨三天），**在变异回 `case -1:` 时它红、原测试仍绿**（原文错误：`Put a catalog three days older = <nil>, want ErrRolledBack`），证明这条钉子才是真正压住守卫的那条。**教训：一条测试恰好落在标量差为 1 的位置时，它测不出"守卫只认 -1"。**

**另两处，同轮一并处理：**

- **`catalogExpiry` 的 TTL 回绕。** `issuedAt + ttlSec` 是 `time.Duration` 上的乘法，而 `ttlSec` 由中台给。大到溢出的值回绕成负 duration ⇒ `expiresAt` 落在过去 ⇒ 一份还剩一小时寿命的目录被存成"早已过期"，`PR-4c` 会照此降级而**没有任何可指向的线索**。超过签名窗口自身跨度的 `ttlSec` 不可能是"两者取早"里的那个早者，故直接忽略（不计算）。替身加 `SetCatalogTTL` 打到这段范围的端点，两条钉子分别压"荒谬 TTL 被忽略"与"短 TTL 仍能缩短窗口"。**删掉夹取 ⇒ 前者红在预测位置**（`expiresAt = 2026-09-12 08:37:43 +0000 UTC, which is already past`）。
- **`catalogstore` 的两处死代码与一处无消费者。** `Options.Now` 没有任何调用方传过（`fetchedAt` / `expiresAt` 由 `Entry` 带进来 —— 知道"中台何时作答"的是拉取路径，不是存储），故 `Open()` 去掉参数；`Path()` 只在测试里用过，删除；`Corrupt()` 无人调用，**不删而是给它消费者** —— 装配处启动时打一条告警，缓存损坏被保留、不致命，但模型列表空了的用户该有一行日志可指（`E6.2` 规则 5）。

---

### `PR-4c1` 会话续期与轮换落盘（`V-39` + `V-42` + `V-43`）（✅ **已落地**；三项已拍板，见 §4.1 第 31–33 行；落地记录见第 7 步）

> **这一条是从 `PR-4c` 里劈出来的，不是新需求。** `PR-4c` 第 1 步的缺口 ③（"启动异步刷新在今天的代码上不可能执行"）并没有被 `V-32` 关闭 —— `V-32` 只解决了"令牌没被读回"，剩下的"读回了也没人用"就是 `V-39`；而为了把 `V-39` 写成可复现的条目去核对现场时，又撞出 `V-42`（轮换后的新 refresh token 从不落盘）。两条都属 `PR-4c` 的"按需刷新"标题，但**用户可见结果与 `PR-4c` 主项不同**（见下方粒度自检），且**阻塞 `L-C3a` 的手工验收**，故提前为独立小 PR。

#### 第 0 步 · 粒度自检

| 事项 | 是不是独立行为 | 判定 |
| --- | --- | --- |
| `V-39` + `V-42`：**重启之后还能不能接着用** | ✅ 是 —— 用户可见结果＝"重启后的**第一次发言成不成**"（`L-C3a` 的入口条件）＋"**下一次启动还要不要重新登录**"（`E6` 验收、`E2E-11`） | **`PR-4c1`** |
| 目录不可用时能不能发消息、为什么（`B4`/`B9`） | ✅ 是，但是**另一条**行为，判据是 `L-C2` | 留在 `PR-4c` |

**结论：拆开，且 `PR-4c1` 在前。** 三条理由，都不是偏好：

1. **判据不同**：`L-C3a` vs `L-C2`。按 `E2E闭环清单` §2.0「一条闭环 = 一次可独立判定的用户可见行为」，两条行为各自的异常分支、落盘与文案完全不同，混在一起评审会得到一条"服务端有半边、界面看不见"的 PR。
2. **`V-39` 今天就在阻塞 `L-C3a`**，而 `L-C3a` 是 `PR-5b2` / `PR-5d` 的共同前置。不先修，之后每次手工验收都要先"重新登录一次"，验收记录里就永远留着一句"需先登录"—— 正是 `V-39` 行里记的那个"按字面能过、测不出问题"的口子。
3. **这一条不能再拆成两个 PR**：只修 `V-39` 会立刻制造 `V-42` 的症状（**第一次刷新就把磁盘上那份凭证作废**）。今天"每次重启都要重新登录"是**稳定的**（因为没人刷新过，旧 token 一直有效）；只修一半会变成"**随机一次重启要重新登录**"，比现状更难诊断、更容易被当成玄学。两条必须同一个 PR。

#### 第 1 步 · 需求分析（按代码核对）

**九条事实。** 前四条是本 PR 的**存在理由**，中间四条是**修法的边界**，最后一条是**上游足迹**（末条是落地途中补的 `V-43`）。

1. **凭证恢复已经落地（`V-32`，已合 `v1`）。** `RestoreSession`（[`session.go:25`](../../../internal/productruntime/session.go)）由装配处调用（`cmd/octo-desktop/product.go:121`），把磁盘上的 refresh token 装回 holder；它只装 refresh token，access token 永不落盘（同文件 `:70` 的注释写明"第一次调用会拿它去换一个，这正是平台期望的轮换"）。
2. **但恢复之后没有任何生产调用点会去换 access token（`V-39`）。** `Platform.Bootstrap` 的生产调用点**只有一处**：`fetchCatalog`（`catalog.go:70`），而它只由登录处理器调用（`runtime.go:387`）。`handleChatModes` 只读 `catalogstore` 的缓存、**不拉取**（`PR-4b` 的设计就是"只分类，拉取只发生在登录"）。⇒ **重启后进程内 `AccessToken()` 恒为空**，与用户做什么无关。
3. **网关 sender 直接读 `AccessToken()`，为空即拒绝**（`internal/productruntime/gateway.go` 的 `Sender`，`PR-5a`）。这个拒绝**本身是对的**（fail-closed、零外发，正是 `L-C5` 要的）；错的是**在它之前没有任何人拿到过令牌**。
4. **客户端已经具备"把 refresh token 换成一对新令牌"的全部实现，包括并发与"不许半应用"**：`refreshSingleFlight`（[`client.go:198`](../../../internal/productclient/client.go)）。它的早退条件是 `cur != "" && cur != stale`（`:202`）—— **空 access token + `stale=""` 时它就会去刷**；刷新失败返回 `ErrSessionExpired`（`:209-211`）。⇒ 按 §3.5 的能力检索，**本 PR 不需要任何新的网络路径、不需要新解析器、不需要新端点**，只需要"叫醒它"（约 6 行的导出方法）。
5. **`doAuthorized` 已有的两条凭证语义必须沿用，不能另发明**（`C12` / `L-A6`「只有 `unauthorized` 清凭证」）：刷新被拒 ⇒ `Clear()`（`:179-183`）；被拒后的**重放**再被拒 ⇒ `Clear()`（`:187-190`）。
6. **轮换落盘今天没有调用点（`V-42`）。** `Creds.Save` 的全仓**唯一生产调用点是登录处理器**（`runtime.go:367`），`Creds.Delete` 两处（logout、`forgetSession`）。`credentialstore.Save` 在 `ObtainedAt` 为空时自己填当前时间（[`store.go:119`](../../../internal/credentialstore/store.go)）⇒ 轮换落盘可以**以磁盘上那份为底、只覆盖 `refreshToken`**，`accountPhoneMasked` / `installId` / `schemaVersion` 一个字节都不重算，也不需要依赖 `productstate` 的字段。
7. **`data/credential.json` 里那份令牌已经是死的** —— 现场实测（`2026-09-13`）：发生过一次真实刷新后文件未被触碰（mtime 与内容都是 01:43 的 `rt_3`），而 `rt_3` 已被替身按契约作废，直接 `POST /v1/auth/refresh` 得 **401**。所以 `V-42` 不是理论风险。
8. **契约侧已经写死轮换**：`中台交付包` §3.2（可轮换且可撤销）与 §5.2 第 3 条（每次刷新必须轮换）；替身按契约 `delete` 旧令牌（`clienttest/server.go:466`，注释「Rotation is mandatory」）。⇒ 落盘不是"优化"，是 `E6` 验收的一半。
9. **上游足迹可以是零字节。** 三处改动全部落在 fork 自己的包里（`internal/productclient`、`internal/productruntime`、`cmd/octo-desktop`），**`internal/server` 一个字节都不用动** —— 见下方方案 A1 与 A2 的对比，这是选 A1 的主要理由。
10. **刷新失败今天被压平成"会话结束"（`V-43`，实现本 PR 的 `EnsureToken` 时按代码核对发现）。** `refreshSingleFlight`（[`client.go:198`](../../../internal/productclient/client.go)）把**任何** `Refresh` 错误都写成 `ErrSessionExpired`（`fmt.Errorf("%w: %v", …)`，原因不可找回），而 `doAuthorized` 以该哨兵为唯一判据 ⇒ 一次**传输抖动**（断网 / 5xx / 超时）也会 `Clear()` 凭证、`forgetSession`、回登录页。这与 `dto.go:53` 自己的定义（"token 没了**或平台拒绝**"）矛盾，也让 `TestOutagesDoNotClearTheCredential` 的意图只在漏斗那一层成立。**本 PR 必须一并修**：`EnsureToken` 要在"被拒"与"没打通"之间做出区分，而压平之后调用方做不出这个区分 —— 修法与它同源，拆开做两次等于在同一个函数上写两遍分类。**修法**：`refreshSingleFlight` 只在 `isUnauthorized(err)` 时返回 `ErrSessionExpired`，其余错误原样带因返回；`doAuthorized` 与 `EnsureToken` 都据此判断是否 `Clear()`（`C12`：只有 `unauthorized` 清凭证）。

#### 第 2 步 · 方案与接缝（待拍板 ①）

要回答的问题是：**"把 refresh token 换成 access token"这一次调用，由谁、在什么时候发起？** 三个候选，代价差别很大，因为唯一"天然带 ctx"的位置（回合解析点）在上游文件里。

- **A1（建议）：按需，接缝在 `GatewayEndpoint`。** 给 `GatewayEndpoint` 加一个字段 `Ensure func(context.Context) error`（**nil ⇒ 今天的行为**，既有钉子不受影响）；`Sender()` 在 `AccessToken()==""` 时用**带超时的子 ctx** 调一次 `Ensure`，成功则照常装配，失败则返回既有的拒绝型错误。**上游足迹 0 字节**；**启动零 I/O**（§3.9.1 无争议，不动任何用户文件）；代价是 `Sender()` 变成"可能做 I/O"，需按 §3.10 给 `C2` 规则 3 补一句作用域：「构造不做 I/O，**除非内存里没有可用令牌**；此时向持有凭证的客户端要一次（单飞、轮换、半应用防护仍全归它）」。首回合多一次往返（**只在重启后的第一次**）。
- **A2：正统，但动上游。** 在回合解析点之前确保令牌 ⇒ 必须把 `ctx` 传进 `senderForSession` / `buildAgent`（今天两者都没有 ctx，`server.go:1501`，5 处调用点 + 4 个测试文件要跟着改）⇒ `internal/server` 的 diff 预算再涨一截，收益只是"取消语义更准"。**不建议**：`V-35`/`V-36` 已让该文件的 diff 成为需要逐行解释的东西，为"优雅"再涨一次不值。
- **B：启动异步续期（`B3` 的字面）。** 装配后起一个 goroutine 做一次已授权调用。**两个硬障碍，都不是能绕开的**：① 现成的已授权调用只有 `Platform.Bootstrap`，而**源码级测试规定 `.Bootstrap(` 只能出现在 `fetchCatalog` 里**（[`catalog_test.go:269`](../../../internal/productruntime/catalog_test.go)）⇒ 要么把它做成"真正的启动目录刷新"（那就把 `PR-4c` 的 TTL / 条件请求 / 降级文案一起拉进来，不再是"小 PR"），要么放宽那条测试（放弃"目录只有一个拉取点"这条已确认的 SSOT 规则）；② 刷新必然轮换 ⇒ **启动即写 `data/credential.json`**，与 §3.9.1「启动不写用户可编辑文件」正面相邻，需要一条**显式豁免**（可写，但要像 `V-12` 那样把承载点转移到测试上）。**若选 B，建议与 `PR-4c` 合并做，而不是单开小 PR。**

> **建议 A1。** 它的取舍说得出口：**用"构造可能做一次 I/O"换"上游零改动 + 启动零写盘 + 刷新时机恰好等于需要它的时刻"**。而 B 的两条障碍里，第 ① 条要动一条已确认的 SSOT 规则、第 ② 条要动一条安全底线，代价与收益不成比例（A1 唯一的代价是首回合多一跳）。

#### 第 3 步 · 轮换落盘的位置、形状与失败语义（待拍板 ②）

- **位置**：`internal/productruntime`。它是 `credentialstore` 与 `productstate` 的既有 owner；`internal/productclient` **不 import 持久化**（它今天一个字节都不写盘），这条边界不破。
- **形状**：`func PersistRotatedToken(creds *credentialstore.Store, tokens *productclient.CredentialHolder) error` —— `creds.Load()` 取磁盘上那份为底，只覆盖 `RefreshToken`，`ObtainedAt` 交给 `Save` 自己填（`store.go:119`）。**不重算 `accountPhoneMasked` / `installId` / `schemaVersion`**（它们不是这次操作的事实）。
- **时机**：刷新成功**之后立即**（在 `Ensure` 闭包里，成功返回前）。
- **幂等**：holder 里的 refresh token 与磁盘上那份**相同**时**不写盘**（`Ensure` 的第二、第三次调用会走到这里；省一次写，也避免在只读盘上产生无意义的报错）。
- **失败语义（要选一个）**：只读盘 / 磁盘满 / 写失败时 ——
  - ㈠ **回合继续，记一条 `slog.Warn`，明说"下次启动需要重新登录"**（建议）。按 §3.9 三问自答：**回落到什么** = 不回落（内存里那个令牌此刻可用，用户这一场能正常用）；**是否被告知** = 一条 warn 日志 + 下一次启动时**明确要求重新登录**（不是静默）；**何时恢复** = 换一块可写盘、或用户重新登录。
  - ㈡ 拒绝回合。更保守，但把"只读盘"从"这一场能用、下次要登录"变成"**完全不能用**"，与 `E6` 的便携只读场景（U 盘写保护）正面冲突 —— 不建议。
- **不删凭证、不改 `loggedIn`**：写盘失败不是"拒绝续期"（第 4 步才是），不能顺手把用户登出。

#### 第 4 步 · 刷新被拒的语义（待拍板 ③）

平台答 401 `unauthorized`（或被撤销的 refresh token）时，沿用第 1 步事实 5 的既有语义，**不发明新文案**：`EnsureToken` 里 `Clear()` 凭证（与 `doAuthorized` 逐字一致），`productruntime` 把 `ErrSessionExpired` 交给既有失败面（`failPlatform` → `L-A6` 的拦截页），状态文件的 `loggedIn` 随之转 false（`forgetSession`）。

**要拍板的是范围**：`EnsureToken` 直接调 `refreshSingleFlight` 时不会自动 `Clear`（那是 `doAuthorized` 的包装行为），所以"清凭证"这件事要么复制两行、要么让 `EnsureToken` 复用 `doAuthorized` 的形状。建议**在 `EnsureToken` 里显式 `Clear()`**，并在注释里写明它与 `doAuthorized` 是同一事实的两处（`C12`：只有 `unauthorized` 清凭证；契约/版本/时钟失败一律 fail-closed 但**不清凭证**）。**不做**：不清凭证而只是拒绝（会让用户卡在一个永远失败的状态里，且 `E6` 的"回登录页"路径不成立）。

#### 第 5 步 · 钉子与变异

**九条钉子**（前五条是"能过"，后四条是"不许变坏"）：

1. `TestARelaunchReachesTheGatewayWithoutSigningInAgain`（`internal/productruntime`，**主钉**）：磁盘只有 refresh token、holder 无 access token ⇒ `Ensure` 后装配成功、holder 拿到 access token、替身收到**恰好一次** `/v1/auth/refresh`。**先红**（今天 `Ensure` 概念不存在，等价实现必被拒）。
2. `TestASecondTurnDoesNotRefreshAgain`：holder 已有 access token ⇒ `Ensure` **零请求**（护"幂等"，否则每回合多一跳，且平台侧会看到无意义的轮换风暴）。
3. `TestTheRotatedTokenIsOnDiskBeforeTheTurnEnds`（`V-42` 主钉）：刷新后 `data/credential.json` 的 `refreshToken` == 新值，且 `accountPhoneMasked` / `installId` / `schemaVersion` **逐字不变**；**再断言"用磁盘上这个新值能换出下一次令牌"** —— 只断言"文件被写过"会漏掉"写进去的是旧值/错值"，这条断言才是真的（`V-31` 的同族教训）。
4. `TestAStaleOnDiskTokenWouldFailTheNextStart`（**反钉/对照**）：把落盘去掉（模拟只修 `V-39`）⇒ 重启路径红。它证明第 3 条不是形式主义，也把"`V-39` 必须与 `V-42` 同 PR"写在测试里。
5. `TestTheTurnWaitsNoLongerThanTheBudget`：`Ensure` 挂住不返回 ⇒ 回合在预算内被拒（而不是永久卡住）。它压住"自建 ctx 不给超时"这个坏实现。
6. `TestARefusedRefreshClearsTheCredentialAndReachesTheBlockedScreen`：🔒 替身答 401 `unauthorized` ⇒ 凭证被删、`loggedIn` 转 false、走既有失败面（`L-A6`），**不重试**、**零外发**。
7. `TestAReadOnlyStickStillChatsAndSaysSo`：`Save` 失败（目录只读）⇒ 回合继续、warn 命中、凭证**不被删**、`product-state.json` **未被替换**（复用 `V-12` 的 `os.SameFile` 手法）。
8. `TestNoCredentialAtAllIsRefusedWithoutAnyRequest`（fail-closed 回归）：holder 与磁盘都空 ⇒ 拒绝、平台命中 0、第三方命中 0（不能因为"要令牌"就发明一次请求，更不能放宽到未授权来源）。
9. **上游回归零改动**：`PR-5a` / `PR-5c` / `PR-5b1` 的既有钉子**一行不改**（`GatewaySender` 签名不变 —— 这是选 A1 的直接结果，也是"没动上游"的机器证据）。

**五个变异**（每个都要落在预测位置）：

| # | 变异 | 应变红 |
| --- | --- | --- |
| M1 | 删掉 `Sender()` 里的 `Ensure` 调用 | 1 |
| M2 | 落盘只写 `refreshToken`、丢掉其余字段 | 3 |
| M3 | 落盘在刷新**之前**（把旧值写回） | 3 |
| M4 | 刷新被拒时不清凭证 | 6 |
| M5 | 落盘失败时拒绝回合 | 7（证明"回合继续"是钉住的，不是碰巧） |

**手工验收**（两终端流程，`productstub` 必须先重启 —— `V-40`）：
① 登录 → 退出程序 → 重启 → **直接发一条消息**，应成功（**今天必被拒**，这就是 `V-39`）；
② 再退出 → 再重启 → 仍应成功（**这一步才证明轮换已落盘**；只修 `V-39` 的实现会在这里失败）；
③ `cat data/credential.json` 的 `refreshToken` 与步骤 ① 之前**不同**，且 `curl -X POST /v1/auth/refresh -d '<旧值>'` 得 **401**（证明是平台认的那个新值）。

#### 第 6 步 · 文档与契约

- **`需求基线` `E6`**：补一条验收 —— "**发生过一次刷新之后**重启，仍能自动登录"（今天这条是隐式的，`V-42` 之后才成立）；`E6.3`（`credential.json` 字段表）补一句 `obtainedAt` 在轮换时更新。
- **`E2E-11`**（复制 `data/` 换机）：补作用域 —— 可复用的前提是"最后一次刷新已落盘"，而落盘发生在刷新成功之后**立即**（窗口 = 一次 `Save` 的时长）。
- **`开发计划` §1**：新增 `PR-4c1` 一行，标在 `PR-4c` **之前**；`PR-5b2` / `PR-5d` 的前置一栏补 `PR-4c1`（否则每次手工验收都要先登录）。
- **`中台交付包`**：**无新增**。轮换已写死（§3.2、§5.2 第 3 条），`PQ27` 不受影响。

#### 第 7 步 · 落地记录（2026-09-13）

**三项拍板全部按建议落**（A1 / 回合继续 + warn / `EnsureToken` 显式清凭证），`V-43` 一并修掉。改动 7 个文件、+275 −25 行，**上游零字节**（`internal/server` 一行未动；`git diff --stat` 里没有它）。

| 文件 | 改了什么 |
| --- | --- |
| `internal/productclient/client.go` | 新增导出的 `EnsureToken(ctx)`（约 6 行：空令牌时叫醒 `refreshSingleFlight`）；`refreshSingleFlight` 改为**只有 `isUnauthorized(err)` 才返回 `ErrSessionExpired`**，其余原样带因返回（`V-43`）；`doAuthorized` 据此只在真被拒时 `Clear()` |
| `internal/productruntime/session.go` | 新增 `SessionRenewer`（`Ensure` + `persistRotation`）；`forgetSession` 从方法提为包级函数，供两处共用（`C12` 单一实现） |
| `internal/productruntime/gateway.go` | `GatewayEndpoint` 新增 `Ensure func(context.Context) error`（nil ⇒ 今天行为）；`Sender()` 仅在 `AccessToken()==""` 时按 `gatewayEnsureBudget`（10s）调一次 |
| `internal/productruntime/runtime.go` | 装配 `SessionRenewer` 并把 `Ensure` 接到 `GatewayEndpoint` 上 |
| `cmd/octo-desktop/product.go` | 同上（桌面装配路径） |

**钉子（13 条，全部绿）** —— 实际命名与第 5 步的草拟名略有出入，以本节为准：

| 文件 | 钉子 | 压的是 |
| --- | --- | --- |
| `internal/productruntime/session_renew_test.go` | `TestARelaunchedProcessReachesTheGatewayWithoutSigningInAgain` | `V-39` 主钉：重启后第一回合能拿到令牌 |
| | `TestTheRotatedTokenIsOnDiskWithTheTurn` | `V-42` 主钉：**新值在盘上、旧值已作废、用盘上那份能换出下一次** |
| | `TestWithoutTheWriteTheNextLaunchIsStranded` | 反钉：只修 `V-39` 会立刻制造"下一次启动被拒" |
| | `TestARefusedRefreshEndsTheSessionAndLeavesNoCredential` | 被拒 ⇒ 删凭证 + `loggedIn` 转 false，且只试一次 |
| | `TestTheTokenExchangeRunsUnderADeadline` | 交换受 10s 预算约束，不永久卡住 |
| | `TestNoCredentialRefusesTheTurnWithoutDialingAnything` | fail-closed 回归：盘与内存都空 ⇒ 零外发 |
| | `TestAReadOnlyDataRootStillChatsAndSaysSo` | 只读盘 ⇒ 本场继续 + warn，且**不删凭证、不换 `product-state.json`** |
| `internal/productclient/ensure_token_test.go` | `TestEnsureTokenTradesTheRefreshTokenForAnAccessToken` | 交换本身 |
| | `TestEnsureTokenIsANoOpWhileATokenIsHeld` | 幂等（第二回合零请求） |
| | `TestARefusedRefreshEndsTheSessionInMemory` | 被拒 ⇒ 内存清空 |
| | `TestATransportFailureDuringTheExchangeKeepsTheCredential` | **`V-43` 主钉**：传输抖动**不**清凭证 |
| | `TestEnsureTokenWithNoCredentialIsRefusedWithoutADial` | 无凭证 ⇒ 不拨号 |

**变异（6 个，每个都落在预测位置）**：

| # | 变异 | 结果 |
| --- | --- | --- |
| M1 | 去掉 `Sender()` 里的 `Ensure` 调用 | 主钉红：`not signed in: … none is held` |
| M2 | 落盘时重建 `Credential{}`（丢掉其余字段） | 主钉红：`accountPhoneMasked = "", want "138****1234"`；`installId = "", want …` |
| M3 | 落盘挪到交换**之前**（写回旧值） | 主钉红：`credential holds "rt_3", want … "rt_5"` |
| M4 | 被拒时不清凭证 | 被拒钉子红：`the credential is still on disk` |
| M5 | 落盘失败时拒绝回合 | 只读盘钉子红：`permission denied` 变成回合失败 |
| M6 | `refreshSingleFlight` 把所有错误压平成 `ErrSessionExpired` | `V-43` 钉子红：`want it NOT to claim the session ended` |

**一个落地时才发现的边界（已写进实现注释）**：`persistRotation` 在"内存里的 refresh token 与盘上那份**相同**"时**不写盘** —— 这既是幂等（`Ensure` 的第 2、3 次调用不再落盘），也是**只读盘场景能"静默"通过第 1 步检查**的原因（真正会失败的只有交换后的那一次写）。代价是：如果用户把可写盘换成只读盘**且期间没有发生过刷新**，落盘失败不会出现 —— 这是可接受的，因为那时盘上那份仍然有效。

---

### `PR-4c` 三条降级 + 四种文案 + 条件刷新（🚧 **方案中**；三项待人工确认，见 §4.1 第 18–21 行）

#### 第 0 步 · 粒度自检

§1 表把这条 PR 写成三件事："三条降级语义 + 空名单四种文案 + TTL 到期后的条件请求与按需刷新"。按 `E2E闭环清单` §2.0 的判据（**一条闭环 = 一次可独立判定的用户可见行为**）逐项过：

| 机制 | 是不是独立行为 | 判定 |
| --- | --- | --- |
| 三条降级语义（`B4`） | ✅ 是 —— 用户可见结果＝"目录不可用时**还能不能发消息**、**为什么**"（`L-C2` 本体的判据） | **留在 `PR-4c`** |
| 空名单四种文案（`B9`） | ❌ 不是独立行为 —— `B9` 自述是 "`B1`/`B4`/`B5` 的**统一出口**"，四条文案就是**同一行为（无可用模型）的四种结局**。§2.0 明写"同一行为的不同结局算一条" | **留在 `PR-4c`**（写进 `L-C2`） |
| 条件请求（`ETag`/`304`）与按需刷新（`B3`） | ❌ 不是独立行为 —— 它产出的正是降级第一行要描述的状态（**"过期且刷新失败"**：没有刷新就没有"刷新失败"） | **留在 `PR-4c`**；**但见下方缺口 ③** |

**结论：三项收成一条，`PR-4c` = 「目录不可用时，用户看到什么、还能做什么」，`L-C2` 是它的判据。**

**但顺序错了 —— 这是本步最要紧的发现。** §1 表把 `PR-4c` 的前置写成 `PR-4b`（与 `PR-4d` 是兄弟），照此开工会撞上一个结构性障碍：**`PR-4c` 的四条文案里有两条没有可上屏的面**。

- `B9` 的四条输入是「无缓存 / 过期 / 验签失败 / 分组空」。前三条与第四条**都发生在模型选择器里**，而**选择器今天的数据源不是目录** —— 它读 `/api/product/chat-modes`（`api.ts:341` `getChatModes`），而后端**根本没注册这条路由**（`V-24`），今天只靠 `devBackend.ts` 的本地 `MODES` 常量接管。让选择器改读签名目录，是 **`PR-4d` 的交付物**。
- 更硬的一条：`B4` 第三行要求"无缓存 ⇒ **选择器空**"，而"选择器为空"这个**可见结果今天不可能出现** —— 数据源还在本地常量里。`PR-4c` 若先做，只能把三条文案挤到别处（例如 composer 横幅），第四条（"分组空"）则**完全无处可去**（没有目录分组）。
- 还有一致性：`PR-4c` 要拦"不得开启新回合"，而今天用户仍能从本地常量里挑一个模型 —— **拦一个本来就不该存在的选择**。`B1` 规则 1 禁止内置名单，`PR-4d` 正是拆掉它的那一步。

> **建议：把 `PR-4c` 的前置从 `PR-4b` 改为 `PR-4d`，即顺序 `4b → 4d → 4c`。** 理由不是偏好：`PR-4c` 的判据（四条文案各出现一次）**在 `PR-4d` 之前无法满足**，先做就会得到一条"服务端有半边、界面看不见"的闭环 —— 正是 `V-23` / `V-29` 反复记录的那个陷阱。`PR-4d` 不受影响：它只要求"列出签名目录里的模型"，登录时 `PR-4b` 已经拉到了目录。
>
> **`PR-4d` 期间的空窗是显式的**：目录不可用时 `chat-modes` 返回**空列表**（这就是 `§2.8` 降级行说的"不给内置名单"，`B1` 的 fail-closed 已满足），**且不新增任何降级文案** —— 文案归 `PR-4c`。（**⚠️ 2026-09-12 更正**：此处原写"一个字的文案都不出"是**错的** —— 选择器对空组的既有文案 `mode.no_models` 照常渲染，"不出文案"指的是**不新增、不区分解释**，不是界面上没有任何文字。`PR-4c` 的交付物是让四条原因**各自可辨**，而今天它们共用那一句。）这与 `PR-4b`「只分类、不定文案」是同一个已接受过的切法。

#### 第 1 步 · 需求分析（五处**必须在开工前定**的缺口）

`L-C2` 的行文是"三条降级"，`B4` 给了三行行为，`B9` 要求四条互不相同的文案。按 §1 表、`需求基线` `B3`/`B4`/`B9`、`本地API契约` §1.3/§2.8 与 `中台交付包` §4.3 核对后，有五处**行文没有覆盖、而实现必须选一个**的地方：

**① 可用性状态（"目录能不能用"）的 owner 与上线形状未定（必须定）。** 它是**一个**事实（能不能开新回合 / 为什么），而今天有三处都能各自造一份意见：`catalogstore`（缓存里有什么）、`catalogOutcome`（上一次拉取怎么了）、前端（自己想）。按 §3.8 只能有一个 owner。

- **不能塞进 `ProductStateDTO`**（`本地API契约` §1.3）：该形状是 `productstate.PublicState` 的镜像、即**磁盘上那个文件**，而可用性是**运行时**事实（`P4-拦截页.md` §2.2 已因同一理由否掉过一次把编译期事实塞进该 DTO 的做法）。
- **不能只挂在 `chat-modes` 上**：composer 的"不得开启新回合"需要在**不开选择器**时也知道，否则它要为了判断而先拉一次模型列表。
- **建议：新增第 14 条端点 `GET /api/product/catalog`**，形状照抄 `PR-2c` 的 `/control-plane`（同样是"运行时装配事实、有唯一 owner、不经状态文件"）。`state` 取值 **四个**：`ready` / `absent`（无缓存）/ `stale`（过期且刷新失败）/ `unverifiable`（验签失败）—— 与 `B9` 的四条输入**一一对应**（第四条"分组空"不进这个枚举，它是投影结果、属 `PR-4d`）。

**② 三个状态与四条文案的对应关系要写死在**一处**（§3.8）。** `B9` 的四条输入里有三条来自目录、一条来自投影。若把"状态 → 文案"散在前端组件里，`PR-4d` 的投影路径与 `PR-4c` 的降级路径会各判一次"为什么空"，两处一旦不同步就会出现"选择器说目录坏了、横幅说分组是空的"。

> **动作**：`B9` 的第四行文案（分组空）与前三行的**判据分开**：前三行由 `catalog.state` 唯一决定（Go 侧产出、前端只做 i18n 取值），第四行由 `chat-modes` 的投影结果决定（`PR-4d`）。**"为什么不给模型"只能有一个判据来源** —— 前三条看 `catalog.state`，第四条看投影为空且 `catalog.state === 'ready'`。

**③ "启动异步刷新"在今天的代码上**不可能执行**（`V-32`，本步新发现）。** `B3` 要求"启动异步刷新"，而启动后**内存里根本没有令牌**：`CredentialHolder` 是**每进程内存**（`client.go:26-35`），装配处传的是空值（`product.go:150` `&productclient.CredentialHolder{}`），而 `credentialstore.Load()` 在全仓**只有测试调用过**（`session_expiry_test.go:81`，用来断言凭证已被删）—— 生产代码**零调用点**。于是：

1. 进程重启后 `product-state.json` 说 `loggedIn:true`，界面直奔主界面，但 `Holder` 是空的 ⇒ **任何已授权调用都 401** ⇒ `doAuthorized` 拿 `Holder` 里的 refresh token 去刷新 —— 而它也是空的 ⇒ `ErrSessionExpired` ⇒ 按 `L-A6` **清凭证、回拦截页**。
2. `需求基线` `E6` 的验收 **"进程退出后重开：`data/credential.json` 存在且能自动登录，不再要求验证码"** 今天**不可能通过**；`L-E2`（换机不重新登录）走同一条恢复路径、同样不可达。
3. `B3` 的"启动异步刷新"没有令牌可用。

> **它至今没人发现，原因值得记一笔**（`V-23` / `V-29` 的同一个机制）：**今天的生产路径里，重启后没有任何已授权调用** —— 目录只在**登录时**拉（`PR-4b` 的 `fetchCatalog` 挂在 `handleLogin` 上）、网关回合还不存在（`PR-5`）、其余端点都不碰中台。所以缺了会话恢复，界面照样能用，`credential.json` 也静静地躺在盘上。**它第一次显形就是 `PR-4c` 的目录刷新** —— 那时用户看到的是"刷新失败 + 被踢回登录页"，而真正的原因是**这个 PR 之前就存在的**。这就是为什么它必须在 `PR-4c` 开工前处置，而不是等它自己冒出来。

**这条不属于 `PR-4c` 的行为，但 `PR-4c` 的一条要求踩在它上面。**

> **✅ 已拍板并落地（用户 2026-09-12 选"单独一个小 PR 先修"）**：见下方「前置修复」小节。`PR-4c` 的"启动异步刷新"因此**可达**，`B3` 不必改条文。

> **建议（**待人工确认**，触及凭证面，§3.7）**：把"启动时把 `credential.json` 的 refresh token 装回 `Holder`"作为 `PR-4c` 的**前置小项**一并做（装配处一处 `Load` + `Set`，约 10 行 + 测试），否则 `PR-4c` 的"启动刷新"是一句空话，且 `E6` 那条验收会一直挂着。**替代方案**：`PR-4c` 只做"陈旧时按需刷新"（去掉"启动刷新"），把恢复整件留给 `PR-7`（`L-E2` 的归属）—— 代价是 `B3` 的要求要改条文，且 `PR-4c` 之后"刚启动、还没开过选择器"的用户仍处于**没有会话**的状态（`L-A6` 会在第一次调用时把他踢回登录页）。

**④ 条件请求用哪种等价物、`/catalog/models` 的应答形状（契约要改，必须先定）。** `中台交付包` §4.3 写的是"必须支持 `If-None-Match`/`ETag` **或等价的 `knownVersion` 参数**"：

- **选 `knownVersion`**（客户端把已有的 `catalogVersion` 带上）。理由：`ETag` 要在缓存里**新增一个不透明字段**（`catalogstore` 的第五项元数据），而 `knownVersion` **复用已有的 `catalogVersion`**，且中台侧只是比两个字符串。
- **但 `/catalog/models` 的应答形状有歧义**：§4.3 写"返回**同一份已签名 `policy.catalog`**"。若它只回 `catalog` 对象，客户端**无法验签** —— 签名覆盖的是整个 policy（`data.policy` 的原始字节），单取 `catalog` 就把它拆散了。

> **动作（契约改动，本文档直接改，不等外部确认）**：① 在 `中台交付包` §4.3 写明 `/catalog/models` 返回的是**与 `bootstrap` 同一个信封形状**（`{policy, policySignature}`）—— "一个已签名目录"只有一种形状，否则就是同一事实两处定义（§3.8）；② 写明未变化时的应答是 **`304`** 或 `{"unchanged": true}` **二者之一**，客户端两种都要认（中台可任选，客户端不得只认一种）；③ 在 `本地API契约` §2.14 登记 `/api/product/catalog` 的形状。

**⑤ "不得开启新回合"由谁强制（必须定，且要写清与 `PR-5` 的边界）。** `B4` 第一行的行为是"不得开启新回合"，而**发起回合的路径今天属于 `PR-5`**（网关流式回合），`L-C5` 的 `RequiresControlPlane()` 也要到那时才有调用点（`V-27`）。

> **动作**：`PR-4c` 交付**判据**（`catalog.state !== 'ready'` ⇒ 不得开新回合）并把它 **落到前端发送路径**（`ChatView.send()`，用户可见结果＝发送被拒且给出原因）；**服务端强制**随 `PR-5` 的 `RequiresControlPlane` 一起接线，届时读**同一个判据**（不新建第二个）。**`PR-4c` 不得自己实现"服务端也拦"** —— 那会在 `internal/app` 里提前长出一条只服务本 PR 的判据。

#### 第 2 步 · 开发计划（交付物 + 能力检索增量 + 测试红清单）

**前置**：`PR-4d` 已合（选择器读目录）；缺口 ③ 的处置已确认。

**能力检索增量（`§3.5`，只列 `4c` 新增的判断）**

```text
既有能力检索：internal/catalogstore（缓存唯一 owner，已存 catalogVersion / expiresAt — 直接复用）
              internal/productruntime/catalog.go 的 fetchCatalog（全仓唯一拉取点 — 扩展它，不新建第二个）
              internal/productclient.Client.doAuthorized（401→刷新的单飞与 fail-closed — 直接复用）
              internal/productruntime/controlplane.go 的「运行时事实走独立端点」形状（PR-2c — 照抄）
              web/src/lib/product.ts 的 failureTier / tierRetryable（PR-2c 的"失败面分类"唯一 owner）
为什么不能复用：B4 的三条是**目录**的可用性，而 failureTier 是中台**请求**的失败分类，
              两者判据不同（一个看缓存的 expiresAt + 最近一次拉取结局，一个看 HTTP 状态码），
              合并成一个枚举会让"断网"与"目录过期"互相冒充 —— 正是 V-21 犯过的错（误分类）。
              故新增 catalogState 枚举，但在前端**沿用** product.tier.* 的呈现纪律。
复用后的形态：catalog.go 多一个 availability 判定 + 一个 refresh 入口；前端多一个 store + 四组 i18n 文案。
```

**交付物（5 件）**

| # | 落在哪 | 做什么 |
| --- | --- | --- |
| 1 | `internal/productruntime/catalog.go` | `catalogState` 四值与**唯一**判定函数（缓存有无、是否过期、最近一次拉取结局 → 四值之一）；`refreshCatalog(ctx, force bool)` 收口现有 `fetchCatalog` 的调用点（**仍是一处发起**，由 `TestOnlyOnePlaceFetchesTheCatalog` 继续钉住） |
| 2 | `internal/productclient`（`dto.go` + `client.go`） | `CatalogModels(ctx, knownVersion)` 客户端方法（`GET /v1/catalog/models`），认 **304** 与 `{"unchanged":true}` 两种"没变"写法 |
| 3 | `internal/productruntime/runtime.go` | 第 14 条端点 `GET /api/product/catalog`，返回 `{state, retryable, catalogVersion, expiresAt}` |
| 4 | `web/src/lib/product.ts` | `catalogState` store + `canStartTurn()`；`ChatView.send()` 接入"不得开新回合" |
| 5 | `web/src/lib/i18n.ts` + 选择器 | `B9` 四条文案（中英各一份），三条由 `catalog.state` 取、一条由投影为空取 |

**测试红清单（先写、先红）**

| # | 测试 | 钉住什么 |
| --- | --- | --- |
| 1 | `TestCatalogStateIsOneJudgement` | 四种输入（无缓存 / 过期+刷新失败 / 验签失败 / 正常）各得一个值，且**判定只有一处**（源码级断言，同 `TestOnlyOnePlaceFetchesTheCatalog` 的手法） |
| 2 | `TestExpiredCatalogBlocksANewTurn` | 过期且刷新失败 ⇒ `canStartTurn` 为 false；**老会话仍可读**（同一断言里正反各一次） |
| 3 | `TestAFailedVerificationIsTerminal` | 验签失败 ⇒ `unverifiable`、**不自动重试**同一份响应（断言替身收到的请求次数不增） |
| 4 | `TestAbsentCatalogDoesNotAskForKeys` | 无缓存 ⇒ `absent`，且**不出现配 Key 向导**（`B4` 禁止项，正反两断言） |
| 5 | `TestKnownVersionGetsA304AndTheFileIsUntouched` | 条件请求命中 ⇒ 缓存 mtime/字节不变（`B3` 验收） |
| 6 | `TestUnchangedAnswerIsRecognisedInBothSpellings` | 304 与 `{"unchanged":true}` 两种都算"没变" |
| 7 | `TestStaleCatalogTriggersOneRefreshOnDemand` | 陈旧时触发**一次**刷新（不多不少） |
| 8 | `TestNoPathFallsBackToALocalModel` | 四条降级路径**逐一**断言没有 `config.yml` / 本地 provider / 内置名单的痕迹 |
| 9 | 前端 `catalogTextsAreFourDistinctStrings` | 四条文案互不相同（`B9` 的验收原话） |

**本 PR 明确不做**：`PR-4d` 的投影与 `displayName` 渲染；`PR-5` 的服务端回合强制与 `RequiresControlPlane` 接线；周期定时器（见下）。

**关于"按 TTL 周期刷新"的取舍（§3.10 写清域）**：`B3` 的原文含"按 TTL 周期刷新"，本方案**不引入后台定时器** —— 它需要一个生命周期 owner（起停、与窗口开关的关系、U 盘拔出时的行为），而 `B3` 自己的验收写的是"**TTL 到期后下一次开选择器触发刷新**"，即**读时判定**已满足判据。定时器登记为 `TODO`，与"拔盘冻结"（`PR-7`）一起定生命周期。

#### 前置修复 · `V-32` 会话恢复（**独立小 PR，✅ 已完成 2026-09-12**）

`PR-4c` 的"启动异步刷新"踩在 `V-32` 上（缺口 ③），故先出一个**不占 `PR-4c` 行为格**的小 PR 把它修掉。用户 2026-09-12 从三个方案（独立小 PR / 并入 `4c` / 留给 `7`）中选了独立小 PR。

**交付物（2 个文件 + 1 处装配）**

| # | 落在哪 | 做什么 |
| --- | --- | --- |
| 1 | `internal/productruntime/session.go`（新） | `RestoreSession(state, creds, tokens)`：把 `data/credential.json` 的 refresh token 装进 holder。**只装 refresh token**（access token 从不落盘，`E6` 规则 2），第一条已授权调用因此走一次**真实 refresh** —— 那是中台要求的轮换，不是多花的一次往返 |
| 2 | `cmd/octo-desktop/product.go` | 造 holder → `RestoreSession` → 交给 `newPlatformClient`（**签名改为接收 holder**）。一个值、一次使用、紧邻，不可能漏配；`productclient` 保持**不碰文件** |
| 3 | `internal/productruntime/session_test.go`（新） | 5 条钉子（见下） |

**为什么落在 `internal/productruntime` 而不是装配处**（这是本 PR 唯一值得记的取舍）：① `handleLogout` 与 `L-A6` 的"清凭证"也在本包 —— **"会话终止"与"会话恢复"是同一个事实的两面**，分开放会漂移；② **夹具与数据根 harness 已在本包**（§3.5 复用，不新建第二个）；③ 最硬的一条：**`cmd/octo-desktop` 是嵌套 module，`go test ./...` 跳过它**，CI 里那条路径只跑 `-run TestPortableServer_WritesOnlyToDataRoot` 一条过滤 —— **钉子放进那里等于不会被任何流程执行**。本 PR 初稿正是先写在 `cmd/octo-desktop/session_restore_test.go`，核实 CI 后整体搬走。

**边界（`§3.9` 有界降级，三条都写明"回落到什么 / 是否被告知 / 何时恢复"）**

| 情形 | 行为 | 用户是否被告知 | 恢复 |
| --- | --- | --- | --- |
| 登出后凭证因只读盘删不掉而残留 | **不复活**（以状态文件的 `loggedIn` 为准），且**不删残留文件**（启动期不写用户文件，`§3.9.1`） | 是（日志一行） | 下次登录覆盖 |
| `credential.json` 损坏 | 照常启动、按未登录走（`E6` 规则 5），**不阻止启动** | 是（日志一行） | 重新登录 |
| 状态说已登录、凭证不存在 | 留空、**不发明会话** | 是（日志一行，解释"第一条平台调用会回登录页"） | 首次成功登录 |

**测试红清单（先写、先红：`undefined: RestoreSession`）**

| # | 测试 | 钉住什么 |
| --- | --- | --- |
| 1 | `TestARestartedProcessStartsSignedIn` | **E6 验收的测试形态**：新 holder + 只靠磁盘凭证 ⇒ `Bootstrap` 成功，**且断言的是一次真实 refresh**（`RefreshCount()==1`）—— 不是"holder 里有个字符串"。夹具扩展 `baseURL` / `login` 两个字段（3 行，不改既有断言） |
| 2 | `TestARestoredSessionCarriesNoAccessToken` | `E6` 规则 2 的另一半：恢复进来的 holder **不带** access token |
| 3 | `TestALoggedOutStateIsNotResurrected` | `E7`：登出后不复活（模型的失败是"删文件没成功"） |
| 4 | `TestADamagedCredentialDoesNotStopStartup` | `E6` 规则 5：损坏文件照常启动 |
| 5 | `TestNoCredentialLeavesTheHolderEmpty` | 首启用例，且防止"无凭证也发明会话" |

**变异验证**：去掉恢复 ⇒ 测试 1 红，原文 **`Bootstrap after a restart = productclient: session expired`** —— **正是 `V-32` 的用户症状**；单独去掉登出门 ⇒ 测试 3 红（`restored a session the logout was supposed to end (token "rt_3")`）。

**全量验证**：`gofmt -l .` 空 / `go vet ./...` 空 / `go test -race ./...` **59 个包全 ok** / 桌面嵌套 module `CGO_ENABLED=1 go build ./...` 通过（仅既有 macOS 部署目标告警）+ 其 P1 探针 `TestPortableServer_WritesOnlyToDataRoot` 通过 / 九个守卫（`portable` `fmt` `brand` `datapath` `norms` `agents` + `reuse-guard` `server-diff-guard` `release-profile-guard` `release-config-guard` `preflight`）全通过。

---

### `PR-4d` 目录投影与上屏（✅ **已完成 2026-09-12**）

#### 第 0 步 · 粒度自检

`§1` 表写的是"投影 + `internal/chatmode` 搬运 + `chat-modes` 去 `fallback` + 前端渲染 `displayName`"。这四件是**同一个用户可见结果**的四段：**选择器列出签名目录里的模型、显示目录给的名字**。判据是 `L-C1b` 一行，拆不开（少任何一段，选择器都还是空的或显示旧名）。**一条 PR。**

#### 第 1 步 · 需求分析（两处**边界必须先划清**，一处结论与直觉相反）

**① `B7` 的资格过滤属本 PR，能力矩阵属 `PR-4e` —— 这条边界没人在文档里划过。** `B7` 的标题是"模型可选资格**与**能力矩阵"，两半的 owner 不同：

| 哪一半 | 是什么 | 归属 | 判据 |
| --- | --- | --- | --- |
| **模型级资格** | 目录里**单个模型**的 `eligible=true` + `transport=gateway` | **本 PR** | `L-C1b`（投影） |
| **功能级能力矩阵** | `capabilities[]` 的 `visible` / `entitled` / `permissionPolicy` —— 指**界面入口**（MCP、词库…），不指模型 | `PR-4e` | `L-C6` |

**不分清的后果是具体的**：若把"只有 `eligible=true` 的模型可选"也推迟到 `4e`，`4d` 的投影会把 `eligible=false` 的模型也列进选择器 —— 用户看得见、点得动、发出去被网关拒（`model_not_allowed`，`中台交付包` §3.2）。**故本 PR 的投影即按 `eligible && transport=="gateway"` 过滤**，`4e` 只做入口可见性。

**② `compositeId` 不删，但要换角色 —— 这一处结论与我先前的猜测相反，值得写下来。** 我原以为 `compositeId`（`"<endpoint>::<model>"`）是 `config.yml` 时代的残留、应当随 `A1` 一起消失。**按 `B8` 规则 1 逐字读，它必须留着**：

> 上游会话存的是 `<endpoint>::<model>` 复合 id（`PATCH /api/sessions/{id}/model`）。正式会话的 endpoint 前缀**固定为内置网关**，可变部分只剩目录 id。

也就是说**会话绑定仍用复合形态**，只是端点那半不再可变。真正要改的是它在选择器里的**角色**：

- **今天**：`compositeId` 是"这个模型在 `config.yml` 里存在吗"的判据 —— `ModeMenu.svelte:42` 用 `if (!model.compositeId) return` 拦住"列在 `chat-modes.json` 但不在 `config.yml`"的模型，`class:disabled={!m.compositeId}` 把它画成灰的。
- **`4d` 之后**：目录投影里的**每一个**模型都已被 `eligible && transport` 过滤过，**"列了但不可选"这个状态不存在了**。该守卫必须去掉 —— **否则它会把目录里的模型全部拦死**（目录模型没有 `config.yml` 端点，`compositeId` 若仍由旧路径产生就是空的）⇒ 选择器点不动。
- **改法**：投影产出 `compositeId = <内置网关>::<目录 id>`（前缀是产品常量，不是中台数据），选择器**不再用它判可选性**，只把它当作"要持久化到会话上的值"。

**③ `displayName` 的渲染要收成一处，且本地表必须删干净（`B6`）。** 三处联动，缺一处 `B6` 的验收就过不了：

| 改哪 | 今天 | 改成 |
| --- | --- | --- |
| `chatMode.ts` 的 `modelDisplayName(id)` | 查 i18n `model.<id>`、回落原 id | **接目录的 `displayName` 对象**，按 `B6` 规则 2：当前语言 → 缺则 `en` → 再缺则 `id`。debug/test 才允许直接回落 `id` |
| `i18n.ts` 的 `model.*` | **8 个键**（4 个模型 × 中英） | **删掉**。留着就是 §3.8 说的"本地表遮蔽服务端"：平台改名后界面一直显示旧名 |
| `api.ts` 的 `ChatModeModel` | `{id, compositeId?}` | 加 `displayName: {zh, en}`；`compositeId` 保留但语义改为①所述 |

**④ `devBackend.ts` 必须同 PR 改。** 它是 DEV 构建下 `chat-modes` 的**当前应答方**（`devBackend.ts:205`，`MODES` 常量带 `compositeId: 'local::…'`）。类型一改它就编译不过；更重要的是它今天发的是**旧形状**（含 `fallback`、无 `displayName`），留着会让 DEV 构建继续演示 `B1` 已作废的行为（`V-24` / `PR-3` 的触发条件）。

#### 第 2 步 · 开发计划（交付物 + 能力检索增量 + 测试红清单）

**能力检索增量（`§3.5`）**

```text
可搬运：archive/feat-chatmode-20260911 的 internal/chatmode/{chatmode.go,chatmode_test.go}
        ——但只有约 30 行可用（三个模式 id 常量 + Builtin() 的「三组、组内无模型」形态）。
不可搬：Load() / Config / ErrUnreadable / SchemaVersion —— 那是 chat-modes.json 的读文件路径，
        B1 规则 2 已把该文件彻底删除（"不再落此文件"），搬过来就是逆着需求走。
可复用：internal/catalogstore（缓存唯一 owner，PR-4b 已落）——投影只读它，不新建第二个读取点。
        internal/catalogstore.Load() 已含验签结果，投影**不得**再验一次签（§3.8：验签一个 owner）。
        web/src/lib/product.ts 的 ProductPhase 纪律（PR-2c）—— 投影为空时的呈现沿用同一套。
为什么不能复用：今天前端的两级选择器外壳（ModeMenu.svelte）是对的，改的是数据源与 displayName；
        **不新建组件、不新建 store 结构**。
```

**交付物（6 件）**

| # | 落在哪 | 做什么 |
| --- | --- | --- |
| 1 | `internal/chatmode/`（新，搬运 + 裁剪） | 三个模式 id 常量 + 三组骨架。**只保留分组事实，不读任何文件** |
| 2 | `internal/productruntime/catalog.go` | `projectCatalog()`：目录 → `eligible && transport=="gateway"` 过滤 → 按 `models[].modeIds` 分到三组 → 每组 `defaultModelId` **校验同组成员资格**（`中台交付包` §4.3：无效则该条作废）→ 产出 `compositeId` |
| 3 | `internal/productruntime/runtime.go` | 注册 `GET /api/product/chat-modes`（`Mount` 第 7 条）；形状按 `本地API契约` §2.8 新形状（去 `fallback`，加 `catalogVersion` / `policyVersion` / `displayName`） |
| 4 | `web/src/lib/api.ts` + `chatMode.ts` | 类型加 `displayName`；`modelDisplayName` 改接目录对象（三档回落） |
| 5 | `web/src/components/chat/ModeMenu.svelte` | **去掉 `compositeId` 的可选性守卫**（步骤②）；`class:disabled` 一并去掉 |
| 6 | `web/src/lib/i18n.ts` + `web/src/dev/devBackend.ts` | 删 8 个 `model.*` 键；devBackend 的 `chat-modes` 改发新形状（DEV 构建不再演示已作废行为） |

**测试红清单（先写、先红）**

| # | 测试 | 钉住什么 |
| --- | --- | --- |
| 1 | `TestTheProjectionListsOnlyGatewayModels` | `eligible=false` 与 `transport!=gateway` 的模型**都不上屏**（两种过滤各一条断言） |
| 2 | `TestTheProjectionGroupsByTheModelsOwnModeIds` | 分组来自 `models[].modeIds`，**不是** `modes[].models`（§4.3 禁止第二处定义） |
| 3 | `TestAnUnknownModeIdIsIgnoredNotInvented` | 目录给未知 mode id ⇒ 忽略并记录，**不新建模式**（模式集合是产品常量） |
| 4 | `TestAnInvalidDefaultModelIsDropped` | `defaultModelId` 指向非本组成员 ⇒ 该条无效（§4.3） |
| 5 | `TestTheCompositeIdCarriesTheGatewayPrefix` | 投影产出的 `compositeId` 前缀是内置网关常量，可变部分等于目录 `id` |
| 6 | `TestChatModesNeverFallsBackToALocalList` | 目录不可得 ⇒ **空列表**，且响应里**没有** `fallback` 字段（`B1` 规则 1/4，正反断言） |
| 7 | `TestNoModelNameTableRemainsInTheFrontend` | 源码级：`i18n.ts` 里搜不到 `model.<id>` 键（`B6` 验收原话"代码里搜不到该模型 id 的文案键"） |
| 8 | 前端 `displayNameFallsBackZhToEnToId` | `B6` 规则 2 的三档回落各一次 |
| 9 | `TestChatModesIsRegisteredAndReachable` | 真机路径可达（`V-24` 的那 404 消失）—— 端到端，`mount_test.go` 的既有手法 |

**本 PR 明确不做**：`PR-4c` 的降级文案（**空列表就是空列表** —— 目录不可得时不新增任何说明；**⚠️ 更正**：方案原文写"一个字的文案都不出"，核对后**不成立** —— `ModeMenu.svelte` 的 `mode.no_models`（「该模式下暂无可用模型」）早已存在并照常渲染，`PR-4d` 照原样带着它上屏。`PR-4c` 要补的是**四种原因各自的说法**：今天「无缓存 / 过期 / 验签失败 / 分组空」共用同一句，用户分不出该重试还是该等）；`PR-4e` 的能力矩阵与入口可见性；`PR-3` 拆除 `devBackend.ts`（本 PR 只改它的 `chat-modes` 应答）。

#### 第 5 步 · 实现绿（✅ 完成记录，2026-09-12）

**六件交付物全部落地**，无一件缩小或推迟。

| # | 落在哪 | 实际做了什么 |
| --- | --- | --- |
| 1 | `internal/chatmode/`（新，2 文件） | 三个模式 id 常量 + `IDs()`（返回副本）+ `IsProductMode()`（精确匹配，不做 trim/大小写归一 —— 需要归一才匹配的值不是模式 id，接受它会把手滑变成第四个分组）。**只保留分组事实，不读任何文件** |
| 2 | `internal/productruntime/catalog.go` | `projectCatalog()`：`eligible=true && transport=="gateway"` → 按 `models[].modeIds` 分组 → 每组 `defaultModelId` **校验同组成员资格** → 产出 `compositeId`；另有一处新增：`productprofile.GatewayEndpointID = "buding-gateway"`（投影产出的复合 id 前缀，会话绑定用它） |
| 3 | `internal/productruntime/runtime.go` | `GET /api/product/chat-modes` 注册为 `Mount` 第 6 条产品路由；形状按 `本地API契约` §2.8（去 `fallback`，加 `catalogVersion` / `policyVersion` / `displayName`） |
| 4 | `web/src/lib/api.ts` + `chatMode.ts` | 类型加 `displayName: {zh,en}`、`compositeId` 改为**必填**；`modelDisplayName` 改接目录对象，三档回落（当前语言 → `en` → `id`），并让 zh-* 与 `i18n.dictFor` 同规则 |
| 5 | `web/src/components/chat/ModeMenu.svelte` | 去掉 `compositeId` 可选性守卫与 `class:disabled`（含样式）；改名走 `modelDisplayName(m)` |
| 6 | `web/src/lib/i18n.ts` + `web/src/dev/devBackend.ts` | 删 8 个 `model.*` 键（中英各 4）；devBackend 的 `chat-modes` 改发新形状（含 `displayName`、`catalogVersion` / `policyVersion`、网关前缀、**无 `fallback`**），并把那 4 个名字移入数据本身 —— 它今天正是"服务端"那一侧 |

**测试：计划 9 条 → 11 条落地**，其中一条改了形态、一条拆成三条。

| # | 计划 | 落地 | 备注 |
| --- | --- | --- | --- |
| 1 | `TestTheProjectionListsOnlyGatewayModels` | ✅ 同名 | 两种过滤各一条断言 |
| 2 | `TestTheProjectionGroupsByTheModelsOwnModeIds` | ✅ 同名（**加强**） | 双向断言：模型自认的模式不在目录里、目录有模式项而模型未认领 —— 两向都不许凭空产生分组 |
| 3 | `TestAnUnknownModeIdIsIgnoredNotInvented` | ✅ 同名 | 忽略并回报，不新建模式 |
| 4 | `TestAnInvalidDefaultModelIsDropped` | ✅ 同名 | 非本组成员 ⇒ 该条作废（模式本身保留） |
| 5 | `TestTheCompositeIdCarriesTheGatewayPrefix` | ✅ 同名 | 前缀是产品常量，可变部分等于目录 id |
| 6 | `TestChatModesNeverFallsBackToALocalList` | ✅ 同名（**正反双向**） | 无缓存 ⇒ 200 + 空 `modes`，且响应里**没有** `fallback` 字段 |
| 7 | `TestNoModelNameTableRemainsInTheFrontend` | ✅ **改了形态** —— `modelDisplayName › keeps no id→name table behind the fallback` | 计划写"源码级搜 `model.<id>` 键"，落地改成**读导出字典的键**（`Object.keys(en/zh)`）。等价而更稳：不依赖文件路径与书写形式，且直接断言"运行时会查的那张表"是空的 |
| 8 | 前端 `displayNameFallsBackZhToEnToId` | ✅ **拆成 3 条** | 三档回落 + **`zh-TW` 也走中文**（与 `dictFor` 同规则）+ 一个客户端从未见过的 id 也能正确上屏 |
| 9 | `TestChatModesIsRegisteredAndReachable` | ✅ 同名 | 真 `internal/server` + 窗口门 + 挂载态端到端，`V-24` 的那个 404 消失 |
| — | （计划外） | ➕ `ModeMenu.test.ts` 3 条 | 渲染目录名（**不是**本地名）、点击产出 `onPick(mode, model)`、切语言即时换名 |
| — | （计划外） | ➕ `TestTheProjectionReportsTheVersionsItCameFrom` | `catalogVersion` / `policyVersion` 如实来自签名策略 |
| — | （计划外） | ➕ `TestTheDisplayNameTravelsVerbatim` | 目录给什么名字就显示什么名字 |
| — | （计划外） | ➕ `TestThisPackageNeverReadsAFile` | `internal/chatmode` **不得**再长出读文件路径（`B1` 规则 2 的机械钉子） |

**变异验证（10 个变异体，全部转红）**：投影侧 7 个（去掉 `eligible` 过滤 / 去掉 `transport` 过滤 / 分组改读 `catalog.Modes[]` / 未知模式改成新建 / 默认模型不校验成员资格 / 复合 id 前缀改字面量 / 版本不回报）＋ 前端 3 个（菜单退回按 id 查本地表 / 去掉 `en` 回退层 / `zh-*` 不再与字典同规则）。

> **一处变异体第一版是钝的，值得记下。** 第 5 条"默认模型不校验成员资格"我最初把断言目标改成了 `want != "" && false`，即**整个默认值都不赋值** —— 那是另一个缺陷，而 `TestAnInvalidDefaultModelIsDropped` 断言的是"无效默认值 ⇒ 该组没有默认值"，于是变异体**照旧绿**。改成真正要防的那条（**不做成员资格判断，直接按 `defaultModelId` 拼复合 id**）后立刻转红（`smart.DefaultModel = "buding-gateway::buding-privacy-1"`）。`§6.4.5` 的原话是"先证明钉子会红"，本轮补上的是它的推论：**证明钉子会红，还得证明变异的是那条规则本身**。

**`compositeId` 换角色的结果**：`ModeMenu.svelte` 的守卫与灰显样式一并删除，类型上 `compositeId` 改为**必填** —— "列了但不可选"这个状态因此**在类型里也无法表达**，比留一个运行时守卫更彻底（守一个到不了的状态，下一个读者会当成它还能到）。产出的值是 `<buding-gateway>::<目录 id>`，正是 `B8` 规则 1 要的会话绑定形态。

**与 `PR-4c` 的接缝（⚠️ 一处更正，2026-09-12 质检）**：目录不可得时 `chat-modes` **返回 200 + 空列表，且不新增任何降级文案**。**方案原文写的"一个字的文案都不出"经核对不成立**：`ModeMenu.svelte` 的 `mode.no_models`（「该模式下暂无可用模型」/ "No models available in this mode"）是既有文案，`PR-4d` 照原样带着它上屏。**正确的接缝表述**：`PR-4d` 让四条不同原因**共用同一句既有文案**（用户分不出「没登录过」与「目录过期了」），`PR-4c` 的交付物是**把四种原因分开说**。`loadChatModes()` 出错时**保留上一份列表**（不是清空）—— 这一条是刻意的：清空会让 `4c` 的"过期"文案误配到"这一秒网络抖了一下"上。

**本 PR 明确不做（复核仍然不做）**：`PR-4c` 的四条文案、`ETag`/`304` 与周期刷新；`PR-4e` 的能力矩阵；`PR-3` 拆除 `devBackend.ts`（只改它的 `chat-modes` 应答）。

---

### `PR-4c0` `V-35` 前置拦截：网关绑定的模型名不得落到默认 sender（✅ **已完成 2026-09-13**）

**为什么会有这条 PR（不是计划里的，是手工验收逼出来的）。** `PR-4d` 的人工验收里，用户选中「布丁隐私版」发了一条消息，界面出现：

```
HTTP 400 (invalid_request_error): The supported API model names are
deepseek-flash, deepseek-v4-pro, but you passed buding-gateway::buding-privacy-1
```

> **这条报错同时证明了两件事，其中一件是好消息。** 报错里的 `buding-privacy-1` **只存在于签名目录的夹具里**（用户机器上那份 `data/chat-modes.json` 是 `buding-local-general` 等旧四项，`devBackend.ts` 的本地表也是那四项）⇒ **`PR-4d` 的投影确实端到端生效了**，选择器读的就是签名目录、复合 id 前缀也确实来自 `productprofile`。另一件是缺陷。

**第 0 步 · 粒度自检＝一条 PR。** 用户可见结果只有一个：**选了目录模型后发消息，请求不再发给第三方，而是本地说明原因。** 四段（检测点 / 拒绝 / 错误码语义 / 文案）拆不开 —— 少任一段，用户要么继续看到那个第三方 400，要么只看到沉默。

**第 1 步 · 需求分析。**

**机制（逐行核实，不是猜测）**：`internal/server/server.go` 的 `senderForSession` 是**所有回合路径的唯一解析点**（`buildAgent` 的四个调用方：REST / WS×2 / 定时任务）。前端把 `model_id = "buding-gateway::buding-privacy-1"` 发到 `PATCH /api/sessions/{id}/model`，`config.EntryByModel` 匹配不到（`config.yml` 里没有这个端点），走 `handlers.go:1373` 的最后一个分支 —— 注释原文就是"Raw model string: keep the default sender, change only the model" —— 存成 **`Model` = 复合 id、`ModelConfig` = `""`**。于是 `senderForSession` 在**第一个** fallback（`sess.ModelConfig == ""`）就把这个字符串交给了默认 sender；默认 sender 来自 `data/config.yml`（用户那台是真 DeepSeek）⇒ 就是那句 400。

**两件事是错的，第二件更重**：① 用户拿到一句看不懂的第三方报错；② **那个请求真的到了 DeepSeek**（错误是它返回的）⇒ 对话内容随该请求一起发了出去。`B4` 禁止的正是第 ② 件。

**规则（一句话）**：**默认 sender 不得收到"网关绑定"的模型名。** 要么由网关 sender 承载，要么这个回合不开。

**scope（§3.10，这一条必须写清否则会被用反）**：**所有 profile 生效，含开发构建**。这**不是** production 的 `RequiresControlPlane` 强制（那条仍归 `PR-5`，见 `V-27`）：开发构建里用 `config.yml` 当模型源是**被允许的**，但"网关绑定的 id 交给 `config.yml` 的端点"在任何 profile 下都必然失败且把内容发给第三方。护栏**只认网关前缀**，所以正常从设置里选真实端点模型的上游流程**完全不受影响**。

**落点取舍（§3.7 第 2 项，已人工确认）**：

| 方案 | 位置数 | 结论 |
| --- | --- | --- |
| **① 在 `senderForSession` 一处拒绝**（**已选**） | 1 | 唯一解析点 ⇒ 一处覆盖全部回合路径。`§4.5` 不改上游函数签名、而它返回 `(Sender, string)` 无 error ⇒ 用**拒绝型 sender**（非惯例做法，已登记） |
| ② 四个 `buildAgent` 调用点各自拦截 | 4 | 同一条规则写 4 遍，漏一个等于没做（`§3.8`） |
| ③ 只改前端 | 1 | 零上游改动，但**不权威**：队列里的消息、REST、**定时任务**仍会照发。`§3.9` 要 fail-closed，这条是 fail-open |

**注入而非 import**：`internal/server` 至今不 import 任何 fork 包（`product_gate_test.go` 明写这条纪律），产品知识经 `Config` 注入 ⇒ 网关前缀走新的 `Config.GatewayModelPrefix`。

**明确不做**：不拦"选择"、只拦"发消息"。今天目录里**每个**模型都是网关绑定的，若连选都不让选，`PR-4d` 的成果在界面上完全看不见、也就无法验收。`B9` 的原话也正是"不得开启新回合"。

**第 2 步 · 计划与交付物。**

| # | 落点 | 交付物 |
| --- | --- | --- |
| 1 | `internal/productprofile` | `GatewayModelPrefix()` —— **分隔符的唯一 owner**（原来 `"::"` 拼在 `productruntime/catalog.go` 里） |
| 2 | `internal/server/server.go` | `Config.GatewayModelPrefix` 注入字段；`senderForSession` 变成薄包装（原逻辑逐字搬进 `resolveSenderForSession`，多回一个 `degraded` 布尔）；`gatewayBound()`；`refusedGatewaySender` |
| 3 | `cmd/octo-desktop/main.go` | 装配处传入前缀（+1 个 import，桌面模块是嵌套 module，`main.go` 必须自带）；`main.go` 与 `product.go` **同包但 import 不共享** |
| 4 | 文档 | 本节 + `需求基线` `V-35` + `E2E闭环清单` `L-C5` 加注 |

**测试红清单（7 条，先写先红 —— 首红为 `unknown field GatewayModelPrefix` / `undefined: refusedGatewaySender`，指向缺失行为本身）**：① 网关绑定模型名 ⇒ 默认 sender **零调用**；② 网关端点**可解析**时护栏自动让路（`PR-5` 的接缝，写成测试）；③ 普通模型**完全不受影响**（含"仍能正常发出并拿到回复"）；④ 不注入前缀 ⇒ 上游行为逐字不变（`octo serve`）；⑤ **陈旧网关绑定**（`ModelConfig` 带前缀但端点已删）同样拒绝；⑥ 陈旧**普通**绑定仍照旧降级（反变异钉子）；⑦ 拒绝信息自解释。

**两处钉子有牙的证明（变异 6 个）**：M1 去掉护栏 ⇒ 3 条红；M2 放宽成"任何回落都拒绝" ⇒ 3 条红；**M3 只看模型名不看绑定 ref ⇒ 1 条红**（见下）；M4 去掉空前缀短路 ⇒ 1 条红；M5 只认绑定 ref ⇒ 2 条红；M6 丢掉模型名 ⇒ 1 条红。

> **两处过程记录，都有价值。**
> **① M3 逼出实现的一处真实漏洞。** 第 ⑤ 条钉子首红时我以为是自己写错，核对后发现是**真漏洞**：网关 id 有**两条**进入会话的路径 —— 作为**模型名**（`PR-4d` 事件形态，`ModelConfig` 为空）和作为**绑定 ref**（绑定时端点存在、之后被删，`Model` 是裸名）。只查模型名会漏掉第二条。这就是 `gatewayBound()` 同时查两半、且注释里写死"两半是刻意不合并"的原因。
> **② 一个"全绿"的变异体其实是选择器写错了。** M4 第一次跑出来是绿的，我差点当成缺口 —— 实际是我用 `-run 'Gateway|Ordinary|Refusal'` 选测试，而 `TestTheGuardIsInertWithoutAnInjectedPrefix` **一个都不匹配**，压根没跑。补上完整选择器（先打印"覆盖 7 条"）后 M4 正常转红。**`§6.4.5` 的推论再加一条：变异体转红之前，先确认被它影响的那些测试真的被选中了。**

**§3.7 登记（§4.1 第 22 行）**：动上游核心文件（`internal/server/server.go`）＋ 非常规逻辑（拒绝型 sender）。人工确认：方案 ①。

**上游足迹**：`internal/server/server.go` **137 → 247 行**（上限 543，`server-diff-guard` 绿）。**这是本 PR 唯一的代价，且要说清**：涨的 110 行里约 60 行是注释（两条路径、为什么是 sender、为什么全 profile），代码本身是一字段 + 一个包装 + 一个前缀判断 + 一个 90 行的类型。选择一处而非四处，是用上游 diff 换"规则只有一份"。

**残余（都不阻塞本 PR 的判定）**：① **拒绝文案是英文**（与这条路径上所有服务端回合错误一致 —— `turn_error` 没有码通道，前端原样渲染 `**Error:** …`）。**本地化归 `PR-4c`**（`§3.8`：界面文案住在前端 i18n）。② production 的 `RequiresControlPlane`（含预设环境变量与第三方 `config.yml`）**仍归 `PR-5`**，本 PR 不碰 `internal/app` —— 这与此前"不提前在 `internal/app` 长判据"的决定一致。③ 前端**不拦选择**，所以用户仍可能选到一个暂时发不出去（也发不出去给第三方）的模型；"选之前就说清"归 `PR-4c` 的可用性状态（§4.1 第 20 行）。

---

### `PR-5a` 网关回合跑通（✅ **已完成**，2026-09-13；含 `V-36` 条件化与 `V-37` 证伪撤回）

#### 第 0 步 · 粒度自检：原 `PR-5` 必须拆

原一条要覆盖 **6 条闭环**（`L-C3a` `b` `c`、`L-C4` `C5` `C7`）+ `V-36` + `V-35` 的 production 半边，按 `§6.4` 第 0 步的判据（一条闭环 = 一次可独立判定的用户可见行为）明显过粗。**拆 5 个子 PR**，见 §1 表。**本轮只做 `5a`**（与 `PR-4` 只做 `4a` 同一做法）：它产出的是用户真正要的那个结果 —— **聊天能用**。

**顺序不是随意的**：`5a` 之后**立刻**做 `5c`（不回落），再做 `5b`/`5d`/`5e`。理由是 `5c` 只**收窄失败路径**，越早接上，后续能力越不会长在一条会回落的路径上 —— 而"回落"正是 `V-35` 已经出过一次事故的那一类（`§3.9`）。

#### 第 1 步 · 需求分析

**① 接缝：产品如何把"网关 sender"交给上游 `internal/server`？**

`Config` 今天**没有任何 sender 注入缝**（只有 `Provider`/`Model` 两个字符串，桌面壳一个都没设），而 `internal/server` 按纪律**不得 import 任何 fork 包**（`product_gate_test.go` 明写）。三条路：

| 方案 | 结论 |
| --- | --- |
| **① 注入 `Config.GatewaySender func(model string) (agent.Sender, error)`**（**建议**） | 与已批准的 `MountAPI` / `WindowToken` / `GatewayModelPrefix` **同一纪律**（产品知识经 `Config` 注入、方向反转）。`senderForSession` 里现有那个 `gatewayBound()` 判断就是**现成的接缝**：`PR-4c0` 在那里拒绝，`PR-5a` 在那里换成"调用工厂" |
| ② 把内置端点写进 `config.yml` | **`C1` 规则 3 明文禁止**（"不写入 `data/config.yml`"），且规则 2 要求它不出现在 endpoints 列表里 |
| ③ 让 `productruntime` 直接往 `Server.senderCache` 注册 | 缓存是**按 ref 长期持有**的，而网关令牌**每回合轮换**（`C2` 规则 1/3）⇒ 缓存住必然过期。工厂**刻意绕过缓存** |

**这一条同时解释了 `C2` 规则 2 为什么能"不改上游"**：`NewSender` 很轻，且限流器按 `limiterKey(provider, baseURL)` 复用 ⇒ 每回合用**当前内存 token**新建一个 sender，既拿到新令牌、又不丢限流预算。`CredentialHolder.AccessToken()` 就是那个 token 读口（`internal/productclient/client.go`）。

**② ✅ 开工前的**伪阻塞**：路径"三不相符" —— 已证伪，无需处理**

原判（`V-37`）：三处对"网关请求打到哪个 URL"各有说法，**没有任何 `BaseURL` 能同时满足**。**该判断已被证伪并撤回**（详见 `需求基线` §5.6 `V-37` 行）：

| 来源 | 说法 | 复核结果 |
| --- | --- | --- |
| `internal/provider/openai/client.go` | 路径常量 `/v1/chat/completions` | ✅ 常量存在，**但同一函数下方有归一化分支**：`if strings.HasSuffix(base, "/v1") { suffix = "/chat/completions" }` ⇒ 尾部 `/v1` 被剥掉 |
| `中台交付包` §5.2（我方提案） | `POST /v1/ai/chat/completions` | ✅ **确实到不了**（路径是常量）⇒ 这一半成立，改为标准 `/v1/chat/completions` |
| `productprofile.GatewayHost` | `https://gateway.invalid/v1` | ✅ 含 `/v1`，**但不会造成双前缀** |

⇒ **`BaseURL = GatewayHost` 无论含不含 `/v1`，都打到 `/v1/chat/completions`。** 证据不止是读代码：`internal/provider/openai/client_test.go:364` **早已断言** `endpointURL()` 不得含 `/v1/v1`（upstream issue #1625）。**证伪它的是本 PR 自己写的反钉** —— 按"双前缀必 404"写的钉子当场红，改成"两种形状都应打到同一路径"的正钉则过。**因此第 4 项交付物（改 `GatewayHost` 语义）从"必需"降为"约定"**（裸 host 与客户端 `DefaultBaseURL` 同形，保留但不再是要求），`validateHost` **不动**。

**③ `V-36`：`ensureSender` 必须条件化。** 它今天在 `buildAgent` **之前**无条件要求一个默认 sender，而产品包按 `B4` **本就不该有第三方 endpoint** ⇒ 不改这一处，网关做出来照样聊不了（`V-36` 已记）。改法：该回合若解析为网关绑定，就不走 `ensureSender` 的默认 sender 要求。

**④ 明确不做（留给兄弟 PR）**：六条失败面（`5c`）、思考块与工具调用（`5b`）、余额（`5d`）、下架校验（`5e`）。**也不做** `C4`/`C5`/`C6`（`clientRequestId`、扩展字段、`retract`）—— 三者均已在 `待解决问题.md` 记为 `D-003`/`D-002`，联调期按"零改动"路径（标准 OpenAI `chat/completions`，`model` 填目录 `id`，不新增字段）。

#### 第 2 步 · 计划与交付物

| # | 落点 | 交付物 |
| --- | --- | --- |
| 1 | `internal/productruntime/gateway.go`（新） | 网关 sender 工厂：读 `CredentialHolder.AccessToken()` → `app.NewSender`（`Protocol: "openai"`、`BaseURL` = `GatewayHost`、`APIKey` = 内存 token）。**不读盘、不刷新、不缓存**（`Accept` 等头由 provider 自己设 —— 复用的一半，不在工厂里重复） |
| 2 | `internal/server/server.go` | `Config.GatewaySender` 注入字段；`senderForSession` 在 `gatewayBound` 时调用它并返回**裸目录 id**（剥前缀）；`ensureSender` 条件化（`V-36`） |
| 3 | `cmd/octo-desktop/product.go` | 用**同一个** `tokens` holder 装配工厂（`V-32` 的恢复路径与它共用一份内存凭证） |
| 4 | `internal/productprofile` | 网关 host 保持裸 host（**约定**，非要求）；值随 `V-37` 撤回一并说明 |
| 5 | `internal/productclient/clienttest` | **替身补出 `POST /v1/chat/completions`**（原来只有 auth/bootstrap）—— 否则"网关回合跑通"只能用单测证明，手工走不通 |
| 6 | 文档 | 本节 + `V-37` 撤回记录 + `中台交付包` §5.2（路径改标准） |

**测试红清单（10 条）**：① 网关绑定会话 ⇒ 返回**真网关 sender**（不再是拒绝型），且 model 为**裸目录 id**；② 用 `httptest` 替身断言**请求形状**：路径、`Authorization: Bearer <内存 token>`、`Accept: text/event-stream`；③ **令牌轮换** ⇒ 下一回合用新 token（**不缓存**）；④ 未登录（无 token）⇒ **明确拒绝且零请求**（fail-closed）；⑤ `config.yml` **无任何 endpoint** 时网关回合仍能跑（`V-36`）；⑥ 非网关会话行为**逐字不变**（反变异钉子）；⑦ 不注入工厂 ⇒ 上游行为逐字不变（`octo serve`）；⑧ 端到端：SSE 替身 ⇒ 内容 delta 上屏 + `usage` 归一（`L-C3a` 的正身）；⑨ **`GatewayHost` 含 `/v1` 与不含 `/v1` 都打到同一路径**（`V-37` 的正钉）；⑩ **真替身进程能答一回合**（工厂 → provider → `clienttest` 全链，即手工验收路径的单测形态）。

**变异验证（5 个）**：缓存住 sender ⇒ ③红；不剥前缀 ⇒ ①红；恢复无条件 `ensureSender` ⇒ ⑤红；去掉 `Accept` 头 ⇒ ②红；token 改成构造期快照 ⇒ ③红。

**§3.7 触发表**：**动上游核心文件**（`internal/server/server.go`，`5a` 之后预计再涨）＋ **认证/凭证**（动态令牌注入）＋ **路由**（内置端点）。三行都要求人工确认。

#### 落地结果（2026-09-13）

**已完成，测试与守卫全绿**（`gofmt -l .` 空 / `go vet ./...` 空 / `go test ./...` 全过 / `cmd/octo-desktop` 可构建 / 守卫 6 项通过 / `node --test scripts/*.test.mjs` 143 条通过）。

| 落点 | 实际落地 |
| --- | --- |
| `internal/productruntime/gateway.go`（新） | `GatewayEndpoint{Host, Tokens}` + `Sender()`：读当前内存 token → `app.NewSender(Provider: custom, Protocol: "openai", BaseURL: Host, APIKey: token)`。三种缺失（host 空 / holder nil / token 空）各自返回**指名道姓**的错误，且**零请求** |
| `internal/server/server.go` | `Config.GatewaySender func() (agent.Sender, error)`；`senderForSession` 拆出 `resolveSenderForSession`（回报 `degraded`），网关绑定时调用工厂并**剥掉前缀**传裸目录 id；`ensureSender` 在注入了工厂时**不再要求默认 sender**（`V-36`）；`refusedGatewaySender` 泛化为 `failingSender`（可携带不同文案） |
| `internal/server/benchmark_handler.go` / `enableSubAgentTools` | 默认 sender 可能为 `nil` 的两处补上显式处理（`V-36` 的连带面：条件化之后 `nil` 从"不可能"变成"可能"） |
| `cmd/octo-desktop/product.go` / `main.go` | 一处装配产出**两个接缝**（`mount` + `gatewaySender`），共用**同一个** `CredentialHolder`（与 `V-32` 的恢复路径同源） |
| `internal/productclient/clienttest` | **新增 `POST /v1/chat/completions`**（SSE，分块）+ 4 条钉子；包注释里"尚无可运行二进制"的过期说法一并更正 |
| `cmd/productstub` | banner 改为分别列 `apiHost` / `gatewayHost` 并说明**形状差异只对 apiHost 是要求** |

**实现与本文档的差异（2 处，均已在代码注释写明理由）**：

1. **`Config.GatewaySender` 不带 `model` 参数**（文档 §4.1 第 25 行写的是 `func(model string)`）。理由：sender 与模型**无关** —— 模型是每回合随请求传给 `SendMessages` 的（`senderForSession` 返回的 model 参数本来就是那个通道），工厂拿它只会得到一个没人用的入参。**签名以实际为准**：`func() (agent.Sender, error)`。
2. **`Accept: text/event-stream` 不在工厂里设**（文档 §2 第 1 项原写"`Headers` 带 `Accept`"）。理由：该头是 `internal/provider/openai` 在**流式路径**上自己设的；工厂再设一遍就是"同一事实两处定义"（`§3.8`）。钉子改为断言**到达 provider 后**该头存在（`TestTheAcceptHeaderIsTheProvidersNotTheFactories`），并把"不重复设"写成反向钉子。

**`V-37` 证伪撤回（本节最重要的一条）。** 反钉 `TestThePreV37HostWouldHaveDoublePrefixedThePath`（按 `V-37` 断言"含 `/v1` 的 host 必 404"）**当场变红** ⇒ 结论不成立。正钉 `TestATrailingV1OnTheHostIsToleratedByTheProvider` 证明两种 host 形状都打到 `/v1/chat/completions`。**污染面 4 处已全部撤回**：`profile.go` 注释、两个 profile 的 `gatewayHost` 值（性质由"要求"改为"约定"）、`release-config-guard` 的一条规则（收窄为只对 `apiHost` 要求 `/v1` —— 那条对应真实的 `V-13`）、`profile_test.go` 的断言（改为只禁 `/v1/v1`）。**第 4 项交付物（改 `GatewayHost` 语义 + `validateHost`）因此作废** —— 不需要动。

**新增的额外交付物（原计划没有）**：替身的 `POST /v1/chat/completions`。原计划的测试红清单第 ⑧ 项只要求"`httptest` 替身"，但**手工验收路径走不通** —— `clienttest` 只有 auth/bootstrap，用户照 `README` 的两进程流程进去聊天会 404。这一条是**用户报告"进去之后聊天报 server not configured"时顺手查出来的**（`V-36` 是第一层原因，替身缺路由是**紧随其后的第二层**，不修的话修完 `V-36` 仍聊不了）。

**顺带发现（未单独立 `V` 条目，因均为文档/注释层且已就地更正）**：① `release-config-guard` 原来对 `apiHost` 与 `gatewayHost` **同时**要求 `/v1`，其 message 会指导出包人做出与 `V-37` 相同的错误改动 —— 已收窄；② `clienttest` 包注释称"尚无可运行二进制"，而 `cmd/productstub` 早已存在 —— 已更正。

---

### `PR-5c` 不回落六条失败面（✅ **已落地并合入 v1**）

#### 第 0 步 · 粒度自检

原 `PR-5` 的一条。本条的边界：**只收窄"请求能发到哪里"** —— 不新增能力、不做 UI、不碰内容安全（`PR-6`）。覆盖 `L-C5` 一条闭环 + `V-27` 一个缺口。

**结论：不再拆。** 机制（一处判据 + 一处拒绝）与六条黑盒钉子**共用同一套夹具与同一处接缝**；拆开会让"机制改了但没人证明六条都不回落"成为可能 —— 那正是 `V-23` / `V-27` 的形态（改了，但没有可判证据）。

**与 `PR-4c` 的边界（必须写清，否则两 PR 会互相踩）。** 目录不可用时的**用户可见文案**（四种）归 `PR-4c`；本条只要求**不回落**（请求不得发到网关以外）。两 PR 改的是同一屏的不同事实 ⇒ 记入 §4.2 一致性矩阵。

**与 `PR-4c0` 的边界（已在代码注释里写明，此处复核成立）。** `PR-4c0` 的 `gatewayBound` **按模型 id** 拒，全 profile 命中；本条的判据**按 profile**，只管 production。二者互补：前者防"网关 id 落到第三方"，后者防"非网关 id 在 production 下落到第三方"。

#### 第 1 步 · 需求分析与事实核对

**逐条按代码核实（不引旧文档）**：

1. `RequiresControlPlane()` = `!AllowEnvironmentModelSource`（`profile.go:171`）。**生产 Go 调用点 = 0** —— 全库 `rg` 只有定义、注释与测试。`V-27` 成立。
2. 回合 sender 只有**一个解析点**：`senderForSession` → `resolveSenderForSession`（`PR-4c0`/`PR-5a` 建立）。除 `degraded` 那一支外，返回的就是**默认 sender**；它来自 `s.sender`，由 `ensureSender`/`resolveProviderAndModel` 从**环境变量或 `data/config.yml`** 装配。⇒ **在 production 下，这一支今天照样能把对话发到第三方。** 这正是 C9 规则 2 里的两条失败面（"`config.yml` 里有可用 endpoint""环境变量里有 key"）。
3. **判据事实已存在且有 owner，本条不新建**：`profile.ControlPlaneConfigured()` / `HasTrustedKeys()`（owner = `internal/productprofile`），且 `cmd/octo-desktop/product.go:127` **装配时已经取到**这两个值，`productruntime.ControlPlaneStatus` 也已承载它们（`runtime.go:88`）。⇒ 只是**接到回合解析点**，属 §3.5 复用。
4. **上游 diff 预算**：`internal/server/server.go` 实测 **365/543**（`server-diff-guard`）⇒ 本条预计 +25~40 行，**不需要**按 §3.4 走破例抬高 ceiling。

#### 第 2 步 · 计划与交付物

**① 机制：一处判据，一处拒绝（§3.8）**

`Config` 新增两个注入字段，**都由装配层从 `productprofile` 取值**，`internal/server` 不 import fork 包（沿用 `MountAPI` / `GatewayModelPrefix` / `GatewaySender` 的注入纪律）：

| 字段 | 取值 | 用途 |
| --- | --- | --- |
| `RequireGateway bool` | `profile.RequiresControlPlane()` | 为真时**默认 sender 一律不可用** |
| `ControlPlaneReady bool` | `profile.ControlPlaneConfigured() && profile.HasTrustedKeys()` | 为假时**在任何 DNS 之前**拒绝（失败面 4） |

`senderForSession` 的行为（`RequireGateway` 为真时）：

- **网关绑定的回合** ⇒ 走 `GatewaySender`（`PR-5a` 已就位），失败则拒绝 —— 不再有"退回默认 sender"这条边。
- **非网关绑定**（裸模型名 / 空模型 / `config.yml` 里确实存在的 endpoint）⇒ **拒绝型 sender**，文案指明"正式构建的会话只能使用目录里的模型，请在列表里重选"。
- **`ControlPlaneReady` 为假** ⇒ 拒绝，文案指明"本构建未配置控制面"。**这一条是有意的**：否则会去打 `*.invalid`，用户拿到一句不可诊断的 DNS 错误（失败面 4 的现状）。

**范围（§3.10）**：`RequireGateway` 只在 production 为真。**开发构建必须照旧**能用 `config.yml` / 环境变量当模型源 —— 这不是宽容，而是 `RequiresControlPlane()` 自己的定义（`allowEnvironmentModelSource: true` 的构建就是允许环境来源的构建）。反钉见下。

**② 六条失败面的黑盒回归（`L-C5` 的验收方式，也是本条真正的交付物）**

每条都断言 **"第三方替身命中数 = 0"**，而不是只断言错误文案 —— 文案是表现，**"零外发"才是 C9 的要求**。替身同时充当网关与控制面（与 `PR-5a` 的 `TestTheStandInTheDeveloperRunsCanAnswerATurn` 同一套夹具）。

| # | 失败面 | 造法 | 断言 |
| --- | --- | --- | --- |
| 1 | 目录不可用 | 替身 `OmitPolicy()` ⇒ 无目录 | 回合**不落到第三方**（网关可服务或明确失败，二者都不是第三方） |
| 2 | 网关 5xx | 替身回合接口返 503 | 回合失败 + 第三方命中 **0** |
| 3 | 401 刷新失败 | 替身 `RefuseSessionsAfterLogin()` | 回合失败 + 第三方命中 **0** |
| 4 | `Profile` 未配置 | `ControlPlaneReady=false` | 明确拒绝 + 第三方命中 0 + **不带 DNS 失败形态** |
| 5 | `config.yml` 里有可用 endpoint | 写一份**指向第三方替身的可用 `config.yml`** + `RequireGateway=true` | 第三方命中 **0** |
| 6 | 环境变量里有 key | 预设第三方 key 环境变量 + `RequireGateway=true` | 第三方命中 **0** |

**反钉（防过度收紧，3 条）**：① dev profile（`RequireGateway=false`）+ 可用 `config.yml` ⇒ 第三方**正常被使用**（命中 ≥1）；② `RequireGateway=false` 且 `ControlPlaneReady=false` ⇒ **不阻碍**本地 provider；③ 不注入新字段（`octo serve` / CLI）⇒ 行为逐字不变。

**变异验证（4 个）**：去掉 `RequireGateway` 判断 ⇒ 5/6 红；去掉 `ControlPlaneReady` 判断 ⇒ 4 红；把"零外发"断言换成"只看错误文案" ⇒ 一个"文案还在但请求仍发出"的变异体应当**红**（用以证明断言测的是外发而非文案）；把范围反转为全 profile ⇒ 反钉 ①②红。

**§3.7 触发表**：**认证 / 凭证 / 模型源路由**（决定回合的目标端点）＋ **动上游核心文件**（`internal/server/server.go`）＋ **新增注入字段**（架构约定）。三项均需人工确认。

**测试红清单（9 条）**：六条失败面 + 三条反钉；其中 **5/6/4 在实现前应直接红**（今天不拦），**1/2/3 可能本就绿** —— 若绿，按 `§6.4` 第 5 步的纪律**不假装是先红后绿**，而是用上表变异体证明该钉子有齿，并把它记为"验证既有行为"而非"修复"。

#### 第 4–5 步 · 落地结果（2026-09-13）

**三项拍板已确认**（§4.1 第 26–28 行，用户 2026-09-13）：在 `senderForSession` 一处收口、两个 bool 直陈、按「零外发」断言。

**实现（`feat/server` 分支，两处代码 + 一处装配）**：

| 文件 | 改动 | 说明 |
| --- | --- | --- |
| `internal/server/server.go` | `Config` +2 字段 | `RequireGateway`（=`RequiresControlPlane()`）、`ControlPlaneReady`（=`ControlPlaneConfigured() && HasTrustedKeys()`），**零值即上游行为** |
| `internal/server/server.go` | `senderForSession` +2 处判断 | ① `RequireGateway && !ControlPlaneReady` ⇒ **在网关分支之前**拒绝（否则会去打 `.invalid`，把"本构建没有服务"变成一句 DNS 错误）；② 走到末尾仍 `RequireGateway` ⇒ 拒绝默认 sender。**网关绑定的那一支未动**（`PR-4c0`/`PR-5a` 的代码原样保留） |
| `internal/server/server.go` | 两个错误构造器 | `errModelNotFromCatalog`（"不是本构建能触达的模型，请在列表里重选"）、`errControlPlaneUnconfigured`（"本构建尚未接入服务"） |
| `cmd/octo-desktop/main.go` | 装配 | 取一次 `profile := productprofile.Current()`，两个字段都从它读 —— **判据事实不新建**（§3.5） |

**测试（`internal/server/gateway_failclosed_test.go`，9 条）**：六条失败面 + 一条前提钉 + 三条反钉。

- **前提钉**（`TestTheThirdPartyStandInIsAWorkingModelSource`）是本文件的地基：替身必须先**真的答出一个回合**，否则每条"命中数 = 0"都可能因为 URL 写错而空绿。
- 六条失败面**全部断言第三方替身命中数 = 0**，替身是**真 HTTP 端点**（不是计数 stub）⇒ 断言的是"有没有发出去"，不是"文案对不对"。
- 三条反钉：dev profile 仍用 `config.yml`（命中 ≥1）、dev profile + 未配置控制面不被阻碍、不注入新字段时解析逐字不变。

**先红后绿实测（未按计划全绿，据实登记）**：实现前是**编译红**（两个字段不存在）。实现后：
- 失败面 **4/5/6 与反钉①是本次修的**（去掉 `RequireGateway` 判断立刻变红，见下）；
- 失败面 **2/3 本就绿**（`PR-5a` 的网关分支已经拒绝回落）⇒ 按 §6.4 第 5 步纪律**记为"验证既有行为"**，不假装是本次修复；
- 失败面 **1 由红转绿**（今天裸模型名会落到默认 sender，即第三方）。

**变异验证（4 个，全部按预期，已跑完删除脚本）**：

| 变异体 | 预期 | 实测 |
| --- | --- | --- |
| 去掉末尾 `RequireGateway` 拒绝 | 失败面 5/6 红 | ✅ 两条都红 |
| 关掉 `ControlPlaneReady` 判断 | 失败面 4 红 | ✅ 红 |
| **把两句拒绝文案改成无意义字符** | **全绿**（证明断言测外发而非文案） | ✅ 全绿 |
| 把末尾判断的范围反转为全 profile | 反钉 ①②红 | ✅ 两条都红 |

**实现偏差（据实登记）**：①**未每回合改写 `Config`** —— 两个 bool 是装配期常量，直接进 `Config` 字面量，不需要 `PR-5a` 那种工厂；②测试用**真 HTTP 替身**替代原计划的 `clienttest.OmitPolicy()`（失败面 1）—— 目录缺席在 `internal/server` 这一层表现为"会话没有网关绑定的模型"，用替身更直接；③`ControlPlaneReady` 只在 `RequireGateway` 为真时被读（反钉②所断言）。

---

### `PR-5b` 思考块与工具调用（🚧 **方案中（5b1），代码未写**）

#### 第 0 步 · 粒度自检

原 `PR-5` 的一条，§1 表里捆绑两个能力：`L-C3b`（思考块）与 `L-C3c`（工具调用）。**结论：拆成 `5b1`（思考块）与 `5b2`（工具调用），本轮只做 `5b1`。**

四条理由：① 两条闭环各有独立的用户可见结果与独立判据；② **代码改动不相交** —— `5b1` 要给网关 sender 多接一个参数，`5b2` 按事实核对（第 5/6 条）**一行产品代码都不用改**；③ 与 `PR-5c` 的"不拆"理由不同 —— 那里的拆分会让"机制改了但无人证明六条都不回落"成为可能，这里的拆分**不产生无人证明的半边**（`5b1` 的钉子证明推理块到达事件流，`5b2` 的钉子证明工具调用过本地权限门，各自可判）；④ 本表自己的教训：原 `PR-5` 把四个能力捆成一条，被判为"本表最大的一条"。**共享物**：替身 `handleCompletions` 的扩展（`5b1` 加 `reasoning_content`，`5b2` 加 `tool_calls`）—— 同一个函数改两次，可接受。

边界：**不做**内容安全（`PR-6`）、不做余额（`PR-5d`）、不做下架校验（`PR-5e`）。

#### 第 1 步 · 需求分析与事实核对（逐条按代码，不引旧文档）

1. **`agent` 层已有思考块的表示与事件**：`ContentBlock.Thinking`（`type=="thinking"`）与 `.Reasoning`（`internal/agent/content.go:68/78`）；`ThinkingDeltaFunc` → `EventThinkingDelta`（`agent.go:101/909`）⇒ 本 PR **不新建块类型**。
2. **网关路径今天不会产生思考块**：`app.sender.reasoningSink` 在 `showReasoning == false` 时返回 nil（`internal/app/sender.go:286-291`），而 `GatewayEndpoint.Sender()` 只传了 `Provider/Protocol/APIKey/BaseURL` —— **既没传 `ShowReasoning`，也没传 `ReasoningEffort`**。
3. **它与系统默认相反（这是本条的要害）**：`config.EffectiveShowReasoning` 未配置时返回 **true**（`internal/config/config.go:422-430`），`Composer.svelte` 的开关默认也是 true ⇒ 产品构建在普通端点上默认**显示**思考，在网关上**永远不显示**。用户可见结果是"同一个模型，走网关就看不到思考"。
4. **取值与读取点都已有 owner，本条只接线**（§3.5 复用）：值由 `internal/config` 定义；`internal/server` 的 `senderForEntry` **已经在为普通端点读它**（`server.go:2037-2047`：`cfg.ReasoningEffort` + `cfg.EffectiveShowReasoning(nil)`）⇒ 不新建判据、不在 fork 包里重读配置。
5. **工具调用的线已经在**：`app.sender` 实现了 `StreamMessagesWithTools` / `SendMessagesWithTools`（`sender.go:357/384`）⇒ 网关回合**已经能携带工具调用**，`L-C3c` **不需要新 provider 代码**（与 §1 表"主要是验证"一致）。
6. **本地权限门与 sender 无关**：门在 `internal/server` **每回合**构建（`handlers_prepare_toolturn.go:122`、`server.go:4013` → `app.NewPermissionGate`，`internal/app/gate.go:28`），不是 sender 的属性 ⇒ 网关回合的工具调用**天然过同一道门**。**这是断言（钉子），不是改动。**
7. **替身今天两种形状都发不出**：`clienttest.handleCompletions` 只发 `content`（`internal/productclient/clienttest/server.go:588+`），**没有 `reasoning_content`、没有 `tool_calls`** ⇒ `L-C3b` / `L-C3c` **今天既无法端到端判、也无法手工判**。这与 `PR-5a` 的"第二层"完全同形（当时替身缺 `POST /v1/chat/completions`，手工必然 404）：**修完第一层（推理偏好接线）用户仍看不到东西**。
8. **前端渲染已存在，但 `V-23` 引的三个路径都是错的（新登记 `V-38`）**：`V-23` 称能力在 `markdown.ts:124` / `ChatView.svelte:118/163` / `ToolGroup.svelte`（无路径）—— 前两个**文件在 `v1` 不存在**。真实位置是 `web/src/lib/stores.ts:323/634`（`thinking_delta` → `type:'thinking'` 的 Thoughts 块）与 `web/src/components/chat/ToolGroup.svelte`（存在，且有 `ToolGroup.test.ts`）；`Composer.svelte:743` 的 `showReasoning` 默认 true。⇒ **前端不需要新代码**，但结论要按真实路径重新落笔。
9. **"要它显示"与"要它产生"是两件独立的事，两个参数今天都没接线**：`showReasoning` 决定**显不显示**（`reasoningSink` 在 false 时返回 nil），`reasoningEffort` 决定**请不请模型推理**（`client.go` 的 `applyReasoning` 在 `effort == ""` 时**根本不发**推理字段，`omitempty`）。UI 的 effort 默认是 `'off'`（`Composer.svelte:679`）⇒ 只接 `showReasoning` 一种参数，用户选 `低/中/高` 也**什么都看不到**；只接 effort 一种，模型真发了推理也**无处显示**。**两者都必须接，这决定了钉子要分两条**（见第 2 步 ④）。
10. **`provider/openai` 有 `Dialect`（按后端决定推理字段的形状），网关的方言是一个契约事实、不是本地选择**（`internal/provider/openai/client.go:40-77`）：`DialectDeepSeek`/`DialectOpenAI`/`DialectOpenRouter`/`DialectBailian`/`DialectKimi`，各把 effort 放到不同字段（扁平 `reasoning_effort` / 嵌套 `reasoning:{effort}` / `enable_thinking` 布尔 / `thinking:{type}`）。**`Dialect == ""` 走 `default` 分支 ⇒ 扁平 `reasoning_effort`，并把 `xhigh`/`max` 夹到 `high`** —— 这正是我方提案里"标准 `/v1/chat/completions`"的形状，故 P0 **不设 `Dialect`**（用默认分支）即可，但**它是一个假设，须写进契约**（见第 2 步 ②）。好处：`GatewayEndpoint` **不 import `internal/provider/openai`**（不新增向上游包的依赖边）。

#### 第 2 步 · 计划与交付物（本轮只做 `5b1`：思考块）

**① 机制：把"推理偏好"接到网关 sender 工厂（一处接线）**

`internal/server` 的注入缝从 `GatewaySender func() (agent.Sender, error)` 变为带调参的签名 —— 原因是这两个值**运行时可改**（`PATCH /api/config/show_reasoning`、`PATCH /api/sessions/{id}/reasoning_effort`），而 `GatewayEndpoint` 是装配期构建一次的，所以只能**逐回合取**：

| 项 | 形状 | 说明 |
| --- | --- | --- |
| `GatewayTuning struct { ShowReasoning bool; ReasoningEffort string }` | `internal/server` 定义 | 与 `app.SenderOptions` 的两个字段同名同义；不直接把 `SenderOptions` 传出去（那会把 provider/协议/方言变成注入方的责任） |
| `GatewaySender func(GatewayTuning) (agent.Sender, error)` | 签名变更 | 调用点在 `senderForSession` 里构造 `GatewayTuning{cfg.ReasoningEffort, cfg.EffectiveShowReasoning(nil)}` —— **与 `senderForEntry` 逐字同一个读法**，`internal/server` 仍是"为 sender 读配置"的唯一位置 |
| `GatewayEndpoint` 的 `Sender(tuning)` | 把两个值转交 `app.NewSender` | fork 包**不 import `internal/config`**，一行判断也不加 |
| `Dialect` | **不传**（保持 `""`） | 走 `provider/openai` 的 `default` 分支 = 扁平 `reasoning_effort`。**这是"我方提案的标准形状"的编码，不是已确认的事实** ⇒ 见 ② |

**② 契约新增一条（`中台交付包` §5.2）**：网关的 `POST /v1/chat/completions` **必须接受扁平 `reasoning_effort`**（取值 `low`/`medium`/`high`/`xhigh`/`max`），并按标准 OpenAI 语义处理；不认识的字段**不得 400**（本 PR 会在 `effort==""` 时完全不发该字段，所以"不发"这条路也必须可用）。**这一条是新增要求，不是既有共识** —— 我方此前只写了路径与鉴权。若中台实际形状不是扁平字段（例如像 DashScope 那样要 `enable_thinking`），改动点**只有 `GatewayEndpoint.Sender()` 一处**（设 `Dialect`），这正是把它做成缝而不是散在请求结构里的原因。**这一条登记为待中台确认**（`PQ27`）。

**③ 替身补 `reasoning_content`（第二层，不补则无法验收）**

`clienttest.handleCompletions` 的流式分支加一段 `delta.reasoning_content`（在 `content` 之前），非流式分支加 `message.reasoning_content`。**必须保持既有钉子绿**：`TestTheStandInTheDeveloperRunsCanAnswerATurn` 断言回复里含裸模型名 —— 推理段不得污染该断言。同时替身要**记下收到的请求体**（至少 `reasoning_effort` 字段），供钉子 3 断言"参数确实到了线上"。

**④ 钉子（`5b1`，7 条）**

| # | 断言 | 实现前 |
| --- | --- | --- |
| 1 | **前提钉**：替身真的发出 `reasoning_content`，且 `provider/openai` 把它解析出来（`types.go:109/298` 已有该字段） | ⬜ 需新增（替身今天没有） |
| 2 | **要它显示**：`effort=medium` + `show=true` ⇒ 网关回合把推理块**送到事件流**（`EventThinkingDelta` 被观测到，内容等于替身发的那段） | 🔴 **红**（`ShowReasoning` 未接线） |
| 3 | **要它产生**：`effort=medium` ⇒ 替身收到的请求体里 `reasoning_effort == "medium"`（证明参数到了网上，而不只是到了本地变量） | 🔴 **红** |
| 4 | `effort=""`（UI 默认 `off`）⇒ 替身收到的请求体里**没有** `reasoning_effort` 字段 | ⬜ 需新增（替身今天不记录请求体）。**它是防"接了线就一律发一个默认值"的回归钉** —— 实现后必须**仍然不发** |
| 5 | `show=false` + `effort=medium` ⇒ **无**思考事件（证明显不显示由那个开关决定，不是替身不会发） | 🟢 今天绿（永远不显示），**实现后必须仍绿** |
| 6 | 工厂**逐回合**收到当前调参：两回合之间改 `show_reasoning` ⇒ 第二回合用的是新值 | 🔴 红 |
| 7 | 反钉：不注入 factory（`octo serve` / CLI）⇒ 解析行为逐字不变；且 `RequireGateway=false` 未注入时，网关绑定 id 仍按 `PR-4c0` 拒绝 | 🟢 今天绿 |

**⑤ 变异验证（3 个）**：把 `ShowReasoning` 硬编码为 `true` ⇒ 钉子 5 红；丢弃 `ReasoningEffort` ⇒ 钉子 3 红；把 `effort==""` 改成发一个固定默认值（如 `"medium"`）⇒ 钉子 4 红。按 `PR-5c` 的做法：**把文案改坏而保持行为不变时钉子必须仍绿**，用来证明断言测的是事件到达与请求体，而不是字符串。

**⑥ 手工验收（`§6.4` 第 5 步的那一半）**：`productstub` + `./octo-desktop`，把推理档位选到 `低/中`，发一条消息 ⇒ 应在正文之前看到可折叠的 **Thoughts** 块；选 `off` ⇒ 不出现。**今天做不到**（两个参数都没接线 + 替身不发推理段），这是 `5b1` 必须带上的第二层。

**§3.7 触发表**：**变更 `PR-5a` 已确认的注入缝形状**（架构约定，且它本身是 `PR-5a` 契约的一部分）＋ **动上游核心文件 `internal/server/server.go`**（调用点构造调参）。两项需人工确认（拟登记 `§4.1` 第 29 行）。

**测试红清单（7 条）**：见 ④；其中 **2/3/6 在实现前红**，**4/5/7 本就绿**（4 是回归钉）—— 按 `§6.4` 第 5 步的纪律，本就绿的记为"验证既有行为 / 防回归"，不假装是先红后绿。

---



范围与验收见 §1 表；每一 PR 各自出方案（含该 PR 的能力检索增量）后再开工，按 `开发规范` §6.4 走完整的五步（粒度自检 → 需求分析 → 开发计划 → 测试红 → 实现绿）。**顺序修正：`PR-4d` 先于 `PR-4c`**（理由见 `PR-4c` 第 0 步），§1 表已改。**`B7` 的两半分属两个 PR**：模型级 `eligible`/`transport` 过滤在 `PR-4d` 的投影里，功能级能力矩阵在 `PR-4e`（理由见 `PR-4d` 第 1 步①）。

---

## 3. 能力检索（§3.5，强制）

**每个"新建 X"的举证责任在新建一方。** 以下四处是本题的全部"新建"。

### 3.1 中台 HTTP 客户端（`internal/productclient`）

```text
既有能力检索：internal/provider/*（4 个 sender 接口）、internal/app/sender.go、
              internal/app/provider.go（ProviderCustom = "custom" 任意 BaseURL）、
              internal/config/config.go:105 Endpoint{BaseURL, Protocol, Headers, RPM}
为什么不能复用：**【可复用，已选择复用】** 见 3.2 —— 模型通信整条链路复用既有 provider 栈。
              productclient 只承载 provider 覆盖不到的那一层：登录/激活/bootstrap 的
              信封、业务错误码、目录验签、凭证生命周期。它不是第二个 HTTP 客户端。
复用后的形态：productclient 持有 agent.Sender 的装配参数（baseURL + token），
              401 刷新后重建 sender；**不自己写 SSE、不自己写 tool-call 聚合、不自己写 usage 归一**。
参考：需求基线 C2（动态凭证注入）与 C3（复用既有 provider 栈）已写明此结论，本文不复述推理。
```

### 3.2 网关流式调用 —— 本题最容易写重的一处

```text
既有能力检索：internal/provider/openai/{client.go,stream.go}（SSE 分帧、tool_calls[i].index
              碎片拼接、stream_options.include_usage、usage 归一）、
              internal/app/sender.go:228 openai.New(apiKey) + Client.BaseURL 覆盖
              （client.go:103 "optional override"）、Client.Headers（config 静态头）
为什么不能复用：**能复用，复用**。网关说 OpenAI 协议 ⇒ `BaseURL` 指向网关 + `Headers` 带网关头，
              就是一个标准 chat/completions。**不新建 SSE 解析器**（`finish_reason` 归一、
              工具调用碎片、`prompt - cached` 都已在 stream.go 里做对）。
              唯一缺口见下。
复用后的形态：**一处窄增量** —— `Endpoint.APIKey` 与 `Headers` 都是**构造期静态明文**
              （config.go:126 明写 "Values are static plaintext；no env-var interpolation"），
              而网关需要**每请求注入轮换的会话令牌**。故需一处"动态认证注入"口子，
              按 C2 规则 2 用「用当前内存 token 装配 sender」实现，**不改 provider 的解析逻辑**。
```

> **这直接回答了一个反复出现的问题**：「不是已经有 Claude / OpenAI 的对接机制了吗，直接用不就行了？」——**是的，就是直接用**，`BaseURL` + `Headers` 已经支持任意网关。本题**只有一处**真正需要新写：动态令牌注入（C2）。其余全是复用。

### 3.3 JCS 规范化 + ed25519 验签（**唯一没有现成实现的一处**）

```text
既有能力检索：internal/productprofile/profile.go —— 已是信任锚的 owner：
              TrustedKeyIDs map[string]string（keyId → base64 ed25519 公钥）、
              validateTrustedKeyIDs()（profile.go:191，校验 base64 与 ed25519.PublicKeySize）。
              internal/upgrade/upgrade.go:6 —— "there is no signature layer"（升级清单不验签）。
              全仓 grep：**无 JCS / RFC 8785 规范化实现；无 ed25519 验签调用**。
为什么不能复用：信任锚可复用，**规范化与验签无可复用实现**。但**不得**让签名方与验签方
              各写一份 —— 两份会漂移，结果是"替身测试全绿、生产验签失败"。
复用后的形态：复用 productprofile.TrustedKeyIDs 作信任锚；JCS+ed25519 只写**一份**
              （`internal/productclient/signing.go`，**随 `PR-4` 的目录/策略落地**），
              **替身签名与客户端验签共用同一实现**。
```

### 3.4 中台替身（`clienttest`）

```text
既有能力检索：web/src/dev/devBackend.ts（前端假后端，替换 globalThis.fetch）、
              httptest（.octorules 指定的 HTTP 测试方式）、
              internal/productclient（PR-1 新建，替身的类型来源）
为什么不能复用：devBackend 顶替的是**本地 /api 边界**，替身顶替的是**中台边界** ——
              不同层，不可互换（E2E闭环清单 §4 已写明这处易混淆点）。
              devBackend 是 TS/前端，无法驱动 Go 侧代码路径。
复用后的形态：**import productclient 的类型**做服务端，不重写 schema。
```

### 3.5 原子写（`internal/atomicfile`）

```text
既有能力检索：internal/agentprofile/store.go:258 writeFile（temp + rename，**未导出**）、
              internal/scheduler/scheduler.go:496、internal/server/session_groups.go:193、
              internal/channel/adapters/weixin/contexts.go:58（各自内联的 tmp 写法）、
              全仓无导出的通用 "WriteFileAtomic"。
为什么不能复用：既有实现全是**各自包内未导出**的实现细节，没有一个可跨包复用；
              agentprofile 的那份还与本包无关的 serialize/Profile 类型耦合。
              但**两个新消费者**（productstate 与 credentialstore）需要同一件事，
              所以在各自包内各写一份同样是错的 —— 那会让一次 rename 逻辑的修正要做两遍。
复用后的形态：抽出 `internal/atomicfile`（无产品名词的通用包），
              **一份实现、两个调用方**；语义按目标盘（FAT32/exFAT）定：
              rename 是唯一原子操作，不试图加锁（该文件系统没有锁）。
```

### 3.6 安装标识（`installId`）的生成

```text
既有能力检索：internal/channel/adapters/weixin/ilink/protocol.go:275 newUUID（**未导出**，
              且在 IM 适配器包内）、go.mod 无 UUID 依赖。
为什么不能复用：唯一实现是别的包里的私有函数，引用它会引入一条与 IM 适配器无关的依赖边。
复用后的形态：在 installId 的 owner（`internal/productstate`）内用 crypto/rand 实现 UUIDv4
              （约 10 行，**不新增第三方依赖**）。归属理由见 `E6.4` 规则 1：
              installId 是「这台安装」的属性，而安装边界＝数据根。
```

---

## 4.1 决策表（含 §3.7 人工确认登记）

**§3.7 登记格式：触发表中哪一行 + 事实 + 选择 + 谁确认的。**

| # | 触发 §3.7 的哪一行 | 事实 | 选择 | 谁确认 |
| --- | --- | --- | --- | --- |
| `1` | **改动敏感面：认证 / 凭证** | 替身要产出真签名，客户端就必须信任它。`developer.json` 的 `trustedKeyIDs` 现为空 `{}`，`HasTrustedKeys()` 为 false | **dev 构建信任一对测试密钥**（公钥进 `developer.json`，私钥仅用于测试与替身）；**生产 profile 不变**，仍用真正式公钥 + fail-closed。按人工指示「做得简单，先走通流程」。**落地时机：随 `PR-4`**（`PR-1` 的四个端点不验签，见 §3.3 与范围修正） | 用户，2026-09-11 |
| `2` | **非常规逻辑：静默降级** | `devBackend.ts` 无条件接管 `globalThis.fetch`，DEV 构建下真后端也永远答假数据，且形状合法、界面正常 | **不立即删除**，保留至「本地 Go 服务能真实应答 `/api/product/*`」之后（`G5` 触发条件）；保留期必须是**显式且默认关闭**的开关 | 用户，2026-09-11 |
| `3` | **新增测试专用组件** | 仓库内无中台替身；`internal/productclient` 尚未存在 | 建 `internal/productclient/clienttest`（库，仅测试引用）于 `PR-1`；**不做可执行替身**（最小范围） | 用户，2026-09-11 |
| `4` | **需求 / 方案不一致（§4.1）** | `E9` 规则 2「余额更新唯一时机 = 终态帧」↔ `C7`「终态帧本批收不到，兜底走拉模式」 | 以 `E9` 为 owner 改写为**两条并列来源**，本批取拉模式；判据分两版。已落地 | 用户，2026-09-11（`V-11`） |
| `5` | **不删除疑似过时文档** | 问「`中台交付包.md` 是否没用了、可否删除」 | **不删除**：它是错误码/字段名的 SSOT owner（`§3.8`/`G2`），也是向中台索取 `S-8` 的凭证；但**性质改为「我方提案（未生效合同）」**，`§9` 的 15 条属本轮已声明不做的层 | 用户，2026-09-11 |
| `6` | `V-6`（`开发规范` 要求而缺失） | 全仓无 `开发计划.md` | 建立本文档 | 依据 `开发规范`，无需另行确认 |
| `7` | **非常规逻辑：启动期写用户文件**（§3.9.1） | `product-state.json` 是用户可编辑文件，而首启必须写出 `installId` 才算存在 | **惰性播种**：只在文件**不存在**时就地写一次；**文件已存在 ⇒ 只读、绝不重写**。写失败 ⇒ 内存默认值 + 错误向上返回，不阻塞启动、不假装已持久化。这是 §3.9.1 允许的"幂等首启种子"例外。规则落在 `需求基线` `E6.4`（`V-7`；规则 2 的措辞于 `V-12` 修正） | 用户，2026-09-11（"先走通流程，别太复杂"）；细节按上述最简解释落地 |
| `9` | **动上游核心文件**（§3.7 / §3.3.1 顺位 3） | `PR-2b2a` 要把 5 个产品端点挂进服务。已核实：便携包打的 `cmd/octo-desktop` **在上游 `main` 上**，`server.New(` 全仓只有两处（都在上游），`internal/server` **没有**可用的扩展缝，且**不能直挂 `s.mux`**（上游 `auth_test.go:266` 硬断言只允许 4 条直接注册，直挂还会绕过 `requireAuth`） | **方案**：`internal/server` 只加**一个可选、无产品名词**的 `MountAPI(func(pattern string, h http.HandlerFunc))`（形状照抄既有 `s.api`，**不给裸 `mux`**）+ 3 行调用；产品装配全在 `cmd/octo-desktop/main.go`。**2 个核心文件 / 约 20 行 / +2 个 `OCTO-FORK`**。四个替代方案已否决并写明理由（§2 `PR-2b2a`） | **✅ 已批准并落地**：用户 2026-09-11 答「继续」，按顺位 2 开工。**实测比预估更小**：核心文件 **2 个**、`cmd/octo-desktop/main.go` **+1 行**、`internal/server/server.go` **+18 行**（其中 11 行是注释，代码 1 个字段 + 3 行调用）、`OCTO-FORK` **+2**；产品装配整份收进**新文件** `cmd/octo-desktop/product.go`（上游零改动）。`main.go` 只多一行是因为装配函数与 `main` 同包 ⇒ 无需新增 import | 用户，2026-09-11（「继续」） |
| `8` | **凭证存储形态（数据根 / 凭证）** | `credential.json` 存**明文** refresh token、随 `data/` 复制 | 已由 `PQ17` 拍板（明文、可复制），安全性作为 `D-004` 延后；本 PR 只保证「**access token 不落盘**」与「损坏即丢弃」两条 `E6.3` 硬约束，不引入加密与权限（目标盘 FAT32/exFAT 无 POSIX 权限） | 用户，2026-09-11（`PQ17`） |
| `10` | **需求 / 方案不一致（§4.1）** | `E6.4` 规则 2 说「**没有任何一次进程启动会写文件**」，而 `productstate.Open` 在文件缺失时就地播种写盘（探针实测：只调 `Open` 即生成 `product-state.json`）⇒ `PR-2b2a` 一旦接线，**启动即写盘** | **改条文**（用户 2026-09-11 选定）：规则 2 改写为「文件不存在 ⇒ 播种；**已存在 ⇒ 只读、绝不重写**」。**安全承载点随之转移**，故补 `TestExistingStateIsReadNotRewritten`：用 `os.SameFile` 判"文件未被替换"（temp+rename 重写必换 inode，**即使字节相同**），并在只读目录下断言 `Open` 仍成功。**该测试已用变异验证会红**（把 `Open` 改成也写一次 ⇒ `reopening replaced the state file`）—— 首版只比字节 + 等报错，变异后**仍绿**，是无效的钉子 | 用户，2026-09-11（`V-12`） |

| `11` | **动上游核心文件**（§3.7 / §3.3.1 顺位 3） | `PR-2b2b` 要把窗口令牌送进窗口，而 `shellURL`（`bridge.go:171`）是窗口 URL 的**唯一**构造点（新建窗口与 `SetURL` 都走它）；且宽门要求在 `internal/server` **再动一次** | **接受**，且压到最小：`bridge.go` **功能仅 +1 行**（片段函数，**无令牌时返回空串** ⇒ 上游 `TestShellURL` 与 `octo serve` 的 URL 形状不变）、`main.go` **功能仅 +1 行**、`server.go` +49（含注释）。**`internal/productruntime` 不动** —— 门在服务端单一注册点，比原设想更小。**不能改 `shellURL` 签名**：上游 `bridge_test.go` 的 `TestShellURL` 直接以 `shellURL(base, hash)` 调用并断言精确字符串 | 用户，2026-09-11 |
| `12` | **改动敏感面：认证 / 路由** | 请求门拦到哪。曾定"全部 `/api/*`"，**同日已推翻**（见下） | **只覆盖本 fork 经 `MountAPI` 缝注册的产品路由**（即 `/api/product/*` 共 5 条），上游 `/api/*` 一条不碰。**推翻"宽门"的原因是实证，不是推理**：门是**请求头**校验，而浏览器给裸 `<img src="/api/…">` 子资源**加不了自定义头** —— 宽门实测把 `GET /api/uploads/{name}`（聊天图片缩略图，`ChatView.svelte:2761`）与 `GET /api/sessions/{id}/artifacts`（artifact 图片，host 文档里的 `<img>`）都打成 `403 product_gate`；又因桌面壳的 `hubAddr` 就是 `127.0.0.1:8088`（`cmd/octo-desktop/main.go:62`）、与 CLI `localServe()` 探测的默认地址同端口，宽门还会把 `octo` CLI 的 `send_message` 打死。**停靠方式**：门改成**注册器**的属性而不是路径前缀 —— `api`（上游）与 `productAPI`（带门）共用 `register`，缝只交 `productAPI`。前缀字面量因此只留在 `productruntime` 一处，`/ws` 与 `GET /api/uploads/*` 被**结构性**排除，不需要任何白名单。**代价**：`P3` §5.4.3「未登录时窗口发起的请求同样不可用」在上游 `/api` 路由上不由本门覆盖，登记为 `D-006` —— 真正实现它需要**服务端会话/激活态**校验，而非窗口身份 | 用户，2026-09-11（推翻同日稍早的"宽门"定夺） |
| `13` | **范围收窄：新通道的鉴权** | `/ws` 的前端已经把 `?window_token=` 带上（浏览器 WS 不能自定义 header），但后端不校验 —— 该参数目前是装饰性的 | **本轮不校验，只登记 `D-005`**（与 `L-F1` 的验收绑定）。理由：`/ws` 有**独立**鉴权路径（`auth.go:78` 为它单开 `?access_key=`），一起改会同时触及两处鉴权语义 | 用户，2026-09-11 |
| `14` | **非常规逻辑 / 唯一无复用的一处** | `PR-4` 的验签：契约原定 `signature = ed25519(JCS(policy 去掉 signature))`，而 **JCS（RFC 8785）全仓无实现、无依赖可用**，且 `policy` 含 `ttlSec: 3600` 这类数字 ⇒ 必须实现最危险的**数字规范化**（ECMAScript `Number::toString` 最短往返）。真正的代价是**两份实现会漂移**（中台签、客户端验），表现是"替身全绿、生产验签失败" | **改契约：签 `data.policy` 的原始字节**（`signature` 移出 `policy`，成为同级 `policySignature`）。验签方与签名方**不可能对"被签字节"产生分歧**。代价：会重新序列化 JSON 的中间人会让验签失败（fail-closed，**不降级安全性、只降可用性**）；本产品壳**直连中台**、无中间人。正规做法（JCS）登记为 `D-007`，含**四条写死的正确做法**（`signature` 必须留在 `policy` 外、只换被签字节、JCS 只写一份、必须用 RFC 8785 官方测试向量） | 用户，2026-09-11（「先简单的实现……别搞太复杂；正规的做法写到待解决问题里面；目前先走通流程」） |
| `15` | **需求 / 验收面不一致（§4.1）** | `PR-4` 需求分析时核实：`B7`（能力矩阵 / 入口可见性 / `eligible`）与 `B8`（会话模型绑定与模型下架）在 33 条闭环里**完全没有任何覆盖**（grep `B7`/`entitled`/`visible` 零命中）—— 需求写了，却"无人能判是否完成"，与 `G4`「不靠人记」直接冲突 | **补成闭环**：新增 `L-C6`（能力矩阵与入口可见性）、`L-C7`（会话模型绑定与模型下架），**33 条 → 35 条**；新增 `⬜` 标记（规格已写清、实现未开始），与 `⚠️`（规格有缺口、做了也判不了）区分。归属：`L-C6` → `PR-4e`、`L-C7` → `PR-5` | 用户，2026-09-11（选「补成闭环」而非"登记待解决"） |
| `16` | **粒度自检：原 PR 过大** | 原 `PR-4` 一条 PR 要覆盖**3 条闭环**（`L-C1a`/`L-C2`/`L-C1b`）外加一次包搬运，违反 `E2E闭环清单` §2.0 的粒度判据 | **拆 5 个子 PR**：`4a` 验签核心（打底、单测可判）→ `4b` 拉取与缓存（`L-C1a`）→ `4c` 三条降级（`L-C2`）→ `4d` 投影与上屏（`L-C1b`）→ `4e` 能力矩阵消费（`L-C6`）。**本轮只做 `4a`** | 用户，2026-09-11（选「只做 `PR-4a`，做完汇报再继续」） |
| `17` | **改动敏感面：认证 / 凭证** ＋ **范围变更：新增 PR** | 本轮核对发现 `L-B2` / `L-B3` 在 §1 的 13 个 PR 里**无归属**、其设计文档 `P4-拦截页.md` 被 `i18n.ts` 引用两处却全仓库不存在（`V-15`）；且 `L-A6` 要求的「清凭证」未接线 —— `IsSessionExpired` 零调用点、`Creds.Delete()` 只在 logout（`V-16`） | **新增 `PR-2c`「拦截页与控制面失败面」**承接三条闭环，第一件交付物是补出 `P4-拦截页.md`；`L-A6` / `L-B3` 的失效清凭证按「**只有 `unauthorized` 清凭证**」实现，不回落到任何未授权来源、不做静默降级（§3.9） | **待人工确认（2026-09-12 登记）** |

| `18` | **粒度自检：PR 顺序错** | `PR-4c` 的四条文案里有**两条没有可上屏的面**：`B9` 的四条输入（无缓存 / 过期 / 验签失败 / 分组空）都发生在模型选择器里，而选择器的数据源是 `/api/product/chat-modes`（`api.ts:341`）—— 后端**未注册**（`V-24`），今天由 `devBackend.ts` 的本地常量接管。让选择器改读签名目录是 `PR-4d` 的交付物。⇒ `PR-4c` 先做的话："无缓存 ⇒ 选择器空"这个可见结果**不可能出现**，"分组空"**完全无处可去**；且"不得开启新回合"会去拦一个本来就不该存在的本地选择（`B1` 规则 1 禁止内置名单） | **把 `PR-4c` 的前置从 `PR-4b` 改为 `PR-4d`**（顺序 `4b → 4d → 4c`）；`PR-4d` 期间目录不可用时 `chat-modes` 返回空列表（fail-closed 已满足）且**不新增降级文案**，文案整批归 `PR-4c`（与 `PR-4b`「只分类、不定文案」同一已接受切法）。**⚠️ 质检更正 2026-09-12**：本行原写"不出文案"不成立 —— 选择器对空组的既有文案 `mode.no_models` 照常渲染，应读作**不新增、不区分解释**；`PR-4c` 的交付物是**把四条原因分开说**。**✅ 已确认（用户，2026-09-12）：换序** —— `PR-4c` 前置改为 `PR-4d`，§1 表已改 |
| `19` | **改动敏感面：凭证** ＋ **需求/实现不一致** | 发现 `V-32`：`credential.json` 的 refresh token **没有任何生产调用点读回内存**（`CredentialHolder` 是每进程内存，装配处传空值 `product.go:150`；`creds.Load()` 全仓只在 `session_expiry_test.go:81` 出现过）。后果：① 进程重启后 `loggedIn:true` 但**任何已授权调用都 401** ⇒ `ErrSessionExpired` ⇒ 按 `L-A6` **清凭证回拦截页** ⇒ **每次重启都要重新短信登录**；② `E6` 的验收「进程退出后重开…能自动登录」**不可能通过**，`L-E2`（换机不重新登录）同路径不可达；③ `B3` 的"启动异步刷新"**没有令牌可用** | **待选**：㈠ **单独一个小 PR 先修**（装配处 `Load` + `Holder.Set`，约 10 行 + 测试），合进 `v1` 后再开 `PR-4c`；㈡ 并入 `PR-4c` 一起做；㈢ `PR-4c` 只做"陈旧时按需刷新"（`B3` 改条文，去掉"启动刷新"），恢复整件留给 `PR-7`。**三个方案的代价见 §2 `PR-4c` 缺口 ③** **✅ 已确认（用户，2026-09-12）：单独一个小 PR 先修**，不并入 `PR-4c`、也不留给 `PR-7`；**已落地并合入 `v1`**（见 §2「前置修复 · `V-32`」） |
| `20` | **SSOT：新增第 14 条本地端点** | 可用性状态（`ready`/`absent`/`stale`/`unverifiable`）是**运行时**事实，三处都能各自造一份意见。不能进 `ProductStateDTO`（它是 `productstate.PublicState` 的镜像 = 磁盘文件，`P4-拦截页.md` §2.2 已否过一次）；也不能只挂 `chat-modes`（composer 的"不得开新回合"要不看选择器就知道） | **新增 `GET /api/product/catalog`**，形状照抄 `PR-2c` 的 `/control-plane`（运行时装配事实 + 唯一 owner + 独立端点）。枚举**四值**与 `B9` 的四条输入前三条一一对应；第四条（分组空）是投影结果、不进该枚举 **✅ 已确认（用户，2026-09-12）：新增 `GET /api/product/catalog`**，形状照抄 `/control-plane`；登记时机随 `PR-4c` 落地（端点本身尚未实现，本 PR 只定形状） |
| `21` | **需求 / 契约不一致（§4.1）** ＋ **范围收窄** | ① `中台交付包` §4.3 写"`/catalog/models` 返回**同一份已签名 `policy.catalog`**"，但**只回 `catalog` 对象就验不了签**（签名覆盖整个 `data.policy` 的原始字节）。② 条件请求允许 `ETag` **或** `knownVersion`；选 `ETag` 要在缓存加第五项不透明元数据，选 `knownVersion` 直接复用已有的 `catalogVersion`。③ `B3` 要求"按 TTL 周期刷新"，而后台定时器需要生命周期 owner（起停 / 窗口开关 / 拔盘冻结） | ① **改契约**：`/catalog/models` 返回**与 `bootstrap` 同一个信封形状**（`{policy, policySignature}`）—— "一个已签名目录"只有一种形状（§3.8）；② 未变化时中台可答 **`304`** 或 `{"unchanged":true}` 二选一，**客户端两种都要认**；③ **选 `knownVersion`**；④ **不引入后台定时器**：`B3` 自己的验收是"TTL 到期后**下一次开选择器**触发刷新"（读时判定即满足），定时器登记 `TODO`、与 `PR-7` 的拔盘冻结一起定生命周期 **✅ 已确认（用户，2026-09-12）：四处全按建议** —— `knownVersion` 条件请求、`/catalog/models` 与 `bootstrap` 同形信封、两种"没变"应答都认、不引入后台定时器。`中台交付包` §4.3 与 `本地API契约` §2.14 的文本改动随 `PR-4c` 一起落（避免文档先于实现） |

| `22` | **动上游核心文件** ＋ **非常规逻辑** ＋ **新增注入字段** | `PR-4d` 手工验收暴露 `V-35`：目录模型的复合 id 被 `senderForSession` 的 fallback 交给默认 sender（=`config.yml` 指向的真第三方端点），用户看到第三方 400，且**对话内容随该请求发到了第三方**。拦截落点有四选（见 §2 `PR-4c0` 第 1 步）：一处（`senderForSession`，须用拒绝型 sender，因 `§4.5` 不改签名）／四处调用点／只改前端 | **一处拦截**（用户选定方案 ①）：`Config.GatewayModelPrefix` 注入（照抄 `MountAPI` / `WindowToken` 的注入纪律，`internal/server` 不 import fork 包）＋ `senderForSession` 包装成一层判断，上游逻辑逐字搬进 `resolveSenderForSession`。**scope 写在字段注释里**：全 profile 生效（不是 production 的 `RequiresControlPlane`，那条仍归 `PR-5`）。代价已记：`internal/server/server.go` 137 → 247 行 | 用户，2026-09-13（在四选一中选定"在 `senderForSession` 一处做"） |

| `23` | **粒度自检（§6.4 第 0 步）** | 原 `PR-5` 一条要覆盖 **6 条闭环**（`L-C3a` `b` `c`、`L-C4` `C5` `C7`）＋ `V-36` ＋ `V-35` 的 production 半边。判据是"一条闭环 = 一次可独立判定的用户可见行为" | **拆 5 个子 PR，执行顺序 `5a → 5c → 5b → 5d → 5e`**，本轮只做 `5a`（同 `PR-4` 只做 `4a`）。**`5c`（不回落）紧跟 `5a`**，因为它只收窄失败路径 —— 越早接上，后续能力越不会长在一条会回落的路径上 | **✅ 已确认（用户，2026-09-13）**：按建议拆 5 个子 PR（`5a`→`5c`→`5b`→`5d`→`5e`），**本轮只做 `5a`** |
| `24` | **~~需求 / 契约 / 代码三不相符（新登记 `V-37`）~~ —— ⚠️ 该阻塞已被证伪撤回，本行保留作记录** | 原判"网关请求的目标 URL 三处各有说法，没有任何 `BaseURL` 能同时满足" | **❌ 前提不成立**：`internal/provider/openai` 的 `endpointURL()` 拼接前**主动剥掉**尾部 `/v1`（`if strings.HasSuffix(base, "/v1") { suffix = "/chat/completions" }`），故含/不含 `/v1` 都打到 `/v1/chat/completions`；该包 `client_test.go:364` 早已断言不得含 `/v1/v1`（issue #1625）。**证伪它的是 `PR-5a` 自己写的反钉**（"双前缀必 404"当场红）。**据此修正原决议**：候选（A）里"`GatewayHost` 语义改为不含 `/v1`"降为**约定**（值保留裸 host，`validateHost` 不动）；"把网关路径定为标准 `/v1/chat/completions`"**仍然有效**（那半是真的：我方提案的 `/v1/ai/…` 因常量到不了） | **✅ 已确认（用户，2026-09-13）原决议；⚠️ 2026-09-13 实现时由本 PR 证伪并修正（无需再确认：修正方向是"少改代码"，且未动任何契约）** |
| `25` | **动上游核心文件** ＋ **认证/凭证** ＋ **路由** | `PR-5a` 要做三件事：① 产品把"网关 sender"交给上游 `internal/server`（`Config` 今天**无任何 sender 注入缝**，且该包按纪律不得 import fork 包）；② 动态令牌注入（`C2`）；③ `ensureSender` 条件化（`V-36`：它在 `buildAgent` 之前无条件要求默认 sender，而产品包按 `B4` 本就不该有第三方 endpoint） | **① 注入 `Config.GatewaySender func(model string) (agent.Sender, error)`**，照抄 `MountAPI`/`WindowToken`/`GatewayModelPrefix` 的注入纪律；现有 `gatewayBound()` 就是接缝（`PR-4c0` 在那里拒绝，本 PR 换成调用工厂）。**② 不改上游 provider**：按 `C2` 规则 2「用当前内存 token 装配 sender」（`NewSender` 轻、限流器按 `limiterKey` 复用）⇒ 每回合新建、**刻意绕过 sender 缓存**（令牌轮换）。**③ `ensureSender` 条件化**：网关绑定的回合不走默认 sender 要求。**⚠️ 实现偏差（2026-09-13）**：签名实为 `func() (agent.Sender, error)`（去掉无人使用的 `model` 参数），`Accept` 头不设（由 provider 自己设，避免同一事实两处定义）| **✅ 已确认（用户，2026-09-13）：三处全按建议** —— 新增 `Config.GatewaySender` 注入缝、复用 `C2` 规则 2 不改上游 provider（每回合新建、绕过 sender 缓存）、`ensureSender` 条件化 |
| `26` | **模型源路由（认证/凭证面）** ＋ **动上游核心文件** —— `PR-5c` 的机制 | C9 规则 2 要求六条失败面全部失败关闭，而 `RequiresControlPlane()` **零生产调用点**（`V-27`，本次按代码复核仍为 0）：`senderForSession` 的"默认 sender"分支来自环境变量 / `data/config.yml`，**在 production 下今天照样能把对话发到第三方** | **在 `senderForSession` 一处按注入判据拒绝**（而非改 `internal/app` 的 sender 构造）：`RequireGateway=true` 时默认 sender 一律不可用 —— 网关绑定的回合走 `GatewaySender`，非网关绑定的回合返回拒绝型 sender。**理由**：该函数已是全部回合路径的唯一解析点（`PR-4c0`/`PR-5a` 建立），在此处收口即"一处判据、一处拒绝"，且不必碰 `internal/app`（`C2` 明确优先不改上游） | **✅ 已确认（用户，2026-09-13）：在 `senderForSession` 一处收口** |
| `27` | **新增注入字段（架构约定）** | `internal/server` 按纪律不得 import fork 包，而本条要的两个判据事实 owner 都在 `internal/productprofile` | **注入两个 bool**：`RequireGateway`（= `RequiresControlPlane()`）与 `ControlPlaneReady`（= `ControlPlaneConfigured() && HasTrustedKeys()`），装配层取值。**候选**：注入一个结构体（`TurnSourcePolicy{RequireGateway, ControlPlaneReady}`）—— 字段同为布尔、同为装配期常量，结构体多一层解引用却不增加语义，故建议两个字段直陈；**取值不新建判据**（两个函数都已存在且 `cmd/octo-desktop/product.go:127` 已在装配时取到） | **✅ 已确认（用户，2026-09-13）：两个 bool 直陈** |
| `28` | **验收方式：黑盒"零外发"断言** | `L-C5` 的原文验收是"六条各造一次，**无一条能把请求发到网关以外的地方**"。只断言错误文案测不到这件事：文案可以正确而请求仍然发出（`V-35` 就是这样：用户看到一句第三方 400，而对话内容**已经出去了**） | **每条失败面都断言第三方替身的命中数 = 0**，替身=一个 `httptest` 第三方端点（同时充当网关与控制面）。**反钉 3 条**防过度收紧（dev profile 下 `config.yml` 仍必须可用、不注入新字段时行为逐字不变） | **✅ 已确认（用户，2026-09-13）：按「零外发」断言** |

| `29` | **变更已确认的注入缝（架构约定）** ＋ **动上游核心文件** | `PR-5b1` 要把推理偏好（`show_reasoning` / `reasoning_effort`）接到网关回合，而 `PR-5a` 刚确认的 `Config.GatewaySender` 是 **`func() (agent.Sender, error)`**（第 25 行，且实现时已把文档里写的 `func(model string)` 参数删掉）—— **不带调参**，而这两个值运行时可改（`PATCH /api/config/show_reasoning`、`PATCH /api/sessions/{id}/reasoning_effort`），装配期构建一次的 `GatewayEndpoint` 无法持有 | **签名加一个调参入参**：`GatewayTuning{ShowReasoning bool; ReasoningEffort string}`（`internal/server` 定义）＋ `GatewaySender func(GatewayTuning) (agent.Sender, error)`；调用点在 `senderForSession` 里按 **`senderForEntry` 逐字同一个读法**取值（`cfg.ReasoningEffort` + `cfg.EffectiveShowReasoning(nil)`），`internal/server` 仍是"为 sender 读配置"的唯一位置（§3.8）；`productruntime` 不 import `internal/config`、不加一行判断（§3.5）。**`Dialect` 刻意不传**（保持 `""` ⇒ 走 `provider/openai` 的 `default` 分支 = 扁平 `reasoning_effort`），因为"网关的推理字段形状"是**契约事实而非本地选择**：把它做成缝上一处可改的值，而不是散在请求结构里 | **✅ 已确认（用户，2026-09-13）：签名加一个调参入参** |
| `30` | **契约新增要求（互操作面）** | 网关回合要产生思考块，就必须**在线路上请求推理** —— 而 P0 之前从未向中台确认过"网关接受哪个推理字段"。`provider/openai` 用 `Dialect` 区分五种形状（扁平 `reasoning_effort` / 嵌套 `reasoning:{effort}` / `enable_thinking` 布尔 / `thinking:{type}`），网关该属哪种**今天无人知道** | **按我方提案的标准形状取值：扁平 `reasoning_effort`**（`low`/`medium`/`high`/`xhigh`/`max`），并**把这一条写进 `中台交付包` §5.2**：网关必须接受该字段并遵循标准语义，**不认识的字段不得 400**；`effort==""` 时客户端完全不发该字段，故"不发"这条路也必须可用。**登记为 `PQ27`（待中台确认）**；若中台实际形状不同，改动点只有 `GatewayEndpoint.Sender()` 一处（见第 29 行） | **✅ 已确认（用户，2026-09-13）：按标准扁平形状，并要求网关不得因未知字段 400** —— 契约文本已随本轮落 `中台交付包` §5.2 |

| `31` | **新增注入缝 / 改注入缝** ＋ **动上游核心文件** ＋ **启动期写用户文件**（§3.9.1） | `V-39` 要有人把 refresh token 换成 access token，而唯一"天然带 ctx"的位置（回合解析点 `senderForSession` / `buildAgent`）**在上游文件里且没有 ctx**（`server.go:1501`）⇒ 三个候选的代价差别极大：A1 接缝在 fork 自己的 `GatewayEndpoint`（上游 0 字节、启动 0 I/O，代价＝构造可能做一次 I/O）；A2 把 ctx 传进上游（5 处调用点 + 4 个测试文件，`internal/server` diff 再涨）；B 启动异步续期（要动"`.Bootstrap(` 只在 `fetchCatalog`"这条已确认的 SSOT 源码测试，或把 `PR-4c` 的 TTL/条件请求拉进来；且**刷新必然轮换 ⇒ 启动即写 `credential.json`**，需给 §3.9.1 一条显式豁免） | **待选**：**A1（建议）** 按需、接缝在 `GatewayEndpoint`（`Ensure func(ctx) error`，nil ⇒ 今天行为；`Sender()` 在无令牌时按预算调一次）／A2 改上游传 ctx／B 启动异步（若选 B 建议并入 `PR-4c`，不单开小 PR）。**选 A1 则 §4.1 需一并改写 `C2` 规则 3 的作用域**：「构造不做 I/O，除非内存里没有可用令牌 —— 此时向持有凭证的客户端要一次，单飞与轮换仍全归它」（§3.10：规则必须写明它管哪个域） | **✅ 已确认（用户，2026-09-13）：选 A1** —— 接缝在 `GatewayEndpoint`（`Ensure func(ctx) error`；nil ⇒ 今天的行为逐字不变），`Sender()` 仅在"持有者没有 access token"时按 10 秒预算调一次；`internal/server` **零改动**。同轮把已确认的 A1 方案落进 `C2` 规则 3 的作用域（`需求基线` §3 `C2`），并新增 `V-43`（见 `PR-4c1` 第 1 步事实 10）|
| `32` | **非常规逻辑：静默降级**（§3.9） ＋ **便携/只读盘** | 轮换落盘会失败（U 盘写保护、盘满、权限），而此刻**内存里的令牌是可用的**。两种口径后果相反：拒绝回合会把"这一场本来能用"变成"完全不能用"（与 `E6` 的便携只读场景冲突）；继续则必须明说"下次启动要重新登录"，否则就是静默降级 | **待选**：㈠ **回合继续 + `slog.Warn` + 下次启动明确要求重新登录**（建议；不删凭证、不改 `loggedIn`、不假装已保存）／㈡ 拒绝回合 | **✅ 已确认（用户，2026-09-13）：选 ㈠** —— 回合继续 + `slog.Warn`（"this launch keeps working and the next one will ask for a sign-in"），不删凭证、不改 `loggedIn`、不静默 |
| `33` | **改动敏感面：凭证** ＋ **需求/实现不一致** | `Ensure` 若直接调 `refreshSingleFlight`，**不会**自动清凭证（`Clear()` 是 `doAuthorized` 的包装行为，`:179-183`/`:187-190`）；而 `C12`/`L-A6` 要求"只有 `unauthorized` 清凭证、并回拦截页" | **待选**：㈠ 在 `EnsureToken` 里显式 `Clear()`，注释写明它与 `doAuthorized` 是同一事实的两处（建议）／㈡ 不清凭证只拒绝（会让用户卡在永远失败的状态，且 `E6` 的"回登录页"路径不成立）。**两案都不得**在契约/版本/时钟失败时清凭证（继续 fail-closed 但保留会话） | **✅ 已确认（用户，2026-09-13）：选"清凭证"** —— `EnsureToken` 在平台明确拒绝时 `Clear()` + 回登录页（与 `doAuthorized`/`L-A6` 同一判据），不留给下一次 |

> **未决、且已知道会卡住哪些 PR 的四项：** `PQ25`（激活幂等 → `L-A1b`）、`PQ26`（客服载体 → `L-A4` 的 UI 判定）、`PQ27`（网关推理字段形状 → `L-C3b` 的**真机**判定）、`N-5`（WS 语义 → `PR-8`）。`V-12` 已由 `PR-2b2a` 收尾（见第 10 行）。


## 4.2 一致性矩阵

**一个取值散落在多处时，改动必须照这张表同步。** 新增跨文档取值时在此加一行。

| 取值 | 散落在哪几处 |
| --- | --- |
| `boxCode` / 盒子编号 | 需求基线 `E1` `E5` `E6.1`；中台交付包 §3.2 登录请求；本地API契约 激活 DTO；`web/src/lib/product.ts`；`web/src/dev/devBackend.ts` |
| 激活错误码（`activation_invalid` / `activation_code_used` / `box_code_unknown` / `box_code_mismatch`） | 中台交付包 §3.2 错误码登记；需求基线 `E1`；本地API契约 错误码总表；Go 端常量（`PR-1` 起，`internal/productclient/dto.go` —— **唯一 owner**） |
| 端点路径（`/api/product/*`） | 本地API契约（唯一登记表）；Go handler；前端调用点 |
| 凭证文件（`data/credential.json`） | 需求基线 `E6`/`E6.3`；中台交付包 §4；`internal/credentialstore`（`PR-2` 起） |
| 中台字段名（全部） | 中台交付包 §3/§4/§5（**提案**）→ `internal/productclient` DTO（**唯一落地处**） |
| 签名信封（`data.policy` + `data.policySignature`） | 中台交付包 §4.3（**定义**）；`internal/productclient/signing.go`（`PR-4a` 起，**唯一实现**）；替身 `clienttest` 复用同一实现；`D-007` 记四条件 |
| 被签名字节 | **`PR-4a` 起 = `data.policy` 的原始字节**（不做规范化）。切回 JCS 时只改这一处定义，信封形状不变（`D-007`） |
| `displayName`（中英双语对象） | 中台交付包 §4.3（**中台下发形状**）；需求基线 `B6` 规则 2（取值与回落规则）；本地API契约 §2.8（本地应答形状）；前端 `web/src/lib/chatMode.ts`（**唯一渲染点**，不建 id→名表） |
| 模式展示名（`mode.privacy`/`smart`/`default`） | **前端 i18n `web/src/lib/i18n.ts` 是唯一来源**（需求基线 `B5` 规则 1「展示名是界面文案」）；中台目录与本地 API **均不下发**（中台交付包 §4.3 / 本地API契约 §2.8 均标"禁止"） |
| 模式 id 组（`privacy`/`smart`/`default`） | 需求基线 `B5` 规则 1（产品常量）；中台交付包 §4.3（`catalog.modes[].id` 只做校验，未知即忽略）；`internal/chatmode`（`V-2`，`PR-4d` 搬运后为唯一 owner） |
| 信任锚（keyId → ed25519 公钥） | `internal/productprofile`（`TrustedKeyIDs`，**唯一 owner**）；`developer.json`（测试密钥对，`PR-4a` 起）；`production.json`（正式公钥，不变） |

## 5. 本轮明确不做

正式中台地址与验签公钥（`D-001`）、需中台共同签字的合同字段 `clientRequestId` / `toolPolicy`（`D-003`）、流中 `retract` 与账本终态帧（`D-002`）、`E2E-09` 更新契约、正式协议与隐私版本确认（`E2E-15`）。

### 5.1 已登记、明确延后 → 见 [`TODO.md`](TODO.md)

2026-09-12 第三轮审计汇总出的"发现但决定不在闭环期修"的项，**已独立成文到 [`TODO.md`](TODO.md)** 并补全了字段（原表只有"为什么延后 + 接手点"，缺"现在什么样"，读者无法判断代价）。

**本文件不再保留那张表**（`开发规范` §3.8：同一个事实只有一个 owner）—— `TODO.md` 是它的唯一 owner，与 [`待确认问题清单.md`](待确认问题清单.md)（还没拍板）、[`待解决问题.md`](待解决问题.md)（已拍板不做）、`需求基线.md` §5.6（`V-n` 事实偏差）四者分工见该文件开头。

一行结论：**8 条延后项，接手点分别落在 `PR-9`（守卫接线、符号检索）、`PR-8`（WS 事件，阻塞于 `N-5`）、`PR-6`（会话明文，需人工拍板）、以及三件与任何 PR 无关的小事（`desktop-dev` 移植、`P2` 文档、`App.svelte` 注释）。** `V-19` 同日已闭环。

## 6. 修订

| 日期 | 说明 |
| --- | --- |
| 2026-09-13 | **新增 `PR-2d`（授权被平台否认时切回激活表单，`V-44`）。** 用户在 `PR-4c1` 手工验收后提问"退出登录后登录提示激活码不正确"，按现场核对得出：重启替身清空了内存账号（`cmd/productstub` 自述 `State is in memory`），而 `data/product-state.json` 仍 `activated: true` ⇒ 本地不发激活凭证 + 平台按激活失败答复 ⇒ 文案（`activation_invalid` → 「激活码不正确」）落在**没有该字段的表单**上，界面内的唯一出路不存在（只能删/改状态文件）。**根与 `V-22` 同源**：`activated` 的 owner 是平台，本地那份只是投影。**粒度自检**：与 `PR-2c`（已完成）同族但不重叠、与 `PR-4c`（目录降级）不相交；**明确不含客服载体（`V-4`/`PQ26`）**。**八条事实按代码核对**（`publicOf:371` 的投影关系；`validateLogin:456` 的 `activationAttempt` 是客户端单方面决定；替身 `server.go:342-347` 自述该答复合法；`productstate` 没有任何降低 `Activated` 的入口）。**方案**：`productstate.WithdrawActivationClaim()` + `handleLogin` 在 `failPlatform` 前按**两个条件同时成立**收回（平台四个激活码 **且** 本次未带激活凭证），应答不变；前端在业务级失败分支 `refreshProductState()`（**刻意不做码清单**，避免前端第二份家族分类）。**七条钉子 + 四个变异**，手工验收＝用户刚走过的四步。已拍板取方案 A（用户，2026-09-13）。 |
| 2026-09-13 | **`PR-4c1` 落地并合入 `v1`（`V-39` + `V-42` + `V-43`）。** 三项拍板全部按建议落（A1 接缝 / 回合继续 + warn / `EnsureToken` 显式清凭证）。改动 7 个文件、+275 −25，**上游零字节**（`internal/server` 一行未动）。**实现中发现并同轮修掉 `V-43`**（`refreshSingleFlight` 把任何刷新错误压平成 `ErrSessionExpired` ⇒ 一次网络抖动就删凭证、把用户登出，与 `dto.go:53` 自己的定义矛盾）：改为只在 `isUnauthorized(err)` 时返回该哨兵，其余原样带因返回；`doAuthorized` 与 `EnsureToken` 都据此决定是否 `Clear()`（`C12`）。**钉子 13 条**（7 条 `session_renew_test.go` + 5 条 `ensure_token_test.go`，含 `V-42` 主钉"盘上那份能换出下一次"与反钉"只修 `V-39` 会制造下一次启动被拒"），**变异 6 个**全部落在预测位置（M2 落盘丢字段 / M3 落盘挪到交换之前 / M4 不清凭证 / M5 落盘失败拒绝回合 / M6 压平错误）。**落地才发现的边界**：`persistRotation` 在"内存与盘上相同"时不写盘 —— 这既是幂等（第 2、3 次 `Ensure` 不落盘），也让只读盘场景只在"真的发生过交换"时才报 warn。**手工验收待用户做**（三步见 §2 第 5 步，第 ② 步"再重启一次仍成功"是唯一能证明落盘生效的一步；跑之前先重启 `productstub` —— `V-40`）。**同轮把三项拍板写回 §4.1 第 31–33 行**，并在 `需求基线` 登记 `V-43`、范围改为 `V-1`–`V-43`。 |
| 2026-09-13 | **新增 `PR-4c1`（会话续期与轮换落盘）方案，并登记 `V-39` 的**不可见另一半 `V-42`**。** 起因是回答"运行桌面端聊天报 server not configured"时把 `V-39` 写成可复现条目，核对现场发现 `data/credential.json` 里那份 refresh token **已经被平台作废**（替身按契约 `delete` 旧值，实测 `POST /v1/auth/refresh` 得 401），而客户端从不把轮换后的新值落盘（`Creds.Save` 唯一生产调用点是登录处理器）⇒ **只修 `V-39` 会立刻制造"第一次刷新毁掉持久会话"这个新失败模式**，故两者必须同 PR。**粒度自检**：与 `PR-4c` 拆开并前置（判据 `L-C3a` vs `L-C2`；且它阻塞 `PR-5b2`/`PR-5d` 的手工验收）。**需求分析九条事实**（`Bootstrap` 全仓生产调用点只有 `fetchCatalog` 一处；`refreshSingleFlight` 在空令牌时本来就会去刷 ⇒ **本 PR 不需要任何新网络路径**；`credentialstore.Save` 自填 `obtainedAt` ⇒ 可"以磁盘那份为底只换 refreshToken"）。**三项待拍板**（§4.1 第 31–33 行）：① 接缝取 A1（`GatewayEndpoint` 按需，上游 0 字节）还是 A2/B；② 落盘失败取"回合继续 + warn"还是"拒绝回合"；③ 刷新被拒时是否在 `EnsureToken` 里显式清凭证。**方案给出 9 条钉子 + 5 个变异 + 三步手工验收**（其中第 ② 步"再重启一次仍成功"是唯一能证明落盘生效的一步）。 |
| 2026-09-13 | **`PR-5b` 拆成 `5b1`（思考块）/ `5b2`（工具调用），本轮只做 `5b1`；出方案（§6.4 第 0–2 步），代码未写。** **粒度自检：拆。** 两条闭环各有独立判据，且②**代码改动不相交** —— `5b1` 要给网关 sender 工厂多接一个入参，`5b2` 按事实核对**一行产品代码都不用改**（见下 ⑤⑥）；与 `PR-5c`"不拆"的理由不同：那里的拆分会让"机制改了但六条无人证明"成为可能，这里拆开**不产生无人证明的半边**。**八条事实核对（逐条按代码）**：① `agent` 层已有 `ContentBlock.Thinking`/`.Reasoning` 与 `EventThinkingDelta`（不新建块类型）；② **`GatewayEndpoint.Sender()` 既没传 `ShowReasoning` 也没传 `ReasoningEffort`** ⇒ 网关回合**永不显示**思考、也**从不请求**推理；③ `config.EffectiveShowReasoning` 未配置时返回 **true** ⇒ 产品构建与系统默认**相反**（普通端点显示、网关不显示）；④ 值的 owner 是 `internal/config`，**读取点在 `senderForEntry` 已存在**（逐字复用，不新建判据）；⑤ `app.sender` **已实现** `StreamMessagesWithTools`（`sender.go:384`）⇒ 工具调用**不需要新代码**；⑥ **本地权限门在 `internal/server` 每回合构建**（`handlers_prepare_toolturn.go:122`）⇒ 网关回合天然过同一道门，`L-C3c` 是**钉子不是改动**；⑦ `clienttest.handleCompletions` **只发 `content`** ⇒ 两条闭环**今天既无法端到端判、也无法手工判**（与 `PR-5a` 的"第二层"同形）；⑧ 前端渲染**已存在**（`stores.ts:323/634` + `chat/ToolGroup.svelte`），但 `V-23` 引的三个路径**有两个文件不存在**（新登记 `V-38`）。**方案**：`GatewayTuning{ShowReasoning, ReasoningEffort}` + `GatewaySender func(GatewayTuning)`（签名变更，逐回合取值）；**`Dialect` 刻意不传**（走 `default` 分支 = 扁平 `reasoning_effort`），因为"网关的推理字段形状"是**契约事实** ⇒ 新增 `中台交付包` §5.2 要求 + `PQ27`。**钉子 7 条**（分开断言"要它显示"与"要它产生"，含 `effort=""` 不得发字段的回归钉）、**变异 3 个**、手工验收见 §2。**两处过程记录**：① 原计划只接 `ShowReasoning`，核对 `applyReasoning` 时发现 `effort==""` **根本不发推理字段**、而 UI 默认就是 `off` ⇒ 只接一个参数用户仍什么都看不到；② `Dialect` 是本轮核对 `applyReasoning` 才发现的既有机制（五种形状），此前方案里完全没提。**待人工确认**：§4.1 第 29–30 行（注入缝签名变更、契约新增推理字段要求）。 **✅ 三项均已确认（用户，2026-09-13）**：① 注入缝按 `GatewayTuning` 加入参；② 契约取标准扁平 `reasoning_effort` 并**已随本轮写入** `中台交付包` §5.2（含"未知字段不得 400"与"档位关闭时不发"两条边界），`PQ27` 待中台确认；③ 方案文档现在提交并合 `v1`。**下一步**：按 §6.4 第 3 步写 7 条钉子先红，再实现（`GatewayTuning` + 工厂签名 + 替身补 `reasoning_content`/记录请求体）。 |
| 2026-09-13 | **`PR-5c` 落地并合入 `v1`。** 三项拍板已确认（§4.1 第 26–28 行）。改动：`internal/server/server.go`（`Config` +2 字段、`senderForSession` +2 处判断、+2 个错误构造器）、`cmd/octo-desktop/main.go`（装配层取一次 `productprofile.Current()`，两个字段都从它读）。测试：新文件 `internal/server/gateway_failclosed_test.go`，9 条钉子（1 前提钉 + 六条失败面 + 3 反钉）。**据实登记两件事**：① 六条失败面里 **2/3 本就绿**（`PR-5a` 的网关分支已拒绝回落），只有 1/4/5/6 是本次修的 —— 不假装全部先红后绿；② 论断是**"第三方替身命中数 = 0"**（真 HTTP 替身），4 个变异体验证过，其中"把拒绝文案改成无意义字符仍全绿"证明断言测的是外发而非文案。`L-C5` 推 ⚠️（`production.json` 仍是占位 ⇒ 真机验收待 `V-8`），`V-27` 收敛。守卫全绿（含 `server-diff-guard`）。 |
| 2026-09-13 | **`PR-5c` 方案（§6.4 第 0–2 步），代码未写。** 粒度自检：**不拆** —— 机制与六条黑盒钉子共用同一接缝与夹具，拆开会让"改了但无人证明六条都不回落"成为可能（`V-23`/`V-27` 的形态）。**事实核对（逐条按代码）**：① `RequiresControlPlane()` 生产调用点**仍为 0**（`V-27` 成立）；② 回合 sender 只有 `senderForSession` 一个解析点，其"默认 sender"分支来自环境变量 / `data/config.yml` ⇒ **production 下今天照样能外发**；③ 判据事实**已有 owner**（`ControlPlaneConfigured`/`HasTrustedKeys`，且装配处已取到）⇒ 本条只接线，不新建判据；④ 上游 diff 预算 `server.go` **365/543** 实测 ⇒ 无需破例。**方案**：注入 `RequireGateway` + `ControlPlaneReady`，在 `senderForSession` 一处拒绝默认 sender；六条失败面各以**"第三方替身命中数 = 0"**验收 + 3 条反钉防过度收紧。三项待人工确认在 §4.1 第 26–28 行。 |
| 2026-09-13 | **`PR-5a` 代码落地完成（网关回合跑通，含 `V-36` 条件化）；同时 `V-37` 被自己的反钉证伪并撤回。** 交付：`internal/productruntime/gateway.go`（sender 工厂，读内存 token、逐回合新建、零 I/O）、`Config.GatewaySender` 注入 + `senderForSession` 的网关分支（返回**裸目录 id**）、`ensureSender` 条件化（`V-36`）、`internal/server` 各处 `nil` 默认 sender 的安全处理（`benchmark`/`enableSubAgentTools`）、`cmd/octo-desktop` 一处装配两处接缝（同一 `CredentialHolder`）、**替身补出 `POST /v1/chat/completions`**（原只有 auth/bootstrap ⇒ 手工根本走不通）。**测试**：工厂 8 条 + 路由 5 条 + 替身 4 条 + 2 条端到端（工厂→provider→真替身），`gofmt`/`vet`/`go test ./...` 全绿。**`V-37` 撤回**：反钉"含 `/v1` 的 host 必 404"**当场红** ⇒ 证伪；`endpointURL()` 会剥尾部 `/v1`，且上游 `client_test.go:364` 早已断言不得含 `/v1/v1`。**四处污染均已撤回**（`profile.go` 注释、两个 profile 值、`release-config-guard` 规则、`profile_test.go` 断言），残留处一律标注"retracted"。**顺带发现**：`release-config-guard` 只对 `apiHost` 要求 `/v1`（那条是 `V-13`，真实需求）；`clienttest` 的包注释"尚无可运行二进制"已过期（`cmd/productstub` 即是）。 |
| 2026-09-13 | **`PR-5` 拆 5 个子 PR（§1 表 + §2 `PR-5a` 第 0 步），并登记 `V-37`（代码未改）。** 粒度自检：原一条覆盖 6 条闭环 + 2 处缺口 ⇒ 拆 `5a`（网关回合跑通）→ `5c`（不回落六条失败面）→ `5b`（思考块+工具调用）→ `5d`（余额）→ `5e`（下架校验），**本轮只做 `5a`**。**`5a` 的需求分析逼出两件事**：① **`V-37` 路径三不相符**（**⚠️ 后经证伪撤回，见上一条**）—— 上游 `openai` 常量写死 `/v1/chat/completions`、我方提案写 `POST /v1/ai/chat/completions`、`GatewayHost` 值又含 `/v1` ⇒ 当时判定"没有任何 `BaseURL` 能同时满足"，定为 `5a` 的第 0 号交付物，三个候选登记在 §4.1 第 24 行。② **接缝** —— `Config` 今天无任何 sender 注入缝，选定新增 `Config.GatewaySender`（同 `MountAPI` 纪律），并确认 `C2` 规则 2「用当前内存 token 装配 sender」**不需要改上游 provider**（`NewSender` 轻 + 限流器按 `limiterKey` 复用 ⇒ 每回合新建、刻意绕过缓存）。**三项待人工确认**：§4.1 第 23–25 行（拆分、路径收敛、注入缝 + `ensureSender` 条件化）。 |
| 2026-09-13 | **新增 `V-36` 并收紧 `PR-5` 的范围（代码未改）。** `PR-4c0` 验收时手工复现：登录后发消息得到上游的「server not configured: complete setup via the Web UI」。逐行核实 —— `data/config.yml` 只剩 `access_key`（无 provider / key）⇒ `resolveProviderAndModel` 返回 nil sender ⇒ `ensureSender`（`ws_handlers.go:736` / `:1417`）在 `buildAgent`（第 740 行）**之前**返回。**两条产出**：① 该文案在产品语境下是**错误指引**（指向 Web UI 的第三方 provider 设置，而 `B4` 要求正式会话只用内置网关）；**它是 `ensureLocalEndpoint()` 被删除后应有的结果** —— 以前那个函数往 `config.yml` 种本地端点，让 `ensureSender` 有 key，于是聊天"能用"但用着非法模型源。② **`ensureSender` 条件化升为 `PR-5` 的硬前置**（已改 §1 `PR-5` 行）：它必须只在回合真要落到默认 sender 时才要求 —— 产品包按 `B4` 本就不该有第三方 endpoint，不先改这一处，网关做出来也仍然聊不了。**对 `PR-4c0` 的连带说明**：`V-35` 的护栏**恰在产品用户最可能处的状态里不可达**（`ensureSender` 先失败），已在 `V-35` 行加注。 |
| 2026-09-13 | **`PR-4c0` 完成（计划外，由 `PR-4d` 手工验收逼出）：新增 `V-35`，`L-C5` 的「网关绑定 id 不落到其它模型源」这一分支落地。** **触发**：用户在 `PR-4d` 验收里选中「布丁隐私版」发消息，界面出现第三方 400（`...but you passed buding-gateway::buding-privacy-1`），**对话内容随该请求发到了 `config.yml` 指向的真第三方端点**。**同一句报错同时证明 `PR-4d` 端到端生效** —— `buding-privacy-1` 只存在于签名目录夹具里（本机 `chat-modes.json` 与 `devBackend.ts` 都是另外四项）⇒ 选择器读的确实是签名目录。**机制逐行核实**：`senderForSession`（全部回合路径的唯一解析点）在 `sess.ModelConfig == ""` 的**第一个** fallback 把复合 id 交给默认 sender；而前端发的 `model_id` 在 `config.yml` 里匹配不到 ⇒ 落进 `handlers.go:1373` 的"raw model string"分支。**落点四处比选**：一处（`senderForSession` + 拒绝型 sender）／四处调用点／只改前端；**用户选定方案 ①**（§4.1 第 22 行，登记"动上游核心文件 + 非常规逻辑"）。**scope 是关键**：护栏**只认网关前缀、全 profile 生效**，与 `RequiresControlPlane`（production 半边，仍归 `PR-5`）是两条不同的规则 —— 开发构建允许用 `config.yml` 当模型源，但不允许把网关绑定的 id 交给它。**测试 7 条先红**（首红为 `unknown field GatewayModelPrefix`），**变异 6 个**全部被对应钉子抓住。**两处过程记录**：① **一个"全绿"的变异体其实是选择器写错了** —— `-run 'Gateway|Ordinary|Refusal'` 不匹配 `TestTheGuardIsInertWithoutAnInjectedPrefix`，该测试压根没跑；补上完整选择器后正常转红，**教训：变异转红之前先确认受影响的测试真被选中**。② **一处真漏洞由钉子逼出**：网关 id 有两条进入会话的路径（**模型名**＝本次事件形态；**绑定 ref**＝端点在绑定时存在、之后被删），只查模型名会漏掉第二条 ⇒ `gatewayBound()` 刻意同时查两半。**上游足迹**：`internal/server/server.go` 137 → 247 行（上限 543，守卫绿），其中约 60 行是注释 —— **这是本 PR 唯一的代价，用上游 diff 换"规则只有一份"**。**残余**：拒绝文案是英文（`turn_error` 无码通道，与这条路径上所有服务端回合错误一致），本地化归 `PR-4c`；production 的 `RequiresControlPlane` 仍归 `PR-5`；前端仍不拦"选择"，"选之前就说清"归 `PR-4c`（§4.1 第 20 行）。 |
| 2026-09-12 | **`PR-4d` 完成（`§6.4` 第 3–5 步）：`L-C1b` 转 ✅，`GET /api/product/chat-modes` 从"⬜ 未注册"转为已注册。** **六件交付物全落地**：`internal/chatmode`（新，2 文件，只持分组事实、不读文件）、`internal/productprofile.GatewayEndpointID = "buding-gateway"`（复合 id 前缀）、`productclient.PolicyEnvelope.DecodePolicy()`（读缓存里的**已验签**信封，不重验 —— 验签仍只有一个 owner）、`productruntime.projectCatalog()`、`GET /api/product/chat-modes`（`Mount` 第 6 条）、前端四件联动（`api.ts` 类型 / `chatMode.ts` 三档回落 / `ModeMenu.svelte` 去守卫 / `i18n.ts` 删 8 键 / `devBackend.ts` 改发新形状）。 **`compositeId` 按第 1 步②换角色而非删除**：类型上改为**必填**，"列了但不可选"因此在类型里就不可表达 —— 比留一个运行时守卫更彻底。 **测试 9 条计划 → 11 条落地**：第 7 条改了形态（源码级搜键 → **读导出字典的键**，等价而更稳）；第 8 条拆成 3 条（三档回落 / `zh-TW` 与 `dictFor` 同规则 / 客户端从未见过的 id 也能上屏）；另加 3 条计划外（分量版本回报、名字逐字透传、`internal/chatmode` 不得读文件的机械钉子）+ `ModeMenu.test.ts` 3 条。 **变异 10 个全部转红**，其中一处**让我改了做法**：第 5 条最初把"默认模型须为本组成员"变异成"根本不给默认值"，那是另一个缺陷，测试照旧绿；改成真正要防的"不判成员资格直接拼复合 id"才转红。**这是 `§6.4.5`"先证明钉子会红"的推论：还得证明变异的是那条规则本身。** **全量验证**：`gofmt` 空 / `go vet` 净 / `go test ./...` 全绿 / 前端 796 测试全绿（65 文件）/ `svelte-check` 0 错 0 警 / `vite build` 通过 / `portable-check` 21 项 / 六个守卫（`norms` `brand` `datapath` `agents` `reuse` `server-diff` `release-profile`）通过。**与 `PR-4c` 的接缝照方案保持**：无缓存 ⇒ 200 + 空列表、不新增降级文案（**一处更正**：方案原文"一字文案不出"不成立 —— 既有的 `mode.no_models` 照常渲染，`4c` 的交付物是让四种原因各自可辨）；`loadChatModes()` 出错**保留上一份列表**而非清空（清空会把 `4c` 的"过期"文案误配到一次网络抖动上）。**质检另发现三处记录（不影响本闭环的正确性，列出备查）**：① `catalogVersion` / `policyVersion` **今天无任何前端消费方**（契约要求上线，真正的消费者是 `4c` 的刷新判定与 `5` 的会话绑定）—— `api.ts` 的注释写"选择器可用它区分目录换版"，属**把将来的用途写成现在的用途**，已改为"供 `4c` 使用"；② 分组 `defaultModel` 同理，今天只上屏、不被消费（选择器按 `currentModelId` 高亮）；③ `ModeMenu.isActive` 的第二个子句（`model.id === currentModelId`）从选择器路径**已不可达**（选择器只发复合 id，`internal/server` 又按"收到什么存什么"绑定），但**不删** —— 会话的 `model` 也可能是别处写入的裸 id，属防御性子句；是否可达留待 `PR-5` 定下绑定时一并判定。 |
| 2026-09-12 | **`PR-4d` 出方案（§6.4 第 0–2 步，代码未写）。** ① **粒度结论＝一条 PR**（四段是同一个用户可见结果：选择器列出签名目录的模型并显示目录给的名字）。② **划清一条文档里从没划过的边界**：`B7` 的标题是"模型可选资格**与**能力矩阵"，两半 owner 不同 —— **模型级 `eligible`/`transport` 过滤属本 PR 的投影**，功能级能力矩阵（入口 `visible`/`entitled`/`permissionPolicy`）属 `PR-4e`。不分清的后果很具体：把模型资格也推给 `4e`，`4d` 就会把 `eligible=false` 的模型列进选择器，用户点得动、发出去被网关按 `model_not_allowed` 拒。③ **一处结论与直觉相反并已核对原文**：`compositeId`（`"<endpoint>::<model>"`）**不删** —— `B8` 规则 1 明写"会话存的是复合 id，正式会话的 endpoint 前缀固定为内置网关，可变部分只剩目录 id"。要改的是它的**角色**：今天 `ModeMenu.svelte:42` 拿它当"在 `config.yml` 里存在吗"的守卫（`if (!model.compositeId) return`），`4d` 之后"列了但不可选"这个状态不存在了，守卫必须去掉 —— **否则会把目录里的模型全部拦死**（目录模型没有 config.yml 端点）。④ **`displayName` 三处联动**（`modelDisplayName` 改接目录对象 / 删 8 个 i18n `model.*` 键 / DTO 加字段），少一处 `B6` 验收过不了；`devBackend.ts` 必须同 PR 改（它今天发的是含 `fallback` 的旧形状）。⑤ 交付物 6 件、测试红 9 条已列出；**明确不做** `4c` 的文案（空列表就出空列表，接缝写清）。 |
| 2026-09-12 | **`V-32` 修复落地（独立小 PR）：重启后不再要求验证码。** 新增 `internal/productruntime/session.go` 的 `RestoreSession` + 5 条钉子（`session_test.go`）+ 装配处一行接线（`product.go` 造 holder → 恢复 → 交给 `newPlatformClient`）。**两处最值得记的：** ① **钉子必须落在根模块** —— `cmd/octo-desktop` 是嵌套 module，`go test ./...` 跳过它，CI 里只跑 `-run TestPortableServer_WritesOnlyToDataRoot` 一条过滤；本 PR 初稿的测试先写在 `cmd/octo-desktop/session_restore_test.go`，核实 CI 后整体搬到 `internal/productruntime`。**② 变异验证拿到的是用户原话**：去掉恢复 ⇒ `Bootstrap after a restart = productclient: session expired`，正是 `V-32` 的症状。同轮还登记了 `PR-4c` 四项拍板（§4.1 第 18–21 行）并出完其第 0–2 步方案。 |
| 2026-09-12 | **`PR-4c` 出方案（§6.4 第 0–2 步，代码未写）；登记 `V-32`；`PR-4c` 与 `PR-4d` 换序。** ① **第 0 步结论是"顺序错了"**：`PR-4c` 的四条文案（`B9`：无缓存 / 过期 / 验签失败 / 分组空）**全部发生在模型选择器里**，而选择器的数据源是 `/api/product/chat-modes`（`api.ts:341`）—— Go 侧**未注册**（`V-24`），今天由 `devBackend.ts` 的本地常量接管；让选择器改读签名目录是 `PR-4d` 的交付物。故 `PR-4c` 先做的话，"无缓存 ⇒ 选择器空"这个可见结果**不可能出现**、"分组空"**完全无处可去**，且"不得开启新回合"会去拦一个本来就不该存在的本地选择（`B1` 规则 1）。**已把 `PR-4c` 前置由 `PR-4b` 改为 `PR-4d`**（顺序 `4b → 4d → 4c`）。② **第 1 步补出五处缺口**：可用性状态的 owner 与上线形状（建议新增第 14 条端点 `GET /api/product/catalog`，形状照抄 `PR-2c` 的 `/control-plane`）；"状态 → 文案"必须收在一处（前三条看 `catalog.state`、第四条看投影为空）；**`V-32`**（启动刷新不可达）；条件请求选 `knownVersion` 且 `/catalog/models` 的应答形状要与 `bootstrap` 同形（契约要改）；"不得开启新回合"在前端落地、服务端强制归 `PR-5`，**不提前在 `internal/app` 长判据**。③ **第 2 步给出 5 件交付物与 9 条测试红清单**，并明确不做后台定时器（`B3` 自己的验收是读时判定）。④ **四项待人工确认**已登记 §4.1 第 18–21 行（换序 / `V-32` 归属 / 新端点 / 契约改动）。**另按 `§3.8` 撤掉一次重复编号风险**：新缺口原拟编号 `V-30`，核对后发现 **`V-30` 是有意留空的空号**，改用 `V-32`。 |
| 2026-09-12 | **`PR-4b` 自审补记：发现第五处缺陷（回滚守卫只拦住"正好旧一天"），并补掉两条测试盖不住的漏网。** ① **流程**：自审那一轮的 8 个文件改动**全部未提交**（`v1` 上留脏工作区），已起 `fix/pr-4b-self-review` 两个提交 `--no-ff` 并回 `v1`（`25e205ac`）。② **缺陷 ⑤（最值得记的一条）**：回滚守卫写 `case -1:`，而 `compareCatalogVersions` 返回**首个不同部分的差值**（跨三天 `-3`），不是 `-1/0/1` 判决 ⇒ **旧三天的目录会静默覆盖当前目录**。原先没被发现的原因是已有测试用的 `.9` vs `.10` **差值恰好为 1** —— 守卫是错的而测试全绿。补 `TestARollbackOfMoreThanOneDayIsStillARollback`；**变异回 `case -1:` 时它红、原测试仍绿**，证明它才是压住守卫的那条。③ **`catalogExpiry` 的 TTL 回绕**：`issuedAt + ttlSec` 是 `time.Duration` 乘法而 `ttlSec` 由中台给，溢出回绕成负 ⇒ 一份还剩一小时寿命的目录被存成"早已过期"，`PR-4c` 照此降级且无线索可指。超签名窗口跨度的 `ttlSec` 不可能是"取早"里的早者，故忽略；替身加 `SetCatalogTTL`，两条钉子（荒谬 TTL 被忽略 / 短 TTL 仍缩短窗口）。删除夹取 ⇒ 前者红在预测位置。④ **`catalogstore` 死代码**：`Options.Now`（无调用方传过 —— 知道"中台何时作答"的是拉取路径，`Entry` 带 `fetchedAt`/`expiresAt` 进来）、`Path()`（仅测试用过）删除；`Corrupt()` **不删而给消费者** —— 装配处启动告警（`E6.2` 规则 5）。⑤ **验收**：`go test -race ./...` 全绿、`go vet` / `gofmt -l .` 干净、六个守卫全过。 |
| 2026-09-12 | **`PR-4b` 完成：`L-C1a` 转 ✅，并顺带把 `L-A2` / `L-A3` / `L-A6` 从 ⚠️ 推到 ✅（`V-29` 收敛）；`E2E闭环清单` 由 `✅8 / ⚠️8 / ⬜18` 变为 `✅12 / ⚠️5 / ⬜17`（机器核对，合计 35）。** **新增 2 个包/文件**：`internal/catalogstore`（`catalog.json` 唯一 owner）、`internal/productruntime/catalog.go`（全仓唯一拉取点，由 `TestOnlyOnePlaceFetchesTheCatalog` **读源码**钉住 —— 纯行为测试看不见"以后新加的第 3 个调用点"）。**改上游足迹**：`clienttest` 加故障开关（`RefuseSessionsAfterLogin` / `FailBootstrap` / `TamperPolicy` / `OmitPolicy` / `SetCatalogVersion` / `SetPolicyAudience` / 两个计数器）、`sign.go` 参数化版本与受众、`dto.go` 内嵌 `PolicyEnvelope`；`internal/productprofile` 与 `internal/brand` **未改**（信任锚与受众走 `Deps.CatalogTrust` 注入，owner 不变）。 **三处缺陷由先红的测试暴露**：① **替身的写入器在毁签名**（默认 JSON 转义把 `&` 改成 `\u0026`，而签的是转义前那串字节 ⇒ 含 `&` 的模型名到客户端必验不过）—— 与计划第 2 步补充 ① 是同一陷阱的另一端，计划只想到了客户端那一端；② **`"policy": null` 与"字段省略"是两种缺席写法**，Go 把前者解成 4 字节字面量，`len(...)==0` 只认后者；新增 `PolicyEnvelope.IsEmpty()` 收成一个判据；③ **缓存不能缩进** —— `json.Indent` 会重排策略内部空白，「给人看」与「字节保真」不可兼得 ⇒ 紧凑单行 + 写盘前**逐字节自检**（`ErrNotByteExact` 拒绝写，而不是留一份永远验不过的缓存）。 **另有一处是测试自己写错**：二次登录漏传 `nickname`（`本地API契约` §2.3 明确列了该字段），被 `nickname_format` 拒掉 —— 已改正测试并把该条写进 `L-A2` 行。 **四处缺陷由先红的测试暴露**（前三条见上，第四条是**版本比较**：初版用字符串序，`2026-09-09.10` 会被判成比 `2026-09-09.9` 更旧 ⇒ 同一天第十次刷新起**静默拒绝刷新到第二天**；已改为解析后按数值比，形状解析不出则退回字符串序）。**变异验证**：去掉 `SetEscapeHTML(false)` ⇒ 5 条红；去掉版本比较 ⇒ 5 条红（跨两包、含挂载态端到端）；去掉字节自检 ⇒ 红；去掉 fail-closed ⇒ 3 条红；版本比较退回字符串序 ⇒ `TestVersionOrderingIsNotStringOrdering` 红。**本轮明确不做**：TTL 到期后的条件请求（`ETag`/`304`）与周期刷新、三条降级文案、投影与前端渲染、`/catalog/models` 客户端方法（前三归 `PR-4c`，后两归 `PR-4d`）。 |
| 2026-09-12 | **`PR-2c` 收尾完成：`L-B2` / `L-B3` 落地，`PR-2c` 标完成。** 新增第 13 条本地端点 `GET /api/product/control-plane` + 前端分页 + 两个「未配置」页 + 四档 UI；`E2E闭环清单` 由 `✅6 / ⚠️9 / ⬜19` 变为 `✅8 / ⚠️8 / ⬜18`。**两处方案缺陷由先红的测试暴露并改正**（分页优先级写反；`account_restricted` 缺「确定动作」），`P4-拦截页.md` 的 §2.2 / §3.3 / §5.2 同步更新（§5.2 的**拟名与实现名不同**，已改为实际名并说明为何收在 `product.tier.*` 前缀下）。**本轮明确不做**：客服渠道载体（`V-4` / `TODO-06`）、`D-006`、`V-27` / `V-28`（`PR-5`）、`L-A6` 的端到端触发（`PR-4b`）。 |
| 2026-09-12 | **按代码逐条核对 35 条闭环 → 补 `V-23`–`V-29`；`PR-4b` 出方案（§6.4 第 0–2 步）；`PR-3` 触发条件收紧。** ① **核对结论**：`E2E闭环清单` 的 `✅ 28` 实际是 **✅ 6** —— `L-C` 全组 10 条、`L-D` 全组 7 条、`L-E3`、`L-B2` 共 19 条完全没有实现（根因 `V-23`：状态列名为"规格状态"、取值 ✅、末尾还有 ✅ 计数，必然被读成进度；已把语义改为**实现状态**并逐条按代码重算）。② **`V-24`（最要紧的一条）**：前端调 11 条 `/api/product/*`，`Runtime.Mount` **只注册 5 条** ⇒ 6 条（`chat-modes` / `prefs` / `nickname` / `sensitive/check` / `sensitive/dict` / `sensitive/dict/import`）**真机 404**，今天只靠 `devBackend.ts` 接管才"能跑"。**它直接推翻 `PR-3` 的触发条件** —— 已收紧为"这 6 条也挂上之后"，并新增 **§1.1 路由归属表**（`chat-modes` → `4d`；`prefs` / `nickname` / `sensitive/*` → `6`）。③ **`V-25`**：四个产品 WS 事件在 Go 侧**零发射点** ⇒ `L-E3` 的冻结 UI 永不出现。**`V-26`**：`L-D2` 输出打码全仓不存在，**敏感内容明文落盘**。**`V-27`**：`L-C5` 的 `RequiresControlPlane()` 零调用点 ⇒ "正式会话不回落"只有声明。**`V-28`**：`L-C4` 余额从任何来源都不更新。**`V-29`**：`L-A2`/`L-A3`/`L-A6` 标 ✅ 但**无端到端测试**，违反本清单自己的 §2.1 第 5 步。④ **`PR-4b` 方案**：第 0 步粒度自检把"TTL/ETag 刷新"**移出到 `PR-4c`**（它是"过期了怎么办"，属 `L-C2` 的判据）；第 1 步补出**六处必须在开工前定的缺口**（入口走 `bootstrap` 还是 `/catalog/models`、缓存 owner、`L-C1a` 异常分支栏不全、触发时机、顺带补 `L-A6` 端到端、失败原因要可区分）；第 2 步给能力检索增量、4 件交付物与 8 条测试红清单。**代码未写。** |
| 2026-09-12 | **`PR-2c` 部分落地：`L-A6` 闭环 + 修掉两条实际缺陷（`V-21` / `V-22`）；`L-B2` / `L-B3` 未做，故 `PR-2c` 不标完成。** ① **`L-A6`**：服务端加了 `productruntime.failPlatform`（**所有中台错误的唯一汇合处**，与前端 `productFetch` 结构对称）+ `forgetSession`（**先清凭证、再应答**；`E7` 保留激活记录与绑定号码）；前端 `noteSessionLost`（`product.ts`）为规则的**唯一 owner**，由 `productFetch` 与 `api.ts` 的 `request` 两处调用。四条新测试（清凭证 / 保激活 / 停机三例**不清**凭证 / 只读盘仍回拦截页，后者实测 `Creds.Delete()` 与 `State.Logout()` **双双 `permission denied`** 而应答仍是 401）。② **`V-21`（会话失效被误报成网络故障）**：`ErrSessionExpired` 是**哨兵错误**、不含 `*productclient.Error`，`writePlatformError` 的 `errors.As` 落空后把它归成 `503 network_unavailable` —— 前端于是给一个**永远不可能成功**的重试按钮。**它是被测试逼出来的**：两条新测试首红的原因就是拿到了 503，不是先知道再补测。修法是在该函数最前面单独判 `errors.Is`，并在 `本地API契约` §3 **补登 `unauthorized` 行**（该码此前**两端都没登记**，这正是误分类长期无人发现的原因）。③ **`V-22`（登出把用户永久锁死，本批次目前最严重单点）**：`logout()` 成功后清空 `productState`，而 `BlockedView` 用 `$productState?.activated ?? false` 决定渲染**激活表单**还是登录表单 ⇒ 登出后改成要用户重输 **U 盘激活码**，而它**一码一用** ⇒ **点一次登出就再也进不去**；`V-4`（无客服渠道）未解决时连求助路径都没有。加重情节：旧测试把 `expect(get(productState)).toBeNull()` **写成了预期**，缺陷被测试固化。已改为 `await refreshProductState()`（状态 owner 是服务端，它会返回 `loggedIn:false, activated:true, account`）并加断言。④ **关闭 `V-15` 的第一件交付物**：`P4-拦截页.md` 已补出，§0 逐节标「已实现 / 半实现 / 待实现」，避免它自己变成 `V` 类缺陷；新增 §4.5「落地记录」与 §4.5.1 一处取舍（**不重读 `/api/product/state`**：会经 `productFetch` 递归，且保留的两个字段恰好都没变）。⑤ **验收**：`go test -race ./...` 全绿、前端 **765 条全绿**、`go vet` 与 `gofmt -l .` 干净、`brand-check` / `datapath-check` / `norms-check` / `agents-check` 全过。⑥ **残余与顺序**：`L-A6` 的**触发点今天只有一处** —— `PR-4b` 的目录拉取（`client.Bootstrap` 是首个走 `doAuthorized` 的调用），所以 `L-A6` 的**人工 E2E 验收要等 `PR-4b`**；`L-B2` / `L-B3` 的 UI 仍未做，`E2E闭环清单` 里这两条**不得**记为可判。 |
| 2026-09-12 | **全库核对 → 补 3 处缺口 + 新增 `PR-2c`（本轮未写任何代码）。** ① **核对**：13 条依赖边与本文 §1「前置」列**逐条一致**；闭环总数 35 条复核无误（清单总表 41 行 − 6 行拆分母行）。② **新增 `V-15` / `V-16` / `V-17`**（`需求基线` §5.6 为 owner，`E2E闭环清单` §5 同步登记归属）：`L-B2` / `L-B3` **无归属 PR** 且其设计文档 `P4-拦截页.md` 被引用却不存在；`L-A6` 的**失效清凭证未接线**；`L-E2` 只有存储半边、**无换机场景测试**。③ **新增 `PR-2c`**（§1 表 + §2 方案位）承接前两条，并把 `PR-7` 行改明 `L-E2` 的场景测试归属；§4.1 加第 17 行登记「新增 PR ＋ 触及认证面」**待拍板**。④ 复核同时纠正了「服务端有半边 ≠ 闭环已落地」的 5 处误判（`L-A6` / `L-B2` / `L-B3` / `L-D3` / `L-E2`）—— 教训与 `PR-2b1` 一致：**单元绿 ≠ 闭环合上**。 |
| 2026-09-11 | **`PR-4a` 落地（验签核心），并修掉一处 `PR-2b2b` 遗留的守卫债务。** ① **代码**：新增 `internal/productclient/policy.go`（信封与目录类型，中央字段名的唯一落点）与 `signing.go`（**签名与验签的唯一一份实现**）；替身 `clienttest` 获得签名能力（`sign.go` 的夹具密钥 + 夹具策略），并把签名信封挂进 bootstrap 应答；`developer.json` 的 `trustedKeyIDs` 填入**测试公钥**（§4.1 第 1 行已批准；`production.json` 不变，仍 fail-closed）。② **契约同步**：`signature` 移出 `policy` 成为同级 `policySignature`，被签名字节 = `data.policy` 的原始字节（`D-007`）；`displayName` 双语对象（修掉与 `B6` 的硬矛盾）；定死 `catalog.modes` 形状（只留 `id` + `defaultModelId`，禁止重复 `modelIds`/`models`/`displayName`）；删掉 `本地API契约` §2.8 的 `modes[].displayName`（与前端 i18n 构成第二份真相）。③ **TDD 执行**：先写 14 条测试（含 1 条走真 `httptest` 的**线上字节往返**测试），**首红为 `undefined: productclient.Policy` 等 —— 指向缺失行为本身**；实现后全绿。**一处测试自身被判定为写错**（§6.4 第 5 步）：`TestVerifyPolicyRejectsASignatureMadeByAnotherKey` 原设计让"攻击者同时替换信任锚"，而信任锚是**编译进二进制**的、攻击者拿不到 ⇒ 该场景不存在，测试断言的前提错了；改为真实威胁（攻击者用自己的私钥签名、声称受信任的 `keyId`，锚不变）。④ **两条钉子经变异验证**：把 `ed25519.Verify` 结果强制为 true ⇒ **恰好 2 条**签名相关拒绝测试转红（其余由各自的独立检查兜住，符合预期）；把 `developer.json` 的公钥改一个字符 ⇒ 漂移钉子 `TestFixtureKeyIsTheDeveloperTrustAnchor` 转红并打印两侧取值。⑤ **修守卫债务**：`server-diff-guard` 在 **`v1` HEAD 上本来就失败**（`internal/server/product_gate_test.go` 由 `PR-2b2b` 引入、未登记收编包），与 `PR-4a` 无关；**上一轮"守卫全过"的结论是错的** —— 该守卫**没有 `make` 目标**，当时跑的是 `make *-check` 集合，所以从未被跑到。已登记 `convergence: 'P0-01A C'`，守卫与它自己的 17 条单测均转绿。**教训**：**没接线的守卫等于没有守卫**；这正是 `V-1`（六条守卫接线，归 `PR-9`）的价值，`PR-9` 应从"顺手做"升为**尽早做**。⑥ 验收：`go test -race ./...` 全绿、`-tags product_production` 下的 `productclient`/`productprofile`/`clienttest` 全绿（漂移钉子按构建标签正确排除）、`go vet` 与 `gofmt -l .` 干净、**135 条守卫单测全绿**、`norms-guard`/`agents-check`/`brand-check`/`datapath-check`/`reuse-guard`/`server-diff-guard`/`release-profile-guard` 全过。 |
| 2026-09-11 | **`PR-4` 粒度自检 + 需求分析（按 §6.4 前两步，未写代码）。** ① **粒度自检**：原 `PR-4` 一条 PR 覆盖 3 条闭环（`L-C1a`/`L-C2`/`L-C1b`）外加一次包搬运 ⇒ **拆 5 个子 PR**（`4a` 验签核心 / `4b` 拉取缓存 / `4c` 三条降级 / `4d` 投影上屏 / `4e` 能力矩阵消费），本轮只做 `4a`（§4.1 第 16 行）。② **需求分析发现并解决 5 处问题**：**(a) 契约硬矛盾** —— `中台交付包` §4.3 的 `displayName` 是纯字符串，与 `需求基线` `B6` 规则 2（同日已拍板的中英双语对象）及 `本地API契约` §2.8 冲突 ⇒ 改契约示例，并把形状与回落规则的 owner 写清（形状在契约、取值规则在 `B6`）。**(b) 签名方案** —— 契约原定 JCS（RFC 8785）规范化，而全仓无实现、无依赖可用，且 `policy` 含数字 ⇒ 必须实现最危险的数字规范化；真正的代价是**中台与客户端两份实现会漂移**（表现为"替身全绿、生产验签失败"）。人工定「先简单实现」⇒ **改签 `data.policy` 的原始字节**（`signature` 移出 `policy` 成为同级 `policySignature`），**JCS 路线原样登记为 `D-007`**，含真实代价、触发条件与四条正确做法（§4.1 第 14 行）。**(c) 覆盖面缺口** —— `B7`（能力矩阵/入口可见性）与 `B8`（模型下架）在 33 条闭环里**零覆盖**，即"需求写了、无人能判是否完成" ⇒ 补 `L-C6`/`L-C7`，**33 → 35 条**，新增 `⬜` 标记（§4.1 第 15 行）。**(d) `catalog.modes` 形状未定义，且会与 `models[].modeIds` 重复** ⇒ 定死「分组事实的唯一 owner 是 `modeIds`」，`modes[]` 只留 `id` + `defaultModelId`，并**禁止** `modelIds`/`models`/`displayName`（§3.8）。**(e) 模式展示名被两处定义** —— 核实前端 i18n 已拥有 `mode.privacy/smart/default`（中英各一份），且 `B5` 规则 1 明写「展示名是界面文案」⇒ 删掉 `本地API契约` §2.8 的 `modes[].displayName`，判为第二份真相。③ **能力检索结论（§3.5）**：`crypto/ed25519` = **stdlib 可复用**（零新依赖）；信任锚 `productprofile.TrustedKeyIDs` **可复用**；JCS **无任何可复用实现**（`go.mod` 无相关依赖）；归档分支 `archive/feat-chatmode-20260911` 上的 `internal/chatmode`（142 行 + 139 行测试）**可搬运**，但它仍从 `data/chat-modes.json` 读，按 `B1` 规则 2 搬运时须**删掉文件加载路径**（归 `4d`）。 |
| 2026-09-11 | **`PR-2b2a` 落地（接线，§3.3.1 顺位 3，用户答「继续」批准）+ `V-12` 定夺。** ① **缝的形状**：`internal/server` 加**一个可选、无产品名词**的 `Config.MountAPI func(api func(pattern string, h http.HandlerFunc))` + 3 行调用；它交出的是既有 `s.api`（**不是裸 `mux`**）⇒ 挂上去的路由自动获得 `requireAuth` 与 `no-store`。**实测比预估更小**：`cmd/octo-desktop/main.go` **+1 行**（装配函数与 `main` 同包 ⇒ 零新增 import）、`internal/server/server.go` **+18 行**（其中 11 行注释），`OCTO-FORK` **+2**；产品词汇整份收进**新文件** `cmd/octo-desktop/product.go`。**未抬任何 ratchet 上限**，`server-diff-guard` 仍绿。② **TDD 执行**：先写 4 条端到端测试，**首红为 `404 page not found`**（指向"缝没接上"这一缺失行为，而非杂散的断言）；实现后全绿。**两处测试自身被纠正**（§6.4 第 5 步要求判定是测试错还是实现错）：一是断言 `installId` 出现在 `/api/product/state`，而契约的 `ProductStateDTO` **有意不暴露它**（那是请求头用的值，界面从不渲染）⇒ 改断 `loggedIn`；二是用"发码计数"证明"本地校验短路"，但计数**无法区分**本地拒绝与中台拒绝 ⇒ 改成"**只改昵称后重试仍然成功**"（若首次泄漏到中台，一次性激活码已被消耗，第二次必报 `activation_code_used`）。③ **`V-12` 收敛**：`E6.4` 规则 2 原称"没有任何一次进程启动会写文件"，而 `productstate.Open` 在文件缺失时就地播种 ⇒ 接线后**启动即写盘**。用户选定**改条文**：规则 2 改为「不存在 ⇒ 播种；**已存在 ⇒ 只读、绝不重写**」。**安全承载点随之从「何时写」移到「已存在就不写」**，故补 `TestExistingStateIsReadNotRewritten`：用 `os.SameFile` 判"文件未被替换"（temp+rename 重写必换 inode，**即使字节相同**），并在**只读目录**下断言 `Open` 仍成功。**该钉子经变异验证**：把 `Open` 改成也写一次 ⇒ 测试红（`reopening replaced the state file`）；而**首版只比字节 + 等报错，变异后仍绿** —— 即一个抓不到东西的假钉子，已废弃。④ 验收：`go vet` 干净、`gofmt` 干净、`go test -race ./...` **57 个包全绿**、四个守卫全过。⑤ 登记 §4.1 第 10 行。 |
| 2026-09-11 | **按人工要求：闭环重拆 + 每条闭环前置「需求分析 → 开发计划 → TDD」三步。** ① [`E2E闭环清单.md`](E2E闭环清单.md) 按粒度判据重审，**25 条 → 33 条**（拆 `L-A1`/`L-B1`/`L-C1`/`L-C3`/`L-D4`/`L-E4` 六组），本文档的 PR 归属与"待决项位置"**已同步到新编号**：`PQ25` 收敛为**只卡 `L-A1b`**（原表述"卡 `L-A1` 的一个分支"会把成功路径一起拖住）、`PQ26` 收敛为卡 `L-A4`；`PR-2b2b` 的闭环改为 `L-B1b`/`L-B2`/`L-E5`（并含 locale 壳带入）；`PR-3` 的解锁范围改为 33 条；`PR-4` → `L-C1a`/`L-C1b`/`L-C2`；`PR-5` → `L-C3a`/`b`/`c`/`L-C4`/`L-C5`。② `开发规范` §6.4 从"三步走"扩为**五步**，前置**需求分析（含与人互动确认）**与**开发计划更新**两步，并新增 §6.4.1（分析七项产出）/ §6.4.2（计划更新五项，含"引用不同步 = 拆分没做完"）/ §6.4.3（单元绿 ≠ 闭环合上）。 |
| 2026-09-11 | **按人工要求补两条规范 + 出 `PR-2b2` 设计。** ① `开发规范` §6.4 细化为**每闭环三步走**（先写测试 → 开发功能 → **单元测试 + 端到端测试**验收），并写明「单元测试绿 ≠ 闭环合上」（`PR-2b1` 的真实教训）。② 新增 `开发规范` **§3.3.1 最小影响核心文件**：三顺位（不改 / 只加通用缝 / 停下来问）+ 走顺位 3 时必须写清的四件事 + 三个自证数。③ 把 `PR-2b2` 拆为 **`2b2a`（接线，动 2 个上游文件约 20 行）** 与 **`2b2b`（产品门 + window token）**，并把已核实事实、拟改动形状、四个被否决的替代方案写入 §2，登记为 §4.1 第 9 行**待人工确认**。 |
| 2026-09-11 | **`PR-2b1` 落地 + `PR-2b` 拆为 `2b1`/`2b2` + 确定 TDD 工作方式。** 新增 `internal/productruntime`（15 个测试，覆盖 71.9%），**先写测试确认全红（`501`）再实现**（§6.4 首次执行，红→绿证据见提交信息）。实施期定下四件契约事：① **信封层级由本地契约决定、不由中台 `field` 决定**（中台把 `activation_invalid` 标成字段、本地契约定为业务级，实现忽略中台那个 `field`）；② `invalid_phone`/`invalid_code` 同名两层的消解规则 = 格式在本地校验拦下、从中台回来的按业务级；③ **`phoneMasked` 必须由中台给**（新增字段）；④ 新增 `control_plane_unconfigured` 使「未配置」与「中台挂了」可区分。接线（`PR-2b2`）**要动上游文件**，后来拆为 `2b2a`（接线）/ `2b2b`（产品门 + token），登记为 §4.1 第 9 行 **待人工确认**。 |
| 2026-09-11 | 建立（关闭 `V-6`）。按人工拍板确定：**`PR-1` 取最小范围**（发码/登录/激活/bootstrap 四个端点 + 契约层 + 替身）、替身落 `internal/productclient/clienttest`、dev 构建信任测试密钥；登记 `§3.7` 六项。能力检索落地四处，其中 §3.3 是唯一**没有现成实现**的一处，§3.2 确认网关调用**复用** `internal/provider/openai`（唯一真正的新增是动态令牌注入）。 |
| 2026-09-11 | **`PR-2a` 落地（持久层）。** 新增 `internal/productstate`（`data/product-state.json` 唯一 owner，逐行对应 `E6.1`）、`internal/credentialstore`（`data/credential.json` 唯一 owner，`E6.3`）、`internal/atomicfile`（一份原子写，两个调用方）。**同时定死 `V-7`**（责任人 = Go 本地服务；时刻 = 首次读取时惰性创建，进程启动不写文件；只读盘 = 内存默认值 + 错误上抛；并写明与 `E6.2` 规则 4/5 的顺序：只有"文件不存在"才播种）。`PR-2` 拆为 `PR-2a`（本次）与 `PR-2b`（HTTP 与产品门）。能力检索新增 §3.5（原子写）与 §3.6（UUID）。 |
| 2026-09-11 | **`PR-1` 落地。** 新增 `internal/productclient`（契约层 + 客户端 + 内存凭证持有者 + 401 单飞刷新与一次重放）与 `internal/productclient/clienttest`（中台替身，共用同一份 DTO）。**实施期修正一处计划偏差**：JCS + ed25519 验签原列在 `PR-1`，但 `PR-1` 的四个端点**没有任何签名要验** —— 需验签的是签名目录与策略信封，而 bootstrap 在合同里同时返回目录，故把 bootstrap 收敛为「账户 + 激活 + 余额」、签名整体下沉 `PR-4`。理由：让 `PR-1` 不含本题唯一没有现成实现的一处（符合"先走通流程"），且 JCS 是 RFC 8785，手写不合规实现比不写更危险。已判：`L-A2`（二次登录不带激活码/盒子编号）、`L-A5`（缺 boxCode 是空值不是报错）、`L-A6`（刷新一次 + 重放一次；被拒则清凭证）、`L-A4`（四个激活码各自可触发）、`L-A1` 一对多回归、平台不可达 → `network_unavailable`。 |
