# 功能清单与覆盖矩阵（快照）

| 项 | 内容 |
| --- | --- |
| 文档状态 | v0.1 草案，待评审 |
| 快照日期 | **2026-09-17** |
| 定位 | **把「需求写了哪些功能」与「本轮验收面接住了哪些」对齐到一张表上。** 它存在的唯一理由是：**这两件事今天只能靠人工交叉两份文档才知道**，而交叉的结果（19 个功能码与 12 条启动规则**完全没有闭环覆盖**）此前散落在 `TODO-10` / `TODO-11` 与各 `V-n` 行里，没有任何一处能一眼看全 |
| **本文不拥有的任何事实** | 见 §0.1。本文是**视图**，不是 owner |

---

## 0. 怎么用这份文档

### 0.1 一个事实一个 owner（`开发规范` §3.8）

本文**不拥有任何事实**，只把两处 owner 并排放。发现自己想"按这份文档改需求"或"按这份文档改状态"时，请直接改 owner：

| 事实 | 唯一 owner |
| --- | --- |
| 功能点**名称**（`A1`…`G5` 的清单）与**要求正文** | [`需求/20260911/需求基线.md`](需求/20260911/需求基线.md) §2 + §3 |
| **实现状态**（本轮怎么判通过） | [`需求/20260911/E2E闭环清单.md`](需求/20260911/E2E闭环清单.md) |
| **闭环 id** `L-*` 与条数 | 同上（条数以该文件 §2 的「合计」一行为准） |
| **PR 归属** | [`需求/20260911/开发计划.md`](需求/20260911/开发计划.md) §1 |
| **缺口登记** `V-n` | `需求基线.md` §5.6 |
| **已决定不做** `D-nnn` | [`需求/20260911/待解决问题.md`](需求/20260911/待解决问题.md) |
| **还没拍板** `PQn` | [`需求/20260911/待确认问题清单.md`](需求/20260911/待确认问题清单.md) |
| **已决定要做、排在闭环之后** `TODO-nn` | [`需求/20260911/TODO.md`](需求/20260911/TODO.md) |
| **本次人工怎么验** | [`需求/20260911/测试用例.md`](需求/20260911/测试用例.md) |

> **所以：本文会过期，owner 不会。** 修正方式见 §5。

### 0.2 状态取值

沿用 `E2E闭环清单.md` §2 的定义（**这是本轮唯一的判定口径，不要另立**）：

| 取值 | 含义 |
| --- | --- |
| ✅ | 已实现且验收合格 |
| 🔵 | 代码就绪，等中台 / 外部条件（客户端零欠账） |
| 🟨 | 可走通但带**已登记**缺口 |
| ⚠️ | 部分实现或不满足 `§6.4` 第 5 步 |
| ⛔ | 被外部件挡住（合同层缺失），或**决定不做** |
| ⬜ | 未实现 |
| 无覆盖 | **本条不在任何 `L-*` 里** —— 没有任何机制会因为漏做而变红 |

---

## 1. 覆盖矩阵总览

### 1.1 功能码总数

| 组 | 功能码 | 个数 |
| --- | --- | --- |
| A 运行环境与 Profile | `A1`–`A5` | 5 |
| B 模型目录 | `B1`–`B9` | 9 |
| C 模型通信 | `C1`–`C12`（`C12` 只在 §3 正文，未列进 §2 总表） | 12 |
| D 敏感词 | `D1`–`D8` | 8 |
| E 用户体系 | `E1`–`E14` | 14 |
| F 便携交付 | `F1`–`F5` | 5 |
| G 工程约束 | `G1`–`G5`（`G5` 只在 §3 正文） | 5 |
| **合计** | | **58** |

### 1.2 覆盖情况（本文最重要的一张表）

| 覆盖情况 | 个数 | 占比 |
| --- | --- | --- |
| 有专属 `L-*` 承接 | **33** | 57% |
| 仅被别条的闭环**顺带**承载（自身无行） | 4 | 7% |
| **完全没有 `L-*` 覆盖** | **21** | **36%** |

**另有独立的一层缺口**：`需求20260906.md` §5.1.2 的 **13 条启动规则里只有第 10 条**有闭环（`L-E3` 接 `F2`）—— **12 条无覆盖**。`L-B` 那组标题叫「门与启动」，但四条接的是 `E8` / `A1` / `C12`，**一条都不落在启动规则上**。详见 §4。

> **「标题里出现过」不等于「被接住了」** —— 这是本文最想让人看到的一句话。

---

## 2. 逐功能点清单

> 要求一栏是**简述**（condensed）；要写代码或改文案时请以 `需求基线.md` §3 正文为准。

### A 运行环境与 Profile

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `A1` | 环境内置配置（编译期 Profile 二分） | 唯一事实来源是 `profiles/{production,developer}.json`，构建标签二选一、运行时不可切；`production` 的 `allow*` 全 false，地址/公钥缺失时 fail-closed 并给「未配置」页 | 机制已落地；两页文案与四档已由 `L-B2` 落地。**`production.json` 仍是 `.invalid` + 空 `trustedKeyIDs`** ⇒ 正式地址/公钥仍缺（`D-001`，owner 中台） | `L-B2`（规则 6）、`L-C5` | `PR-2c`、`PR-5c`；外部阻塞 `D-001` |
| `A2` | 生产包拒绝开发旁路 | `production` 忽略开发 WebView URL、provider/model/key 环境变量、`serve.env`、`OCTO_DATA_ROOT`；`Validate()` 在 production 下任一开关为 true 即 panic | 机制已在；判据接线由 `C9` 完成 ⇒ 代码就绪，**等 `D-001` 才能真机判** | `L-C5`（🔵） | `PR-5c` |
| `A3` | 数据根与便携工作区 | 数据根 = `<exe 同级>/data`，只此一种；全部路径走 `internal/datapath`，禁 `os.UserHomeDir()` 与 `".octo"` 字面量 | ✅ 已实现，`datapath-guard` 守 | **无** | `base` 已实现 |
| `A4` | 生产包不出现「配 API Key」向导 | production 任何情况都不进上游首次运行向导；后端须真把 `suppressOnboarding` 置 true，且内置网关在无用户 key 时也算「已配置」 | ⚠️ **只有前端半**：抑制位在，但后端 `detectOnboardPhase()` 仍是上游逻辑、**不认内置网关** | **无** | 未登记（`TODO-11`，待人工定紧急度） |
| `A5` | `product_production` 出包门禁 | 每条出包路径都带 `product_production` 标签，否则 CI 失败；被标签排除的代码单独编译与测试 | ✅ 已在（`release-profile-guard` + `checkProductionBinary()`）；`make test-production` 由 `PR-9` 接进 CI | **无** | `PR-9`（接线），本体在 `base` |

### B 模型目录（聊天框模型列表）

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `B1` | 目录唯一来源＝中台签名目录 | 可选模型只能来自中台签名目录，不内置名单、不读 `config.yml` / `chat-modes.json`；目录不可得 ⇒ 空列表 + 提示 | ✅ 已实现 | `L-C1a`、`L-C1b`、`L-C2` | `PR-4b`、`PR-4d`、`PR-4c` |
| `B2` | 目录验签与本地缓存 | 对目录 `data.policy` 的**原始 JSON 字节**做 Ed25519 验签，通过才原子写 `data/catalog.json`；签名/版本/audience/时钟任一失败 fail-closed 且不覆盖缓存 | ✅ 已实现（`internal/catalogstore` 唯一 owner；版本单调按**数值**比而非字符串序） | `L-C1a` | `PR-4a`、`PR-4b`、`PR-4c` |
| `B3` | 目录刷新时机 | 启动异步刷新、按 TTL 周期刷新、陈旧时按需刷新、换账号强制刷新；`304` 不重写缓存 | ⚠️ **部分**：登录后强制 + 开选择器按需 + 条件请求已落地；**按 TTL 的周期定时器仍是 `TODO`**（节奏与生命周期 owner 未定） | **无独立 `L-*`**（只在 `L-C2` 的「残余」里被点名） | 周期刷新 + 定时器 owner 归 `PR-7` |
| `B4` | 目录过期与不可用语义 | 过期且刷新失败 / 验签失败 / 无缓存三种不可用，各有用户可见行为与恢复条件；任一都不得回落 `config.yml`、本地 provider 或内置名单 | ✅ 已实现（`assessCatalog` 四输出；**验签失败压过可用缓存**） | `L-C2` | `PR-4c` |
| `B5` | 选择器结构与模式分组 | 两级选择器（先模式后模型）；三个模式 id 是产品常量、分组归属来自目录；会话级「模式」与「模型」是两个**独立**属性、分别持久化 | ✅ 已实现 | `L-C1b`、`L-C8` | `PR-4d`、`PR-4d1` |
| `B6` | 模型展示名 | 原样渲染目录 `displayName`（中/英，缺当前语言回落 `en` → `id`）；前端**不保留** id→名表；`id` 英文 ASCII | ✅ 已实现（8 个 i18n `model.*` 键已删，断言 `model.` 前缀为空） | `L-C1b` | `PR-4d` |
| `B7` | 模型可选资格与能力矩阵 | `eligible=true` 且 `transport=gateway` 才可选；~~能力矩阵~~ | ✅ 可选资格已实现；**能力矩阵 ⛔ 永不实现**（2026-09-15 人工拍板） | `L-C6`（⛔） | `PR-4e` **已取消** |
| `B8` | 会话模型绑定与模型下架 | 会话绑定目录模型 `id`；下架后老会话可继续读，下一条消息前提示「该模型已下架，请重新选择」，**不静默换模型**、不报硬错 | 🟨（服务端在工厂之前拒绝 + 前端第五档文案，28 条钉子 / 7 变异）；欠 `internal/server` 真 socket 端到端钉子与手工走查 | `L-C7` | `PR-5e` |
| `B9` | 空名单与不可用提示 | 任何「没有可用模型」状态都必须有文案且**不弹配 Key 向导**；「目录不可用（可重试）」与「目录可用但分组为空」**不得共用一句话** | ✅ 已实现（四条文案：`catalog.absent` / `catalog.stale` / `catalog.unverifiable` + `mode.no_models`） | **无独立 `L-*`**（只在 `L-C2` 状态文字里被点名） | `PR-4c` |

### C 模型通信（走中台网关）

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `C1` | 内置网关端点 | 正式会话唯一出口是内置网关端点，不可编辑/删除/被 `config.yml` 同名 id 覆盖，也不出现在 Settings→Endpoints | ✅ 已落地 | `L-C3a` | `PR-5a` |
| `C2` | 动态凭证注入与 401 刷新 | access token 只驻内存不落盘；401 单飞刷新一次并重放一次，失败清 token 落 blocked | ✅ 已落地（`Ensure` 接缝 + 轮换落盘）。**网关那条路的 401 自愈是 `V-92`，见下** | `L-C3a` | `PR-5a`、`PR-4c1` |
| `C3` | 复用既有 provider 栈 | 内容/工具/思考/usage 的流式解析一律复用 `internal/provider/openai`，**不得**为「中台专属」新写第二套 SSE 聚合 | ✅ 已落地 | `L-C3a` | `PR-5a` |
| `C4` | 每次网关调用一个 `clientRequestId` | 每次网关调用生成独立 UUID 放请求体；供重复计费防护与对账入口 | ⛔ **联调期不实现**（`D-003`，走「零改动」路径不发该字段）；**上线前必须补** | **无** | `D-003` |
| `C5` | 网关扩展字段的载体 | 明确 4 个非 OpenAI 字段挂在哪并落成实现 | ⛔ **联调期不实现**（`D-003`）；**上线前必须补** | **无** | `D-003` |
| `C6` | 流中安全终止 `retract` 的载体 | `retract` 必须被真正执行（按 `scope.fromDeltaIndex` 原子撤掉已渲染 delta）；必须保留兜底：解析到未知顶层字段要报错/打 error 日志，**不得静默 `continue`** | ⚠️ **暂不解决**（`D-002`）：本轮只跑内容/工具/usage。缺失面：**安全需求静默失效的风险仍未关闭** | **无** | `D-002` |
| `C7` | 用量与账本终态上屏 | 余额与用量的权威是网关账本，客户端只做只读投影、**不本地扣费**；供应商未上报 usage 时显示「用量核对中」 | 🟨 **部分**：token 数归一与 `usage` 上屏已落；余额唯一来源=拉账本；**「用量核对中」需中台扩展字段，账本终态帧不交付** | **无独立 `L-*`**（由 `L-C3a` 的 usage 半边与 `L-C4a` 承载） | `PR-5d1`、`PR-5d2`；终态帧归 `D-002` |
| `C8` | 错误信封 → UI 状态 | 错误码取值唯一登记在契约；客户端只做 `code → i18n key` 映射，**不解析 `message`**、不自己发明码值；`retryAfterSec` 驱动冷却 | 🟨 **部分**：扁平信封与 `code` 通道已落，`402 insufficient_credits` / `model_withdrawn` 两条有中文；**其余码仍走 `message` 兜底**（`V-67`） | `L-C4c`（依据含「`C8` 规则 2」）；**其余码无覆盖** | `PR-5d3`；完整码表 = `V-67` |
| `C9` | 不回落本地 provider / `config.yml` | 正式会话在任何失败路径上都不得回落本地 provider、环境变量模型源或 `config.yml` 端点；六条失败面全部失败关闭 | 🔵 **代码就绪**（一处收口，9 条钉子 + 4 变异，六条失败面各断言第三方命中 0）；**真机判不了** —— production 仍是 `.invalid` 占位（`D-001`） | `L-C5` | `PR-5c` |
| `C10` | 工具调用仍过本地权限门 | 网关返回的工具调用只表示「模型请求工具」，执行前仍过本地权限门；**不得因来自网关就默认 `allow`** | ✅ 已实现并被钉子证明（**一行产品代码未改**；9 条 + 1 条平台侧钉子、4 变异全中） | `L-C3c` | `PR-5b2` |
| `C11` | 取消与限流 | 取消与限流复用既有实现；取消要同时 `context cancel` 与**告知网关**；退避读 `retryAfterSec`，不得固定 sleep | ✅ 复用既有实现；**「告知网关」那一半因 `clientRequestId` 不做而无法完整**（`D-003`） | `L-C3a` 的异常分支 | `PR-5a`；完整语义受 `D-003` 制约 |
| `C12` | 控制面失败 vs 网关失败：两套口径 | 控制面失败分四档，逐档定文案、可重试性与是否清凭证；档位判定只在 `internal/productclient` **一处** | ✅ 已实现（四档 UI + 只有 `unauthorized` 清凭证 + 单点判定）。残余：客服载体仍缺（`V-4` / `PQ26`） | `L-B3` | `PR-2c` |

### D 敏感词

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `D1` | 输入检测（可关，关前确认） | 默认开启；发送前检测，命中即本轮不发、框内打码、提示；**权威在后端**，回合路径必须再检一次 | ✅ 已落地（`check` 路由 + 三个回合入口的服务端再检；判据落在**顺序**上） | `L-D1` | `PR-6b3` |
| `D2` | 输出检测（不可关，只打码不截断） | 命中片段一律 `***`（连续命中合并）、不截断整段；**落盘即打码**；用户不可关闭 | ✅ 已落地（判据读会话文件**字节**，靶子是跨 chunk 拆开的词；19 变异全中） | `L-D2` | `PR-6a` |
| `D3` | 昵称检测 | 命中即拒绝保存（不写 `***`、不改脱敏值）；**始终开启**，不受输入开关影响 | ✅ 已落地（三条反钉；格式规则与前端逐值镜像） | `L-D3` | `PR-6b1` |
| `D4` | 三层词库 | 有效词库 = 内置 ∪ 用户 ∪ 服务器**只读**（并集，不互相覆盖）；用户词缺失 = 空词表而非错误 | ✅ 已落地 | `L-D4a`、`L-D4b`、`L-D4c` | `PR-6b2`、`PR-6c`、`PR-6` |
| `D5` | 服务器词库同步 | 按版本/签名同步、原子写缓存；验签失败/版本回退 ⇒ **保留旧版继续用并知会用户**；不得 fail-open 到「不检测」 | ✅ 已落地 | `L-D5` | `PR-6` |
| `D6` | 匹配与替换规则的唯一实现 | 规范化 + 子串匹配 + 合并打码只有**一处**实现；符号表只有一处定义 | ✅ 已落地：owner = `internal/sensitive/normalize.go`；一致性由 `scripts/sensitive-norm-guard.mjs` 断言。**断言范围只含符号表字符集**，顺序与算法其余部分不在内 | **无 `L-*`**（由守卫替代） | `PR-6a` + 守卫族（`PR-9`） |
| `D7` | 用户词库管理 | 一个页面完成查/增/删/导入；导入是**合并**不是覆盖；热加载；用户词不得写入服务器层或内置层 | ✅ 已落地 | `L-D4a`、`L-D4b`、`L-D4c` | `PR-6b2`、`PR-6c` |
| `D8` | 与服务端内容安全的边界 | 客户端敏感词与服务端内容安全是两层，职责不重叠；**不得把用户原文送第三方审核** | ⚠️ **客户端半边已实现**；**服务端半边（`retract` / 安全终态）未做** ⇒ 折进 `D-002` | **无** | `D-002`（最晚时点：内容安全验收开始前） |

### E 用户体系

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `E1` | 首次激活 | 五字段激活；本地只做格式校验、业务判定交中台；激活码**一次性**、与盒子编号**一对多**；须有客服纠错通道 | ⚠️ **三半不等**：成功路径 ✅；失败分流 ⚠️（文案齐但**客服载体仍缺**）；响应丢失 ⛔（缺幂等键） | `L-A1a`、`L-A1b`、`L-A4` | `PR-2b1`、`PR-2b2a`、`PR-2d`、`PR-2e`；阻塞 `PQ25` / `PQ26` / `V-4` |
| `E2` | 二次登录 | 已有绑定记录时不出现激活码/盒子编号，昵称预填可改；号不一致回脱敏绑定号；全新 `data/` 也可只凭手机号+验证码登进已有账号 | ✅ 已实现 | `L-A2` | `PR-4b`、`PR-2e` |
| `E3` | 手机号规范化 | 去空格与短横线 → 剥 `+86`/`86` → 必须 `1` 开头 11 位；实现只有前端 `phone.ts` + 服务端权威各一处 | ✅ 已实现 | `L-A2` | `PR-2b1`、`PR-4b` |
| `E4` | 验证码 | 由中台签发与校验，客户端只有倒计时与冷却；冷却秒数取服务端 `cooldownSec` / `retryAfterSec`，**不硬编 60** | ✅ 已实现 | `L-A2` | `PR-2b1`、`PR-2b2a`、`PR-4b` |
| `E5` | 激活状态与授权到期 | 授权页只出现「已激活」或「已过期」；展示剩余天数 + 脱敏手机号 + **盒子编号（不脱敏）**；缺 `boxCode` 显示「—」不拦截 | ✅ 已实现（三要素同页 + 6 条组件测试，判据全在进程内） | `L-A5` | 车道 2 第 3 步 |
| `E6` | 登录态与凭证存储 | access token 只驻内存永不落盘；refresh token 明文存 `data/credential.json`；`product-state.json` 不含任何令牌；首启种子幂等且**启动不写文件** | ✅ 已实现 | `L-E1`、`L-E4a`、`L-E4b`、`L-A6` | `PR-2a`、`PR-2c`、`PR-4b` |
| `E7` | 退出登录 | 退出登录 ≠ 退出进程：清登录态、回拦截页、进程与托盘仍在；积分/会话/激活绑定保留；**同时清服务端登录态** | ✅ 已实现（端到端「激活→登出→再登录」；登出真实调用平台撤销） | `L-A3`、`L-E2`（撤销半边） | `PR-4b`、`PR-2f` |
| `E8` | 产品门（界面门 + 请求门） | 三态 `unknown`/`blocked`/`ready`；无窗口令牌 ⇒ 门不生效；`state` 读失败 ⇒ blocked **不得** ⇒ ready；门只覆盖 `/api/product/*` | ✅ 已实现（含**门范围钉子**）；手工联调待桌面壳重建后走一遍 | `L-B1a`、`L-B1b` | `PR-2b2a`、`PR-2b2b` |
| `E9` | 积分与用量 | 权威在中台；**余额唯一来源 = 中台账本、唯一写入路径 = 拉 `GET /v1/credits/ledger`**；任何信号都只是「去拉一次」的触发器；不本地扣费 | 🟨 已落地（10 条钉子 / 8 变异）。缺口：周期定时器待定、`credits_update` 无发射方、手工走查未做 | `L-C4a` | `PR-5d1`；定时器归 `PR-7`；跨窗口归 `PR-8` |
| `E10` | 套餐与充值 | 升级/充值入口**常驻可见**（不因余额充足而隐藏）；支付走外链或占位，客户端不处理支付、不存支付凭证 | ✅ 已实现（**外链未给** —— 契约/`brand.json`/`productprofile` 三处都无 URL 字段，占位即终态） | `L-C4b` | `PR-5d2` |
| `E11` | 用户协议与隐私政策 | 登录前可读；同意是**显式**的、带版本号、可追溯；政策更新后重新征得；不得以「继续使用」代替显式同意 | ⚠️ **只有壳**：`LegalModal` 在，但**正文与版本未定**（`brand.json` 里是占位）；版本 + 同意时间落盘未做 | **无** | 未排期（`TODO-11`） |
| `E12` | 设备会话与撤销 | 能列出当前账号的登录设备会话并撤销；撤销后本机若在被撤之列 ⇒ 落 `blocked` | 🟨 **部分**：撤销路径已落地（清凭证回拦截页 + 登出撤销平台会话）；**设备会话列表 UI 未做**（`PQ10`） | `L-A6`、`L-E2`（撤销半边） | `PR-2c`、`PR-2f`；列表 UI 归 `PQ10` |
| `E13` | 账号删除与数据导出 | 账号删除申请（服务端动作 + 保留期说明 + 提示不可撤销 + 删除后清本地凭证）与数据导出（只导用户可见数据，不含凭证） | ⬜ **零实现**（**在任何 `V-n` / `PQn` / `L-*` 里都不存在**）；合规相关，时点由上线时间表定 | **无** | 未排期（`TODO-11`） |
| `E14` | 客服工单与 `requestId` 关联 | 报障时能给出可定位的 `requestId` / `clientRequestId`；诊断包带最近请求 id 列表但不带内容；帮助页留客服占位 | ⚠️ **部分**：帮助页占位在（**载体仍缺**）；`clientRequestId` 侧**不做**（`D-003`）；诊断包侧依赖 `F4`（零实现） | **无** | 依赖 `D-003` 与 `F4` |

### F 便携交付

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `F1` | 便携目录与单实例 | 主交付是便携目录，**不在主机桌面/开始菜单建快捷方式**；同机只跑一份（端口固定 8088）；被占用时明确提示后退出，**不换端口不改数据根** | ✅ 已实现并在 `base` 验收；启动规则新增已收敛：`V-82`（数据根可写性/空间预检）、`V-84`（端口占用规定文案）、`V-83`（跨程序目录二次启动提示） | **无 `L-*`** | `DEV-0911-04A`、`DEV-0911-04B` |
| `F2` | 拔盘冻结与冷启动恢复 | 程序目录消失 ⇒ 立即冻结界面并提示重新接入、**禁止改写主机**；同路径恢复可继续，换盘符必须退出后从新路径双击 | ⚠️ **壳侧冻结已实现并有判据**（写门 + watchdog + 启动失败链 fail-closed）；**冻结页今天仍不会出现** —— 由 `datastore:lost` 驱动，该事件归 `PR-8`、卡 `G1`。另 `V-88`：解冻分不出「同盘插回」与「异盘挂同路径」 | `L-E3` | `PR-7`（壳侧）+ `PR-8`（事件） |
| `F3` | 便携更新 | 更新走签名清单 + 用户确认 + 失败回退；便携形态**关闭**自动检查更新与托盘更新入口；做不到安全写回就保持「即将支持」占位 | 🟨 **部分**：关闭三处已收敛（`V-86`：`productUpdatesEnabled=false`，`autoUpdateLoop` / 托盘项 / `canInplaceUpdate` 一并到不了）；**正式的签名清单更新流与回退未做** | **无** | `V-86` 已收敛；更新流阻塞外部（`F5`） |
| `F4` | 诊断包（脱敏） | 一键导出，内容走**白名单**（版本号、Profile 名、平台信息、最近请求 id）；**不含**会话正文、模型输出、token、手机号明文、词库全文、U 盘激活码与盒子编号 | ⬜ **零实现**（**不在任何 `V-n` / `PQn` / `L-*` 里**）；`A1` 规则 6 要求的诊断信息也因此无处可出 | **无** | 未排期（`TODO-11`） |
| `F5` | 发布签名与校验和 | 发布物有签名/校验和，客户端能校验更新包；`production.json` **不得**留 `.invalid` 占位与空 `trustedKeyIDs` 出包；验签失败 ⇒ fail-closed 不安装 | ⚠️ **部分**：出包门禁在（当前报 3 条**咨询**警告，正是 `production.json` 那条已知事实）；**更新包验签链依赖 `F3`，未做** | **无** | 守卫 `PR-9`；内容阻塞外部 |

### G 工程约束

| Code | 名称 | 要求（简述） | 实现状态 | 覆盖闭环 | 归属 PR |
| --- | --- | --- | --- | --- | --- |
| `G1` | 上游 `internal/server` 不新增产品逻辑 | 产品逻辑落 `internal/productruntime` 及下游；例外须登记、单独评审、带黑盒回归 | 🟨 **部分满足**：`server-diff-guard` 在册，但棘轮有**抬升记录**（每处都写明理由，属「受控越界」而非零新增） | **无** | `PR-9`；抬升随各功能 PR |
| `G2` | 单一真相来源 | 一个事实只有一个 owner；派生文档只写结论 + 链接 | 🟨 **部分收敛**：`D6` 已加断言；但契约字段名仍是**提案**，下游派生只能指向不能复制；文档容器类漂移仅由 `docs-table-guard` 兜住表格行列数，**符号检索（`TODO-08`）未做** | **无** | `PR-9`（部分） |
| `G3` | 有界降级 | 每个降级回答三问（回落什么/是否告知/何时恢复）；不得静默降级到未授权来源；签名/版本/audience/时钟一律失败关闭；**启动期不得写用户可编辑文件** | 🟨 **机制已落地且被多条闭环引用并做判据**；但**没有专属于 `G3` 的断言清单**，验收栏要求的「§3.9 检查表逐条对照」**未做** | **无**（分散在各闭环里体现） | 无专属 PR |
| `G4` | 守卫与 CI | 新约束必须落成守卫或测试，**不靠人记**；闭环须在 CI 里可重放 | 🟨 **部分**：7 个 `make` 目标 + 5 个 CI job 已接线，`sensitive-norm-guard` 已落地；**仍欠**：① 验收栏要求的「**先红后绿**」未做；② `C9`「不回落」黑盒回归与 `E6`「`data/` 内无 token 明文」扫描两条守卫**都还没写**；③ `TODO-08` 符号检索未做 | **无**（它是全部闭环的自动回归面） | `PR-9`（接线） |
| `G5` | 开发期假后端的生命周期（**已退役**） | 规则留存：任何未来的替身必须显式开关且默认关闭、开启时可见、未注册路径必须回 404、不允许隐式回退 | ✅ **已退役**（`devBackend.ts` 已删除，`rg` 无命中）。**留下的账**：4 个产品 WS 事件在它下面不可测，事件至今仍无发射方 | **无** | `PR-3` |

---

## 3. ⚠️ 完全没有 `L-*` 覆盖的功能码

**这是本文最重要的一节。** 以下功能码在 `E2E闭环清单.md` 的 38 条里**没有任何一行承接**（既不是「依据」，也不出现在某行的状态文字里）。按 `TODO-10` / `TODO-11` 的判据：

> **没有任何机制会因为漏做而变红，也没有人认领。**

| 组 | 无覆盖码 | 具体代价 |
| --- | --- | --- |
| A | `A3`、`A4`、`A5` | `A3` 已实现但只做「回归」；**`A4` 后端半边未做**；`A5` 已在但无判据 |
| B | `B3`、`B9` | `B3` 的 TTL 周期刷新是 `TODO`，节拍与 owner 未定；`B9` 的四条文案已随 `L-C2` 验，但自身无行 |
| C | **`C4`、`C5`、`C6`**（另 `C7` / `C8` 仅顺带承载） | `C4`/`C5` = `D-003` 联调期不做；`C6` = `D-002` 暂不解决。**三条都写着「上线前必须补」** |
| D | `D6`、`D8` | `D6` 改由守卫替代（**断言范围仅符号表字符集**）；`D8` 折进 `D-002`，服务端撤回半边未做 |
| E | **`E11`、`E13`、`E14`**（`E12` 的会话列表 UI 半边同样无行） | 三条**在任何 `V-n` / `PQn` / `L-*` 里都不存在** —— 不是"排在后面"，是**不在任何一张表上** |
| F | `F1`、`F3`、`F4`、`F5` | `F1`/`F3` 属「需求 §5.1.2 启动规则」这一整节从未进闭环面（§4）；**`F4` 零实现** |
| G | `G1`、`G2`、`G3`、`G4`、`G5` | 五条工程约束全部无 `L-*`，靠守卫与纪律承载；**`G4` 自己还欠两条守卫未写** |

### 3.1 需要区分两类

它们**性质不同**，下一步动作也不同：

| 类 | 有哪些 | 性质 |
| --- | --- | --- |
| **已登记、有接手点** | `B3`、`C4`、`C5`、`C6`、`D8`、`F3`、`F5`、`G1`–`G4` | 缺的是**时间或外部条件**；接手点在 `TODO-10` / `待解决问题.md` / `开发计划.md` |
| **从未被登记** | `A4`、`E11`、`E13`、`E14`、`F4`、`A3`、`A5`、`F1` | 缺的是**一个决定**（谁做、什么时候做）。`TODO-11` 是它们唯一的落点 |

---

## 4. `需求20260906.md` §5.1.2 启动规则的覆盖情况

**13 条里只有第 10 条有闭环。**

| 情况 | 条数 |
| --- | --- |
| 有 `L-*` 承接（第 10 条 → `L-E3`，依据 `F2`） | 1 |
| **无任何 `L-*`** | **12** |

**为什么 `L-B` 那组没能接住它们**：`L-B` 的标题是「门与启动」，但它四条接的是 `E8`（界面门 / 请求门）、`A1`（未配置两页）、`C12`（控制面四档）—— **一条都不落在 §5.1.2 的启动规则上**。所以"标题里出现过启动"不等于"被接住了"。

**这 12 条里已经收敛的**（先例：唯一 owner + 只加门控不删上游代码 + 钉子先证红）：

| `V-n` | 内容 | 状态 |
| --- | --- | --- |
| `V-86` | 便携未关闭自动更新与托盘更新入口 | ✅ 2026-09-15（分支 `portable/v86-no-auto-update`） |
| `V-82` | 数据根可写性 / 空间预检 ＋ 失败页只能退出 | ✅ 2026-09-16（`DEV-0911-04A`） |
| `V-84` | 端口占用规定文案（丢了归因与动作） | ✅ 2026-09-16（`DEV-0911-04A`） |
| `V-83` | 跨程序目录二次启动提示 | ✅ 2026-09-16（`DEV-0911-04B`） |
| **`V-85`** | **便携隐藏开机自启开关** | ❌ **仍未实现**，归 `DEV-0911-04C`，无前置阻塞 |

`V-85` 的方案**已拍板**（产品，2026-09-16）：取「**整行不渲染**」（与需求第 9 条原文「隐藏该开关」一致）。今天 `SettingsModal.svelte` 那一行只被 `{#if $nativeShell}` 门控，而便携构建**正是** native shell ⇒ 开关照常渲染，用户一开就写主机注册表。

---

## 5. 这份快照怎么再生成

**不要手改这张表。** 两处 owner 任一改动后，按下面顺序重新生成，避免出现"第二个写状态的地方"：

1. 功能码与要求：读 `需求基线.md` §2 + §3。
2. 实现状态：读 `E2E闭环清单.md` §2 —— **以状态列为准，不要读 §3 的「现状」段落**（多处早于代码，见下）。
3. 覆盖列：把每个功能码在 `E2E闭环清单.md` 里全文检索一次，区分"是某行的依据"与"只在某行状态文字里被顺带提到"。
4. PR 归属：读 `开发计划.md` §1。
5. 更新本文的表头「快照日期」，并在 §6 记一行。

### 5.1 生成时发现的两处文档不一致（不是本文的错，但会误导读者）

1. **`E2E闭环清单.md` 里 `L-C3b` / `L-C3c` 的「依据」列编号与 `需求基线.md` §2 错位**：表中 `L-C3b`（思考块）依据写 `C10`、`L-C3c`（工具调用）依据写 `C11`；但按 §2，`C10` 是「工具调用仍过本地权限门」、`C11` 是「取消与限流」。按**内容**判断 `L-C3c` 对应的是 `C10`、`L-C3b` 更接近 `C7`/`C6` 那一族而非 `C10`。**本文按内容归类并在此标注，未按字面照抄** —— 建议由 `E2E闭环清单.md` 的 owner 裁定后一次改齐。
2. **`需求基线.md` §3 的「现状」段落普遍早于代码**：最明显的三处是 `A1`（写"两个判定函数还没有调用方"，已被 `L-B2` / `PR-2c` 推翻）、`C2`（写"完全缺失"，已被 `PR-5a` / `PR-4c1` 推翻）、`D6`（写"Go 侧尚不存在"，已被 `PR-6a` 推翻，2026-09-15 已就地更正）。**判断实现状态一律以 `E2E闭环清单.md` 为准。**
3. **`E2E闭环清单.md` 的分组标题陈旧**：`L-C` 组的标题写「`L-C` 模型（10）」，而实际有 **13** 行（`L-C1` 拆成 `a/b`、`L-C3` 拆成 `a/b/c`、`L-C4` 拆成 `a/b/c` 之后标题未跟）⇒ 分组标题的数字比实际行数少 3。**状态列与「合计」行是对的**（38 条逐格可数），只有这个标题陈旧。**这类"容器"错与 `V-52` / `V-56` 同族**（内容对、容器错），当时的处置是不加守卫（要机器判必须解析单元格，整行正则会假绿）。

---

## 6. 修订

| 日期 | 说明 |
| --- | --- |
| 2026-09-17 | 建立。把 `需求基线.md` §2/§3 的 58 个功能码与 `E2E闭环清单.md` 的 38 条 `L-*` 做了一次逐条交叉，产出三个此前没有单点视图的结论：① **21 个功能码完全没有 `L-*` 覆盖**（33 有专属闭环 / 4 仅顺带承载 / 21 全无）；② `需求20260906.md` §5.1.2 的 **13 条启动规则里 12 条无覆盖**，「标题叫门与启动」的 `L-B` 组实际一条都没接住它们；③ 上述无覆盖项里，**8 个（`A3`/`A4`/`A5`/`E11`/`E13`/`E14`/`F4` 及 `F1`）连 `V-n` 登记都没有**，`TODO-11` 是它们唯一的落点。§5.1 另记了两处生成时核出的文档不一致（`L-C3c` 的依据列编号错位、`需求基线` §3「现状」段落普遍早于代码）。**范围（§3.10）**：本文是**视图**，不拥有任何事实（§0.1）；事实修正请改各自 owner。 |
