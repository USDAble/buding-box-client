# 阶段 6：工程欠账

## 目标

在主要闭环完成后收敛不会直接阻塞用户路径、但会影响后续维护与开发效率的欠账。

## 任务

1. `TODO-08`：文档引用失效源码符号的机械守卫。
2. `TODO-01`：从 `archive/feat-desktop-dev-hotreload-20260911` 按文件搬运 `make desktop-dev` 热重载。
3. `TODO-07`：下次修改 `App.svelte` 时同步修正错误注释引用。
4. `PR-9`：为尚未验证的守卫补逐条先红后绿变异证据。
5. `VersionBadge.svelte`：先做 import 可达性确认；涉及删除上游文件时先评审，不直接清理。

## 退出条件

- 新守卫有自己的单测、Makefile 目标、CI 接线和有效变异证据。
- 热重载从归档能力搬运并对齐当前路径，不重写第二套。
- 注释不再引用不存在的符号或文档位置。
- 所有删除或 allowlist 变化均有明确人工确认和收敛理由。
- `make gate` 通过；适用时 `make test-production` 通过。

## 执行记录

- 待填写。

阶段完成后，将总结写入本目录的 `阶段总结.md`。
