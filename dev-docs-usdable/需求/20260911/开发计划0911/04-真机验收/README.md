# 阶段 4：真机验收

## 目标

完成人工才能给出的最后一跳证据，并把结果同步回闭环与开发计划 owner。

## 必做

| 闭环 | 验收重点 |
| --- | --- |
| `L-C7` | 模型下架后不静默换模型；输入归还；服务端拒绝；网关零命中 |
| `L-C3a` | 使用真实供应商 key 验证真流式和真实 usage |
| `L-C4a` | 聚焦、进页、手动、回合结束刷新；失败保留旧余额 |
| `L-E2` | 把同一份 `data/` 复制到第二台电脑后无需重新激活/登录，并验证撤销后的旧副本不可继续使用 |

## 顺手复核

- `L-B1b` 窗口令牌与产品路由门。
- `L-C3b` 思考块显示与关闭。
- `L-C3c` 本地工具权限询问、允许与拒绝。
- `L-C4b` 充值/升级占位与零余额入口。

## 退出条件

- 必做四项均有操作者、设备、日期和结果。
- 新现象按实际行为登记，不通过改预期掩盖缺陷。
- `E2E闭环清单.md` 对应状态行已更新。
- `开发计划.md` 对应 PR 的残留项已更新。
- 合计行由合并者按状态重新统计。

## 执行记录

- 待填写。

阶段完成后，将总结写入本目录的 `阶段总结.md`。
