# TODO · 已登记、闭环期不做

| 项 | 内容 |
| --- | --- |
| 文档状态 | 正式登记（2026-09-12 建立） |
| 关联 | [`开发计划.md`](开发计划.md) §5、[`需求基线.md`](需求基线.md) §5.6、[`待解决问题.md`](待解决问题.md)、[`待确认问题清单.md`](待确认问题清单.md) |

## 这份文件管什么

本文件登记**已经被发现、已经决定「闭环期不做」、但明确要做**的工作项。

| 文件 | 登记的是什么 | 判据 |
| --- | --- | --- |
| [`待确认问题清单.md`](待确认问题清单.md) | **还没拍板、需要有人回答**的问题（`PQn` / `Qn`） | 缺一个决定 |
| [`待解决问题.md`](待解决问题.md) | **已拍板「暂不解决」**的技术问题（`D-nnn`） | 缺一个决定，且决定是"不做" |
| [`需求基线.md`](需求基线.md) §5.6 | **文档与现实不符、或新增的规格缺口**（`V-n`） | 是一个需要修正的**事实偏差** |
| **本文件** | **已决定要做、但排在闭环之后的工作项**（`TODO-nn`） | 缺的是**时间**，不是决定，也不是事实修正 |

**四者的关系**：`V-n` 是"发现了什么"（`需求基线` §5.6 是它的唯一 owner），`TODO-nn` 是"什么时候做、谁接"。**所以每个 `TODO` 条都指向它对应的 `V-n`，且不复制 `V-n` 的描述**（`开发规范` §3.8：衍生文档只引结论 + 链接，不复述推理）。

**判据（`开发规范` §3.10 写清范围）**：进本清单的项必须同时满足 ① 与任何 `L-*` 闭环的**验收无关**或 ② 是**大功能**，修它会挤掉真正卡验收的工作。**不满足者不得进本清单** —— 如果是闭环要求的，就必须在闭环期修完，缺的只是 PR 归属，那种情况记在 `开发计划.md` §1.1 或该 PR 的方案里。

## 登记约定

- 每条格式：**编号 / 关联 `V-n` / 标题 / 为什么可以等 / 现在什么样（用户可见）/ 接手点 / owner**。
- **「现在什么样」必须写实**：不写"体验欠佳"，要写清用户或开发者此刻会看到什么。含糊的登记等于没登记（沿用 [`待解决问题.md`](待解决问题.md) 的同一条约定）。
- **「接手点」不得为空**。写"以后再说"的条目不是记录，是逃避 —— 要么给 PR 号，要么给触发条件（"下次改 X 时"）。
- 做完一条**不删行**，把状态改为 ✅ 并注明落在哪个 commit/PR（保留"曾经记过"的痕迹，便于回溯为什么当时不修）。

## 总览

| 编号 | 关联 | 标题 | 为什么可以等 | 接手点 |
| --- | --- | --- | --- | --- |
| `TODO-01` | `V-14` | `make desktop-dev` 热重载开发模式 | 纯开发者体验，与全部闭环无关 | `PR-9` 之后，或单独 PR |
| `TODO-02` | `V-1` | 六个守卫的 CI 接线 | 脚本与单测都已在 `v1`，缺的只是调用 | `PR-9` |
| `TODO-03` | `V-20` | `P2-启动与生命周期.md` 不存在 | 纯文档缺口，不影响代码 | 补文档（无 PR 依赖） |
| `TODO-04` | `V-25` | 产品 WS 四事件没有发射方 | 前置是 `N-5`（载荷形状未拍板） | `PR-8`，阻塞于 `N-5` |
| `TODO-05` | `V-26` | 会话文件**明文**落敏感内容 | 要先定「打码 / 加密 / 不落」三选一 | `PR-6` 期间 + 人工拍板 |
| `TODO-06` | `V-4` | 客服入口的渠道载体未定 | 依赖**外部**决定，不是本项目能定的 | `PR-2c` 收尾时按"确定动作"处理 |
| `TODO-07` | — | `web/src/App.svelte:93` 引用勘误 | 改它要动 `.svelte`、触发前端重测 | 下次改 `App.svelte` 时顺手 |
| `TODO-08` | `V-31` | 文档引用的源码符号的机械检查 | 工程卫生，与闭环无关 | `PR-9` 守卫族 |

**闭环期已闭环掉的、曾在本类的**：`V-19`（错误码漂移守卫，2026-09-12 已合并 `v1`）。

---

## 逐条

### `TODO-01` `make desktop-dev` 热重载开发模式（`V-14`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **纯开发者体验**（Vite 热重载调界面）。终端用户路径不受影响：出包走 `make desktop-portable` / `desktop-portable-all`，与它无关。与 35 条 `L-*` 闭环**零交集** |
| 现在什么样 | 开发期改前端要重新 `make desktop` 走完整构建；`make desktop-dev` **目标根本不存在**，照着 `本地开发与运行.md` §3.4 敲会得到 `No rule to make target`。该节已加 ⚠️ 标注（2026-09-12） |
| 缺什么 | 两件：① `Makefile` 的 `desktop-dev` 目标；② 窗口 URL 可被环境变量覆盖的机制（`OCTO_DESKTOP_DEV_URL` → 壳读它改窗口 URL，默认 `http://127.0.0.1:8088`） |
| 为什么是"搬运"不是"新功能" | 两件都**已实现过**，落在 `archive/feat-desktop-dev-hotreload-20260911`。按 `开发规范` §3.5，**从 tag 取文件，不要重写** |
| 注意 | `本地开发与运行.md` §3.4 给出的 `Makefile:130-135` / `main.go:92` 行号来自归档 tag，**在 `v1` 上不成立** —— 搬运时按 tag 里的实际位置取，不要按那两个行号 |
| 接手点 | `PR-9` 之后，或任意时候单独一个 PR |
| owner | 未指定 |

### `TODO-02` 六个守卫的 CI 接线（`V-1`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 六个守卫的**脚本与单测都已在 `v1`**（`scripts/*-guard.mjs`），缺的只有 `Makefile` 目标与 `go.yml` job。不影响任何功能与闭环验收 |
| 现在什么样 | 守卫写了却**没有人调用** —— 它断言的事今天无人守护，回归不会被发现。这正是 `V-1` 的性质：不是功能缺失，是**自动回归缺失** |
| 缺什么 | 六条接线：`reuse-check` / `server-diff-check` / `release-profile-check` / `preflight-check` / `test-production` 接进 `Makefile` + `go.yml`。**接线位置在 `v1` 上不存在，在归档 tag 上**（`archive/feat-reuse-guard-20260911` 等五个，逐个对应） |
| 注意 | `开发规范.md` §9 第 7–10 项的接线位置已于 2026-09-12 从"未合并的分支"改正为归档 tag —— 接手时按 tag 取 |
| 接手点 | **`PR-9`**（已排） |
| owner | 未指定 |

### `TODO-03` `P2-启动与生命周期.md` 不存在（`V-20`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **纯文档缺口**。`本地开发与运行.md` 两处引用它，代码不依赖它 |
| 现在什么样 | 引用处已改为全路径 `需求/2260906/技术方案/P2-启动与生命周期.md` 并加 ⚠️ 标注（不存在，见 `V-20`），读者不会扑空但会知道缺一份 |
| 缺什么 | 该文档本身（启动顺序、生命周期、与 `P1` 数据根 / `P3` 产品门的交界） |
| 接手点 | 补文档，无 PR 依赖 |
| owner | 未指定 |

### `TODO-04` 产品 WS 四事件没有发射方（`V-25`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 前置是 **`N-5`**（四事件的载荷形状未拍板）。**前端四个 handler 都已就位**，只等生产端 |
| 现在什么样 | 事件名在 Go 源码里**零命中** ⇒ 界面上依赖它们的四处（余额刷新、输入命中提示、`datastore:lost`、模型下架提示）今天**永远收不到**；`L-E3`（拔盘冻结提示）也挂着 |
| 缺什么 | 四个事件的发射方；以及 `N-5` 对载荷形状的拍板 |
| 接手点 | **`PR-8`**（已排，阻塞于 `N-5`） |
| owner | 未指定 |

### `TODO-05` 会话文件**明文**落敏感内容（`V-26`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **不是"可以不做"，是"要先把方案定了"**。当前不阻断任何闭环验收，但**上线前必须定** |
| 现在什么样 | **输出打码全仓不存在**：`internal/agent` 零敏感词引用；持久化 `internal/agent/session.go` 的 `Save` → `rewriteAll` / `appendDelta` 把消息**逐字写入**；前端也没有渲染层过滤。`L-D2` 要求的"**落盘即打码**、重启后重读历史仍是打码后文本" **完全不成立** —— 今天重启后看到的是**未打码原文** |
| 性质比"没做"更重 | 文档声称这条是"隐私面的硬要求"（`L-D2`），实现是零。这是 `V-23` 那类"标 ✅ 但没做"里最容易被当成已完成的一条 ⇒ 已登记在 `需求基线` §5.6 |
| 缺什么 | 一个决定：**打码 / 加密 / 不落盘**三选一。三者代价不同（打码不可逆、加密需密钥、不落盘丢历史），**不是研发能单方面拍的** |
| 接手点 | `PR-6`（内容安全三层）期间处理；**决定需要人工拍板**（`开发规范` §3.7） |
| owner | 未指定 |

### `TODO-06` 客服入口的渠道载体未定（`V-4`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 依赖**外部决定**（渠道形式与联系方式），不是本项目能定的。`E1` 规则 2 / `E5` 规则 3 / `E14` 规则 3 / `C12` 的 `account_restricted` 档四处都要求"给出客服入口" |
| 现在什么样 | 四处要求都**没有可落地的载体** |
| 缺什么 | 渠道与联系方式。`PR-2c` 的 `account_restricted` 档按既定原则处理：**只给"文案 + 一个确定动作"，绝不留"请联系客服"却没有任何联系方式的死路**（`P4-拦截页.md` §3.3 已写明这条约束） |
| 接手点 | `PR-2c` 收尾时确认一次；载体定了再补进文案 |
| owner | 未指定 |

### `TODO-07` `web/src/App.svelte:93` 引用勘误

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 是**代码注释里的错误引用**（把 `suppressOnboarding`（属 `A4`）引作 `P9 §3.1`），不影响行为 |
| 现在什么样 | 注释指错文档，读者按它去 `P9 §3.1` 找 `suppressOnboarding` 会找不到。已在 `需求基线.md` 修订里记为"引用勘误" |
| 缺什么 | 一处注释改正 |
| 为什么不当场改 | 改 `.svelte` 会触发前端重测；与本轮闭环无关 |
| 接手点 | **下次改 `App.svelte` 时顺手** |
| owner | 未指定 |

### `TODO-08` 文档引用的源码符号的机械检查（`V-31`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **工程卫生**，与全部闭环无关 |
| 现在什么样 | `V-31` 是一**类**（"规范性/方案文档把已归档或已删除的代码当作现存事实引用"），已发现两起实例（`P1` §2、`开发规范` §3.9.1），**都已就地勘误**；但没有任何机制阻止第三起 |
| 缺什么 | 一条机械检查：把文档里带反引号的函数符号拿到全仓源码里回找，命不中就**要求同一句出现"已删除/已归档/不存在"之类的说明**。检索办法与"5 个命中里 3 个是正确写法"的判据写在 `需求基线` `V-31` 行内 |
| 接手点 | **`PR-9`** 的守卫族（与 `TODO-02` 同一批接线） |
| owner | 未指定 |

---

## 附：已认领的缺口（**不在本清单**）

列在这里**仅为对照**，避免读者以为它们没人管。它们与闭环**直接相关**，因此必须在闭环期修完：

| `V-n` | 归属 |
| --- | --- |
| `V-24` 6 条 `/api/product/*` 路由无 Go handler | `PR-4d`（`chat-modes`）、`PR-6`（`nickname` / `prefs` / `sensitive/*` 共 5 条）。见 `开发计划.md` §1.1 路由归属表（**该表是唯一 owner**） |
| `V-27` `L-C5`「不回落」未接线（`RequiresControlPlane()` 零调用点） | `PR-5` |
| `V-28` `L-C4` 余额与用量（无 `GET /v1/credits/ledger`、充值按钮 `disabled`） | `PR-5` |
| `V-29` 端到端测试缺失（`L-A2` / `L-A3` / `L-A6`） | `PR-4b`（走 `bootstrap` 使链路可达）+ `PR-6` |
| `V-15` / `V-16` 拦截页两页/四档未做、`L-A6` 已落地 | **`PR-2c`**（进行中） |
| `V-17` `L-E2` 换机场景测试 | `PR-7` |

## 修订

| 日期 | 说明 |
| --- | --- |
| 2026-09-12 | 建立。从 `开发计划.md` §5.1 的"延后清单"独立成文并补全字段（原表只有"为什么延后 + 接手点"两列，缺"现在什么样"，读者无法判断代价）。**这 8 条的共同判据是 `开发规范` §3.10 的范围：要么与任何 `L-*` 闭环的验收无关，要么是大功能，修它会挤掉真正卡验收的工作。** 逐条核对了归属：`TODO-02` / `TODO-08` 归 `PR-9`，`TODO-04` 归 `PR-8`（阻塞于 `N-5`），`TODO-05` 随 `PR-6` 但**决定需人工拍板**，`TODO-01` / `TODO-03` / `TODO-07` 与任何 PR 无关（分别属开发者体验、文档、注释），`TODO-06` 依赖外部决定。**同时补了"附：已认领的缺口"表** —— 审计中发现的 `V-24` / `V-27` / `V-28` / `V-29` 一度看起来无归属，实测分别由 `PR-4d`/`PR-6`、`PR-5`、`PR-5`、`PR-4b`+`PR-6` 认领，列此对照以免被当成遗漏。`V-19`（错误码漂移守卫）同日已闭环，不再列入。 |
