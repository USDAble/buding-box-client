# 阶段 3：PR-8 产品事件

## 前置

`G1` 已对事件载荷、作用域、顺序和断线重放语义完成产品拍板，并登记到 canonical `开发计划.md` §4.1。

## 目标

补齐 `credits_update`、`datastore:lost`、`datastore:restored` 三个事件的 Go 发射方；`input_sensitive` 只做回归，不重复实现。

## 任务

- 明确每个事件的唯一状态 owner 和发射时机。
- 状态未变化时不重复广播。
- 按 `G1` 实现窗口/会话作用域与断线重放。
- 验证窗口令牌和事件作用域不会串窗口。
- 前端继续读取既有 handler，不建立第二份权威状态。
- 为三事件分别写正向、重复抑制、错误作用域和重连测试。

## 退出条件

- 三个事件有真实生产发射点。
- 事件只在规定状态跃迁时出现。
- 重放与作用域完全符合 `G1`。
- `L-F1` 和 `L-E3` 的事件侧判据通过。
- `make gate`、`make test-production` 通过，owner 文档同步。

## 执行记录

- 待填写。

阶段完成后，将总结写入本目录的 `阶段总结.md`。
