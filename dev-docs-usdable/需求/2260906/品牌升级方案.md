# 品牌升级方案

| 项 | 内容 |
|---|---|
| 文档状态 | 已定值部分不再讨论；实现细节可在评审中调整 |
| 日期 | 2026-09-07 |
| 范围 | **只有品牌**。用户可见的名称、文案、Logo、图标、系统元数据、打包显示名。不含登录、敏感词、模式、便携数据根等其它需求项 |
| 关联 | 需求见同目录 `需求20260906.md` §5.2；本方案是它的实现方案，并吸收 `origin/dev-zxy` 分支的实测经验 |
| 覆盖 | 桌面端、内嵌 Web UI、移动端、落地页、文档站、博客、Relay 推送、Windows/macOS/Linux 打包 —— **涉及到的全部替换** |

---

## 0. 一句话方案

**一个来源（`branding/brand.json` + 一张 1024 源图），两条管线（文案键插值 / 资产派生），一条命令（`make brand`），CI 全程守卫漂移。**

三条不可动摇的原则：

1. **唯一来源。** 换名字只改 `brand.json`，换 Logo 只换那张 1024 源图，两件事都不需要动代码（§3.4 有量化演练）。
2. **路径与标识符一律英文 ASCII，永不多语言。** 只有「给人读的句子」才有中英两套值；路径、文件名、命令、协议名、端口、bundle id 全是单一 ASCII 字符串，界面上原样展示、不翻译（§2.5，CI 强制）。
3. **文案用占位符插值，不做运行时全局替换。** 品牌名在词条里是 `{brand}`，不是被擦掉的 `Octo`（§4）。

界面上不出现 Octo 与章鱼标；`octo` 命令、`~/.octo` 配置目录、Go 模块路径等兼容标识本期一律不动。

---

## 1. 范围

### 1.1 在本方案内

1. 产品名称、简称、口号、版权方、协议标题的多语言取值与分发。
2. 全部用户可见文案里的品牌位置改造（Web UI 词典 + 桌面原生字符串）。
3. Logo 与图标资产管线：从一张 1024×1024 源图**一次生成**全平台产物并同步到所有消费点。
4. 系统元数据：exe 文件名、便携目录名、单实例 ID、Windows VERSIONINFO、macOS Info.plist、Linux desktop entry、Capacitor appName、Inno Setup 显示名。
5. 网页标题、favicon、og-image、Relay 推送通知标题。
6. CI 守卫：品牌漂移检测、资产漂移检测、翻译表禁止字面量品牌名。

### 1.2 不在本方案内

| 项 | 归属 |
|---|---|
| 登录 / 激活 / 拦截页**功能** | 需求 §5.3。本方案只提供拦截页要用的 Logo、产品名、协议页文案取值 |
| 敏感词、模式与模型、积分、个人中心 | 需求 §5.5 / §5.6 / §5.4 |
| 便携数据根 `data/`、cwd、PID 落位**行为** | 需求 §5.7。本方案只改「默认工作区」那条**文案** |
| IM / MCP / Light Apps 入口隐藏 | 需求 §5.4.3 |
| Go 模块路径、CLI 命令名、`~/.octo` 目录名 | 需求 §5.2.5，本期不改 |
| 代码签名 | 需求 §7 |

---

## 2. 已定值（不再讨论）

### 2.1 名称与文案

| 用途 | 中文（zh-CN） | 英文（en-US） | 用在 |
|---|---|---|---|
| 全称 | 布丁盒子 | Pudding Box | 窗口标题、关于页、拦截页主标题、欢迎页、版权行、导出标题 |
| 简称 | 布丁盒子 | **Pudding** | 任务栏过长、托盘提示与菜单动词、通知标题 |
| 口号 | 让本地工具、任务与智能代理，在一个工作台里协作。 | One workspace for local tools, tasks, and agents. | 拦截页副文案、关于页、落地页 |
| 版权方 | 布丁盒子工作室 | Pudding Box Studio | exe 文件属性 CompanyName、关于页、安装器 Publisher |
| 版权行 | © {年} 布丁盒子工作室 | © {year} Pudding Box Studio | 关于页页脚、VERSIONINFO LegalCopyright |
| 用户协议标题 | 布丁盒子用户协议 | Pudding Box Terms of Use | 拦截页页脚、关于页 |
| 隐私政策标题 | 布丁盒子隐私政策 | Pudding Box Privacy Policy | 同上 |

中文简称与全称同值（四个字不会被裁切）；英文托盘/通知一律用 `Pudding` 以免任务栏与通知栏截断。

**语言覆盖：** 本期只保证 `zh-CN` 与 `en-US` 两套值齐全。`zh-TW` **不填中文值**，按需求 T10 落到英文分支；其它语言键本期不预填（填了就是承诺，而没人校对）。

### 2.2 系统标识符

| 项 | 值 | 说明 |
|---|---|---|
| exe 文件名 | `PuddingBox.exe` | 便携交付与安装器内均用此名 |
| CLI 可执行文件名 | `octo.exe` | **不改**，需求 §5.2.5 |
| 便携目录文件夹名 | `PuddingBox` | 纯 ASCII 无空格（C 类，§2.5）。用户看见的中文名靠窗口标题，不靠目录名 |
| 单实例 ID | `app.puddingbox.desktop` | **必改**，替换 `dev.octo-agent.desktop`（`cmd/octo-desktop/main.go`）。这是运行期锁名，改了正好达到「不与本机已装 Octo 抢窗口」的目的 |
| Windows VERSIONINFO InternalName | `pudding-box-desktop` | |
| Windows VERSIONINFO OriginalFilename | `PuddingBox.exe` | 必须与实际交付文件名一致，否则 Windows 属性页自相矛盾 |
| Windows 文件属性 产品名称 | `Pudding Box` | **固定单值，不随 UI 语言**，见 §2.2.1 |
| Windows 文件属性 文件说明 | `布丁盒子` | **固定单值，不随 UI 语言** |

#### 2.2.1 为什么文件属性只写一套

需求 §5.2「例外」条明确把 exe 文件名、文件夹名、文件属性列为不可随 UI 热切的系统字段，按固定 mock 处理。因此 VERSIONINFO **只写一个 translation block**（`LangDefault`），值为上表的固定值。

不要像 dev-zxy 那样写 en-US + zh-CN 两个 block —— 那会让中文 Windows 的属性页显示中文产品名、英文 Windows 显示英文产品名，恰好与「固定」相反，而且验收表逐字对不上。

### 2.3 视觉

| 项 | 值 |
|---|---|
| 标志图形 | 由设计提供的 **1024×1024 源图**，见 §5.1。研发不再自拟图形 |
| 主色 | 从源图取，写入 `brand.visual.colors.primary`，供界面强调色与占位背景复用 |
| 图形风格约束 | 不含章鱼元素；16px 下可辨；不依赖 logo 内嵌文字传达产品名 |

### 2.4 保持不变的兼容标识（动了会破坏已装版本）

| 标识 | 当前值 | 为什么不改 |
|---|---|---|
| Go 模块路径 | `github.com/open-octo/octo-agent` | 内部路径，用户不可见 |
| CLI 命令 | `octo` / `octo serve` | 需求 §5.2.5 |
| 配置目录 | `~/.octo` | 本期兼容标识；便携数据根是需求 §5.7 的独立议题，不在本方案内 |
| 环境变量前缀 | `OCTO_` | 同上 |
| protocol | `octo-ui` / `octo-pair` | 已发出的配对链接要继续可用 |
| Inno Setup `AppId` | `{{8F2A6B1C-…}}` | 改了会让新安装器不认旧安装，变成并存两份 |
| macOS `CFBundleIdentifier` | `dev.octo-agent.desktop` | 改了会丢失通知授权、登录项、钥匙串条目 |
| Capacitor `appId` | `dev.octo.mobile` | 改了等于换 App，商店链路与已安装用户全断 |
| 安装目录 | `%LOCALAPPDATA%\Programs\octo` | 与 AppId 同理 |

这些写入 `brand.json` 的 `identifiers.current`（当前值）与 `identifiers.future`（正式发布时的目标值），把「现在不动」和「以后要动」显式记账，避免下一个人以为是漏改。

### 2.5 值的三类：什么能有多语言，什么绝对不能

这是本方案的一条结构性约束。一个值属于哪一类，决定它在 `brand.json` 里长什么样、能不能进 `copy` 段、CI 用哪条规则校验它。**分类先定，再谈实现。**

| 类 | 定义 | 在 `brand.json` 里的形态 | 举例 |
|---|---|---|---|
| **A 本地化文案** | 用户读的句子与名称，随界面语言实时切换 | 多语言 map `{"zh-CN": …, "en-US": …}` | 全称、简称、口号、版权方、版权行、协议标题、全部界面文案 |
| **B 固定显示值** | 用户能看见，但由操作系统渲染、不随界面语言热切 | **单个字符串** | Windows 文件属性的产品名称与文件说明 |
| **C 标识符与路径** | 机器读的，不是给人看的 | **单个字符串，且必须是无空格的 ASCII** | 路径、文件名、目录名、单实例 ID、bundle id、协议名、环境变量前缀、端口、配置键名、模型 id、资产文件名、URL |

#### 2.5.1 C 类硬规则（不接受例外）

1. **一律英文 ASCII，永不本地化。** 不做多语言 map，不随界面语言变，不存在中文版本。
2. 不含中文字符、不含空格、不含全角标点。
3. 大小写只用两种约定：**用户可见的可执行文件与交付目录用 PascalCase**（`PuddingBox.exe`、`PuddingBox`，随 Windows 习惯）；**仓库内资产与配置文件用小写 kebab-case**（`logo-1024.png`、`tray-16-light.png`、`brand.json`）。同一目录内不得只靠大小写区分两个文件 —— Windows 不区分大小写、Linux 区分，那是跨平台事故。
4. 它出现在界面上时，是**被展示的字面量**，不是可翻译文案。设置里的默认工作区中英文界面都显示 `data/workspace/` 这五个字符，不翻译成「数据/工作区」，也不插值。

#### 2.5.2 为什么要单独立这条

dev-zxy 用运行时 `replaceAll('Octo', 品牌名)` 处理文案，把 `settings.workspace_dir_desc` 里的 `~/Octo` 一起换了：英文界面变成 `~/Pudding Box`，中文界面变成 `~/布丁盒子`。一个带空格的假路径，一个带中文的假路径，两个都不存在。

根因不是替换逻辑写错，而是**没有把「路径」和「文案」当成两类不同的值来管**。只要这两类值混在一个池子里，任何全局处理手段都会误伤，换成插值也只是把事故概率降低而不是消除。所以要在 schema 层面把它们分开，并让 CI 结构性地拦住。

#### 2.5.3 C 类清单（本期）

| 值 | 说明 |
|---|---|
| `data/`、`data/workspace/`、`data/tmp` | 便携数据根与会话工作区。界面原样展示，不翻译 |
| `~/.octo`、`OCTO_`、`octo`、`octo serve`、`octo-ui`、`octo-pair` | 上游兼容标识，本期不改 |
| `PuddingBox.exe`、`PuddingBox`、`octo.exe` | 交付文件与目录名 |
| `app.puddingbox.desktop` | 单实例 ID |
| `dev.octo-agent.desktop`、`dev.octo.mobile`、Inno `AppId` | bundle id 类，本期不改 |
| `pudding-box-desktop` | VERSIONINFO InternalName |
| `%LOCALAPPDATA%\Programs\octo` | 安装目录 |
| `8088` | 固定端口 |
| `buding-local-general` 等模型 id | 需求 §5.6.3：**id 固定，展示名随语言**（展示名是 A 类） |
| `branding/` 下全部资产文件名 | kebab-case |
| i18n 键名、配置键名、JSON 字段名 | 永不本地化 |
| `links` 下全部 URL 与路由 | 含应用内路由 `/legal/terms` |

**一个容易误判的例子：** B 类的「Windows 文件说明 = `布丁盒子`」是中文，但它既不是路径也不是标识符，而是给人看的固定显示值，所以允许是中文 —— 不违反本节规则。判断标准是「谁来读」，不是「是不是中文」。

#### 2.5.4 CI 结构性强制

`brand-guard` 里加三条 schema 校验，让错误分类在提交阶段就过不去，而不是靠评审看出来：

| 规则 | 校验什么 |
|---|---|
| A 类必须是 locale map | `product.names` / `product.shortName` / `product.tagline` / `about.teamName` / `about.copyright` / `copy.*` 的每个叶子必须是 object，且**同时含 `zh-CN` 与 `en-US` 且两者非空** |
| B 类必须是单字符串 | `display.*` 下的叶子必须是 string，不能是 object（防止有人给文件属性加多语言） |
| C 类必须是无空格 ASCII | `identifiers.*` 与 `links.*` 下的叶子必须是 string 且匹配 `^[\x21-\x7E]+$`（可打印 ASCII、无空格）。这一条同时挡住「给路径加中文」和「给路径加多语言 map」 |

---

## 3. 架构：一个来源，两类消费

### 3.1 文件职责

```
branding/
  brand.json              ← 唯一 source of truth（文本值）。人工编辑只改这里
  source/
    logo-1024.png         ← 唯一 source of truth（图形）。设计交付物
    logo-mark.svg         ← 可选。有则 favicon 与界面组件走真矢量
    tray-light.png        ← 可选。深色任务栏用的单色图形
    tray-dark.png         ← 可选。浅色任务栏用的单色图形
  assets/                 ← 全部由工具生成，禁止手改
    logo-{16..1024}.png
    favicon.ico  favicon-32.png  apple-touch-icon-180.png
    icon-192.png  icon-512.png
    tray-{16,20,24}-{light,dark}.png
    tray-icon-Template.png  tray-icon-Template@2x.png
    og-image.png

cmd/generate-brand-assets/   ← 工具一：源图 → assets/ 全套派生
scripts/sync-branding.mjs    ← 工具二：brand.json + assets/ → 各消费端
```

### 3.2 数据流

```
                     ┌─ internal/brand/brand.json        (go:embed，桌面端 + 服务端)
                     ├─ web/src/lib/brand.config.json    (Vite import)
brand.json ──sync──> ├─ mobile/src/brand.config.json
                     ├─ landing/brand.config.json
                     ├─ cmd/octo-relay/internal/push/brand.json  (嵌套模块，独立 embed)
                     └─ packaging/windows/brand.iss      (Inno #define)

logo-1024.png ─generate─> branding/assets/* ─sync─> ┌─ cmd/octo-desktop/build/windows/icon.ico
                                                    ├─ cmd/octo-desktop/build/darwin/icon.icns
                                                    ├─ cmd/octo-desktop/build/darwin/tray-icon.png
                                                    ├─ cmd/octo-desktop/build/linux/icon.png
                                                    ├─ web/public/favicon.*
                                                    ├─ blog/public/favicon.*
                                                    ├─ docs/public/favicon.*
                                                    └─ landing/{favicon.*,og-image.png}
```

生成目标全部进版本库（CI 与打包不重跑图形处理，构建可复现），但用 `.gitattributes` 标 `linguist-generated=true`，让 PR 里默认折叠，评审只看 `branding/` 下的两个源。

### 3.3 brand.json schema（`schemaVersion: 2`）

dev-zxy 的 v1 按「这个值以后会不会变」分组（`compatibility` / `futureMigration` / `platform`）。本方案改成**按值的类别分组**（§2.5 的 A / B / C），这样每一段只有一条校验规则，CI 能机械执行，人也不会往错的段里填值。

```jsonc
{
  "schemaVersion": 2,
  "brandId": "puddingbox",

  // ── A 类：本地化文案。每个叶子必须是 locale map，且 zh-CN / en-US 齐全 ──
  "product": {
    "names":     { "zh-CN": "布丁盒子", "en-US": "Pudding Box" },
    "shortName": { "zh-CN": "布丁盒子", "en-US": "Pudding" },
    "tagline":   { "zh-CN": "让本地工具、任务与智能代理，在一个工作台里协作。",
                   "en-US": "One workspace for local tools, tasks, and agents." },
    "description": { "zh-CN": "…", "en-US": "…" }
  },
  "about": {
    "teamName":  { "zh-CN": "布丁盒子工作室", "en-US": "Pudding Box Studio" },
    "copyright": { "zh-CN": "© {year} 布丁盒子工作室", "en-US": "© {year} Pudding Box Studio" },
    "termsTitle":   { "zh-CN": "布丁盒子用户协议", "en-US": "Pudding Box Terms of Use" },
    "privacyTitle": { "zh-CN": "布丁盒子隐私政策", "en-US": "Pudding Box Privacy Policy" }
  },
  "copy": { /* 其余需要跨端复用的品牌文案，同样是 locale map */ },

  // ── B 类：固定显示值。叶子必须是单字符串，不随界面语言 ──
  "display": {
    "windows": {
      "productName":     "Pudding Box",
      "fileDescription": "布丁盒子"
    }
  },

  // ── C 类：标识符与路径。叶子必须是无空格 ASCII ──
  "identifiers": {
    "current": {
      "exeName":             "PuddingBox.exe",
      "cliExeName":          "octo.exe",
      "portableDirName":     "PuddingBox",
      "singleInstanceId":    "app.puddingbox.desktop",
      "windowsInternalName": "pudding-box-desktop",
      "dataRoot":            "data/",
      "workspaceDir":        "data/workspace/",
      "port":                "8088",
      "cliCommand":          "octo",
      "configDir":           "~/.octo",
      "envPrefix":           "OCTO_",
      "uiProtocol":          "octo-ui",
      "pairingProtocol":     "octo-pair",
      "goModule":            "github.com/open-octo/octo-agent",
      "macBundleId":         "dev.octo-agent.desktop",
      "innoAppId":           "{{8F2A6B1C-3D4E-4F50-9A6B-7C8D9E0F1A2B}",
      "mobileAppId":         "dev.octo.mobile",
      "installDir":          "%LOCALAPPDATA%\\Programs\\octo"
    },
    "future": {
      // 正式发布才动的目标值，本期只记账不生效（见 §2.4）
      "cliCommand": "puddingbox", "configDir": "~/.puddingbox", "envPrefix": "PUDDINGBOX_",
      "uiProtocol": "puddingbox-ui", "pairingProtocol": "puddingbox-pair",
      "macBundleId": "com.puddingbox.desktop", "mobileAppId": "com.puddingbox.mobile"
    }
  },

  // ── C 类：链接。全部 ASCII，inApp 为应用内路由，external 必须真实可打开 ──
  "links": {
    "inApp":    { "terms": "/legal/terms", "privacy": "/legal/privacy" },
    "external": { "license": "https://github.com/open-octo/octo-agent/blob/main/LICENSE.txt" }
  },

  // ── C 类：资产文件名与色值 ──
  "visual": {
    "logo": { "mark": "assets/logo-mark.svg", "favicon": "assets/favicon.svg" },
    "colors": { "primary": "#…" }
  }
}
```

相对 dev-zxy v1 的实质修正：

1. `product.shortName.en-US` = `Pudding`，不是全称 —— 托盘与通知需要短名。
2. `product.names` 只留 `zh-CN` / `en-US`。删掉未校对的 `zh-TW` / `ja-JP` / `ko-KR` / `es-ES` / `fr-FR`：填了就是承诺，而没人校对，`zh-TW` 尤其要删（需求 T10 规定本期繁体走英文）。
3. `platform.windows.fileDescription` 从多语言 map 改成 `display.windows.fileDescription` 单字符串，配合 §2.2.1 的「文件属性固定单套」。
4. `compatibility` / `futureMigration` 合并成 `identifiers.current` / `identifiers.future`，统一受 C 类 ASCII 规则约束。**路径与标识符从此在 schema 里就没有多语言的位置可填。**
5. `links` 拆 `inApp` / `external`。`external.license` 必须是真实可打开的 URL —— dev-zxy 填的 `https://github.com/<your-org>/pudding-box-client/...` 带字面占位符 `<your-org>`，让关于页「查看许可证」打开一个坏链接。正式外链未定前指向上游真实 LICENSE 或应用内本地页，**禁止把占位符当 URL 发出去**。
6. 协议标题从 `copy` 提到 `about`，与版权方放在一起 —— 它们是同一类「法律与归属」文案。

**schemaVersion 约束：** 加字段不用升版本；改字段的类别（A↔B↔C）、改段名、删字段必须升 `schemaVersion`，并同步更新 Go 与 TS 两侧的类型定义。消费端读到不认识的 `schemaVersion` 应当构建期报错，而不是运行期静默取空值。

Go 侧 `internal/brand/brand.go` 的 `Load()` / `localized()` 结构可直接沿用（`go:embed` + 三级回退：精确 locale → 语言前缀 → `en-US`），按新 schema 调整字段并追加 `ShortName(locale)`、`Identifier(key)`、`Display(platform, key)`。

### 3.4 替换演练：这套结构到底省了什么

「以后好替换」不是愿望，是可以量化验收的。三个典型场景：

**场景 A：只改名字**（例如英文名从 `Pudding Box` 换成别的拼写，或整个换中文名）

```bash
# 改 branding/brand.json 里 product.names / shortName（+ about 里的版权方与协议标题）
node scripts/sync-branding.mjs
```

改动量：**1 个文件的几行 JSON，0 行代码。** 窗口标题、托盘菜单、通知、关于页、输入框占位、导出标题、落地页、安装器显示名、Windows VERSIONINFO 全部跟着变，因为它们没有一处写死品牌名 —— 这就是 §4 那套占位符插值的回报。

**场景 B：只换 Logo**

```bash
# 用新图覆盖 branding/source/logo-1024.png
make brand
```

改动量：**1 张源图，0 行代码。** Windows ico、macOS icns 与托盘 template、Linux png、四个站点的 favicon 系列、落地页 og-image、界面内 Logo 组件全部更新，因为它们同源（§5.7）。

**场景 C：整体白标**（把产品换给另一个客户/另一条产品线）

```bash
# 1. 替换 branding/brand.json（名称、口号、版权方、色值、链接）
# 2. 替换 branding/source/logo-1024.png（+ 可选的矢量与托盘图）
make brand
make brand-check   # 与 CI 同一套判断
```

改动量：**2 个源 + 1 条命令。**

**仍然需要改代码的边界**（写清楚，免得以后误以为"配置万能"）：

| 情况 | 为什么必须改代码 |
|---|---|
| 新增一种界面语言 | 要在 `i18n.ts` 增加词典、在 `dictFor()` 与 `resolveLang()` 增加分支 |
| 新增一个消费端（例如新开一个站点） | 要在 `sync-branding.mjs` 的 targets 表加一行 |
| 改 C 类标识符（bundle id、AppId、CLI 名） | 配置能改，但要配套迁移方案（§2.4），不是改个值就完事 |
| 新增一个需要品牌名的界面位置 | 要写词条，但**用 `{brand}` 占位符**，不写死 |

**验收这一节：** §9「流程」部分的三条演练检查项就是对着这里写的 —— 真的换一张图、真的改一次名，看是不是只动了预期的文件。

---

## 4. 文案层：占位符插值，禁止运行时替换

### 4.1 为什么不用 replaceAll（dev-zxy 的实测教训）

dev-zxy 在 `web/src/lib/i18n.ts` 和 `cmd/octo-desktop/lang.go` 里都用了「取出文案后把 `Octo` 全局替换成品牌名」。实测四类问题，全部是这个做法的必然结果，不是实现瑕疵：

| 问题 | 具体表现 |
|---|---|
| 误伤非品牌文本 | `settings.workspace_dir_desc` 里的 `~/Octo` 是**路径**，被改成 `~/Pudding Box`，给用户一个不存在的路径 |
| 中文多空格 | `"Octo 正在等待你的输入"` → `"布丁盒子 正在等待你的输入"`；`"关于 Octo"` → `"关于 布丁盒子"`；`"退出 Octo"` → `"退出 布丁盒子"`。中文排版硬伤，且验收表是逐字对的 |
| 短名无处接入 | 没有替换目标可以区分「这里该用全称」和「这里该用简称」 |
| 评审无抓手 | 翻译文件里看不出哪条含品牌词，回归靠肉眼；每次 `t()` 都全串扫描一遍，渲染热路径上纯开销 |

而需求 §5.2.1 已经写明「名称是文案键，不是写死的字符串」。运行时替换恰好是把名称写死在英文串里再擦掉。

### 4.2 Web 侧做法

词典里用占位符，`t()` 只做插值，并对不含占位符的词条走零分配快路径：

```ts
// web/src/lib/i18n.ts
export const t = derived(locale, ($l) => {
  const dict = dictFor($l)
  const name = brandName($l)
  const short = brandShortName($l)
  return (key: string): string => {
    const value = dict[key] ?? en[key] ?? key
    // 绝大多数词条不含占位符，直接返回，避免每次渲染都分配新串。
    if (!value.includes('{brand')) return value
    return value.replaceAll('{brand}', name).replaceAll('{brandShort}', short)
  }
})
```

> 两个占位符互不干扰（`{brandShort}` 里 `brand` 后面跟的是 `S` 不是 `}`，不会被 `{brand}` 匹配到），替换顺序无所谓。但如果以后新增 `{brandX}` 形式的占位符，要检查是否存在前缀包含关系。

词条改写示例：

| 键 | 英文 | 中文 |
|---|---|---|
| `chat.placeholder` | `Message {brand}… (…)` | `发给{brand}…（…）` |
| `header.notif_question_body` | `{brand} is waiting for your input` | `{brand}正在等待你的输入` |
| `onboard.title` | `Welcome to {brand}` | `欢迎使用{brand}` |
| `m.about_octo` | `About {brand}` | `关于{brand}` |
| `settings.about.footer` | `{brand} · {tagline} · © {year}` | 同结构 |
| `settings.about.version_desc` | `Currently installed {brand} version` | `当前安装的{brand}版本` |
| `settings.update_desc` | `Check whether a newer version of {brand} is available.` | `检查是否有新版本的{brand}。` |
| `settings.autostart_desc` | `Start {brand} automatically when you log in to this computer` | `登录这台电脑时自动启动{brand}` |

**例外，不要插值的（C 类，见 §2.5）：** `settings.workspace_dir_desc` 里的 `~/Octo` 是**路径**不是品牌名，按需求 §5.2.2 直接改成 `data/workspace/`，中英文界面都展示这五个 ASCII 字符，不翻译、不插值。

凡是「看起来像 Octo 但其实是 C 类」的位置一律同样处理，逐一列明以便实现时核对：

| 词条里的字面量 | 它是什么 | 怎么处理 |
|---|---|---|
| `~/Octo` | 会话工作区路径 | 改成 `data/workspace/`，原样展示 |
| `~/.octo` | 配置目录 | 保持不变（本期兼容标识） |
| `octo serve` / `octo` | CLI 命令 | 保持不变 |
| `octo-ui://` / `octo-pair://` | protocol scheme | 保持不变 |
| `OCTO_*` | 环境变量名 | 保持不变 |
| `8088` | 端口 | 保持不变 |
| `octo-desktop.exe` | 上游 exe 名 | 改成 `PuddingBox.exe`（C 类换值，不是插值） |

判断口诀：**这个字符串用户会不会拿去敲进终端、粘进地址栏、或在文件管理器里找？** 会，就是 C 类，不进插值管线。

### 4.3 桌面原生侧做法（`cmd/octo-desktop/lang.go`）

去掉 dev-zxy 的反射遍历。把两个 `uiStrings` 变量改成构造函数，品牌位置在源码里可见：

```go
func enStrings(name, short string) uiStrings {
	return uiStrings{
		trayShow: "Show " + short,
		trayQuit: "Quit " + short,
		quitTitle: "Quit " + name,
		takeoverTitle: name,
		updAvailableFmt: name + " %s is available.",
		// …不含品牌的条目原样保留
	}
}

func zhStrings(name, short string) uiStrings {
	return uiStrings{
		trayShow: "显示" + name,   // 中文无空格
		trayQuit: "退出" + name,
		quitTitle: "退出" + name,
		takeoverTitle: name,
		updAvailableFmt: name + " %s 已发布。",
	}
}

func applyLang() {
	cfg := brand.Load()
	if resolveLang() == "zh" {
		s := zhStrings(cfg.Name("zh-CN"), cfg.ShortName("zh-CN"))
		active.Store(&s)
	} else {
		s := enStrings(cfg.Name("en-US"), cfg.ShortName("en-US"))
		active.Store(&s)
	}
}
```

好处：编译期就能看出哪些串带品牌；中文无空格；短名有明确位置；不需要 `reflect`。

窗口标题（`bridge.go`）、`application.Options{Name, Description}`（`main.go`）、托盘 tooltip 走同一份取值，语言切换时一起变。原生对话框跟随当前语言 —— 中文界面不能弹英文 `Quit Pudding Box`。

### 4.4 中文排版规则（写进 review checklist）

1. 中文文案里产品名与中文字符**之间不加空格**：`显示布丁盒子`、`关于布丁盒子`、`布丁盒子正在等待你的输入`。
2. 中文文案里产品名与**英文/数字**相邻时保留一个空格：`布丁盒子 v1.2.0 已发布。`
3. 英文文案照英文习惯：`Show Pudding`、`About Pudding Box`。

### 4.5 CI 守卫

新增 `brand-guard` job，七条检查全部为硬失败：

| # | 检查 | 命令 / 判据 | 拦什么 |
|---|---|---|---|
| 1 | 文案表无字面品牌名 | `rg -n '\bOcto\b' web/src/lib/i18n.ts cmd/octo-desktop/lang.go` 必须无输出 | 有人新增词条时又把品牌名写死 |
| 2 | A 类必须是 locale map | `product.*` / `about.*` / `copy.*` 叶子为 object 且 `zh-CN`、`en-US` 均非空 | 少一种语言就发版；或把文案写成单字符串 |
| 3 | B 类必须是单字符串 | `display.*` 叶子为 string，不是 object | 给 Windows 文件属性加多语言（dev-zxy 的错） |
| 4 | **C 类必须是无空格 ASCII** | `identifiers.*` / `links.*` / `visual.logo.*` 叶子为 string 且匹配 `^[\x21-\x7E]+$` | **给路径或标识符加中文、加空格、加多语言 map** |
| 5 | 配置分发未漂移 | `node scripts/sync-branding.mjs --check` | 改了 `brand.json` 忘了同步 |
| 6 | 资产未漂移 | `go run ./cmd/generate-brand-assets --check` | 换了源图忘了重新生成（dev-zxy 就出现过换资产没同步组件） |
| 7 | 格式 | `gofmt -l .` 必须无输出 | dev-zxy 因 `//go:embed` 前缺空行挂在这一项上 |

第 1 条只匹配大写词边界 `\bOcto\b`，所以小写 `octo`、`octo-ui`、`~/.octo`、`octo-desktop` 等 C 类兼容标识天然放过。

第 4 条是 §2.5 那条「路径一律英文」的落地方式。**规则写在文档里只是约定，写进 CI 才是约束** —— 有了它，「把 `data/workspace/` 翻译成中文」或「给 `exeName` 加个 `zh-CN` 键」这类改动在提交阶段就过不去，不依赖评审逐行看。

---

## 5. Logo 资产管线（一次生成，全仓同步）

这一节是本方案的重点：**设计只交一张图，研发跑一条命令，全平台产物与所有消费点一次到位。**

### 5.1 输入契约（设计交付给研发的东西）

| 文件 | 规格 | 必需 |
|---|---|---|
| `branding/source/logo-1024.png` | 1024×1024，PNG-32 带 alpha，**透明底**。图形的 alpha 包围盒须占画布 ≥ 88%，居中偏差 ≤ 1% | **必需** |
| `branding/source/logo-mark.svg` | 同一图形的矢量版，`viewBox` 正方形 | 可选，强烈建议 |
| `branding/source/tray-light.png` | 512×512 单色浅色图形（用于深色任务栏） | 可选 |
| `branding/source/tray-dark.png` | 512×512 单色深色图形（用于浅色任务栏） | 可选 |
| `branding/source/wordmark-zh.svg` / `wordmark-en.svg` | 横版 Logo（标志 + 右侧文字），随语言切换 | 可选 |

**为什么卡「包围盒 ≥ 88%」这条：** dev-zxy 那张 1024 源图底部约 25% 是空白（那里有一行白色文字，白底上完全看不见）。缩到托盘的 16/20/24 尺寸后，图形只占上四分之三，视觉偏上且比同尺寸系统图标小一圈。这个坑必须由工具在生成阶段拦住，不能靠人眼复查。

**为什么 logo 里不要放文字：** 16px 下文字必然糊成一团；产品名靠窗口标题和侧栏文字传达，不靠图标。源图若自带文字，工具的 16px 可读性检查会告警。

### 5.2 工具一：`cmd/generate-brand-assets`（源图 → 全套派生）

dev-zxy 已有一个雏形，架构可取（纯 Go、无外部依赖、ICO 存 PNG 帧、ICNS 存标准 PNG chunk，同一条命令在三个 OS 上都能跑）。本方案在它基础上补齐六件事：

| # | 能力 | 说明 |
|---|---|---|
| 1 | **输入校验** | 必须 1024×1024 PNG；alpha 包围盒 fill ratio < 0.88 时**报错退出**，打印实际值并提示 `--trim` |
| 2 | **`--trim` 规范化** | 自动裁掉透明边距，按 4% 安全边重新居中补白，产出规范化的 `branding/assets/logo-1024.png`，后续所有尺寸从它派生 |
| 3 | **16px 可读性检查** | 缩到 16×16 后非透明像素占比 < 12% 时告警（图形过细，托盘会看不清） |
| 4 | **真 favicon** | 见 §5.5。**禁止**再产出 base64 内联 PNG 的假 SVG |
| 5 | **托盘双套 + macOS template** | 见 §5.6 |
| 6 | **`--check` 模式** | 只计算不落盘，与仓库现有产物逐字节比对，不一致则非零退出。给 CI 用 |

保留 dev-zxy 已实现且正确的部分：`draw.CatmullRom` 缩放、手写 ICO（多帧 PNG）、手写 ICNS（`icp4/icp5/icp6/ic07…ic14` chunk）。给 `makeICO` 的帧集合补上 **24px**（需求 §5.2.4 要求 16/24/32/48/256），并给 `makeICNS` 加缺失尺寸的显式报错，别静默写出空 chunk。

派生尺寸：`1024, 512, 256, 192, 180, 152, 144, 128, 120, 96, 72, 64, 48, 32, 24, 16`。

### 5.3 工具二：`scripts/sync-branding.mjs`（分发）

沿用 dev-zxy 的实现思路（一张 targets 表 + `--check` 漂移检测），扩展为同时分发**文本配置**和**图形产物**，目标见 §3.2 与 §5.4。

两点改进：

1. **生成文件带来源标记。** JSON 加顶层 `"_generated": "branding/brand.json via scripts/sync-branding.mjs"` 字段，`.iss` 已有注释头。任何人打开生成文件都知道不该手改。
2. **降低评审噪音。** `.gitattributes` 里把所有生成目标标 `linguist-generated=true`，GitHub PR 默认折叠。dev-zxy 一次 PR 里 170 行 JSON × 7 份副本的 diff 让评审几乎无法进行。

### 5.4 产出清单与消费点

| 产物 | 分发到 | 消费方 |
|---|---|---|
| `favicon.ico` `favicon-32.png` `apple-touch-icon-180.png` `icon-192.png` `icon-512.png` | `web/public/` `blog/public/` `docs/public/` `landing/` | 浏览器标签页、iOS 添加到主屏、PWA |
| `logo-mark.svg`（若设计提供矢量） | `web/public/favicon.svg` 等同上 | 优先于位图 favicon |
| `icon.ico`（16/24/32/48/256） | `cmd/octo-desktop/build/windows/icon.ico` | 嵌入 exe，任务栏 / Alt+Tab / 文件图标 |
| `icon.icns` | `cmd/octo-desktop/build/darwin/icon.icns` | macOS Dock、Finder |
| `icon.png`（256） | `cmd/octo-desktop/build/linux/icon.png` | AppImage / desktop entry |
| `tray-icon-Template.png` + `@2x` | `cmd/octo-desktop/build/darwin/` | macOS 菜单栏（见 §5.6） |
| `tray-{16,20,24}-{light,dark}.png` | `cmd/octo-desktop/build/` | Windows / Linux 托盘 |
| `og-image.png`（1200×630） | `landing/` | 社交分享卡片 |
| `logo-1024.png` | `branding/assets/` | 规范化后的母版，其它尺寸的唯一来源 |

桌面端图标当前通过 `//go:embed build/darwin/tray-icon.png` 与 `//go:embed build/linux/icon.png`（`cmd/octo-desktop/main.go`）进二进制，Windows 图标通过 `generate-syso` 进 `.syso`。同步脚本只需把文件放到这些路径，嵌入机制不用改。

### 5.5 favicon：不做 base64 假矢量

dev-zxy 的 `pudding-box-mark.svg` 是把 1024×1024 PNG base64 塞进 `<image>` 标签的壳，**218,652 字节**，还被复制成 3 份进仓（约 650 KB）。原 favicon 是几百字节真矢量。这是净退步。

本方案的规则：

- 设计提供了 `logo-mark.svg` → 直接分发为 `favicon.svg`，`<link rel="icon" type="image/svg+xml">`。
- 没有矢量 → 产出**位图 favicon**：`favicon.ico`（16/24/32/48）+ `favicon-32.png` + `apple-touch-icon-180.png` + `icon-192.png` + `icon-512.png`，HTML 用带 `sizes` 的多个 `<link rel="icon" type="image/png">`。
- 单个 favicon 文件 **≤ 25 KB**，由 `--check` 顺带断言。
- **明确禁止**把位图 base64 内联进 `.svg` 冒充矢量。

### 5.6 托盘图标

| 平台 | 做法 |
|---|---|
| macOS | 菜单栏必须用 **template image**：`tray-icon-Template.png`（16×16）+ `tray-icon-Template@2x.png`（32×32），纯黑 + alpha，系统按浅色/深色菜单栏自动反色。文件名后缀 `Template` 是 AppKit 的约定，不能省 |
| Windows / Linux | 彩色图标，出 16/20/24 三个尺寸 × light/dark 两套；按系统任务栏色调选用 |
| 降级 | 设计没提供 `tray-light.png` / `tray-dark.png` 时，工具从主图自动派生：macOS template 用 alpha 转纯黑；Windows/Linux 先只出一套并打印告警，不阻塞 |

dev-zxy 直接把 64px 彩色主图当 macOS 托盘图标用，在深色菜单栏下会糊。

### 5.7 界面内 Logo 组件

`web/src/components/layout/OctoLogo.svelte` 更名为 `BrandMark.svelte`，三个使用点一起改：`Sidebar.svelte`、`FirstRunSetup.svelte`、`ChatView.svelte`。

组件从 `brand.visual.logo.mark` 取路径渲染 `<img>`，**与 favicon / exe 图标同源**。

dev-zxy 在这里踩了两个坑，都要避免：

1. **两套 Logo 并存。** 最后一个 commit 换了全部 PNG/ICO/ICNS 资产，但没同步 Svelte 组件，结果侧栏显示橙色布丁笑脸、任务栏显示蓝色立方体。根因是界面 Logo 与资产不同源 —— 本方案用同一份文件消除这个可能。§4.5 的资产漂移检查是第二道防线。
2. **SVG 渐变 id 冲突。** 组件内联 SVG 时 `<defs>` 用了硬编码 `id="pb-box"`。这个组件在同一页面渲染三次，产生重复 id（非法 HTML），且首个实例卸载后其余实例的 `url(#pb-box)` 引用会失效变透明。若确实需要内联 SVG，id 必须按实例唯一化；用 `<img>` 则天然没有这个问题。

横版 Logo（标志 + 文字）用于关于页与拦截页主标题，随语言在 `wordmark-zh.svg` / `wordmark-en.svg` 之间切换。设计未提供时，用「方形 Logo + `{brand}` 文字」的组合布局兜底。

### 5.8 工作流：设计换图后研发做什么

```bash
# 1. 把新的 1024 源图放到 branding/source/logo-1024.png
# 2. 一条命令
make brand

# make brand 展开为：
#   go run ./cmd/generate-brand-assets --input branding/source/logo-1024.png --trim
#   node scripts/sync-branding.mjs

# 3. 自检
make brand-check   # = 两个 --check，与 CI 同一套判断
git status         # 应只看到 branding/assets/ 与各生成目标变动
```

改文案（改名、改口号、改版权方）只需编辑 `branding/brand.json` 后跑 `node scripts/sync-branding.mjs`，不碰图形。

### 5.9 已有资产的处置

dev-zxy 分支里 caoyan 产出的蓝色立体 BOX 立方体图标**不直接沿用**（底部 25% 空白 + 白字不可见，见 §5.1）。设计按 §5.1 契约重新提供一张 1024 源图后，走 §5.8 的流程重新生成全套。若沿用同一图形，只需把画布重排（去空白、去文字、居中撑满）再交付即可。

---

## 6. 覆盖矩阵

「取值来源」列写明该位置的值从哪来，实现时逐行核对。

| # | 位置 | 上游现状 | 目标 | 取值来源 |
|---|---|---|---|---|
| 1 | 窗口标题 | `Octo` | 全称，随语言 | `brand.Name(locale)` |
| 2 | 应用名 / 描述（`application.Options`） | `Octo` / `Octo Agent` | 全称 / 口号 | 同上 |
| 3 | 网页 `<title>` | `Octo Agent` | 全称，随语言 | `App.svelte` 的 `<svelte:head>`；`web/index.html` 静态标题填**英文全称**单语言，避免首屏闪双语 |
| 4 | 任务栏 / Alt+Tab | `Octo` | 全称，过长用简称 | 随 exe 与窗口标题 |
| 5 | 托盘菜单「显示 / 退出」 | `显示 Octo` / `Quit Octo` | `显示布丁盒子` / `Quit Pudding` | `lang.go`（§4.3） |
| 6 | 托盘 tooltip | `Octo` | 简称 | 同上 |
| 7 | 原生对话框标题与正文 | 含 `Octo` | 全称，随当前 UI 语言 | 同上 |
| 8 | 侧栏品牌名文字 | `Octo` | 全称，随语言 | `brandName($locale)` |
| 9 | 侧栏 / 空会话 / 助手头像 Logo | 章鱼标 | `BrandMark.svelte` | §5.7 |
| 10 | favicon | 章鱼 SVG | 新 favicon | §5.5 |
| 11 | exe 图标 | Octo ico | 新 ico（16/24/32/48/256） | §5.4 |
| 12 | exe 文件名 | `octo-desktop.exe` | `PuddingBox.exe` | §2.2 |
| 13 | Windows 文件属性 | `AppName=Octo` | 产品名称 `Pudding Box` / 说明 `布丁盒子`，单套固定 | §2.2.1，`generate-syso` |
| 14 | 单实例 ID | `dev.octo-agent.desktop` | `app.puddingbox.desktop` | `main.go` |
| 15 | 输入框占位 | `Message Octo…` | `发给{brand}…` / `Message {brand}…` | i18n 键 |
| 16 | 系统通知 | `Octo 正在等待…` | `{brand}正在等待…`（英文用简称） | i18n 键 |
| 17 | 欢迎 / 首次配置 | `欢迎使用 Octo` | `欢迎使用{brand}` | i18n 键 |
| 18 | 关于页版本说明 / 更新说明 | 含 `Octo` | `{brand}` | i18n 键 |
| 19 | 关于页版权行 | `Octo · 年份` | `© {年} 布丁盒子工作室` | `brand.Copyright(locale)`，注意用 §4.2 的 locale 规范化，别直接 `map[$locale]` |
| 20 | 关于页许可证链接 | 真实 GitHub URL | 真实可打开的 URL | `brand.links.external.license`，禁止占位符 |
| 21 | 用户协议 / 隐私政策 | 无 | 标题见 §2.1，正文为应用内本地占位页 | `brand.links.inApp.*` |
| 22 | 导出 HTML 标题 | `Octo Export` | `{brand} 导出` / `{brand} Export` | i18n 键 |
| 23 | 默认工作区文案 | `~/Octo` | `data/workspace/` | **不插值**，直接改文案（§4.2 例外） |
| 24 | 自启动开关文案 | `Start Octo automatically…` | `{brand}` | i18n 键（便携包不渲染该开关，但键与翻译要齐） |
| 25 | 随包默认技能 / 默认 Agent 里的 `Octo` | 含 `Octo` | 产品名 | 逐个 `.md` / frontmatter 检查，需求 §5.2.3 |
| 26 | Linux desktop entry | `Name=Octo` | 全称（英文） | `octo-desktop.desktop` |
| 27 | Linux AppRun 缺依赖提示 | 含 `Octo` | 全称（英文） | `AppRun` |
| 28 | macOS `CFBundleName` / `CFBundleDisplayName` | `Octo` | `Pudding Box` | `Info.plist`（`CFBundleIdentifier` 不改） |
| 29 | Windows 安装器显示名 / Publisher / 开始菜单项 | `Octo` / `open-octo` | 全称 / `Pudding Box Studio` | `brand.iss` → `octo.iss`（`AppId`、安装目录不改） |
| 30 | Capacitor `appName` | `octo` | `Pudding Box` | `capacitor.config.ts`（`appId` 不改） |
| 31 | 移动端连接引导页文案 | `octo` / `连接到你的 octo serve` | 全称 + 中文描述 | `mobile/src/boot.ts` |
| 32 | Relay 推送通知标题 / 正文 | `Octo` / `Octo has new activity` | 英文全称 + 对应句 | `push.go`，嵌套模块独立 embed |
| 33 | 落地页 title / description / og / JSON-LD | Octo + 章鱼隐喻 | 全称 + 新口号 | `landing/index.html` + `brand.config.json` |
| 34 | 落地页 og-image | 章鱼图 | 新 og-image | §5.4 |
| 35 | 文档站 / 博客 favicon 与站点名 | Octo | 全称 + 新 favicon | `docs/` `blog/` |
| 36 | README / PRODUCT.md / SECURITY.md 等仓库文档 | Octo | 按各文档定位酌情替换 | 非界面，优先级最低，可最后一个 PR 处理 |

---

## 7. 打包与系统元数据

### 7.1 Windows

- `generate-syso` 继续负责 icon + manifest + VERSIONINFO。dev-zxy 把 VERSIONINFO 拆成可单测的 `buildVersionInfo(appVersion)` 是好做法，保留；但按 §2.2.1 改成**单 translation block**。
- `OriginalFilename` 必须等于 `PuddingBox.exe`，与实际交付文件名一致。
- 构建产物改名为 `PuddingBox.exe`：影响 `.github/workflows/desktop.yml`、`release.yml`、`windows-installer-check.yml` 的 `-o` 参数与 `octo.iss` 的 `[Files]` / `[Icons]` 条目。CLI 仍是 `octo.exe`。
- `octo.iss`：`AppName` / `AppPublisher` / `AppPublisherURL` / `UninstallDisplayName` / `[Icons]` 从 `brand.iss` 的 `#define` 取；`AppId`、`DefaultDirName` 保持不变。
- dev-zxy 把 `FileCopy` 改成 `CopyFile` 本身是对的（Inno Setup **6.4.0** 把 `FileCopy` 更名为 `CopyFile`，旧名仍可用但编译会告警），但这把最低 ISCC 版本抬到 **6.4.0+**。要在 `packaging/README.md` 与 CI 的安装步骤里写明这个下限，否则本机装 6.2 的人会突然编不过。

### 7.2 macOS

`Info.plist` 改 `CFBundleName` / `CFBundleDisplayName`，`CFBundleIdentifier` 与 `CFBundleExecutable` 不动（§2.4）。`icon.icns` 与 `tray-icon-Template.png` 由管线生成。`packaging/macos/distribution.xml` 的显示标题随品牌。

### 7.3 Linux

`octo-desktop.desktop` 的 `Name` / `Comment` 随品牌，`Exec` / `Icon` 保持 `octo-desktop`（文件名不改）。`AppRun` 里三处提示文案随品牌。

### 7.4 移动端

`capacitor.config.ts` 只改 `appName`。`mobile/src/boot.ts` 的引导层文案改成从 `brand.ts` 取值，**用 locale 规范化函数取**，不要直接 `brand.product.names["zh-CN"]` 硬取一种语言。`mobile/tsconfig.json` 需要 `resolveJsonModule` + `allowSyntheticDefaultImports` 才能 import JSON —— 这一项 dev-zxy 已经处理对了，直接沿用（但别顺手把整个文件重排格式，那是无谓的 diff）。

### 7.5 Web / 落地页 / 文档站 / 博客

四个站点各自有独立构建根，各分发一份 `brand.config.json` 与 favicon 系列。`web/index.html` 的静态 `<title>` 填**英文全称**（单语言），运行时由 `App.svelte` 的 `<svelte:head>` 覆盖成当前语言。dev-zxy 填的 `布丁盒子 · Pudding Box` 把两种语言写死在一个串里，正是需求 §5.2 要避免的形态。

### 7.6 Relay 推送

`cmd/octo-relay` 是嵌套 Go 模块，不能引用主模块的 `internal/brand`，所以它需要自己那份 `brand.json` + 独立 `go:embed`。dev-zxy 的做法正确，沿用。注意 `//go:embed` 指令前要有空行分隔文档注释，否则 `gofmt` 会挂（dev-zxy 正是栽在这里）。推送正文保持 content-free，只替换品牌词。

---

## 8. 实施顺序（拆 PR）

一个 PR 一件事。dev-zxy 把品牌改造、三个 Windows 兼容修复、打包脚本混在一起，93 个文件，无法有效评审。

| PR | 内容 | 产出可验证什么 |
|---|---|---|
| 1 | `branding/brand.json`（v2 schema，A/B/C 分段）+ `internal/brand` + `scripts/sync-branding.mjs` + `.gitattributes` + CI `brand-guard` 的第 2–5 条（三条 schema 结构校验 + 配置漂移） | 配置层可用；`--check` 能拦漂移；**给路径加中文或多语言在提交阶段就过不去** |
| 2 | 资产管线：`cmd/generate-brand-assets`（含校验、`--trim`、`--check`、真 favicon、托盘双套）+ `make brand` / `make brand-check` | 换图后一条命令全平台产出 |
| 3 | 文案层：i18n 词条占位符化 + `lang.go` 构造函数化 + CI「文案表无字面 `Octo`」检查 | 中英文界面文案全部到位，无多余空格 |
| 4 | 界面 Logo：`BrandMark.svelte` + 三个使用点 + favicon 落位 | 界面与任务栏 Logo 同源一致 |
| 5 | 系统元数据与打包：单实例 ID、`PuddingBox.exe`、VERSIONINFO 单 block、Info.plist、desktop entry、Inno Setup、Capacitor | 属性页、安装器、单实例行为可验收 |
| 6 | 外围：落地页、文档站、博客、Relay 推送、仓库文档 | 对外物料一致 |

PR 1、2 可以并行；3–5 依赖 1；6 依赖 1、2。

**dev-zxy 里那三个与品牌无关的修复**（`internal/tools/glob.go` 绝对路径规范化、`internal/browser/cdp.go` 32 位 atomic 对齐、`internal/agent/session.go` `ContentUpdatedAt` 单调递增）看着都是对的，单独开一个 PR 走，不要挂在品牌改造上。

---

## 9. 验收清单

**名称与文案**

- [ ] 中文界面：窗口标题、侧栏、拦截页、关于页、输入框占位、托盘菜单全部显示「布丁盒子」，看不到 Octo、看不到章鱼标。
- [ ] 切英文后上述位置变为 `Pudding Box`；托盘与通知用 `Pudding`；**无需重启**即可换文案（exe 图标不要求热换）。
- [ ] 中文文案里产品名前后**无多余空格**：`显示布丁盒子`、`关于布丁盒子`、`布丁盒子正在等待你的输入`。
- [ ] 关于页在**中文界面**显示中文版权方「布丁盒子工作室」（不是英文）。dev-zxy 因为直接用 `map[$locale]` 索引、而 locale store 的值是 `"zh"` / brand.json 的键是 `"zh-CN"`，中文界面落到了英文名 —— 这条专门验它。
- [ ] 关于页「查看许可证」能打开一个**真实存在**的页面（不是含 `<your-org>` 的坏链接）。
- [ ] 原生对话框语言跟随界面语言：中文界面不出现英文 `Quit Pudding Box`。
- [ ] 随包默认技能 / 默认 Agent 聊几句读不到「Octo」。

**路径与标识符（C 类，§2.5）**

- [ ] 设置里「默认工作区」中英文界面**都显示 `data/workspace/`** —— 不是 `~/Octo`，不是 `~/Pudding Box`（带空格的假路径），也不是 `~/布丁盒子`（带中文的假路径），更没有被翻译成「数据/工作区」。
- [ ] 界面上出现的每一个路径、命令、端口、协议名都是 ASCII，切换界面语言时**一个字符都不变**。
- [ ] 便携目录名与 exe 名均为 ASCII 无空格：`PuddingBox` / `PuddingBox.exe`。放到含中文的上级目录里仍能双击启动。
- [ ] `brand.json` 里 `identifiers` 与 `links` 两段下**没有任何多语言 map**，全是单字符串。
- [ ] 故意给 `identifiers.exeName` 加一个 `zh-CN` 键、或把 `workspaceDir` 改成中文 → CI 第 4 条检查失败。

**Logo 与图标**

- [ ] 侧栏 Logo、favicon、任务栏图标、托盘图标**是同一个图形**（dev-zxy 出现过侧栏橙色布丁 / 任务栏蓝色立方体两套并存）。
- [ ] 托盘图标在 16px 下图形居中撑满，不偏上、不比相邻系统图标明显小。
- [ ] macOS 菜单栏图标在浅色与深色菜单栏下都清晰（template image 生效）。
- [ ] `web/public/favicon.*` 单文件 ≤ 25 KB；仓库里不存在把位图 base64 内联的假 `.svg`。
- [ ] 同一页面内多处渲染 Logo 组件，浏览器 DOM 里无重复 element id；卸载其中一处后其余 Logo 不变透明。

**系统与打包**

- [ ] exe 文件名为 `PuddingBox.exe`；文件属性产品名称 `Pudding Box`、文件说明 `布丁盒子`，**中文与英文 Windows 上显示同一套值**。
- [ ] 单实例 ID 为 `app.puddingbox.desktop`；本机已装 Octo 的情况下，双击本产品不会被带到 Octo 窗口。
- [ ] Windows 安装器显示名与开始菜单项为 `Pudding Box`；`AppId` 与安装目录未变（旧版可原地升级，不并存两份）。
- [ ] macOS Dock / Finder 显示 `Pudding Box`；`CFBundleIdentifier` 未变。
- [ ] Linux desktop entry 与缺依赖提示显示 `Pudding Box`。
- [ ] 移动端应用名为 `Pudding Box`；`appId` 未变。
- [ ] 落地页 title / description / og / JSON-LD 与 og-image 全部为新品牌。

**流程与可替换性（对着 §3.4 的三个场景验）**

- [ ] **场景 A：** 只改 `brand.json` 里的 `product.names` 后跑 `node scripts/sync-branding.mjs` → 窗口标题、托盘、通知、关于页、输入框占位、导出标题、落地页、安装器显示名、Windows VERSIONINFO 全部跟着变，**代码零改动**（`git diff` 里没有 `.go` / `.ts` / `.svelte`）。
- [ ] **场景 B：** 只换 `branding/source/logo-1024.png` 后跑 `make brand` → 全平台图标产物与所有消费点一次更新完，**无需手工拷文件、代码零改动**。
- [ ] **场景 C：** 同时换配置与源图 → `make brand && make brand-check` 一次通过。
- [ ] 故意改 `brand.json` 不跑同步 → CI `brand-guard` 失败并指出哪些目标过期。
- [ ] 故意换源图不重新生成 → CI 资产检查失败。
- [ ] 故意在 i18n 词条里写死 `Octo` → CI 失败。
- [ ] 交一张底部有 25% 空白的源图 → 工具报错并提示 `--trim`。
- [ ] `gofmt -l .` 无输出；`go vet ./...`、`go test ./...`、`svelte-check` 全绿。

---

## 10. 从 dev-zxy 迁移：留什么、改什么、丢什么

| 判定 | 项 |
|---|---|
| **留（核心资产，必须借鉴）** | **① `branding/brand.json` 作唯一文本来源 + ② 一张 1024 源图作唯一图形来源 + ③ 生成/分发管线 + ④ CI `--check` 漂移检测** —— 这四件是「以后好替换」的全部依据，本方案原样继承并加固。此外：`internal/brand` 的 `go:embed` + `localized()` 三级回退；`scripts/sync-branding.mjs` 的 targets 表；`generate-syso` 的 VERSIONINFO 与可单测的 `buildVersionInfo`；`cmd/generate-brand-assets` 的纯 Go 缩放 / 手写 ICO / 手写 ICNS；`packaging/windows/brand.iss` 的 `#define` 注入；`scripts/package-desktop-windows.ps1`；`futureMigration` 的记账思路（改名为 `identifiers.future`）；`mobile/tsconfig.json` 的 `resolveJsonModule`；`octo.iss` 的 `FileCopy` → `CopyFile` |
| **改** | 英文名 `Pudding Box`、简称 `Pudding`（原为全称）；schema 升到 v2 并**按值的类别重组**（A/B/C，§3.3），`compatibility` + `futureMigration` + `platform` 合并为 `identifiers` + `display`；删未校对的 5 种语言键；`links` 拆 inApp / external 并去掉 `<your-org>` 占位符；fileDescription 改单字符串；`generate-brand-assets` 补输入校验 / `--trim` / 16px 检查 / 真 favicon / 托盘双套 / `--check` / ICO 加 24px；VERSIONINFO 改单 translation block；`web/index.html` 静态标题改单语言 |
| **丢** | `i18n.ts` 的 `value.replaceAll('Octo', …)` → 改占位符插值；`lang.go` 的 `withBrand` 反射遍历 → 改构造函数；`OctoLogo.svelte` 的橙色布丁内联 SVG（含硬编码 `<defs>` id）→ 改 `BrandMark.svelte`；218 KB base64 假矢量 `pudding-box-mark.svg` → 改真 favicon；底部 25% 空白的 1024 源图 → 按 §5.1 重交；`brand.about.x[$locale]` 直接索引 → 改走 locale 规范化 |
| **拆出去** | `glob.go` / `cdp.go` / `session.go` / `terminal_output_test.go` 四处 Windows 与 32 位兼容修复，单独开 PR |

---

## 11. 待确认（不齐不影响先做结构）

| 编号 | 项 | 本期怎么做 | 齐了怎么改 |
|---|---|---|---|
| B1 | 1024 源图正式稿 | 用设计当前提供的版本按 §5.1 校验通过即可开工 | 放新图跑 `make brand` |
| B2 | 是否提供 `logo-mark.svg` 矢量 | 无矢量则 favicon 走位图方案 | 有了自动切矢量 favicon |
| B3 | 横版 Logo 与托盘深浅双套 | 缺则由主图降级派生并告警 | 放入 `branding/source/` |
| B4 | 正式官网 / 文档 / 反馈外链域名 | 落地页保持现有域名；应用内协议页走本地占位 | 只改 `brand.json` 的 `links` |
| B5 | 用户协议与隐私政策正文 | 本地占位页，正文为「本页为开发占位，非正式协议。」 | 替换正文与链接配置 |
| B6 | 正式发布时是否改 `CFBundleIdentifier` / Inno `AppId` / Capacitor `appId` | 本期不改，值记在 `futureMigration` | 另开需求，需配套迁移方案 |
| B7 | 主色是否用于全局界面强调色 | 本期只写入 `brand.json` 供 Logo 与占位背景用，不改全局主题 | 另开需求 |

---

## 12. 修订

| 日期 | 说明 |
|---|---|
| 2026-09-07 | 初稿。定名中文「布丁盒子」/ 英文「Pudding Box」/ 英文简称「Pudding」；系统标识符统一 Pudding 拼写；版权方与口号采用 dev-zxy 值；覆盖范围为全部消费端；确立「文案键插值而非运行时替换」与「一张源图一条命令生成全平台资产」两条主线 |
| 2026-09-07 | 评审修订：① 新增 §2.5「值的三类」，把**路径与标识符一律英文 ASCII、永不多语言**升格为结构性约束，并配 §4.5 第 4 条 CI 强制；② schema 升 v2，按值的类别（A/B/C）重组为 `product`/`about`/`copy` + `display` + `identifiers`，路径与标识符在 schema 里没有多语言的位置可填；③ 新增 §3.4「替换演练」，把「以后好替换」量化成换名 / 换图 / 白标三个可验收场景；④ §4.2 补 C 类字面量逐条处理表与判断口诀；⑤ 验收清单新增「路径与标识符」分节与三场景演练项；⑥ §10 迁移表把「brand.json + 1024 源图 + 管线 + `--check`」四件明确标为必须借鉴的核心资产 |
