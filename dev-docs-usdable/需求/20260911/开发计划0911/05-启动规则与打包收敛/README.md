# 阶段 5：启动规则与打包收敛

## 目标

收敛 `TODO-10` 剩余四项启动规则和 `V-81` 打包品牌元数据债务。

## 工作包

| 工作包 | 内容 | 说明 |
| --- | --- | --- |
| 5A | `V-82` 数据根可写性/空间预检 + `V-84` 端口占用规定文案 | 共用启动失败路径，可同 PR；失败只允许退出 |
| 5B | `V-83` 跨程序目录二次启动提示 | 先验证 `SecondInstanceData` 在三平台的路径能力 |
| 5C | `V-85` 便携构建开机自启入口 | 产品先决定隐藏或禁用；涉及主机注册表，须 §3.7 确认 |
| 5D | `V-81` macOS/Linux 元数据和发布资产名 | 复用现有构建 token，从 `branding/brand.json` 同源生成 |

## 退出条件

- 数据根异常在启动时给出明确原因且只允许退出。
- 跨目录二次启动不会让用户误以为打开了另一份。
- 端口提示包含规定归因和操作；“Octo”例外注释正确。
- 便携构建不提供会写主机注册表的活入口。
- macOS/Linux 品牌元数据不再硬编码，对应 allowlist 债务删除。
- 真实跑过适用平台打包，不只通过源码守卫。

## 执行记录

- 待填写。

阶段完成后，将总结写入本目录的 `阶段总结.md`。
