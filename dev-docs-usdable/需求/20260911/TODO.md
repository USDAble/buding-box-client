# TODO · 已登记、闭环期不做

| 项 | 内容 |
| --- | --- |
| 文档状态 | 正式登记（2026-09-12 建立） |
| 关联 | [`开发计划.md`](开发计划.md) §5、[`需求基线.md`](需求基线.md) §5.6、[`待解决问题.md`](待解决问题.md)、[`待确认问题清单.md`](待确认问题清单.md) |

## 这份文件管什么

本文件登记**已经被发现、已经决定「闭环期不做」、但明确要做**的工作项。

| 文件 | 登记的是什么 | 判据 |
| --- | --- | --- |
| [`待确认问题清单.md`](待确认问题清单.md) | **还没拍板、需要有人回答**的问题（`PQn` / `Qn`） | 缺一个决定 |
| [`待解决问题.md`](待解决问题.md) | **已拍板「暂不解决」**的技术问题（`D-nnn`） | 缺一个决定，且决定是"不做" |
| [`需求基线.md`](需求基线.md) §5.6 | **文档与现实不符、或新增的规格缺口**（`V-n`） | 是一个需要修正的**事实偏差** |
| **本文件** | **已决定要做、但排在闭环之后的工作项**（`TODO-nn`） | 缺的是**时间**，不是决定，也不是事实修正 |

**四者的关系**：`V-n` 是"发现了什么"（`需求基线` §5.6 是它的唯一 owner），`TODO-nn` 是"什么时候做、谁接"。**所以每个 `TODO` 条都指向它对应的 `V-n`，且不复制 `V-n` 的描述**（`开发规范` §3.8：衍生文档只引结论 + 链接，不复述推理）。

**判据（`开发规范` §3.10 写清范围）**：进本清单的项必须同时满足 ① 与任何 `L-*` 闭环的**验收无关**或 ② 是**大功能**，修它会挤掉真正卡验收的工作。**不满足者不得进本清单** —— 如果是闭环要求的，就必须在闭环期修完，缺的只是 PR 归属，那种情况记在 `开发计划.md` §1.1 或该 PR 的方案里。

## 登记约定

- 每条格式：**编号 / 关联 `V-n` / 标题 / 为什么可以等 / 现在什么样（用户可见）/ 接手点 / owner**。
- **「现在什么样」必须写实**：不写"体验欠佳"，要写清用户或开发者此刻会看到什么。含糊的登记等于没登记（沿用 [`待解决问题.md`](待解决问题.md) 的同一条约定）。
- **「接手点」不得为空**。写"以后再说"的条目不是记录，是逃避 —— 要么给 PR 号，要么给触发条件（"下次改 X 时"）。
- 做完一条**不删行**，把状态改为 ✅ 并注明落在哪个 commit/PR（保留"曾经记过"的痕迹，便于回溯为什么当时不修）。

## 总览

| 编号 | 关联 | 标题 | 为什么可以等 | 接手点 |
| --- | --- | --- | --- | --- |
| `TODO-01` | `V-14` | `make desktop-dev` 热重载开发模式 | 纯开发者体验，与全部闭环无关 | `PR-9` 之后，或单独 PR |
| `TODO-02` | `V-1` | 六个守卫的 CI 接线 | 纯工程项，脚本与单测都已在 `v1`，缺的只是调用 | ✅ **已完成（2026-09-14，`PR-9`）** |
| `TODO-03` | `V-20` | `P2-启动与生命周期.md` 不存在 | 纯文档缺口，不影响代码 | ✅ **已完成（2026-09-15，分支 `docs/p2-startup-lifecycle`，已合本地 `v1`）** —— 见逐条 |
| `TODO-04` | `V-25` | 产品 WS 四事件没有发射方 | 前置是 `N-5`（载荷形状未拍板） | `PR-8`，阻塞于 `N-5` |
| `TODO-05` | `V-26` ✅ / `V-71` ✅ | 会话文件**明文**落敏感内容 | ~~要先定「打码 / 加密 / 不落」三选一~~ —— 已拍板「落盘前打码」（`G2`）；范围那一半（`V-71`）已拍板不扩展 | ~~`PR-6` 期间 + 人工拍板~~ 两条都已定 |
| `TODO-06` | `V-4` | 客服入口的渠道载体未定 | 依赖**外部**决定，不是本项目能定的 | `PR-2c` 收尾时按"确定动作"处理 |
| `TODO-07` | — | `web/src/App.svelte:93` 引用勘误 | 改它要动 `.svelte`、触发前端重测 | 下次改 `App.svelte` 时顺手 |
| `TODO-08` | `V-31` | 文档引用的源码符号的机械检查 | 工程卫生，与闭环无关。**注意：`PR-9` 的 `docs-table-guard` 不是它** —— 那个查的是表格容器（行列数），本条要查的是"文档把已删除的函数当现存事实引用"，两回事 | `PR-9` 之后（本轮未做） |
| `TODO-09` | `V-23` | **真机验证批次**（代码已就绪，缺的是一次人工执行）：`L-C7` / `L-C3a` / `L-C4a` 三条走查 ＋ `L-E2` 的**换机**场景 | **三条走查的原因不是"没空"，是判据本身要真人看界面**（`§6.4` 第 5 步是唯一不能由 CI 代替的一步）；`L-E2` 更是**一台机器做不了** —— 它要的判据是"把 `data/` 拷到**另一台电脑**直接可用" | [`手工走查单.md`](手工走查单.md) 已给出全部命令与期望；走完把状态列改成 ✅ 并登记新现象 |
| `TODO-10` | `V-83` / `V-85`（`V-82` ✅ / `V-84` ✅ / `V-86` ✅） | **需求 §5.1.2 的启动规则有五条从未落地** —— `V-86`（便携未关闭自动更新与托盘更新入口）**已于 2026-09-15 收敛**；`V-82`（数据根可写性/空间预检 ＋ 失败页只能退出）与 `V-84`（端口占用规定文案）**已于 2026-09-16 收敛**（`DEV-0911-04A`，落地记录见 `开发计划0911/00-总计划.md` §4.4）；**剩两条**：跨目录二次双击提示（`V-83`）、便携隐藏开机自启（`V-85`） | **根因是"这一节从未被纳入闭环面"**：`E2E闭环清单` 的 13 条启动规则里**只有第 10 条**有对应条目（`L-E3`），其余 12 条没有任何 `L-*` 承接 ⇒ 它们既不进闭环验收，也就没人认领。剩下两条的详情见 `需求基线` §5.6，**本文不复述**（§3.8） | 见逐条。三条做完的先例一致：**唯一 owner ＋ 每处上游代码只加门控不删 ＋ 钉子先证红**；`V-85` 触及主机注册表（§3.7 要人工确认），`V-83` 要先做平台验证（`SecondInstanceData` 在 macOS/Linux 上是否真带路径）。**注意 `V-82`/`V-84` 的收缩顺带解决了一处入口文件自相矛盾** —— 品牌规则第二条例外的口径在五份入口里原本就有两派，见 `00-总计划.md` §4.4 |
| `TODO-11` | `E11` / `E13` / `F4`（新登记，**无 `V-n`**） | **三条需求规则从未进入闭环面**：`E13` 账号删除与数据导出、`E11` 协议与隐私政策、`F4` 诊断包导出 | **根因与 `TODO-10` 同型、但更彻底**：`TODO-10` 那五条至少还有 `需求基线` §5.6 的 `V-82`–`V-86` 逐条登记，而这三条**在任何 `V-n` / `PQn` / `L-*` 里都不存在** —— 它们在 `E2E闭环清单` 的 38 条里没有对应项，也没有任何机制会因为漏做而变红。**为什么可以等**：三者的紧急度取决于**上线时间表**（协议与隐私政策通常随首发页面上线、账号删除涉及合规），不是本项目内部能定的 | 待人工定紧急度后按 §6 排期。**先例是 `V-86`**：唯一 owner ＋ 只加门控不删上游代码 ＋ 钉子先证红。注意 `E11` / `E13` 都要"给出客服入口"，而客服载体卡 `PQ26`（`TODO-06`） |

**闭环期已闭环掉的、曾在本类的**：`V-19`（错误码漂移守卫，2026-09-12 已合并 `v1`）。

---

## 逐条

### `TODO-01` `make desktop-dev` 热重载开发模式（`V-14`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **纯开发者体验**（Vite 热重载调界面）。终端用户路径不受影响：出包走 `make desktop-portable` / `desktop-portable-all`，与它无关。与 35 条 `L-*` 闭环**零交集** |
| 现在什么样 | 开发期改前端要重新 `make desktop` 走完整构建；`make desktop-dev` **目标根本不存在**，照着 `本地开发与运行.md` §3.4 敲会得到 `No rule to make target`。该节已加 ⚠️ 标注（2026-09-12） |
| 缺什么 | 两件：① `Makefile` 的 `desktop-dev` 目标；② 窗口 URL 可被环境变量覆盖的机制（`OCTO_DESKTOP_DEV_URL` → 壳读它改窗口 URL，默认 `http://127.0.0.1:8088`） |
| 为什么是"搬运"不是"新功能" | 两件都**已实现过**，落在 `archive/feat-desktop-dev-hotreload-20260911`。按 `开发规范` §3.5，**从 tag 取文件，不要重写** |
| 注意 | `本地开发与运行.md` §3.4 给出的 `Makefile:130-135` / `main.go:92` 行号来自归档 tag，**在 `v1` 上不成立** —— 搬运时按 tag 里的实际位置取，不要按那两个行号 |
| 接手点 | `PR-9` 之后，或任意时候单独一个 PR |
| owner | 未指定 |

### `TODO-02` 六个守卫的 CI 接线（`V-1`）（✅ **已完成 2026-09-14**）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 六个守卫的**脚本与单测都已在 `v1`**（`scripts/*-guard.mjs`），缺的只有 `Makefile` 目标与 `go.yml` job。不影响任何功能与闭环验收 |
| 现在什么样 | ✅ **已接线，本条关闭。** `PR-9`（2026-09-14 合 `v1` 并推送）把七个目标与五个 CI job 一起接上：`Makefile` 新增 `reuse-check` / `server-diff-check` / `release-profile-check` / `release-config-check` / `preflight-check` / `test-production` / **`docs-table-check`**（第八条，本轮新增，见 `V-59`），`go.yml` 新增 `reuse-guard` / `server-diff-guard` / `release-profile-guard` / `test-production` / `docs-table-guard` 五个 job。**判据：七条目标在本地各跑过一次并通过**（`reuse-check` 的扫描目前是 no-op —— `internal/productclient/gateway` 尚未建立，与 `P0-01 B0` 一致；`release-config-check` 的 3 条警告正是"production.json 仍是 `.invalid` 占位"这条已知事实，属咨询项） |
| 缺什么 | 无。**留下一件仍欠的事**：验收栏要求"先红后绿"（每条守卫故意红一次再恢复），这一步**没做** —— 接上不等于拦得住；做它需要一个刻意的坏改动，归下一次有人动这块时顺手 |
| 注意 | `开发规范.md` §9 第 7–10 项的状态行已按本条更新为 ✅ 已接线；`需求基线` §5.6 `V-1` 行同样收口 |
| 接手点 | **已完成**，无需接手 |
| owner | （已关闭） |

### `TODO-03` `P2-启动与生命周期.md` 不存在（`V-20`）（✅ **已完成 2026-09-15**）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **纯文档缺口**。`本地开发与运行.md` 两处引用它，代码不依赖它 |
| 现在什么样 | ✅ **已补出（2026-09-15，分支 `docs/p2-startup-lifecycle`，已合本地 `v1`）**：`需求/2260906/技术方案/P2-启动与生命周期.md`。写法是**"写今天真实是什么样，不重述需求"** —— §5.1.2 的 13 条仍是唯一 owner，`P2` §5 只给逐条实现状态表，正文另给 `main()` 的 20 步启动顺序、单实例/端口/接管的三分解、三种离开。**同轮撤掉了两处已不成立的说法**：`本地开发与运行.md` 的「⚠️ 该文档目前不存在」；`SettingsPage.svelte` 里引用**不存在的** `AutostartAvailable()` 的注释（见 `V-85`） |
| 缺什么 | 无。**留一件**：`P2` §5 引用的 `V-82`–`V-86` 就是它自己核出来的五条 —— 补文档的副产品是**发现缺的比登记的更多**，登记见 `TODO-10` |
| 接手点 | **已完成**，无需接手 |
| owner | （已关闭） |

### `TODO-04` 产品 WS 四事件没有发射方（`V-25`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 前置是 **`N-5`**（四事件的载荷形状未拍板）。**前端四个 handler 都已就位**，只等生产端 |
| 现在什么样 | 事件名在 Go 源码里**零命中** ⇒ 界面上依赖它们的四处（余额刷新、输入命中提示、`datastore:lost`、模型下架提示）今天**永远收不到**；`L-E3`（拔盘冻结提示）也挂着 |
| 缺什么 | 四个事件的发射方；以及 `N-5` 对载荷形状的拍板 |
| 接手点 | **`PR-8`**（已排，阻塞于 `N-5`） |
| owner | 未指定 |

### `TODO-05` 打码到底盖住哪些「上屏文字」（`V-26` 已落地，剩下 `V-71`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **决定本身不阻断任何闭环验收**，但它是**合规特性范围**的口子 —— 上线前必须定 |
| 现在什么样 | ✅ **`V-26` 已收敛（`PR-6a`，2026-09-14）**：`G2` 拍板「落盘前打码」，装饰在 sender 上 ⇒ agent 写进 history 的就是打码后文本；`L-D2` 的判据改为**读会话文件的字节**，19 个变异全中。**剩下的口子是范围问题（`V-71`）**：打码今天只覆盖**供应商回来的可见文本**；`tool_use` 入参（有意不打码，打了会改坏 dispatch 的 JSON）与 `thinking` 块（要带签名回传）之外，**任务清单（`task_*` 的 `subject`）与工具输出（终端输出、抓回来的网页正文）仍以明文上屏并落盘**。**2026-09-15 补**：这条边界另一侧的**用户输入**已落地（`PR-6b3`，`L-D1`）—— 手敲正文命中即拒、打码后交回输入框，**与这里的两处「有意不打码」互不影响**（它们不是输入） |
| 性质 | 与 `V-58` 同类：**不是漏做，是方案只对一类文字下了结论**，其他面的默认（不打码）从未被写下来。`E2E闭环清单` §3.4 已把两个「有意不打码」与这一处「待拍板」分开写清 |
| 缺什么 | ~~一个决定：① 是否把打码扩展到任务清单与工具输出；② 若扩展，放在哪一层~~ —— **✅ 已定（用户，2026-09-14）：不扩展。** 两个候选落点的硬代价都写明了：**渲染层**需要词表进浏览器（与「词表不出服务器」冲突），**落盘层**会改坏 agent 自己要读回来继续用的状态（清单的 `subject` 是**机器的**状态）。判据是「**没有任何验收口子要求它**」：`P8` §3.4 只对「模型答话」下了结论；用户输入另走 `L-D1` 的输入检测 |
| 接手点 | ~~`PR-6b2`~~ —— **不再是「待拍板」而是「已拍板为不做」，剩下的是把结论钉住**：两处「不打码」写成**明处的例外 ＋ 各一条反钉**（断言命中词**逐字不变**地落进会话 JSON），判据随 `PR-6a` 的 `L-D2` 钉子所在的测试文件走（归档到 `E2E闭环清单` §3.4 的表里）。**2026-09-14 晚更新**：`V-71` 已拍板不扩展（用户），本条从「等决定」转为「已闭环登记」；`PR-6b2` 已按 §2.0 再判粒度、只做词库三条路由（`L-D1` 挪 `PR-6b3`），**与本条无关** |
| owner | 未指定 |

### `TODO-06` 客服入口的渠道载体未定（`V-4`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 依赖**外部决定**（渠道形式与联系方式），不是本项目能定的。`E1` 规则 2 / `E5` 规则 3 / `E14` 规则 3 / `C12` 的 `account_restricted` 档四处都要求"给出客服入口" |
| 现在什么样 | 四处要求都**没有可落地的载体** |
| 缺什么 | 渠道与联系方式。`PR-2c` 的 `account_restricted` 档按既定原则处理：**只给"文案 + 一个确定动作"，绝不留"请联系客服"却没有任何联系方式的死路**（`P4-拦截页.md` §3.3 已写明这条约束） |
| 接手点 | `PR-2c` 收尾时确认一次；载体定了再补进文案 |
| owner | 未指定 |

### `TODO-07` `web/src/App.svelte:93` 引用勘误

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | 是**代码注释里的错误引用**（把 `suppressOnboarding`（属 `A4`）引作 `P9 §3.1`），不影响行为 |
| 现在什么样 | 注释指错文档，读者按它去 `P9 §3.1` 找 `suppressOnboarding` 会找不到。已在 `需求基线.md` 修订里记为"引用勘误" |
| 缺什么 | 一处注释改正 |
| 为什么不当场改 | 改 `.svelte` 会触发前端重测；与本轮闭环无关 |
| 接手点 | **下次改 `App.svelte` 时顺手** |
| owner | 未指定 |

### `TODO-08` 文档引用的源码符号的机械检查（`V-31`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **工程卫生**，与全部闭环无关 |
| 现在什么样 | `V-31` 是一**类**（"规范性/方案文档把已归档或已删除的代码当作现存事实引用"），已发现两起实例（`P1` §2、`开发规范` §3.9.1），**都已就地勘误**；但没有任何机制阻止第三起 |
| 缺什么 | 一条机械检查：把文档里带反引号的函数符号拿到全仓源码里回找，命不中就**要求同一句出现"已删除/已归档/不存在"之类的说明**。检索办法与"5 个命中里 3 个是正确写法"的判据写在 `需求基线` `V-31` 行内 |
| 接手点 | **`PR-9`** 的守卫族（与 `TODO-02` 同一批接线）。**2026-09-14 更新：`PR-9` 只完成了那一批接线，没有做本条** —— 借同一批落地的是 `docs-table-guard`，它查表格容器（行列数），与本条要查的"已删除符号被当现存事实引用"是两件事，只是**动机同源**（`V-31` 与 `V-56` 都属"文档与现实不符"，一类靠符号检索、一类靠表格解析） |
| owner | 未指定 |

---

### `TODO-09` 真机验证批次（`V-23`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **它不是"缺时间"，是"判据本身要另一台机器/另一双眼睛"**。三条走查（`L-C7` / `L-C3a` / `L-C4a`）按 `§6.4` 第 5 步要真人执行 —— 那是唯一不能由 CI 代替的一步，代码侧**零欠账**（钉子与变异都在）；`L-E2` 的换机场景更是**一台机器做不了**。代码没写完就登记"等走查"是自欺，所以这条等到今天才立 |
| 现在什么样 | 五条闭环都停在"可走通、判据差最后一跳"：`L-C7` 🟨、`L-C3a` 🟨、`L-C4a` 🟨、`L-E2` 🟨 —— 用户**看得见结果**（模型下架会提示、流式会逐字出、余额会跟着服务器动），但状态列还不能改 ✅ | 
| 缺什么 | ① **一次真机执行**（三条走查的命令、夹具、期望已在 [`手工走查单.md`](手工走查单.md)）；② **`L-E2` 需要第二台电脑** —— 判据是"把 `data/` 拷过去直接可用，不重新激活、不重新登录"。**注意**：`L-E2` 的存储 / 恢复 / 撤销三边**都已判**（含"登出后旧副本签不进去"那条主动触发路径），只差"换机"这个动作 |
| 接手点 | 走查单已备好；走完按 `§2` 改两处登记（`E2E闭环清单` 状态列 ＋ `开发计划` 对应 PR 的落地记录），**汇总行由合并者改**。走查中若发现新现象，按现象改代码而不是改期望 |
| 阻塞（2026-09-15 已解除） | ~~**走查单里 3 处需改 Go 源码才能造故障** —— `cmd/productstub` 只暴露 4 个 flag（`-tool`/`-upstream`/`-key`/`-model`），对状态面一个都没有；`clienttest.Server` 上的 `SetBalance` / `SetCatalogVersion` 等 setter **全部已存在但没接线**。见走查单 §3~~ **已处置（2026-09-15，分支 `tooling/productstub-fault-flags`）**：13 个注入 flag 接线完成，走查单 §0 有对照表、§1 四处 `✎ 需改源码` 换成命令、§3 改写为"已解决"。**本条的阻塞面因此清零，剩下的只有"要人 + 要第二台机器"** —— 那两件本来就不该由代码解决 |
| owner | 待指定 |

---

### `TODO-10` 需求 §5.1.2 的启动规则有五条从未落地（`V-82`–`V-86`）

| 项 | 内容 |
| --- | --- |
| 为什么可以等 | **本批次的验收面是 [`E2E闭环清单.md`](E2E闭环清单.md) 的 `L-*`，而这五条不在里面** —— 查证过的根因，含一处**必须先排掉的近似项**：`E2E闭环清单` 的 13 条启动规则里**只有第 10 条**有对应闭环（`L-E3`，依据列写 `F2`）；**`L-B` 那一组标题虽叫「门与启动」，四条接的却是 `E8` / `A1` / `C12`**（界面门、窗口令牌、控制面两页与四档），**一条都不落在 §5.1.2 的启动规则上**。所以"标题里出现过启动"不等于"被接住了"。⇒ 这一节从未进入闭环面，因此既不会被验收、也没人认领。这不是"押后"，是**从未被登记**（发现于 `TODO-03` 补文档时逐条核代码） |
| 现在什么样 | 用户/开发者**能看见**的**四件事**，逐条详情在 `需求基线.md` §5.6（本条不复述，§3.8）：① 数据根写保护或盘满时**没有预检**，病根会在后面某一步以"莫名其妙的失败"现形（`V-82`）；② 双击**另一份程序目录**时只激活已有窗口、**零提示**，用户以为打开了第二份（`V-83`）；③ 端口被占的提示**丢了归因与动作** —— 只说"可能有其他程序占用"，不点明本机 Octo，也不告诉用户"先退出再打开"（`V-84`）；④ 便携构建**照常渲染开机自启开关**，一开就写主机注册表（`V-85`）。**曾有的第五件（`V-86` 自动更新全在跑、Windows release 会原地替换便携目录）已于 2026-09-15 收敛**。**`V-82` 与 `V-84` 已于 2026-09-16 全部收敛（`DEV-0911-04A`）**：启动期现在并列三条检查（路径不可用／不可写／空间不足）**且都在"接管其他后端"之前**，失败页的单按钮改成「退出」；端口文案逐字落成规定句、归因与动作都回来了。详细证据、变异记录与一处**实测的钉子盲区**见 `开发计划0911/00-总计划.md` §4.4。**剩下两件**：`V-83`、`V-85` |
| 缺什么 | 两条各自缺的东西不同，**但共同缺的还是"归谁"** —— 没有一个 PR 认领 §5.1.2 的最后两条。逐条：`V-83` 要在 `OnSecondInstanceLaunch` 里比路径（**且要先验证 `SecondInstanceData` 在 macOS/Linux 上是否真的带路径**，不能假定）；`V-85` 要先定"整行不渲染"还是"渲染但禁用并说明"（**触及主机注册表，§3.7**） |
| 优先级 | **`V-86`（2026-09-15）、`V-82`／`V-84`（2026-09-16）均已收敛。** 剩下两条：`V-85` 次之（会写主机注册表），`V-83` 是纯提示。`V-85` 原判"与 `V-82` 同档"，现在 `V-82` 已做完，它单独成为这一节里最重的一条 |
| 接手点 | 未派 PR。**建议做法**：两条**不要合成一个 PR**（`V-85` 要人工确认是否整行不渲染、`V-83` 要先验证 `SecondInstanceData` 在 macOS/Linux 上是否真带路径）。前三条已按"唯一 owner ＋ 只加门控不删上游代码 ＋ 钉子先证红"做完，可直接照抄那个形状 |
| owner | 待指定 |

### `TODO-11` 三条从未进入闭环面的需求（`E11` / `E13` / `F4`）

**登记于 2026-09-15**，来源是一次对 `E2E闭环清单` 的逐条复核。

| 项 | 内容 |
| --- | --- |
| 现在什么样 | 三条**零实现**（`F4` 诊断包导出、`E13` 账号删除与数据导出）或**只有壳**（`E11`：`LegalModal` 组件在，但**协议与隐私政策的正文与版本**未定，`branding/brand.json` 里是占位文本） |
| 为什么可以等 | **判据是"缺的是决定、不是时间"** —— 三者的时点由上线时间表定（`E11` 通常随首发页面、`E13` 涉及合规），本项目内部无法判断 |
| 接手点 | 待人工确认紧急度后按 `00-总计划` §6 排期。`F4` / `E13` 都属"新功能"而非"补缺口"，建议各自独立 PR，不合并 |
| 两条只被顺带提到的 | `A4`（只在 `TODO-07` 的"注释勘误"里出现，**后端半边未做**）、`E14`（只在 `TODO-06` 客服那一项里出现）。它们**不是完整登记**，暂并列在此以免读者以为已覆盖 |
| `D8` 的归属 | 与矩阵无关 —— 它折进 `D-002`（客户端打码有效、服务端撤回无效），而 `D-002` 自己写了最晚时点："内容安全验收开始前，不能拖到上线" |
| 为什么登记而不是排期 | 本条的动作是**消除"没人知道该不该做"**这一个状态。`TODO-10` 已经证明这类缺口能藏很久的原因不是没人做，是**没有任何一行的失败会指向它** |

## 附：已认领的缺口（**不在本清单**）

列在这里**仅为对照**，避免读者以为它们没人管。它们与闭环**直接相关**，因此必须在闭环期修完：

| `V-n` | 归属 |
| --- | --- |
| `V-24` 6 条 `/api/product/*` 路由无 Go handler | **✅ 2026-09-15 清零**：`chat-modes`：`PR-4d`；`nickname` ＋ `prefs`：`PR-6b1`；词库两条 —— `GET`/`PUT` `sensitive/dict` 与 `import`：`PR-6b2`（2026-09-14）；最后一条 `sensitive/check`：**`PR-6b3`**（2026-09-15）。**口径**：契约 §1.4 里这一条占**四行**（`dict` 是 `GET` ＋ `PUT` 两行），所以"剩下 1 条路由"对应"表里 1 行"。见 `开发计划.md` §1.1 路由归属表与 `本地API契约.md` §1.4 的状态列（**§1.4 是存在性的唯一 owner**：它 2026-09-14 起标的是「这条路由今天在不在」，此前那一列只表达「形状定了没有」，正是本条长期不可见的原因） |
| `V-27` `L-C5`「不回落」未接线（`RequiresControlPlane()` 零调用点） | `PR-5` |
| `V-28` `L-C4` 余额与用量（无 `GET /v1/credits/ledger`、充值按钮 `disabled`） | `PR-5` |
| `V-29` 端到端测试缺失（`L-A2` / `L-A3` / `L-A6`） | `PR-4b`（走 `bootstrap` 使链路可达）+ `PR-6` |
| `V-15` / `V-16` 拦截页两页/四档未做、`L-A6` 已落地 | **`PR-2c`**（进行中） |
| `V-17` `L-E2` 换机场景测试 | `PR-7` |

## 修订

| 日期 | 说明 |
| --- | --- |
| 2026-09-15 | **新增 `TODO-11`（`E11` / `E13` / `F4` 三条从未进入闭环面的需求）。** 触发：逐条复核 `E2E闭环清单` 的 38 条与 `需求基线` 的规则集时，发现这三条**在任何 `V-n` / `PQn` / `L-*` 里都没有落点** —— 不是"排在后面"，是**不在任何一张表上**。与 `TODO-10` 的根因同型（一节规则从未进入闭环面），但更彻底：`TODO-10` 那五条至少还有 `需求基线` §5.6 的逐条 `V-n`，而这三条连登记都没有。**本条只做登记，不排期**：紧急度取决于上线时间表，由人工定。**同轮**：把两条"只被顺带提到"的（`A4` 后端半边、`E14`）与 `D8` 的真实归属一并写明，避免读者把"某处提过一次"当成"已登记"。**范围（§3.10）**：本清单（延后项登记）；三条的内容以 `需求基线` 的 `E11` / `E13` / `F4` 为唯一 owner，本条不复述。 |
| 2026-09-15 | **`TODO-10` 里的 `V-86` 已收敛（分支 `portable/v86-no-auto-update`），条目按四条改写。** 这一条是**先例**而不是普通销账：它验证了这批"需求明文要求、却没有任何 `L-*` 承接"的缺口该怎么修 —— **唯一 owner（一个常量）＋ 每处上游代码只加门控不删（硬规则 3）＋ 钉子先证红**。**动它时又查出两处登记没数到的面**（`GET /api/version` 的出站查询、完整设置里那个会说"已是最新"的假结论），所以"按登记的三处做完"本来会留下两个"同义的第二个面"—— 这正是 `V-79` 与 `V-85` 的同一种缺陷（改了一处、留下另一处同义实现）。**范围（§3.10）**：本清单（延后项登记）；`V-86` 的完整登记在 `需求基线` §5.6。 |
| 2026-09-15 | **关 `TODO-03`（`P2-启动与生命周期.md` 已补出），并新增 `TODO-10`（`V-82`–`V-86`）。** **为什么新增的是"一条批次项"而不是五条**：五条同源 —— 都出自 `需求20260906.md` §5.1.2 的 13 条启动规则，而这 13 条在 `E2E闭环清单` 里**只有第 10 条**有对应条目（`L-E3`）⇒ 该节从未进入闭环面，于是这五条既不进验收、也没人认领。**一个根因、一个 owner 缺失，拆成五行会把"没人认领"这件事藏起来**（这也正是它们能藏到今天的原因）。**逐条内容一律不复述**，只引 `需求基线` §5.6 的 `V-` 号（§3.8）。**为什么它们可以进本清单**：判据是「与任何 `L-*` 闭环的验收无关」—— 不是"不重要"，是**不在本批次验收面上**；`TODO-10` 的优先级一栏写明了五条内部的轻重（`V-86` 最重：便携目录会自我替换）。**同轮**：`TODO-03` 的「现在什么样」按事实改写（它撤掉的那处 ⚠️ 与那处假注释也一并记明）。**范围（§3.10）**：本清单（延后项登记 ＋ 归属对照）；五条的完整登记在 `需求基线` §5.6，`P2` §5 有逐条实现状态。 |
| 2026-09-15 | **`PR-6b3` 落地后同步两处：附表 `V-24`（最后一条路由 `sensitive/check` 挂上 ⇒ **清零**）与 `TODO-05` 的「现在什么样」（用户输入那一侧已由 `L-D1` 落地，与两处「有意不打码」互不影响）。** **本清单这一次只动这两处**：本条目的判据是「缺的是时间、不是决定」，而 `PR-6b3` 的两条产物 —— 路由注册与输入门 —— 都是**闭环验收口子要求的东西**（`L-D1` / `D1`），属落地、不属"延后"；`TODO-05` 之所以仍要改一行，是因为它的范围结论**另一半今天才有现实对应物**（此前写「用户输入另走 `L-D1` 的输入检测」时那条检测还不存在）。**范围（§3.10）**：本清单（延后项登记 ＋ 归属对照）。 |
| 2026-09-14 | **附表 `V-24` 由「剩 3 条」改为「剩 1 条」（词库两条与 `import` 由 `PR-6b2` 挂上）；`V-76` 登记后归 `PR-6c`。** **本清单这一次只动两处**：附表那一行（**六条路由的欠账今天只剩 `sensitive/check`**），以及本条修订。`TODO-05`（打码范围）已在同日由人工拍板为「不扩展」，见该行。**范围（§3.10）**：本条清单（延后项登记）；`V-76` 的完整登记在 `需求基线` §5.6。 |
| 2026-09-14 | **`V-71` 拍板为「不扩展」（用户），`TODO-05` 由「等决定」转为「已闭环登记」；附表 `V-24` 的剩余三条按 `PR-6b2`／`PR-6b3` 劈分。** **为什么把一条「不做」的结论也记进清单**：它的代价在现场是有用的（两个候选落点各自的硬代价），而它的判据是「**没有任何验收口子要求它**」—— 下一次有人想扩展时，先要推翻这两句而不是推翻一个态度。**同轮**：`PR-6b2` 按 §2.0 再判粒度后只做词库三条路由，`sensitive/check` ＋ 输入检测生效（`L-D1`）挪 `PR-6b3`（它要动上游文件的注入缝，属 §3.7 确认项）。 **范围（§3.10）**：本条清单（延后项登记）；两条结论都在 `需求基线` `V-71` 与 `E2E闭环清单` `L-D2` 各有落点。 |
| 2026-09-14 | **`PR-6b1` 落地后同步两处：`TODO-05` 的接手点由「`PR-6b`」改为「`PR-6b2`」（`PR-6b` 已按 §2.0 再劈一刀，且 `PR-6b1` 刻意没碰打码面 —— 本条等的那个决定仍未做）；附表里 `V-24` 的「6 条」改为「已挂 3 条 / 剩 3 条」。**为什么只改这两处**：本清单的条目判据是「缺的是时间、不是决定」，而 `PR-6b1` 落地的 `V-72` / `V-73` / `V-74` 三条在本轮**当场收敛**（都属「事实偏差」，归 `需求基线` §5.6，不进本清单），`V-71` 才需要人工拍板 —— 它的载体是 `TODO-05`，本条本轮只负责把接手点改对。 |
| 2026-09-12 | 建立。从 `开发计划.md` §5.1 的"延后清单"独立成文并补全字段（原表只有"为什么延后 + 接手点"两列，缺"现在什么样"，读者无法判断代价）。**这 8 条的共同判据是 `开发规范` §3.10 的范围：要么与任何 `L-*` 闭环的验收无关，要么是大功能，修它会挤掉真正卡验收的工作。** 逐条核对了归属：`TODO-02` / `TODO-08` 归 `PR-9`，`TODO-04` 归 `PR-8`（阻塞于 `N-5`），`TODO-05` 随 `PR-6` 但**决定需人工拍板**，`TODO-01` / `TODO-03` / `TODO-07` 与任何 PR 无关（分别属开发者体验、文档、注释），`TODO-06` 依赖外部决定。**同时补了"附：已认领的缺口"表** —— 审计中发现的 `V-24` / `V-27` / `V-28` / `V-29` 一度看起来无归属，实测分别由 `PR-4d`/`PR-6`、`PR-5`、`PR-5`、`PR-4b`+`PR-6` 认领，列此对照以免被当成遗漏。`V-19`（错误码漂移守卫）同日已闭环，不再列入。 |
