# 阶段 0：准备与外部确认

## 目标

在不污染当前 `v1-qingfeng` 暂存改动的前提下建立可复现基线，并取得后续开发必需的人工决定与外部信息。

## 任务

- 确认 `.gitignore`、`Composer.svelte` 两处暂存改动的 owner。
- 确认远端写权限、SSH key 和 `origin/main` 访问。
- 确认中台、发布、产品/运营联系人。
- 在干净任务分支执行 `make gate`、`make test-production`、`make preflight-check`。
- 推动并记录 `PQ30`、`G1`、`PQ26`、`G6`、`V-85`、`V-77` 的结论。
- 向中台同步追踪 `D-001`、`PQ25`、`PQ27`、`PQ28`。
- 准备 Windows 真机、第二台电脑和真实模型测试 key。

## 退出条件

- 基线门禁结果有记录。
- `PQ30` 和 `G1` 已有明确结论，或已有具名 owner 与答复时间。
- 后续各阶段所需权限、设备和联系人已经可用。
- 所有人工确认已写入 canonical `开发计划.md` §4.1。

## 执行记录

- 待填写。

阶段完成后，将总结写入本目录的 `阶段总结.md`。
