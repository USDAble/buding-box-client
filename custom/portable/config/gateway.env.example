# Copy this file to gateway.env in the same directory and set the endpoint/model.
# Keep the real API key out of this file; the launcher requests it at startup.
AI_GUARD_UPSTREAM_URL=https://api.openai.com
AI_GUARD_UPSTREAM_MODEL=
