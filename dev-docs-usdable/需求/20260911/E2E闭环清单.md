# 本轮 E2E 闭环清单（UI + 交互 + Sandbox）

| 项 | 内容 |
| --- | --- |
| 文档状态 | v0.1 草案，待评审 |
| 日期 | 2026-09-11 |
| 定位 | **本轮交付面的 E2E 验收依据**。与《[中台交付包](中台交付包.md)》§9 的 15 条正式验收**分工不同**，见 §1 |
| 上游 | [需求基线](需求基线.md) |
| 编号规约 | 本轮闭环用 **`L-<组><序>[<字母>]`**（如 `L-A1`、`L-C3b`）；`V-n` / `PQn` / `D-n` 沿用既有编号，不重编。**字母后缀仅用于「一条原闭环被拆分」**（§2.0） |

---

## 0. 本轮目标（人工明确，2026-09-11）

> **在不真正接入正式中台的前提下，把全部 UI、交互与 Sandbox 接通，所有功能细节做完。**

拆成三条，缺一条都会让验收标准漂移：

1. **做：** 界面、交互、**全部本地逻辑**（数据根、门、词库、模式、便携、诊断）、**Sandbox 联调链路**。
2. **不做：** 正式中台地址与验签公钥（`D-001`）；需要中台共同签字的合同字段 —— `clientRequestId` / `toolPolicy`（`D-003`）、流中 `retract` 与账本终态帧（`D-002`）。
3. **不因此降级：** §0 原则的两条底线照旧 —— **失败关闭**、**不静默降级**。少做功能与放松安全是两件事，见 §0「不适用面」。

**"做完"的判据不是"页面能点"，而是本文档 §2 的 35 条闭环** `L-*` **各自能独立跑通并判定**。

---

## 1. 两套 E2E 依据的分工（不得混用）

|  | 《中台交付包》§9 | 本文档 |
| --- | --- | --- |
| 性质 | **正式验收** —— 客户端与中台**共同签字** | **本轮开发验收** —— 本地可独立判定 |
| 前提 | 正式中台 + 合同字段已交付 | 本地 Go 服务 + **中台替身**（§4 `P1`）或 Sandbox |
| 条数 | 15（`E2E-01`…`E2E-15`） | 35（`L-A1a`…`L-C7`） |
| 本轮 | **大部分跑不了** —— `E2E-03`/`05`/`06`/`13`/`15` 依赖本批明确不做的 `clientRequestId` / 幂等键 / 终态帧 / 版本确认 | **本轮以此为准** |

> **判断准则：一条用例若必须"中台侧一起看日志"才能判通过，它属于《中台交付包》§9，不属于本文档。**

**为什么必须分开写：** 现在两边的字段名、错误码都还是**提案**（`中台交付包.md` 头部即写"待共同确认；确认后可作为 OpenAPI 与联调依据"）。把"提案里的验收"当成"本轮能判的验收"，会让联调期出现"这条到底算不算通过"的争议 —— 而这正是两个文档都要避免的。本轮要的是**本地能判死**的闭环。

---

## 2. 闭环总表（35 条）

**状态列：** ✅ 规格完整、可独立跑通并判定 ｜ ⚠️ 规格已写但有**已知缺口**，正向路径可跑、判定不完整 ｜ ⛔ 被未决项或缺失件挡住。

> **状态是「规格状态」，不是「开发进度」。** 它回答"这条闭环的规格够不够我照它写测试"，不回答"代码写完没有"。开发进度按 `开发规范` §6.4 的五步逐条登记在 [`开发计划.md`](开发计划.md)。

### 2.0 为什么从 25 条拆成 33 条（2026-09-11 人工要求；后又补 2 条 → **35 条**，见 §6 修订）

**人工要求：** 「每一个闭环最好别太大」。**拆分判据（`开发规范` §6.4 第 0 步）：** 一条闭环 = **一次可独立判定的用户可见行为**；能用一句话说清"用户看到什么变了"才算过关。按此复检，有 6 条实际上是**多个独立判据捆在一起**，拆开：

| 原 ID | 拆为 | 为什么捆着不对 |
| --- | --- | --- |
| `L-A1` | `L-A1a` 激活成功 ｜ `L-A1b` 响应丢失 | 成功路径**不该被 `PQ25` 拖住**；两者判据完全不同（一个判"进不进主界面"，一个判"幂等键有没有用"） |
| `L-B1` | `L-B1a` 界面门三态 ｜ `L-B1b` 窗口令牌与请求门 | **两套机制**：界面路由 vs HTTP 令牌。`PR-2b2b` 只关于后者 |
| `L-C1` | `L-C1a` 拉取与验签 ｜ `L-C1b` 缓存与投影 | 失败面不同（签名不过 vs 投影错）；**拆开后 `L-C1a` 不再被 `V-2` 挡住**（模式分组只属于 `L-C1b`） |
| `L-C3` | `L-C3a` 基础流式 ｜ `L-C3b` 思考块 ｜ `L-C3c` 工具调用 | 原来把**四个能力**（内容/思考/工具/usage）捆成一条，是本表最大的一条 |
| `L-D4` | `L-D4a` 读取与三层合并 ｜ `L-D4b` 用户词增删 ｜ `L-D4c` 导入 | 读 / 写 / 导入是**三种操作三种语义**（尤其导入是**合并非覆盖**） |
| `L-E4` | `L-E4a` 版本与缺字段 ｜ `L-E4b` 损坏与超前 | `PR-2a` 本来就是**两组独立测试**（缺字段不迁移 vs fail-closed 保留原文件） |

**拆分的净收益不是"看起来更细"，是两件事：** ① 阻塞被**隔离**（`L-A1` 原来因 `PQ25` 整条 ⚠️，拆开后只有 `L-A1b` 被挡，成功路径 `L-A1a` 可判）；② 每条闭环的"通了"变得**可以说清**，从而能变成断言。

**引用已同步**（`开发规范` §6.4.2："引用不同步 = 拆分没做完"）：本文档 §2 / §3 / §4、[`开发计划.md`](开发计划.md) 的 PR 归属与能力检索、[`需求基线.md`](需求基线.md)、[`README.md`](README.md)。

### L-A 账号与授权（7）

| ID | 闭环（正向路径） | 关键异常分支 | 落盘 / 可见结果 | 依据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `L-A1a` | **首次激活成功**：五字段（手机号、验证码、U 盘激活码、盒子编号、昵称）→ 中台校验 → 写激活记录与凭证 → 进主界面 | ① **本地校验拦截**：手机号/验证码/昵称格式不合法 ⇒ **不发请求**，字段级信封（`invalid_phone` / `invalid_code` / `nickname_format`）；② **已存在状态不得重复播种**（`E6.4`：只有"文件不存在"才播种）。*（四码失败分流见 `L-A4`；响应丢失见 `L-A1b`）* | `product-state.json` + `credential.json`；`boxCode` **来自服务端**而非本地输入；**`product-state.json` 里搜不到任何 token** | `E1` `E6.4` | ✅ **端到端已合上（`PR-2b2a`，2026-09-11）**：`internal/productruntime/mount_test.go` 4 条测试经**真 `internal/server` + 真 loopback socket** 跑通 —— 可达性、`requireAuth` 未被绕过（转发请求 401）、五字段激活成功、**本地被拒的登录不消耗一次性激活码**。此前只有单元测试（`PR-2b1`），**单元绿 ≠ 闭环合上**。**2026-09-11 手动走通**（生产产物 `./octo-desktop` + 真替身进程，经浏览器 `?window_token=demo` 入口 —— **壳内入口属 `PR-2b2b`**）：落盘四要素逐项核对通过 —— `loggedIn`/`activated` 为真、`boxCode` 来自服务端且未脱敏、`credential.json` 只含 `refreshToken`、`product-state.json` 搜 `rt_`/`at_` **零命中**。本轮联调另修 `V-13`（路径重复 `/v1`）、登记 `V-14`（`make desktop-dev` 有文档无目标） |
| `L-A1b` | **响应丢失**：请求已送达但响应丢失 → 用户重试 | 一次性激活码已被消耗 ⇒ 无从判断是"已成功"还是"没成功" | 应有幂等键但没有 | `E1` 规则 2、`PQ25` | ⛔ **被 `PQ25` 挡住**：合同缺幂等键 ⇒ 付费用户可能永久锁死，本条**无法形成合格闭环** |
| `L-A2` | **二次登录**：手机号 + 验证码（**无**激活码 / 盒子编号）→ bootstrap 读回激活状态与 `boxCode` → 进主界面 | `phone_mismatch` / `invalid_code` / 发码冷却 | `credential.json` 更新；授权页能看到 `boxCode`（来自服务端，非本地输入） | `E2` `E3` `E4` | ✅ |
| `L-A3` | **退出 → 再登录**：退出清凭证与登录态、**保留激活记录** → 再次登录回到已激活态 | 退出后重启进程 → 停在拦截页 | `credential.json` 删除；`activated` 保留 | `E7` | ✅ |
| `L-A4` | **激活失败分流**：四个业务码 → 各自专属文案 + 客服入口 + **不写激活记录** | 同一盒子编号配两个不同激活码 → **两次都成功**（一对多回归） | 不落盘任何激活记录 | `E1` 规则 2、验收补充 | ⚠️ `PQ26` |
| `L-A5` | **授权到期展示**：剩余天数 `max(0, 到期 − 今天)` + 脱敏手机号 + **盒子编号不脱敏** | 老数据缺 `boxCode` → 该行显示「—」，不拦截 | 授权页离线可读 | `E5` `PQ19` | ✅ |
| `L-A6` | **凭证失效 / 会话撤销**：401/403 → 清本地凭证 → 回拦截页，不静默继续 | 刷新失败一次后**不无限重试** | `credential.json` 删除 | `E6` 规则 6、`E12` | ✅ |

### L-B 门与启动（4）

| ID | 闭环 | 关键异常分支 | 可见结果 | 依据 | 状态 |
| --- | --- | --- | --- | --- | --- |
| `L-B1a` | **界面门三态**：未登录 / 未激活 → 拦截页；已登录 → 放行主界面 | 未配置 / 控制面不可达时的落点（见 `L-B2` / `L-B3`） | 界面层的门 | `E8` | ✅ |
| `L-B1b` | **窗口令牌与请求门**：令牌随请求带（`X-Octo-Window-Token`）。**门的范围只覆盖本 fork 的产品路由**（`/api/product/*`）；上游 `/api/*`、`/ws`、`GET /api/uploads/*` **一律不设门** —— 这是**写明的边界**，也是实测后的修正（宽门会把聊天图片缩略图与 artifact 图片打成 `403`、并打死 CLI 的 `send_message`，见 `开发计划.md` §4.1 第 12 行 / `D-006`） | 令牌缺失 / 失效 / 非本窗口 | 产品路由 `403 product_gate` + 前端落回登录页 | `E8`、[`本地API契约.md`](本地API契约.md) §4 | ✅ 实现与自动化已验（`internal/server` 门契约 + **范围钉子**、`internal/productruntime` 真 socket 端到端、前端头传递）；**手工联调待桌面壳重建后走一遍** |
| `L-B2` | **未配置 / 无公钥**：两种状态 → **两页不同文案** | 诊断包带 `profile.name` / `ControlPlaneConfigured()` / `HasTrustedKeys()` | 不出现 URL、不出现技术名词 | `A1` 规则 6 | ✅ |
| `L-B3` | **控制面四档失败**：`network_unavailable` / `upstream_unavailable` / `unauthorized` / `account_restricted` | **只有 `unauthorized` 清凭证**；档位判定只在 `internal/productclient` 一处 | 四档各有 UI、重试性、凭证动作 | `C12` | ✅ |

### L-C 模型（10）

| ID | 闭环 | 关键异常分支 | 依据 | 状态 |
| --- | --- | --- | --- | --- |
| `L-C1a` | **目录拉取与验签**：拉签名目录 → 验签 → 落缓存 | 签名不过 → 不上屏（不得回落 `config.yml` / 本地 provider） | `B1` `B2` | ✅ **无 `V-2` 依赖**（模式分组不在这条） |
| `L-C1b` | **目录投影与上屏**：投影（含**模式分组**）→ 选择器两级上屏 | `displayName` 双语；`id` 英文 ASCII | `B5` `B6` `PQ6` | ✅ 实现前提：`internal/chatmode`（`V-2`） |
| `L-C2` | **目录三条降级**：过期且刷新失败 / 验签失败 / 无缓存 | **三条都不得**回落 `config.yml` / 本地 provider / 内置名单；无缓存时**不弹配 Key 向导** | `B4` | ✅ |
| `L-C3a` | **基础流式回合**：选模型 → 网关 `chat/completions` → 内容 delta → `usage` → 终态 | 取消与限流复用既有实现 | `C1` `C2` `C3` | ✅ 本批**不含** `clientRequestId` / `toolPolicy` / `retract`（`D-002` `D-003`，有意为之） |
| `L-C3b` | **思考块**：`reasoning` / thinking 内容单独成块、与正文分开呈现 | 无思考块的模型不得出现空块 | `C10` | ✅ |
| `L-C3c` | **工具调用**：工具调用 → 本地执行 → 结果回灌 → 续轮 | 工具调用仍过**本地权限门** | `C11` | ✅ |
| `L-C4` | **余额与用量上屏**：终态帧驱动余额变化；充值入口**常驻**；0 余额 → `402` 文案 | 预估只展示、**不作为扣费依据** | `E9` `C7` `PQ8` | ✅ **`V-11` 已收敛**：`E9` 规则 2 现写明**两条并列来源**，本轮取拉模式（回合结束 / 界面重获焦点 / 手动刷新；不得流中轮询），`D-002` 交付后切回终态帧 —— 判据分两版，本轮可判 |
| `L-C5` | **不回落**：`production` 下预设环境变量 + 写含第三方 endpoint 的 `config.yml` → 正式会话仍只用内置网关 | 六条失败面各造一次 | `A2` `C9` | ✅ |
| `L-C6` | **能力矩阵与入口可见性**：签名策略里的 `capabilities[]` 生效 → `visible=false` 的入口从侧栏与空状态**移除**；`entitled=false` 的入口**展示为不可用并给出原因**；`permissionPolicy` 默认 `ask` | 目录/策略不可得时**不得**默认全可见（fail-closed）；未知 capability `id` 忽略并记录，**不得**新建入口；`available=true, visible=false` 时功能保留而入口隐藏（隐藏 ≠ 删除） | 入口可见性由能力矩阵决定；被隐藏入口的 `curl` 直连不返回功能 | `B7`、`中台交付包` §4.4 | ⬜ **新增（2026-09-11）**，本轮**未实现**：`capabilities` 与 `catalog` 同在一个签名 `policy` 内 ⇒ 验签随 `PR-4a` 落地，**消费**（`productpolicy` + 入口可见性接线）归 `PR-4e`。**此前 33 条闭环完全没有覆盖 `B7`** —— 需求有、无人能判是否完成 |
| `L-C7` | **会话模型绑定与模型下架**：会话绑定的目录模型 `id` 被下架 → 老会话**可继续读**；下一条消息前提示「该模型已下架，请重新选择」 | **不得静默换模型**、不得报硬错；换机器后绑定随 `data/` 走 | 提示重选、不换模型、不报硬错 | `B8` | ⬜ **新增（2026-09-11）**，本轮**未实现**：归属 `PR-5`（提示发生在"下一条消息前"，是网关回合的前置校验）。**此前 33 条闭环完全没有覆盖 `B8`** |

### L-D 内容安全（7）

| ID | 闭环 | 关键异常分支 | 依据 | 状态 |
| --- | --- | --- | --- | --- |
| `L-D1` | **输入检测**：发送前即时检测 → 命中按规则替换 / 拦截 | 开关可关，**关前需确认**；关闭只影响输入检测 | `D1` | ✅ |
| `L-D2` | **输出打码**：命中片段 → `***`（连续命中合并），**不截断整段** | **落盘即打码**；重启后重读历史仍是打码后文本；不可关闭 | `D2` | ✅ |
| `L-D3` | **昵称检测**：命中 → 拒绝保存（不写 `***`、不改成脱敏值） | 检测**始终开启**，不受输入开关影响 | `D3` | ✅ |
| `L-D4a` | **词库读取与三层合并**：内置 ∪ 用户 ∪ 服务器 → 生效词表 | 用户词缺失 = 空词表**不是错误**；服务器词只读 | `D4` `D7` | ✅ |
| `L-D4b` | **用户词增 / 删**：新增、删除用户词 | 用户**删不掉内置词**（删除只对用户层生效） | `D4` | ✅ |
| `L-D4c` | **词库导入**：导入用户词 → **合并非覆盖** | 导入空文件 / 重复词 / 与内置冲突词各一次 | `D4` | ✅ |
| `L-D5` | **服务器词库同步故障**：验签失败 / 版本回退 → **保留旧版并继续用** + 知会用户 | **不得** fail-open 到"不检测"，也不得导致检测整体失效 | `D5` | ✅ |

### L-E 便携与数据（6）

| ID | 闭环 | 关键异常分支 | 依据 | 状态 |
| --- | --- | --- | --- | --- |
| `L-E1` | **首启初始化**：生成 `installId`（UUIDv4，永不因登录/登出变）+ 种 `product-state.json` 初值 | 幂等、不阻塞启动、不与用户编辑竞争（§3.9.1 的唯一例外）；**版本超前或文件损坏都不得进入播种路径** | `E6.1` `E6.2` `E6.4` | ✅ **已判（`PR-2a`）**：`V-7` 已定并实现 —— 责任人 = Go 本地服务、时刻 = 首次读取惰性创建（**进程启动不写文件**）、只读盘 = 内存默认值 + 错误上抛。测试覆盖：首启播种合法 UUIDv4、反复打开文件字节不变、登录→登出→再登录后 `installId` 不变、只读/损坏/版本超前三条不进入播种 |
| `L-E2` | **`data/` 换机复用**：拷 `data/` 到另一台电脑 → **直接可用**，不重新激活、不重新登录 | 撤销 refresh token 后旧副本必须重新登录 | `E1` 规则 7、`E6` 规则 3、`D-004` | ✅ **存储侧已判（`PR-2a`）**：凭证随 `data/` 走、明文可复制、登出＝删文件而激活绑定保留。端到端待接线（`PR-2b2a`） |
| `L-E3` | **拔盘冻结 / 恢复**：程序目录消失 → 立即冻结并禁止改写主机；同路径恢复可继续 | 换盘符 → 必须退出后从新路径重开 | `F2` | ✅ |
| `L-E4a` | **版本与缺字段**：`schemaVersion` 单调；缺字段 = 未登记；**不做写入式迁移** | 老数据缺 `boxCode` → 不拦截、不要求补填；含未知字段 → 加载正常且**文件不被改写** | `E6.2` `PQ19` | ✅ **已判（`PR-2a`）**：老数据（含未知字段、缺 `boxCode`）加载正常且文件未被改写 |
| `L-E4b` | **损坏与超前 fail-closed**：JSON 损坏 → 保留原文件；`schemaVersion` 超前 → 拒绝加载 | 两者都**不得**进入播种路径、**不得**覆盖原文件 | `E6.2` | ✅ **已判（`PR-2a`）**：超前 → `ErrIncompatibleVersion` 且文件保留；损坏 → `ErrCorrupt` + 原文件保留 + 降级未登录 |
| `L-E5` | **偏好与语言**：locale 由桌面壳带入一次（跟随系统）→ 可改 → 持久化 | 前端**不猜**、不读 `navigator.language`；未登录也允许改语言 | `E6.1` `PQ18` | ✅ **已合上（2026-09-11 复核，`PR-2b2b` 收尾）**：原挂在 `PR-2b2b` 的「壳带入」实为 **`PR-2b2a` 已落地** —— `mountProductAPI` 把 `resolveLang()`（**配置优先 → 否则问平台**：macOS `AppleLanguages` / Windows `GetUserDefaultLocaleName`）交给 `productstate.Open` 的 `Locale`，正是 `E6.4` 规则 1 / `PQ18` 要的「带入一次」；存储侧三条护栏与「未登录可改语言」均有测试。前端侧已核实**不猜产品语言**：唯一的 `navigator.language` 命中在 `ChatView.svelte:2013` 的 `getExportLocale()`，那是**导出文档**的 `<html lang>`（先读应用已设的 `document.documentElement.lang`），与产品 UI 语言无关 |

### L-F 产品事件（1）

| ID | 闭环 | 关键异常分支 | 依据 | 状态 |
| --- | --- | --- | --- | --- |
| `L-F1` | **四个产品 WS 事件**：`credits_update` / `input_sensitive` / `datastore:lost` / `datastore:restored` | WS 握手带令牌 | [`本地API契约.md`](本地API契约.md) §4 | ⚠️ `N-5`：**四个事件的作用域与重放语义未定**；且 `datastore:lost` 与 `L-E3` 的拔盘冻结**是否同一件事、谁负责**没有写 |

**合计 35 条：✅ 30 ｜ ⚠️ 2 ｜ ⛔ 1 ｜ ⬜ 2。** 逐条列出：⚠️ `L-A4`（`PQ26` 无客服载体）、`L-F1`（`N-5` 事件语义未定）；⛔ `L-A1b`（`PQ25` 缺幂等键 ⇒ 合同层缺失，不是实现问题）；⬜ `L-C6`（`B7` 能力矩阵）、`L-C7`（`B8` 模型下架）—— **2026-09-11 新增**，此前 33 条完全没有覆盖 `B7`/`B8`，即"需求写了但无人能判是否完成"。**2026-09-11 `PR-2b2b` 复核**：`L-E5` 的「壳带入」实为 `PR-2b2a` 已落地 ⇒ ⚠️ → ✅（⚠️ 3 → 2）。

> **`⬜` 的含义**：规格已写清、**实现尚未开始**（新增条目的初始态）。它与 `⚠️`（规格有缺口、无法判）不同 —— `⬜` 是**能判但还没做**，`⚠️` 是**做了也判不了**。

> **拆分后 ⚠️ 从 4 降到 3、并首次出现 1 条 ⛔ —— 这是进步不是退步。** 原来的 `L-A1` 因为`响应丢失`这一个分支整条挂在 ⚠️，而它的**成功路径其实完全可判**；拆开后成功路径独立成 ✅、被挡的那条独立成 ⛔，**阻塞不再污染它不属于的判据**。同理 `L-C1a`（验签）摆脱了 `V-2`。

### 2.1 每条闭环的五步流程（`开发规范` §6.4）

**状态列只管"规格够不够我照它写测试"，不管"代码写完没有"。** 真正的开工顺序是五步，**每条闭环都要走完**：

| 步 | 阶段 | 产出 / 完成判据 |
| --- | --- | --- |
| **0** | **粒度自检** | 能用**一句话**说清"用户看到什么变了"；说不清就回来拆（`§2.0` 的 6 组拆分就是这么来的） |
| **1** | **需求分析**（可与人互动） | 七项清单：依据 / 可见结果 / **异常分支 ≥1** / 落盘变化 / 未决阻塞 / **能力检索 + 单一 owner** / 反直觉行为。**有歧义就停下来问人**（`§3.7`），答复写回文档 |
| **2** | **开发计划更新** | 在 [`开发计划.md`](开发计划.md) 登记 PR 归属、拆分、依赖、**可断言的验收判据**，以及"依赖没到位时能判到什么程度" |
| **3** | **写测试 → 红** | 正向 + 关键异常 + 防回归；**看到语义上的红**（断言失败，不是编译失败） |
| **4** | **写实现 → 绿** | 最小实现让红变绿；不顺手加需求外的东西 |
| **5** | **验收** | **单元测试 + 端到端两条都过**，才允许把这行标 ✅ |

**第 5 步为什么是"两条都要"（本批次真实教训）。** `PR-2b1` 15 个单元测试全绿、端点逻辑正确，但端点**没挂进 `internal/server`** ⇒ 前端够不到 ⇒ **闭环没合上**。**单元绿证明"逻辑对"，端到端证明"路通了"，缺一条都不算完。**

**本文档不登记开发进度**，只登记规格与状态 —— 进度在 `开发计划.md`，避免两处维护同一事实（`§3.8`）。

---

## 3. 需要单独说明的闭环

### 3.1 `L-C4` 余额上屏：两处口径直接冲突（`V-11`）

- `E9` 规则 2：**「余额更新唯一时机 = 收到终态帧」**。
- `C7` 抬头：**「账本终态帧收不到 ⇒『只有 `settled` 才更新余额』这条链路不通」**，兜底改为 `GET /v1/credits/ledger` **主动查询（拉模式）**。

**两处不能同时为真**，而 `L-C4` 的验收判据（"余额变化只在终态帧之后"）正是照 `E9` 规则 2 写的 ⇒ **本轮按这条判会必挂**（终态帧本批不交付）。

**处置（§3.8，结论只能有一个 owner）：** 以 `E9` 为 owner，把规则 2 改写为**两条并列的合法来源**，并写明本批取哪条：

1. 有终态帧时（`D-002` 交付后）：**只有 `settled` 才更新**，流中不预扣不回滚。
2. 无终态帧时（**本批**）：走 `GET /v1/credits/ledger` **拉模式**，且只在以下时机拉 —— 回合结束、界面重新获得焦点、用户手动刷新。**不得**流中轮询。

**本批验收改为：** 一个回合结束后（或手动刷新）余额能刷到中台的值；流中不出现余额跳变；`402` 有明确文案、充值入口常驻。这条改动让 `L-C4` 从"必挂"变成"可判"。

### 3.2 `L-F1` 四事件 vs `L-E3` 拔盘：两套机制管同一件事？

- `F2` 的**拔盘冻结**：桌面壳层，程序目录消失 → 冻结界面。
- `datastore:lost` / `datastore:restored`：服务端经 WS 推出的**产品事件**。

**两者是否同一状态的两个视角、谁先谁后、只出现一个时算不算通过 —— 都没写。** 这是 `N-5` 的一部分，但影响 `L-E3` 与 `L-F1` 两条闭环的判定，所以单列。**开工前必须定**（建议：壳负责冻结 UI，服务端负责事件；两者都要，事件带 `reason`）。

### 3.3 `L-A1b` / `L-A4`：`PQ25` 与 `PQ26` 是这两条的**判定前提**，不是装饰

**一次性激活码 + 响应丢失 + 无客服载体**，三者叠加卡住两条闭环，且**卡的是不同环节**：

- **`L-A1b`（响应丢失）卡在 `PQ25`：** 合同没有幂等键 ⇒ 用户重试时，中台无法回答"这个激活码是已成功还是没成功"。**这条不是实现能补的**，要中台在合同里加幂等键（或明确"重复提交同一激活码返回原结果"）。在此之前它只能是 ⛔。
- **`L-A4`（失败分流）卡在 `PQ26`：** 界面会说"请联系客服"，而用户无处可联系。**这两条在 `PQ26` 落地前只能判「文案正确」，不能判「闭环成立」。**

**拆分带来的好处在这里最明显：** 原来 `L-A1` 把"激活成功"和"响应丢失"捆在一起，于是 `PQ25` 这个合同层缺陷**污染了成功路径的判定**（整条挂 ⚠️）。拆开后 `L-A1a` 独立判 ✅ —— 它可以先做完、先验收，不必等中台加字段。

---

## 4. 闭环依赖的公共件（缺一件则多条闭环不成立）

| # | 公共件 | 不成立的闭环 | 现状 |
| --- | --- | --- | --- |
| `P1` | **中台替身（CI 可重放）** —— 见 `V-10` | **几乎全部**：没有它，35 条只能靠人工对着 Sandbox 点 | ✅ **已落地 `PR-1`**，且自 `PR-2b1` 起**已被真正驱动**（`productruntime` 的 15 个测试全部走替身，不再只是形状对齐）（客户端侧 `L-A1a` `L-A2`–`L-A6`）：库 `internal/productclient/clienttest`，与客户端**共用同一份 DTO**，可编排场景；**签名能力随 `PR-4`**（`PR-1` 的四个端点不验签）。设计见 [`开发计划.md`](开发计划.md) §2 |
| `P2` | **Sandbox 地址 + 签名公钥 + 重置测试激活码的能力**（`V-8` / `S-8`） | `L-A1a` `L-A1b` `L-A2`–`L-A6`、`L-C1a` | ❌ 待中台。**2026-09-11 起 `developer.json` 的 `apiHost`/`gatewayHost` 指向本地替身进程**（`http://127.0.0.1:8788/v1`，由 `cmd/productstub` 提供）⇒ `ControlPlaneConfigured()==true`，闭环**可手动对着替身走通**（`L-A1a` 已走通）。**但 Sandbox 地址、签名公钥、重置测试码的能力仍缺** —— 替身**不是** Sandbox：它只让流程可走，不验签、不代表真环境行为；拿到地址后把 `developer.json` 换掉、`cmd/productstub` 闲置即可。**注意**：`P1` 落地不等于 `P2` 到位 —— 前者让闭环**能在 CI 里判**，后者让闭环**能对着真环境判** |
| `P3` | **拆除 `devBackend.ts`**（`V-9` / `G5`） | **全部** —— 不拆则联调结果无效（假数据形状合法、界面正常） | ⏸ **保留期已定为受控态**（2026-09-11 拍板）：拆除触发条件 = **本地 Go 服务能真实应答 `/api/product/*`（即 Sandbox / 替身通了之后）**；保留期必须是显式且默认关闭的开关。见 [`开发计划.md`](开发计划.md) `PR-3` |
| `P4` | **`internal/chatmode`**（`V-2`） | `L-C1b`（模式分组；`L-C1a` 验签**不依赖**它） | ❌ 不在 `v1`，在 `archive/feat-chatmode-20260911`（§3.5 搬运） |
| `P5` | **六个守卫接线**（`V-1`） | 闭环的**自动化回归**（`G4`"不靠人记"） | ❌ 目标与 CI job 不在 `v1` |
| `P6` | **首启初始化时序**（`V-7`） | `L-E1` | ✅ **已定并实现（`PR-2a`）**：责任人 = Go 服务、首次读取时惰性创建、只读盘写失败上抛；规则见 `需求基线` `E6.4` |

> **`PR-2b1` 之后的现状（2026-09-11）。** 5 个本地端点的**服务端侧**已落地并有 15 个测试（`internal/productruntime`，覆盖率 71.9%），**但闭环仍未合上** —— 端点还没挂进 `internal/server`（那一步动上游文件，属 `PR-2b2a`，待人工确认），所以前端**仍然够不到**它们，`P3` 的拆除条件**依旧不成立**（`devBackend.ts` 继续在 `fetch` 层劫持 `/api*`）。这段差距值得写在这里，因为「服务端写完了」最容易被误读成「闭环通了」。
>
> **`P1` 是本轮最容易被漏掉的一件。** 目标既然定为"不真正接入正式中台"，那么 E2E 就必须能**不依赖远程**地跑 —— 否则"闭环"退化成"某天有人手动点过一次"。`P1` 与 `devBackend.ts` **不是一回事、也不能互相替代**：`devBackend` 顶替的是**本地 Go 服务**，`P1` 顶替的是**中台**。这两层必须分开说，混为一谈会得到"有 Sandbox 就不用 P1"的错误结论。
>
> **`P1` 也不能在"被它镜像的东西"之前写出来。** 替身是 `internal/productclient` 的镜像 —— 而该包在 `v1` 上并**不存在**（8 个中台侧包一个都没有，2026-09-11 核实）。先写替身会发明一份契约，再写客户端又发明一份，两份必然漂移（`§3.8`）。因此正确顺序是**一份 Go 类型、两个实现**，这也是 `PR-1` 把「契约层 + 替身 + 客户端骨架」放在同一个 PR 的原因。详见 [`开发计划.md`](开发计划.md) §2。

---

## 5. 本轮新增缺口

| # | 缺什么 | 为什么必须补 | 归属 |
| --- | --- | --- | --- |
| `V-10` | **CI 可重放的中台替身（Go 侧 fixture server）** | 本轮目标明确"不接正式中台"，但没有它，`§2` 的 35 条闭环**无法自动回归**，只能人工对 Sandbox 点；`G4` 的"不靠人记"随之失效。它必须实现与 `internal/productclient` 同源的 DTO/错误码（否则又成第二份契约，§3.8），且**只在 `developer` 构建与测试里启用** | 客户端 |
| `V-11` | **余额上屏的口径**（见 §3.1） | `E9` 规则 2 与 `C7` 兜底冲突，照 `E9` 判会必挂 | 客户端（改 `E9` 规则 2） |
| `V-15` | **`L-B2` / `L-B3` 无归属 PR，且其设计文档 `P4-拦截页.md` 被引用却不存在** | 清单把两条标 ✅（规格完整），而**无人认领 ⇒ 无方案 ⇒ 无 UI**，读起来等于「已做完」；服务端半边（`ControlPlaneConfigured()` / `HasTrustedKeys()` / `control_plane_unconfigured`）都在，更会加固这个错觉 | **新增 `PR-2c`（拦截页与控制面失败面）**；第一件交付物是补出 `P4-拦截页.md` |
| `V-16` | **`L-A6` 的「清凭证」未接线**：`productruntime.IsSessionExpired` 是**死导出**（全仓零调用点），`Creds.Delete()` 只在 logout 端点调用 | `L-B3` 明确「**只有 `unauthorized` 清凭证**」，而 `statusForCode` 把 403 原样透传。会话已失效而 refresh token 仍在盘上，与 `E6` 规则 1 的凭证明文存储叠加后放大 | **`PR-2c`**（与 `L-B3` 的 `unauthorized` 档同属一处） |
| `V-17` | **`L-E2` 只做了存储半边**：`PR-2a` 的「打底」在闭环语境里被读成已完成，实际**无任何「拷 `data/` 到另一台机」的场景测试** | 换机复用是本产品的核心卖点（`E1` 规则 7），其验收判据是一条**场景**而不是一组单元测试；缺它则 `L-E2` 今天判不了 | **`PR-7`**（`开发计划` §1 的 `PR-7` 行已同步写明） |

---

## 6. 修订

| 日期 | 说明 |
| --- | --- |
| 2026-09-11 | **新增两条闭环 `L-C6`（`B7` 能力矩阵与入口可见性）、`L-C7`（`B8` 会话模型绑定与模型下架）：33 条 → 35 条。** 来源：`PR-4` 需求分析时核实——`capabilities` 与 `catalog` 同在一个签名 `policy` 内，但 33 条闭环**完全没有任何条目覆盖 `B7`/`B8`**（grep `B7`/`entitled`/`visible` 零命中）。也就是说这两条需求写了、却"无人能判是否完成"，与 `G4`「不靠人记」直接冲突。人工决定：**补成闭环**而非登记为待解决项。新增 `⬜` 状态标记（规格已写清、**实现尚未开始**），与 `⚠️`（规格有缺口、**做了也判不了**）区分开。复算 **✅ 30 ｜ ⚠️ 2 ｜ ⛔ 1 ｜ ⬜ 2**。归属：`L-C6` → `PR-4e`（`capabilities` 的**验签**随 `PR-4a`，**消费**单独成 PR）；`L-C7` → `PR-5`（提示发生在"下一条消息前"，是网关回合的前置校验）。 |
| 2026-09-11 | **`L-A1a` 手动走通 + 两条联调缺口（`V-13`/`V-14`）。** 手动验收：生产产物 `./octo-desktop` + 真替身进程（`cmd/productstub`），五字段激活落盘四要素逐项核对通过。**本轮联调暴露两个「只在真跑时才现形」的问题**：`V-13` —— `internal/productclient` 的路径常量自带 `/v1`，而 `productprofile` 的 host 也自带 `/v1`（`production.json` + 一条断言），`client.go:232` 的 `baseURL + path` 因此组成 `/v1/v1/…` **必 404**；**175 条单元测试全绿也没抓到**，因为两个 harness 都传了不带 `/v1` 的裸 origin —— 一个 profile 永不产生的形状。已修（路径改为相对版本段）+ 两个 harness 改传带版本 URL；把同一改动后的 harness 跑一遍**当场全红**，反证此前是被 harness 掩盖。`V-14` —— 手册 §3.4 的 `make desktop-dev` 有文档无目标、`OCTO_DESKTOP_DEV_URL` 无任何消费方；只登记未重建（属功能决定）。**教训与本表既有主题一致：单元绿 ≠ 闭环合上；测试里用的形状必须与运行时会产生的形状一致。** |
| 2026-09-11 | **按人工要求重审闭环粒度：25 条 → 33 条（拆 6 组，见 §2.0）。** 判据 = `开发规范` §6.4 第 0 步"一条闭环 = 一次可独立判定的用户可见行为"。拆分：`L-A1`→`a/b`、`L-B1`→`a/b`、`L-C1`→`a/b`、`L-C3`→`a/b/c`、`L-D4`→`a/b/c`、`L-E4`→`a/b`。**净收益是阻塞被隔离**：`L-A1` 原来因 `PQ25` 整条 ⚠️，拆开后 `L-A1a`（成功路径）独立判 ✅、`L-A1b`（响应丢失）独立成 ⛔；`L-C1a`（验签）也摆脱了 `V-2`。复算 **✅ 29 ｜ ⚠️ 3 ｜ ⛔ 1**（⚠️：`L-A4` `E5` `F1`；⛔：`L-A1b`）。新增 §2.1 登记**每条闭环的五步流程**（粒度自检 → 需求分析 → 开发计划 → 测试红 → 实现绿 → 单元+端到端验收），并明确本文档**只登记规格状态、不登记开发进度**（进度在 `开发计划.md`，避免两处维护同一事实）。所有引用已同步（`开发计划.md` 的 PR 归属与待决项位置、`需求基线.md`、`README.md`）。 |
| 2026-09-11 | `PR-2b1` 落地后更正两处过时状态：`P6`（`V-7` 首启时序）**❌ 未定义 → ✅ 已定并实现**；`P1`（中台替身）注明**自 `PR-2b1` 起已被真正驱动**（15 个测试全部走替身）。并补一段「服务端侧已就绪、但端点未挂进服务 ⇒ 闭环仍未合上」的现状说明，避免把「服务端写完」误读成「闭环通了」。 |
| 2026-09-11 | `PR-2a` 落地后复算：**✅ 21 ｜ ⚠️ 4 ｜ ⛔ 0**（逐条机器计数）。**`L-E1` 转 ✅** —— `V-7` 首启时序已定并实现（责任人、时刻、初值、只读盘行为），四条验收各有测试。**`L-C4` 转 ✅** —— `V-11` 的口径冲突已收敛为「两条并列来源 + 分两版判据」，本轮按拉模式可判。`L-E2` / `L-E4` 的存储侧已判（凭证随 `data/` 走；缺字段/损坏/版本超前三条护栏）。`L-E5` 转 ⚠️（存储侧就绪，壳带入语言待 `PR-2b`）。剩余四条 ⚠️：`L-A1`、`L-A4`（`PQ26`）、`L-E5`（`PR-2b`）、`L-F1`（`N-5`）。 |
| 2026-09-11 | 建立。按人工明确的**本轮目标**（UI + 交互 + Sandbox，不接正式中台）拆出 **25 条闭环**作为本轮 E2E 依据；先与《中台交付包》§9 的 15 条正式验收划清分工（§1）；逐条标状态，暴露 4 条 ⚠️（`L-A1` `L-A4` `L-C4` `L-E1` `L-F1`）；新增 `V-10`（CI 中台替身）与 `V-11`（余额口径冲突）；列 6 个公共件并说明 `devBackend` 与 `P1` 为何不能互相替代。 |
